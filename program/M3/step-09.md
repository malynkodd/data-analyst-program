# M3.09. Ставит PostgreSQL и переносит на него базу модуля, доказывая числами, что перенос ничего не потерял

Умение: — (инфраструктура). Шаг ставит PostgreSQL и переносит на него
базу модуля. Ни одно из 30 умений части 1 blueprint он не закрывает и в
покрытии умений не участвует — решение 23 `design/decisions.md`, которое
отменило пункт 3 решения 19 (там этот шаг был объявлен «частью 6 из 7»
умения A2). Критерий готовности при этом остаётся строгим наравне с
любым другим шагом: шесть чисел, расхождение 0 по каждому. Умения
продолжаются со `step-10.md` (A2 на перенесённом коде), `step-11.md`
(A3, часть 2 из 2) и `step-12.md` (A4, часть 2 из 2)
Модуль: M3
Требуется до этого: `step-01.md`–`step-08.md` (вся SQLite-часть модуля;
база `m3.db` уже собрана и содержит `schema.sql` + `seed.sql` +
`retention_seed.sql`)
Новые файлы модуля, объявляемые этим шагом: `program/M3/data/schema_pg.sql`
(схема тех же 5 таблиц для PostgreSQL), `program/M3/data/export_for_postgres.py`
(выгрузка таблиц `m3.db` в CSV). Новых таблиц и новых колонок к сквозной
схеме модуля шаг не добавляет.
Время: 6–8 ч (оценка проектная, не измерена)

## 1.1. Цель и связь с умением

После шага у вас на машине работает PostgreSQL, в нём лежат те же 5
таблиц модуля, и вы доказали числами — а не осмотром — что перенос не
потерял ни одной строки и не изменил ни одной суммы.

Шаг инфраструктурный: умения он не закрывает. A1–A4 уже отработаны на
SQLite в `step-01.md`–`step-08.md`, а `step-10.md`–`step-12.md` применят
их к перенесённой базе. Здесь — перенос и доказательство того, что
переносить было безопасно. Проверка от этого мягче не становится: она
устроена так же, как у шагов с умением, — эталон, порог, допуск,
расхождение 0.

Зачем переход вообще: SQLite не встречается в выборке
`research/market.md` ни разу — даже среди 6 вакансий (4.9%), где диалект
SQL назван явно; из этих 6 семейство PostgreSQL (включая Redshift) — 3,
то есть большинство там, где диалект вообще указан [М]. Полное
обоснование и разобранные альтернативы — решение 15 `design/decisions.md`;
почему переход стоит именно здесь, после `step-08.md`, — решение 19.

## 1.2. Теория своими словами

**Файл против сервера.** SQLite — библиотека: база это один файл
(`m3.db`), никакого отдельного процесса нет, «подключение» — это открытие
файла. PostgreSQL — сервер: отдельный процесс, который держит файлы базы
и принимает подключения по сети (по умолчанию порт 5432). Отсюда всё
остальное различие в обращении: у PostgreSQL есть пользователь, пароль,
имя базы внутри одного сервера (`createdb m3`), и клиент (`psql`),
который к нему подключается. Файл базы PostgreSQL руками не открывают и
не копируют.

**Строгие типы против гибких.** SQLite объявленный тип колонки трактует
как пожелание: в колонку `INTEGER` можно записать строку, и ошибки не
будет (`sqlite.org/quirks.html`, разделы 3 и 10). PostgreSQL тип
проверяет: несовпадение — ошибка вставки. Для переноса это означает, что
типы нельзя отобразить механически, слово в слово: три из них в этой
схеме означают на двух движках разное.

| В `schema.sql` (SQLite) | Наивное отображение | Что берём и почему |
|---|---|---|
| `order_date TEXT` (дата строкой) | `TEXT` | `DATE`. У SQLite отдельного типа даты нет вовсе (`quirks.html`, 3.2), поэтому даты в M3 лежат текстом; у PostgreSQL тип есть, и держать дату текстом в нём — сознательно хуже. Цена: `substr()`/`strftime()` по дате перестают работать — разбор в `step-10.md` |
| `amount REAL` (деньги) | `REAL` | `NUMERIC(10,2)`. `REAL` в PostgreSQL — 4 байта, 6 десятичных знаков; в SQLite `REAL` — 8 байт. Отображение `REAL` → `REAL` теряет точность **молча**, замер в 1.3 |
| `customer_id INTEGER PRIMARY KEY` | `SERIAL` / `GENERATED ... AS IDENTITY` | `INTEGER PRIMARY KEY` как есть. Автогенерация нужна, когда id не задан; здесь все id приходят из CSV явно, генерировать нечего |

Документация, на которую опирается таблица: типы PostgreSQL —
[datatype-numeric](https://www.postgresql.org/docs/17/datatype-numeric.html)
(там же прямо: «для денежных сумм используйте `numeric`»); отличия SQLite
от остальных СУБД — [sqlite.org/quirks.html](https://www.sqlite.org/quirks.html).

**Почему не Docker.** Docker Desktop на Windows работает поверх WSL2 или
Hyper-V, то есть требует ровно того, чего решение 13 `design/decisions.md`
велит не требовать на пустом месте. Установщик PostgreSQL под Windows
самодостаточен и WSL не использует — он и берётся основным путём.

**Строка подключения — пять частей, и на работе вам дадут именно её,
а не файл базы.**

До этого шага база была файлом: `m3.db` лежал в папке, и `sqlite3` его
открывал. PostgreSQL так не работает — это сервер, и обращаются к нему
по адресу. Адрес записывается одной строкой (URI) или пятью
параметрами по отдельности:

```
postgresql://<пользователь>:<пароль>@<хост>:<порт>/<база>
postgresql://analyst:secret@localhost:5432/m3
```

| Часть | Что это | Значение на вашей машине |
|---|---|---|
| пользователь | учётная запись **в базе**, не в Windows | `postgres` или заведённый вами |
| пароль | задаётся при установке сервера | ваш |
| хост | где сервер | `localhost` — здесь же |
| порт | 5432 по умолчанию | `5432` |
| база | какая именно база на сервере | `m3` |

На работе от этой строки меняются два первых и два следующих: хост
будет не `localhost`, а адрес сервера или туннеля; пользователь — ваш,
с правами только на чтение. Всё остальное устроено так же.
Документация:
[Connection Strings](https://www.postgresql.org/docs/current/libpq-connect.html#LIBPQ-CONNSTRING).

**Пароль в командной строке и в коде не пишется.** Строка с паролем
попадает в историю оболочки, а из кода — в git. Штатных способа два:
переменная окружения `PGPASSWORD` (её читает `psql`) и файл
`%APPDATA%\postgresql\pgpass.conf` — по строке на подключение, в
формате `хост:порт:база:пользователь:пароль`
([The Password File](https://www.postgresql.org/docs/current/libpq-pgpass.html)).
Второй способ и есть тот, которым пользуются на работе: строка
подключения в репозитории тогда не содержит пароля вовсе.

**Клиент с окном, а не только `psql`.** `psql` — консольный клиент,
которым делается всё в этом модуле. Рядом существуют pgAdmin (идёт в
установщике PostgreSQL) и DBeaver (отдельный, работает с любым движком).
Оба нужны для одного: посмотреть список таблиц и их колонки глазами,
когда база чужая и незнакомая. Проходить модуль они не помогают —
критерии здесь проверяются числами, а не осмотром, — но на работе
первое, что вам дадут, будет строка подключения и предложение
«посмотреть».

**Чтение чужой схемы: три вопроса и три запроса.**

Ваша база — пять таблиц, объявленных в `step-00.md`. На работе их будет
двести, документации не будет, и спрашивать будет некого. Порядок
разбора всегда один.

*Какие вообще есть таблицы:*

```sql
SELECT table_name
FROM information_schema.tables
WHERE table_schema = 'public'
ORDER BY table_name;
```

*Какие в таблице колонки и каких типов:*

```sql
SELECT column_name, data_type, is_nullable
FROM information_schema.columns
WHERE table_name = 'orders'
ORDER BY ordinal_position;
```

*Какая из таблиц фактовая, а какие справочники* — по размеру:

```sql
SELECT relname AS table_name, n_live_tup AS approx_rows
FROM pg_stat_user_tables
ORDER BY n_live_tup DESC;
```

`information_schema` — стандарт SQL, он есть и в PostgreSQL, и в
BigQuery (M7), и почти везде
([The Information Schema](https://www.postgresql.org/docs/current/information-schema.html)).
`pg_stat_user_tables` — уже специфика PostgreSQL, и число строк там
**приблизительное**: оно обновляется сборщиком статистики, а не
пересчитывается на каждый запрос. Для вопроса «где тут таблица фактов»
приблизительного достаточно — порядок величины между справочником и
фактами отличается в тысячи раз. Для любого числа, которое пойдёт в
отчёт, — не достаточно, там только `COUNT(*)`.

Правило разбора чужой схемы, которое стоит запомнить: **самая большая
таблица — почти всегда факты, самые маленькие — справочники, а связи
между ними видны по совпадению имён колонок**. Это гипотеза, а не
знание: проверяется она счётом непарных строк (`step-04.md`, 1.2), и до
этой проверки на ней ничего не считают.

## 1.3. Разобранный пример на сквозном датасете

Датасет тот же: `m3.db` после `schema.sql` + `seed.sql` +
`retention_seed.sql` (252 клиента, 366 заказов, 6 товаров, 23 позиции, 13
платежей — числа из `step-00.md` и `data/README.md`).

**Шаг 1. Установка.** Страница загрузки —
[postgresql.org/download/windows](https://www.postgresql.org/download/windows/):
там два варианта, «Interactive installer by EDB» (обычный установщик,
ставит сервер, `psql` и pgAdmin) и «zip archive» — те же бинарники без
установщика, для случая, когда прав администратора на машине нет.
Платформа программы — Windows (решение 21 `design/decisions.md`), и
проверялся только Windows-путь; страницы загрузки для других ОС на сайте
есть, но в программе не используются и не проверялись. При установке
запрашивается пароль пользователя `postgres` — запишите его, он
понадобится при каждом подключении.

После установки `psql` может не оказаться в `PATH`: тогда его запускают
ярлыком «SQL Shell (psql)» из меню либо добавляют в `PATH` папку `bin`
установки. Проверка, что сервер поднялся:

```
$ psql -U postgres -c "SELECT version();"
```

На референсной машине автора (Windows 11, вариант с zip-архивом, без прав
администратора) это напечатало:

```
PostgreSQL 17.6 on x86_64-windows, compiled by msvc-19.44.35213, 64-bit
```

Ваша версия и строка сборки будут другими; проверяется не совпадение
строки, а то, что команда вообще вернула ответ, а не ошибку подключения.

**Шаг 2. База и схема.**

```
$ createdb -U postgres m3
$ psql -U postgres -d m3 -f program/M3/data/schema_pg.sql
```

`schema_pg.sql` — те же 5 таблиц с типами из таблицы в 1.2. Вывод — пять
строк `CREATE TABLE`.

**Шаг 3. Выгрузка данных из SQLite.**

```
$ python program/M3/data/export_for_postgres.py m3.db
```

Скрипт кладёт по одному CSV на таблицу в папку `pg_csv` и печатает число
строк по каждой. Прогон на референсной машине:

```
customers: 252 строк -> pg_csv\customers.csv
products: 6 строк -> pg_csv\products.csv
orders: 366 строк -> pg_csv\orders.csv
order_items: 23 строк -> pg_csv\order_items.csv
payments: 13 строк -> pg_csv\payments.csv
Итого выгружено строк: 660
```

Порядок в выводе не случайный: он же — порядок загрузки. `customers` и
`products` идут раньше `orders`, `orders` раньше `order_items` и
`payments`, потому что внешний ключ отклонит строку, у которой родителя
ещё нет.

**Шаг 4. Загрузка в PostgreSQL.** По одной команде на таблицу, в том же
порядке:

```
$ psql -U postgres -d m3 -c "\copy customers FROM 'pg_csv/customers.csv' CSV HEADER"
$ psql -U postgres -d m3 -c "\copy products FROM 'pg_csv/products.csv' CSV HEADER"
$ psql -U postgres -d m3 -c "\copy orders FROM 'pg_csv/orders.csv' CSV HEADER"
$ psql -U postgres -d m3 -c "\copy order_items FROM 'pg_csv/order_items.csv' CSV HEADER"
$ psql -U postgres -d m3 -c "\copy payments FROM 'pg_csv/payments.csv' CSV HEADER"
```

`\copy` — команда клиента `psql`, а не сервера: файл читает `psql` с
вашей машины и с вашими правами
([app-psql](https://www.postgresql.org/docs/17/app-psql.html)). Похожая по
имени серверная команда `COPY` читала бы файл с диска сервера и требовала
бы прав суперпользователя — на своей машине разница незаметна, на рабочем
сервере это две разные вещи. Вывод каждой команды — `COPY <число строк>`:
252, 6, 366, 23, 13.

**Шаг 5. Наивное отображение типа, которое проходит молча.** Проверьте,
чего стоило решение из 1.2 взять `NUMERIC(10,2)`, а не `REAL`:

```sql
CREATE TEMP TABLE t_real (amount REAL);
CREATE TEMP TABLE t_dbl  (amount DOUBLE PRECISION);
INSERT INTO t_real SELECT amount FROM orders;
INSERT INTO t_dbl  SELECT amount FROM orders;

SELECT 'NUMERIC(10,2)'    AS mapping, SUM(amount)::text AS total FROM orders
UNION ALL SELECT 'REAL (float4)',     SUM(amount)::text FROM t_real
UNION ALL SELECT 'DOUBLE PRECISION',  SUM(amount)::text FROM t_dbl;
```

Результат на референсной машине:

| mapping | total |
|---|---|
| `NUMERIC(10,2)` | 317959.87 |
| `REAL (float4)` | **317959.84** |
| `DOUBLE PRECISION` | **317959.87000000005** |

В SQLite та же сумма (`SELECT SUM(amount) FROM orders` на `m3.db`) —
**317959.87**. То есть отображение `REAL` → `REAL`, самое очевидное из
трёх, расходится с исходной базой на 0.03 — без ошибки, без
предупреждения, при полностью успешной загрузке всех 366 строк.

Отдельно проверяется, что расходится именно агрегат, а не отдельные
значения:

```sql
SELECT COUNT(*) FROM (SELECT amount, amount::real::numeric(10,2) AS back FROM orders) s
WHERE amount <> back;
```

Результат — **0**: ни одно из 366 значений по отдельности не искажено
настолько, чтобы отличаться в двух знаках после запятой. Поэтому построчная
сверка «глазами» такой перенос принимает, а сумма — нет. Это ровно тот
механизм, что в частях 1–5 умения A2: проверять надо то число, которое
ломается, а не то, которое сходится.

## 1.4. Задание

База перенесена (шаги 1–4 из 1.3). Теперь докажите, что перенос корректен,
и оформите доказательство как отчёт из 6 чисел.

1. Посчитайте число строк в каждой из 5 таблиц PostgreSQL и сверьте с
   выводом `export_for_postgres.py`.
2. Посчитайте `SUM(amount)` по всем 366 заказам в PostgreSQL и в SQLite,
   сравните.
3. Посчитайте `SUM(amount)` по `completed`-заказам исходного набора
   (`order_id < 200`) — контрольная сумма модуля, объявленная в
   `step-00.md`.
4. Проверьте инвариант `order_items`: для каждого заказа
   `SUM(quantity * unit_price)` равен `orders.amount`. Ожидается 0 строк
   расхождения.
5. Проверьте инвариант `payments`: платёж существует тогда и только
   тогда, когда заказ `completed`. Ожидается 0 несовпадений по всем 19
   исходным заказам.
6. Отдельно: воспроизведите замер из шага 5 в 1.3 и запишите, на сколько
   разошлась сумма при отображении `REAL` → `REAL`.


После того как перенос сошёлся:

10. Соберите строку подключения к своей базе в виде URI и запишите её в
    `program\M3\work\connection.md` — **без пароля**, на его месте
    `<пароль>`. Рядом одной строкой: каким из двух штатных способов
    пароль у вас передаётся.
11. Выполните три запроса разбора схемы из 1.2 на своей базе и
    запишите: сколько таблиц в схеме `public`, сколько колонок в
    `orders`, и какая таблица самая большая по `n_live_tup`.
12. Сравните ответ последнего запроса с точным `COUNT(*)` по той же
    таблице и запишите обе величины. Объясните в одном предложении,
    почему они могут разойтись.

## 1.5. Критерий готовности

**Отдельно: подключение и разбор схемы (задания 10–12).**

- В `work\connection.md` есть строка вида
  `postgresql://<пользователь>:<пароль>@localhost:5432/m3`, и настоящего
  пароля в файле нет ни в каком виде. Названо, чем пароль передаётся —
  `PGPASSWORD` или `pgpass.conf`.
- Задание 11: таблиц в схеме `public` — **5**; колонок в `orders` — то
  число, которое объявлено схемой модуля в `step-00.md`, расхождение 0;
  самая большая таблица по `n_live_tup` — `orders` (366 строк против 252
  у `customers`).
- Задание 12: обе величины записаны. Объяснение называет, что
  `n_live_tup` — приблизительная оценка сборщика статистики, а не счёт,
  и что после массовой вставки она может отставать до следующего
  `ANALYZE`. Порог: названа именно природа числа, а не «PostgreSQL
  ошибся».


- Число строк в PostgreSQL по каждой из 5 таблиц: `customers` 252,
  `products` 6, `orders` 366, `order_items` 23, `payments` 13 —
  расхождение 0 по каждой таблице.
- `SUM(amount)` по всем заказам в PostgreSQL равен **317959.87** и
  совпадает с той же суммой в SQLite; расхождение 0.
- `SUM(amount)` по `completed`-заказам с `order_id < 200` равен
  **8600.00** — то же число, что во всех шагах модуля начиная со
  `step-05.md`; расхождение 0.
- Инвариант `order_items` даёт **0** строк расхождения, инвариант
  `payments` — **0** несовпадений.
- Записано численно, на сколько разошлась сумма при наивном отображении
  типа: **317959.84 против 317959.87, разница 0.03** — и названо, что
  ошибки при этом не возникло ни одной.
- Норматив: ≤2 ч на установку и перенос, ≤40 мин на 6 проверок. Если
  установка заняла больше 2 ч, причина фиксируется в `research/self.md`
  отдельной строкой — это ровно та цифра, которая в решении 15 названа
  барьером и оценена без замера.

## 1.6. Типичные ошибки

**Ошибка:** кладёте пароль прямо в строку подключения в коде или в
команду `psql`.
**Почему выглядит правильно:** работает сразу, ничего настраивать не
надо, а база локальная и «всё равно своя».
**Почему неправильно:** строка попадает в историю оболочки и, если
код лежит в репозитории, — в git. Это тот же класс необратимости, что
`.pbix` в истории коммитов (`DEFERRED.md`, «Чистка бинарных хвостов»):
удалить из истории дороже, чем не класть. На локальной базе цена нулевая,
привычка — та же, и переносится она на рабочую базу целиком.
**Как правильно:** `PGPASSWORD` или `pgpass.conf`, а в репозитории —
строка с `<пароль>` вместо значения.

**Ошибка:** берёте `n_live_tup` как число строк и ставите его в отчёт.
**Почему выглядит правильно:** запрос быстрый, число похоже на правду,
на маленькой таблице совпадает с `COUNT(*)` точно.
**Почему неправильно:** это оценка сборщика статистики, а не счёт.
На только что заполненной базе модуля она может показывать 0 по всем
пяти таблицам до первого `ANALYZE` — при том что `COUNT(*)` по `orders`
даёт 366. Ноль в отчёте выглядит как «данных нет», а не как «статистика
не собрана».
**Как правильно:** `n_live_tup` — только для вопроса «где тут большая
таблица» при первом знакомстве со схемой. Любое число, которое пойдёт
дальше отчёта, — `COUNT(*)`.


**Ошибка:** загружаете таблицы в том порядке, в каком они перечислены в
`schema.sql`, — `customers`, `orders`, `products`, `order_items`,
`payments`.
**Почему выглядит правильно:** это порядок объявления таблиц в схеме
модуля, он же порядок, в котором таблицы читаются глазами.
**Почему неправильно:** `order_items` ссылается внешним ключом и на
`orders`, и на `products`; при таком порядке `products` (6 строк) ещё не
загружены к моменту загрузки 23 строк `order_items`, и PostgreSQL
отклоняет вставку целиком: `insert or update on table "order_items"
violates foreign key constraint`. Ноль загруженных строк вместо 23.
**Как правильно:** порядок из вывода `export_for_postgres.py` — сначала
`customers` и `products`, потом `orders`, потом `order_items` и
`payments`.

**Ошибка:** отображаете `REAL` в `REAL`, потому что слово то же самое.
**Почему выглядит правильно:** тип называется одинаково, загрузка всех
366 строк проходит без единой ошибки, построчная сверка значений
расхождений не даёт (0 строк, замер в 1.3).
**Почему неправильно:** `SUM(amount)` по таблице даёт 317959.84 вместо
317959.87 — расхождение 0.03 на 366 строках, и оно растёт с числом строк.
Отчёт по выручке, собранный на такой базе, не сходится с исходным ни
одной итоговой цифрой, при том что каждая отдельная строка выглядит
правильной.
**Как правильно:** для денег — `NUMERIC(точность, знаки)`, у которого
хранение точное; `REAL`/`DOUBLE PRECISION` в PostgreSQL — приближённые
типы, о чём документация
[datatype-numeric](https://www.postgresql.org/docs/17/datatype-numeric.html)
предупреждает прямо.

**Ошибка:** переносите и схему, и данные одним файлом — выгрузкой
`.dump` из SQLite, поданной в `psql`.
**Почему выглядит правильно:** `.dump` выдаёт готовый SQL со всеми
`CREATE TABLE` и `INSERT` сразу, и это выглядит как самый прямой путь,
без промежуточных CSV и без отдельного файла схемы.
**Почему неправильно:** проверено прогоном — загрузка обрывается на 25-й
строке: `ERROR: relation "orders" does not exist`. Причина та же, что в
первой ошибке этого раздела: `.dump` выдаёт таблицы в порядке их создания
в SQLite (`customers`, затем `order_items`), а `order_items` ссылается на
`orders`, которая идёт ниже. SQLite при выгрузке это не смущает,
PostgreSQL отклоняет. Дальше каждая строка отвечает `current transaction
is aborted`, и после отката в базе остаётся **0 таблиц** (проверено
запросом к `information_schema.tables`). Отдельно: `sqlite3` начинает
выгрузку строкой `PRAGMA foreign_keys=OFF;`, на которой `psql` отвечает
`syntax error at or near "PRAGMA"`. И даже если бы файл загрузился, он
воссоздал бы типы SQLite дословно — `order_date TEXT` и `amount REAL`, то
есть ровно те два отображения, которые отвергнуты в 1.2.
**Как правильно:** схему задавать отдельным файлом под PostgreSQL
(`schema_pg.sql`, где порядок таблиц и типы выбраны осознанно), данные
переносить в формате, который принимают оба движка, — CSV через
`export_for_postgres.py` и `\copy`.

**Ошибка:** проверяете корректность переноса тем, что открываете пару
таблиц и смотрит, что данные «на месте».
**Почему выглядит правильно:** строки действительно на месте, кириллица
отображается, суммы похожи на исходные.
**Почему неправильно:** на этом переносе такая проверка пропускает
расхождение 0.03 в `SUM(amount)` (317959.84 против 317959.87) — оно не
видно ни в одной отдельной строке, потому что каждое из 366 значений по
отдельности совпадает (0 строк расхождения при построчной сверке).
**Как правильно:** сверять числа, которые считаются по всей таблице
сразу, — число строк и контрольные суммы, объявленные в `step-00.md`.

## 1.7. Честная оценка времени

4–5 ч. Оценка проектная, не измерена. Основной источник неопределённости
— установка. На референсной машине автора она уложилась в 4 минуты, но
это не ориентир для вас: там был вариант с zip-архивом, скриптовая
распаковка, отсутствие интерактивного установщика и уже настроенный с M0
Python с терминалом. Типовые задержки первой установки (пароль
пользователя `postgres`, отсутствие `psql` в `PATH`, занятый порт 5432)
на этой машине не воспроизводились ни разу и в 4 минуты не входят.
Решение 15 `design/decisions.md` называет установку барьером прямо и
оценки ей не даёт — эта строка и есть первый кандидат на калибровку по
`research/self.md` во всём модуле.

Прежняя вилка — 4–5 ч; +2–3 ч добавлены вместе с блоком про строку
подключения, передачу пароля, клиентов с окном и разбор чужой схемы через
`information_schema`, и тремя заданиями (решение 41 `design/decisions.md`).
Шаг остаётся инфраструктурным: умения он по-прежнему не несёт, а
добавленное — то, с чего начинается работа с любой незнакомой базой.

## 1.8. Вопрос уровня собеседования

**Вопрос:** «Вы перенесли таблицу из одной СУБД в другую. Загрузка прошла
без ошибок, число строк совпало. Что ещё проверите, прежде чем сказать,
что перенос корректен?»

**Эталон (ответ покрывает ≥3 из 4 пунктов):**
1. Контрольные суммы по числовым колонкам (`SUM`, `AVG`) на обеих
   сторонах, а не только число строк: совпадение количества строк не
   гарантирует совпадения значений.
2. Проверяет, во что отобразились типы — отдельно денежные колонки
   (точный `NUMERIC` против приближённого `REAL`/`DOUBLE PRECISION`) и
   даты (настоящий тип даты против строки).
3. Проверяет инварианты между таблицами, которые были верны до переноса
   (сумма позиций равна сумме заказа; платёж есть только у оплаченного
   заказа), а не каждую таблицу по отдельности.
4. Называет, что расхождение может быть невидимо построчно и видно
   только в агрегате, — то есть проверять надо то число, которое
   ломается, а не то, которое сходится.
