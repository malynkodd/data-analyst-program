# M3.00. Декларация модуля (умения, схема, движок)

Не шаг для учащегося — служебный файл, требуемый разделом 1.3 скилла
curriculum-design: схема и датасет объявляются на весь модуль один раз,
до первого содержательного шага. Пишется для M3 сейчас, хотя написан
только один образец шага (`step-05.md`, умение A2) — именно чтобы не
повторить историю этого же пилота: schema сначала на 2 таблицы под один
шаг, потом расширение под A1 задним числом. Здесь схема сразу на 5
таблиц, рассчитанных на все умения A1–A5.

## Умения модуля и статус шагов

Модуль M3 закрывает A1–A5 (часть 2.1 blueprint), 60–80 ч.

| Умение | Шаг | Статус |
|---|---|---|
| A1 | `step-06.md` | написан |
| A2 | 5 частей — `step-01.md`–`step-05.md` | написаны все 5 |
| A3 | 2 части — `step-07.md` (SQLite), `step-11.md` (PostgreSQL) | написаны обе |
| A4 | 2 части — `step-08.md` (SQLite), `step-12.md` (PostgreSQL) | написаны обе |
| A5 | закрыт в M1 (A5 назначен M1, не M3 — часть 2.2 blueprint), в M3 не переписывается | — |
| — | `step-09.md` — инфраструктура (установка PostgreSQL и перенос данных), умения не несёт, покрытием не считается (решение 23) | написан |
| A2 | `step-10.md` — не часть разбивки: применение уже закрытого A2 к перенесённому на PostgreSQL коду | написан |

Модуль M3 написан целиком — 12 шагов (`step-01.md`–`step-12.md`).
Часовой бюджет: построчная сумма всех 12 шагов —
3–4 + 4–5 + 5–6 + 4–5 + 3–4 + 8–10 + 5–6 + 5–7 + 4–5 + 6–8 + 6–8 + 7–9 =
**60–77 ч**, против вилки части 6.1 blueprint 60–80 ч. **Сумма
укладывается в вилку.**

До написания шагов 09–12 сумма составляла 37–47 ч и в вилку не
укладывалась (разрыв 13–33 ч). Разрыв объяснялся отсутствующим
содержанием, а не завышенной вилкой: решение 15 `design/decisions.md`
требует перехода на PostgreSQL к концу модуля, и шагов про это не было
(`program/pilot-report.md`, заход 5 — фиксация разрыва; заход 6 —
закрытие). Часы части 6.1 не менялись.

**Что делают шаги 09–12 и почему они не части разбивки.** Заход 6
отнёс `step-09.md` и `step-10.md` к A2 как «части 6 и 7 из 7» (решение
19, п. 3). Это отменено решением 23: пять шагов из семи продолжали
объявлять «часть N из 5», а `step-05.md` и `step-10.md` одновременно
называли себя финальной частью одного умения — у A2 оказалось два
конца. Теперь:

- `step-09.md` — **инфраструктура**: установка PostgreSQL и перенос
  данных. Умения не несёт, в покрытии не участвует. Критерий готовности
  у него есть и остаётся строгим (шесть чисел, расхождение 0) —
  инфраструктурный не значит непроверяемый.
- `step-10.md` — **A2, но не часть разбивки**: применение уже закрытого
  умения к перенесённому коду. Форма проверки та же, что в части 1
  blueprint (найти причину, исправить, объяснить в ≤3 предложениях), и
  материал тот же по существу — молчаливая потеря, находимая счётом:
  `LIKE 'COMPLETED'` даёт 360 строк в SQLite и 0 в PostgreSQL без
  единого сообщения об ошибке.
- `step-11.md` и `step-12.md` — **части 2 из 2** умений A3 и A4. Здесь
  разбивка законна: движок в формулировках A3 и A4 не назван, решение 15
  требует итоговой проверки на PostgreSQL, и вторая часть — то же умение
  на другом движке, а не установка движка.

## Разбивка A2 на 5 частей — `step-01.md`–`step-05.md`, все написаны

Найдено при подготовке к написанию базовых шагов M3: часть 1 blueprint
уже не оставляет свободного умения для `SELECT`/`WHERE`/`GROUP BY`/
`INNER JOIN` — A1 (CTE+оконные функции), A3 (retention) и A4
(оптимизация) уже закрыты `step-06.md`–`step-08.md`, A5 закрыт в M1.
Правило скилла («если шаг не ложится ни на одно из 30 умений — шага не
существует, писать его нельзя») требует легального ID в шапке каждого
шага. Решение — тот же приём, что уже применён в M2 к D1/D2/D3
(`program/M2/step-01.md`–`step-07.md`): A2 разбита на 5 частей,
`step-05.md` (уже написан) стал частью 5 из 5.

Разбивка не произвольная — части 3–4 напрямую строят понятийный
фундамент, на котором держится сам баг A2 (несовпадение `INNER JOIN`
против `LEFT JOIN` в обращении с непарными строками), части 1–2 —
более общая предпосылка (без базового `SELECT`/`WHERE`/`GROUP BY`
читать и писать запрос вообще невозможно, это не специфично для A2, но
без этого части 3–5 не имеют смысла):

| Часть | Шаг | Содержание | Связь с финальным багом A2 |
|---|---|---|---|
| 1 из 5 | `step-01.md` | `SELECT`/`WHERE`/`ORDER BY` на одной таблице | Общая предпосылка: без чтения условия `WHERE` не опознать, что оно фильтрует |
| 2 из 5 | `step-02.md` | `GROUP BY`/агрегаты (`COUNT`/`SUM`/`AVG`) | Общая предпосылка: разница `WHERE` (до группировки) и `HAVING` (после) |
| 3 из 5 | `step-03.md` | `INNER JOIN` на 2–3 таблицах | Прямая: показывает, что `INNER JOIN` **отбрасывает** непарные строки — контрастный случай к части 4 |
| 4 из 5 | `step-04.md` | Корректный `LEFT JOIN` (без бага) | Прямая: показывает, что `LEFT JOIN` **сохраняет** непарные строки, справа — `NULL` — ровно то поведение, которое ломает баг части 5 |
| 5 из 5 | `step-05.md` | Диагностика бага `WHERE` вместо `ON` (написан) | Сам баг — `WHERE` на `NULL` тихо возвращает `LEFT JOIN` к поведению части 3 |

Части 1–4 написаны отдельным заходом (`program/pilot-report.md`, заход
5), после частей A1/A3/A4. Критерий готовности каждой части —
собственный числовой порог на том же
сквозном датасете (`schema.sql`+`seed.sql`), а не переиспользование
критерия части 5 («4 из 5 подстав») — так же, как части D2 в M2 имеют
разные подмножества из 10 задач, а не один и тот же критерий,
скопированный 3 раза.

## Схема — 5 таблиц, зафиксирована на весь модуль

`program/M3/data/schema.sql`, `seed.sql`. Таблицы:

| Таблица | Строк | Роль |
|---|---|---|
| `customers` | 12 | Клиенты. Использована в `step-05.md`, определение колонок не менять — переопределение сломает уже написанный шаг |
| `orders` | 19 | Заказы, статусы `completed`/`cancelled`/`pending`. Использована в `step-05.md`, не менять |
| `products` | 6 | Каталог: 2 подписки, 2 надстройки, 2 услуги. Пока не используется ни одним написанным шагом |
| `order_items` | 23 | Позиции заказа. `SUM(quantity*unit_price)` по `order_id` равен `orders.amount` — проверено скриптом (см. `data/README.md`) |
| `payments` | 13 | По одному платежу на каждый `completed`-заказ (13 из 19); `cancelled`/`pending` — платежа нет. Использована в `step-04.md` (`LEFT JOIN orders→payments`, `COUNT(*)` против `COUNT(payment_id)`) — тот же класс бага, что в `step-05.md` на `customers→orders`, но другой пример |

Контрольные суммы (проверены выполнением, не на глаз):
- `SUM(orders.amount) WHERE status='completed'` = 8600.00.
- Для каждого `order_id`: `SUM(order_items.quantity*order_items.unit_price)` = `orders.amount`, расхождение 0 по всем 19 заказам.
- `payments` существует ⟺ `orders.status='completed'`, проверено по всем 19 заказам, несовпадений 0.

Будущие шаги M3 могут добавлять данные (новых клиентов, заказы), но не
менять уже существующие строки — иначе числа в `step-05.md` (7 строк в
сломанном запросе, 12 в исправленном, сумма 8600.00) перестанут быть
верными.

## Порядок загрузки данных по шагам

Правило — решение 20 `design/decisions.md`. Этот раздел — **план**, а не
инструкция к запуску: сам `step-00.md` ничего не загружает и команд
учащемуся не даёт (он служебный файл, см. шапку). Каждый файл данных
загружается ровно в том шаге, где его данные нужны впервые; команда
загрузки написана в самом этом шаге.

Таблица «какой шаг что загружает» — `program/M3/data/README.md`, раздел
«Порядок загрузки»; здесь она не дублируется, чтобы не разошлась в двух
местах. Она же — вход для `tools/check_consistency.py`. Ниже — только
обоснование, почему порядок именно такой.

**Почему порядок именно такой, а не «загрузить всё сразу в начале
модуля».** Эталоны шага 06 (`a1_task*.csv`, 13 строк) посчитаны на базе
из `schema.sql` + `seed.sql`. После `retention_seed.sql` те же запросы
возвращают 360 строк — 347 добавленных заказов тоже `completed`. Если бы
`retention_seed.sql` загружался раньше шага 06, учащийся провалил бы все
6 критериев A1, не сделав ни одной ошибки. Обратная зависимость — у
шага 08: `generate_activity_log.py` берёт `customer_id` из таблицы
`customers`, поэтому контрольная сумма из `reference_answers.md`
(`93fdb9a1…`) воспроизводится только тогда, когда `retention_seed.sql`
уже загружен. Проверено прогоном: на базе без `retention_seed.sql` тот
же генератор даёт `9903c5f7…`. Оба ограничения выполняются
одновременно ровно при порядке из таблицы в `data/README.md`.

Сверку эталонов с этим порядком делает `tools/check_consistency.py`:
для каждого шага, у которого есть эталонные CSV, он собирает базу из
накопительного списка файлов до этого шага включительно, выполняет
эталонный запрос из `data/reference_answers.md` и сверяет с CSV.

## SQL-движок

Решение 15 (`design/decisions.md`): модуль начинается на SQLite
(поддерживает CTE и оконные функции — проверено на версии 3.50.4),
ближе к концу модуля — переход на PostgreSQL.

**Предусловие модуля — консольный клиент `sqlite3`.** Ни Windows, ни
поставка Python его не содержат: библиотека `sqlite3`, которой
пользуются скрипты программы, и консольный клиент — разные вещи.
Формулировка решения 15 «ставится в 0 действий» относилась к библиотеке
и для клиента неверна; поправка внесена в само решение 2026-07-30.
Фактическая цена — одна распаковка архива без прав администратора.
Инструкция — `step-01.md`, 1.3 (здесь её нет намеренно: `step-00.md`
объявляет, но не инструктирует, решение 20).

**Момент перехода определён: после `step-08.md`** — решение 19
`design/decisions.md`, принято при написании шагов 09–12. Шаги
`step-01.md`–`step-08.md` целиком на SQLite, шаги `step-09.md`–
`step-12.md` — на PostgreSQL, включая повторный прогон A1, A3 и A4 на
перенесённой базе.

Проверено прогоном на обоих движках (SQLite 3.45.1 и PostgreSQL 17.6,
одна машина, тот же датасет): найдено 9 расхождений диалектов на уже
написанных запросах модуля — 8 падают с ошибкой, 1 (`LIKE` и регистр)
не падает и возвращает 0 строк вместо 360. Полная таблица — `step-10.md`,
1.4; эталонные числа — `data/reference_answers.md`, раздел «Переход на
PostgreSQL».

## Зависимости модуля

`step-01.md`–`step-04.md` (базовый `SELECT`/`WHERE`/`ORDER BY`,
`GROUP BY`/агрегаты, `INNER JOIN`, корректный `LEFT JOIN`) написаны и
идут по порядку перед `step-05.md`. Установка базы (`schema.sql` +
`seed.sql`) описана в `step-01.md` как первом шаге модуля; шаги 02–06
на неё ссылаются, не повторяя инструкцию заново. Остальные загрузки —
по таблице раздела «Порядок загрузки данных по шагам» выше.

## Дополнение (заход про A1/A3/A4): данные сверх исходных 5 таблиц

Правило раздела 1.3 скилла curriculum-design разрешает будущим шагам
**добавлять** к декларации модуля то, чего не хватало на момент
написания шага 0 — это ровно тот случай, а не откат назад: при
написании `step-06.md`/`step-07.md`/`step-08.md` обнаружилось, что три
из четырёх умений модуля физически не проверяются на 5 таблицах из
12 строк `customers`/19 строк `orders`, объявленных выше. Ниже — что
добавлено, для каждого умения отдельно; ни одна из существующих строк
`customers`/`orders`/`products`/`order_items`/`payments` не изменена и
не удалена, числа `step-05.md` (7/12 строк, сумма 8600.00) не
затронуты.

### A1 (`step-06.md`) — новых данных не требует

Битва из 6 задач CTE+оконная функция решается на тех же 5 таблицах,
что и `step-05.md`. Новых строк, таблиц или файлов не добавлено.

### A3 (`step-07.md`) — расширение `customers`/`orders`, без новых таблиц

Когортный retention на 12 когортах не считается на 12 клиентах с
заказами в диапазоне двух месяцев (май–июнь 2026) — когорт получилось
бы 2, а не 12. Добавлено (`program/M3/data/generate_retention.py`,
детерминированный генератор, `SEED = 20260803`, третья категория
датасета по разделу 1.3 скилла — сквозной модуль вне проектной
привязки, синтетика с генератором):

- 240 новых строк `customers`, `customer_id` 101–340 (не пересекаются с
  1–12) — только `name`/`city`, определение колонок то же самое.
- 347 новых строк `orders`, `order_id` 20001+ (не пересекаются со
  101–119) — 20 клиентов на когорту, когорта = календарный месяц
  первого заказа клиента, помесячно с 2025-01 по 2025-12 (12 когорт).
  У части клиентов (по независимым вероятностям, заданным в генераторе
  и убывающим по когортам) есть второй заказ в следующем месяце и/или
  третий — ещё через месяц: это и есть сигнал retention на горизонтах
  M1 и M2, оба используются в `step-07.md` (M1 — разобранный пример,
  M2 — задание).
- Файл `program/M3/data/retention_seed.sql` — сгенерированный вывод,
  закоммичен (347 + 240 строк текстом — не превышает лимит `>50 МБ`
  из I1). Загружается поверх `seed.sql` — и не раньше `step-07.md`, где
  когорты нужны впервые (раздел «Порядок загрузки данных по шагам»;
  команда — в самом `step-07.md`, 1.3).
- `order_items`/`payments` для этих клиентов не заполняются: A3
  проверяет только когортный retention по `orders`, не выручку по
  позициям — контрольная сумма `order_items` из раздела выше остаётся
  верна только для исходных 19 заказов, как и было.

### A4 (`step-08.md`) — новая таблица `activity_log`, НЕ коммитится

Оптимизация запроса (40 с → <5 с) требует таблицы, на которой
неиндексированный запрос реально медленный — 5 таблиц модуля (максимум
23 строки в `order_items`) для этого малы на порядки. Добавлена
таблица:

```sql
CREATE TABLE activity_log (
    log_id      INTEGER PRIMARY KEY,
    customer_id INTEGER NOT NULL,
    event_type  TEXT NOT NULL,
    event_date  TEXT NOT NULL
);
```

Заполняется `program/M3/data/generate_activity_log.py`
(детерминированный, `SEED = 20260804`, по умолчанию 4 800 000 строк,
второй необязательный аргумент CLI — переопределить число строк) —
учащийся запускает скрипт сам, локально, в `step-08.md`, то есть после
`step-01.md` (`schema.sql` + `seed.sql`) и `step-07.md`
(`retention_seed.sql`). Порядок обязателен, а не желателен: генератор
берёт `customer_id` из таблицы `customers`, и контрольная сумма ниже
воспроизводится только на полном наборе из 252 клиентов. Сама
таблица (в файле базы — около 150 МБ на референсной машине автора при
значении по умолчанию) в репозиторий не коммитится: превышает лимит
`>50 МБ` из I1 части 1 blueprint. В репозитории — только генератор
(несколько килобайт), как для любого синтетического датасета
программы (аналогично `program/M0/data/generate_orders_log.py`,
`program/M2/data/generate_sales_extract.py`). `activity_log` не
связана внешним ключом с `customers`/`orders` — это отдельная
фактовая таблица только для упражнения на оптимизацию, не часть
основной схемы модуля.

**Контрольная точка (правило раздела 1.3 curriculum-design для
локально генерируемых, не коммитящихся датасетов).** Эталоны A4
считаются на копии автора; у учащегося своя копия того же генератора
даёт то же самое только при совпадении версии скрипта и порядка
загрузки (`step-01.md` → `step-07.md` → генератор в `step-08.md`;
на базе без `retention_seed.sql` тот же скрипт даёт другой хэш —
`9903c5f7…` вместо `93fdb9a1…`, проверено прогоном). Скрипт сам
печатает после генерации число строк и sha256
первых 1000 строк (`ORDER BY log_id`) — учащийся сверяет оба числа с
`program/M3/data/reference_answers.md`, раздел «A4», **до** того, как
приступать к задачам `step-08.md`. sha256 не зависит от того, сколько
всего строк сгенерировано (переопределение объёма для порога из 1.5
`step-08.md` не портит контрольную точку — расходится только число
строк, которое в этом случае и должно отличаться от 4 800 000).

### Итоговый список файлов `program/M3/data/` после этого захода

| Файл | Роль | В git? |
|---|---|---|
| `schema.sql` | 5 таблиц модуля для SQLite (без изменений) | да |
| `seed.sql` | исходные 12 клиентов / 19 заказов и т.д. (без изменений) | да |
| `retention_seed.sql` | +240 клиентов / +347 заказов для A3 | да (347+240 строк, не превышает лимит) |
| `generate_retention.py` | генератор `retention_seed.sql`, детерминированный | да |
| `generate_activity_log.py` | генератор `activity_log`, детерминированный | да |
| `activity_log` (таблица в базе учащегося) | 4.8 млн строк для A4 | **нет** — генерируется локально, не коммитится (>50 МБ) |
| `reference_answers.md` | эталонные числа A1/A3/A4 и перехода на PostgreSQL, из реального прогона | да |
| `schema_pg.sql` | те же 5 таблиц для PostgreSQL, типы выбраны осознанно (`step-09.md`) | да |
| `export_for_postgres.py` | выгрузка таблиц `m3.db` в CSV для `\copy` | да |
| `pg_csv/` (у учащегося) | CSV для переноса, создаётся скриптом | **нет** — промежуточный результат, с `activity_log` около 160 МБ |

Число заказов в `retention_seed.sql` — **347**, не 313: 366 строк
`orders` после загрузки минус 19 исходных. В первой редакции этой
таблицы стояло «+313», при том что абзац выше по тексту называл верные
347; расхождение найдено прогоном (заход 6) и исправлено здесь.

**Область действия эталонов A1.** Файлы `a1_task*.csv` посчитаны на базе
`schema.sql` + `seed.sql` — это ровно то состояние, в котором база
находится на `step-06.md` по таблице раздела «Порядок загрузки данных по
шагам»: `retention_seed.sql` загружается шагом позже. После него те же
запросы возвращают 360 строк вместо 13 — 347 добавленных заказов тоже
`completed`; это свойство датасета, а не движка (проверено на SQLite и
PostgreSQL одинаково).

Шаги `step-09.md`–`step-12.md` работают уже на полной базе, поэтому в
`step-10.md` те же 6 запросов выполняются с фильтром `order_id < 200` —
там это не искусственное условие, а часть задачи шага: сверить перенос
кода на PostgreSQL с уже известным результатом. С фильтром все 6 файлов
сходятся на обоих движках построчно.
