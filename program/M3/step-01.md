# M3.01. Отбирает и сортирует строки одной таблицы: SELECT, WHERE, ORDER BY

Умение: A2 (часть 1 из 5 — общая предпосылка. Часть 5 — `step-05.md`,
уже написана — финальный, интегрированный баг `LEFT JOIN` с фильтром в
`WHERE`. Части 1–4 строят к ней фундамент: эта часть — без точного
понимания, что именно отбирает `WHERE` и в каком порядке вычисляются
условия, невозможно опознать, что фильтр в `WHERE` вместо `ON` меняет
поведение `JOIN`, а не просто «дополнительно фильтрует» результат)
Модуль: M3
Требуется до этого: M0 (терминал), M1 (постановка задачи), M2 (таблицы)
— это первый шаг модуля M3, установка базы данных описана здесь целиком
(`program/M3/step-00.md`, часть 3.1/3.2 blueprint)
Время: 3–4 ч (оценка проектная, не измерена)

## 1.1. Цель и связь с умением

После шага вы по описанию задачи на естественном языке пишете `SELECT`
с условием `WHERE` (в том числе с несколькими условиями через `AND`/
`OR`) и явной сортировкой `ORDER BY`, точно зная, какие строки вернёт
запрос, не проверяя результат вслепую. Часть 1 из 5 умения A2 — общая
предпосылка: без этого невозможно ни писать корректные условия для
частей 2–5, ни опознать баг `WHERE`/`ON` из `step-05.md`.

## 1.2. Теория своими словами

`SELECT` возвращает набор колонок из таблицы. `WHERE` отбирает строки до
того, как они попадут в результат: условие считается отдельно для каждой
строки, и строка либо проходит, либо нет. Никакой памяти о соседних
строках у него при этом нет — это пригодится помнить дальше.

Дальше место, на котором спотыкаются почти все. При нескольких условиях
через `AND` и `OR` приоритет такой же, как у `*` и `+` в арифметике:
сначала `AND`, потом `OR`. То есть `A OR B AND C` читается как
`A OR (B AND C)`, а вовсе не как `(A OR B) AND C`.

Последствие простое. Задумали ограничить условием «C» обе части — и «A»,
и «B», — а без скобок оно применилось только к «B». Синтаксической
ошибки нет, красным ничего не подсвечено, запрос отработал и вернул
правдоподобные строки. Просто не те.

`ORDER BY` задаёт порядок вывода явно. Без него СУБД не обязана ничего:
она вправе вернуть строки в любом порядке. На маленькой таблице вывод
обычно совпадает с порядком вставки, и это подкупает — кажется, что
порядок есть. Он есть ровно у этого движка, для этого запроса, на этот
момент. Добавили строку — и совпадение кончилось.

## 1.3. Разобранный пример на сквозном датасете

Схема: `customers` (12 строк), `orders` (19 строк, статусы `completed`/
`cancelled`/`pending`) — `program/M3/data/schema.sql`, `seed.sql`.

### Шаг 0. Рабочий каталог

Все команды этого шага и всех следующих шагов модуля выполняются из
каталога `program/M3/data` вашего клона репозитория. Файлы `schema.sql` и
`seed.sql` лежат там, и команды ниже обращаются к ним по имени, без пути.
Из корня репозитория те же команды не сработают — клиент создаст пустую
базу и сообщит, что файла `schema.sql` нет.

База `m3.db` будет создана в том же каталоге. Она в репозиторий не
коммитится (`.gitignore`), как и всё, что вы генерируете локально.

### Шаг 1. Консольный клиент `sqlite3`

Он нужен до первой команды и его на машине может не быть. Проверьте:

```
$ sqlite3 --version
3.53.4 2026-07-24 19:02:57 bf7c7f30031888f4e796e429ab3978879485813aaca6f641c7b33e4e09459bcc (64-bit)
```

Если вместо номера версии выводится сообщение о том, что имя `sqlite3` не
распознано, — клиента нет. Его нет ни в Windows, ни в поставке Python, и
это не то же самое, что модуль `sqlite3` из стандартной библиотеки
Python: библиотекой считаются эталоны программы, а вы весь модуль будете
писать запросы в консольном клиенте. Это разные программы, и одна не
заменяет другую. Обходного пути через `python -m sqlite3` на этой машине
тоже нет: встроенный REPL появился в Python 3.12, здесь 3.11.9.

Установка — распаковка архива, без установщика и без прав
администратора:

1. Откройте [Precompiled Binaries for Windows](https://www.sqlite.org/download.html)
   и скачайте `sqlite-tools-win-x64-<версия>.zip` (около 6.6 МБ).
2. Распакуйте в любой каталог, куда у вас есть право на запись —
   например, `D:\sqlite-tools`. Внутри архива четыре `.exe` лежат плоско,
   без вложенной папки; нужен из них только `sqlite3.exe`.
3. Допишите этот каталог в `PATH` на уровне пользователя (не системы —
   для системного нужны права администратора). После этого команда
   `sqlite3` заработает в **новом** окне терминала; в уже открытом старое
   значение `PATH` останется.
4. Повторите `sqlite3 --version`. Клиент должен напечатать номер версии
   и завершиться.

Версия у вас будет новее той, на которой посчитаны эталоны модуля (3.45.1
— версия библиотеки, встроенной в Python автора). Для запросов этого
модуля разницы не ожидается: CTE поддержаны с 3.8.3, оконные функции — с
3.25. Если какой-то эталон не сойдётся, версия клиента — последнее, что
стоит проверять, а не первое.

### Шаг 2. Загрузка схемы и данных

Этот шаг — единственное место, где загружаются эти два файла; шаги 02–06
работают на том же состоянии базы и ничего в неё не добавляют:

```
$ sqlite3 m3.db ".read schema.sql"
$ sqlite3 m3.db ".read seed.sql"
```

`.read`, а не `<`-редирект: перенаправление ввода в PowerShell работает
не так, как в bash; `.read` — внутренняя команда самого `sqlite3`,
поэтому ведёт себя одинаково в любом шелле.

**Повторный запуск.** Эти две команды не идемпотентны. Запущенные второй
раз поверх уже созданной `m3.db`, они выдают по ошибке на каждую таблицу
и ничего не загружают:

```
Parse error near line 6 of schema.sql: table customers already exists
```

Это не поломка базы: первая загрузка осталась в силе, вторая не
применилась. Но если вы дошли до этого места, потеряв уверенность в том,
что лежит в базе, — соберите её заново с нуля. Удалите файл `m3.db`
любым способом (в проводнике, в редакторе, командой вашего шелла) и
повторите обе команды. Файл базы — обычный файл, отдельной процедуры
удаления у SQLite нет, сервер останавливать не требуется.

Третий файл данных модуля, `retention_seed.sql`, сейчас **не**
загружается: он нужен только с `step-07.md` и там же загружается.
Загруженный раньше, он добавит 347 заказов в `orders`, и эталонные
числа шагов 01–06 (13 `completed`-заказов, 19 заказов всего) перестанут
сходиться. Полная таблица порядка загрузки — `step-00.md`, раздел
«Порядок загрузки данных по шагам».

### Шаг 3. Режим вывода, в котором результат сверяется с эталоном

Начиная со `step-06.md` критерии готовности требуют сверить вывод вашего
запроса с эталонным CSV из `program/M3/data`. Ни один режим вывода
клиента по умолчанию для этого не годится, и это стоит настроить один раз
сейчас, а не на шаге 06.

Клиент печатает результат по-разному в зависимости от того, как его
запустили. Запущенный интерактивно (`sqlite3 m3.db`, дальше запросы в
приглашении `sqlite>`), он рисует таблицу рамками:

```
╭──────────┬───────────┬────────╮
│ order_id │  status   │ amount │
╞══════════╪═══════════╪════════╡
│      111 │ pending   │  390.0 │
╰──────────┴───────────┴────────╯
```

Тот же запрос, переданный аргументом (`sqlite3 m3.db "SELECT ..."`),
печатает значения через `|` и без заголовка:

```
111|pending|390.0
```

Эталон — CSV с запятой и заголовком. Ни рамки, ни `|` с ним не
сравниваются. Нужный режим включается двумя точечными командами, а
результат отправляется в файл третьей:

```
sqlite> .mode csv
sqlite> .headers on
sqlite> .once my_task1.csv
sqlite> SELECT order_id, status, amount FROM orders WHERE status = 'pending';
```

`.once` направляет в файл **только следующий** запрос, дальше вывод снова
идёт на экран. Именно `.once`, а не перенаправление вывода шелла
(`> my_task1.csv`): клиент пишет конец строки сам, а стандартный вывод
Windows добавляет к нему свой, и в файле получается двойной перевод
строки. Через `.once` клиент пишет файл сам, и этого не происходит —
проверено прогоном.

Сравнивать файлы глазами не нужно и не следует: побайтово они не
совпадут, даже когда запрос верен. В режиме `csv` клиент берёт в кавычки
любое значение с пробелом или кириллицей (`Київ` выводится как `"Київ"`),
а в эталоне кавычек нет — по правилам CSV это одно и то же значение.
Сверку делает готовый скрипт `compare_csv.py` из того же каталога
(запускаете, не пишете — умение I2 из M0):

```
$ python compare_csv.py my_task1.csv a1_task1_running_total.csv
СОВПАДАЕТ: 13 строк, 5 колонок, расхождение 0.
```

При расхождении он печатает первую разошедшуюся строку и колонку:

```
РАСХОЖДЕНИЕ в строке 2, колонка 'running_total': у вас '850.0', в эталоне '2050.0'
```

На этом шаге сверять нечего — эталонных CSV у шагов 01–05 нет, их
критерии проверяются счётом строк. Режим настроен здесь потому, что здесь
единственное место модуля, где ставится и настраивается инструмент.

Задача: «Показать заказы, которые `pending` или `cancelled`, но только
с суммой больше 300».

Написанный «в лоб», без скобок, запрос:

```sql
SELECT order_id, status, amount
FROM orders
WHERE status = 'pending' OR status = 'cancelled' AND amount > 300;
```

Результат — 4 строки:

| order_id | status | amount |
|---|---|---|
| 111 | pending | 390.0 |
| 116 | pending | 260.0 |
| 117 | cancelled | 320.0 |
| 118 | cancelled | 410.0 |

Строка `116` (`pending`, `260.0`) не должна была попасть в результат —
`260 ≤ 300`. Причина: `AND` связал `amount > 300` только с
`status = 'cancelled'`, а условие на `pending` осталось без ограничения
по сумме — запрос фактически выполнил «`pending`, ЛЮБАЯ сумма, ИЛИ
`cancelled` с суммой больше 300».

Исправление — скобки вокруг `OR`-части:

```sql
SELECT order_id, status, amount
FROM orders
WHERE (status = 'pending' OR status = 'cancelled') AND amount > 300;
```

Результат — 3 строки:

| order_id | status | amount |
|---|---|---|
| 111 | pending | 390.0 |
| 117 | cancelled | 320.0 |
| 118 | cancelled | 410.0 |

Письменное объяснение (эталонная форма для 1.4): «`AND` вычисляется
раньше `OR`, поэтому `amount > 300` связался только с `cancelled`, а не
с обеими частями условия. Из-за этого заказ 116 (`pending`, 260)
попал в результат, хотя не проходит условие по сумме. Фикс — скобки
вокруг `status = 'pending' OR status = 'cancelled'`.»

## 1.4. Задание

Дан другой запрос на той же схеме, с той же ошибкой (отсутствие
скобок), решающий задачу «заказы `completed` или `pending` с суммой
больше 500»:

```sql
SELECT order_id, status, amount
FROM orders
WHERE status = 'completed' OR status = 'pending' AND amount > 500;
```

1. Выполните запрос как есть, запишите результат (число строк).
2. Сравните с полным списком `completed`-заказов (`SELECT COUNT(*) FROM
   orders WHERE status = 'completed'`) и найдите, сколько строк в
   результате нарушают условие «сумма больше 500».
3. Исправьте запрос скобками.
4. Объясните причину в ≤3 предложениях (эталонная форма — 1.3).

## 1.5. Критерий готовности

- В сломанном запросе найдено, что он возвращает 13 строк — ровно все
  `completed`-заказы независимо от суммы (сравнение с `SELECT COUNT(*)
  FROM orders WHERE status='completed'` = 13, расхождение 0).

  Почему ровно 13, а не больше: правая часть условия
  (`status = 'pending' AND amount > 500`) на этих данных не даёт ни одной
  строки. `pending`-заказов два — 111 (390.0) и 116 (260.0), — и ни один
  не проходит `amount > 500`. То есть сломанный запрос вырождается в
  «все `completed`», и совпадение с `COUNT(*)` — следствие датасета, а не
  свойство ошибки. Это названо здесь вслух, потому что критерий от этого
  зависит: если правка `seed.sql` когда-нибудь добавит `pending`-заказ с
  суммой выше 500, число перестанет быть 13, и причина будет не в
  вас. Числа 111/390.0 и 116/260.0 — из раздела «Устройство данных»
  `program/M3/data/README.md`.
- Найдены все 3 строки, нарушающие условие `amount > 500`: `order_id`
  103 (430), 112 (455), 114 (330).
- Исправленный запрос возвращает ровно 10 строк, без этих трёх.
- Объяснение — не больше 3 предложений, называет приоритет `AND` перед
  `OR` или прямо указывает на отсутствие скобок как причину.
- Норматив: ≤30 минут (часть 1 из 5 проще финальной части A2 в
  `step-05.md`, порог снижен пропорционально; решение пилота, не строка
  источника).

**Не сошлось — не переделывайте вслепую:** раздел 1.6 разбирает 4 ошибки
этого шага, каждую с признаком, по которому её видно в своём результате.

## 1.6. Типичные ошибки

**Ошибка:** пишете условие с `AND`/`OR` без скобок, рассчитывая, что оно
прочитается слева направо, как в обычном тексте.
**Почему выглядит правильно:** запрос синтаксически верен, выполняется
без ошибок и возвращает строки.
**Почему неправильно:** строк возвращается больше, чем нужно. На примере
1.3 в результат попадает лишняя строка 116 (260 ≤ 300); на задании 1.4 —
сразу три лишних (103, 112, 114), не проходящие условие по сумме.
**Как правильно:** ставить скобки вокруг части с `OR`, если
дополнительное условие должно ограничивать её так же, как вторую часть.

**Ошибка:** полагаетесь на порядок строк без `ORDER BY` — считаете,
например, что первой в выводе `cancelled`-заказов идёт хронологически
самая ранняя.
**Почему выглядит правильно:** на этой таблице так и есть. 4 строки
`cancelled` выводятся в порядке вставки: 106, 110, 117, 118.
**Почему неправильно:** по дате порядок другой — 117 (2026-05-07), 110
(2026-05-22), 106 (2026-06-01), 118 (2026-06-09). Взяли «первую строку»
без `ORDER BY order_date` и назвали её самой ранней — получили 106 вместо
117. Совпадение с порядком вставки не гарантия, а состояние таблицы на
сегодня; оно ломается при первой же вставке или удалении.
**Как правильно:** порядок важен — пишется `ORDER BY` по нужной колонке.
Явно, каждый раз.

**Ошибка:** сравниваете строку без учёта регистра: `WHERE status =
'Completed'`.
**Почему выглядит правильно:** слово написано верно, только с большой
буквы — выглядит как стилевая деталь.
**Почему неправильно:** сравнение строк оператором `=` в SQLite
регистрозависимо. `WHERE status = 'Completed'` возвращает 0 строк —
вместо 13, которые возвращает `WHERE status = 'completed'`.
**Как правильно:** сверять точное значение, как оно записано в данных
(здесь — везде нижний регистр), а не полагаться на то, что выглядит
эквивалентным на глаз.

**Ошибка:** путаете `>` и `>=` на границе — ждёте, что заказ ровно на
300 пройдёт условие `amount > 300`.
**Почему выглядит правильно:** в разговоре «больше 300» и «не меньше
300» звучат почти одинаково, и в постановке задачи это редко уточняют.
**Почему неправильно:** заказ 106 (`cancelled`, 300.0) под `amount > 300`
не подходит: 300 не больше 300. Под `amount >= 300` подошёл бы. В примере
1.3 это ровно та строка, которой не хватает до 4 `cancelled`-заказов.
Получили 3 вместо 4 — граница операторов проверяется первой.
**Как правильно:** читать формулировку буквально. «Больше» — `>`, «не
меньше» и «от» — `>=`.

## 1.7. Честная оценка времени

3–4 ч. Оценка проектная, не измерена. Часть общего бюджета модуля M3
(60–80 ч) — построчная сумма по всем 12 шагам модуля посчитана в
`step-00.md`, не в этом файле.

Оценка **не поднята** после того, как в 1.3 добавились установка клиента
и настройка режима вывода. Установка измерена ручным гейтом
(`research/tools-gate.md`, 1.5): скачать 6.6 МБ, распаковать, дописать
`PATH` — минуты, а не час, и они помещаются в разницу между 3 и 4 ч.
Поднимать цифру, не имея замера, значит заменить одну догадку другой
(решение 24 `design/decisions.md`). Разрешит вопрос `research/self.md` —
фактический лог прохождения, на 2026-07-31 пустой.

## 1.8. Вопрос уровня собеседования

**Вопрос:** «Вы написали `WHERE status = 'pending' OR status =
'cancelled' AND amount > 300` и получили на одну строку больше, чем
ожидали. С чего начнёте искать причину?»

**Эталон (ответ покрывает ≥3 из 4 пунктов):**
1. Проверяет приоритет `AND`/`OR` в условии — `AND` вычисляется первым.
2. Явно расставляет скобки и сравнивает результат до/после.
3. Смотрит на сами строки результата (не только на количество), чтобы
   найти конкретную лишнюю строку, а не гадать по сумме или счётчику.
4. Предлагает конкретный фикс — скобки вокруг `OR`-части, а не
   «переписать запрос иначе».
