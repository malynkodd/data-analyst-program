# Данные проекта P2 («Почему упала конверсия в первую оплату»)

## Статус этого файла

Полная декларация — `program/P2/step-00.md`. Датасет — синтетика с
генератором (часть 5 blueprint, «Синтетические данные — P1, P2, P5»):
события пользовательского уровня для сервиса подписки не публикуются
нигде в реальном мире, а те, что есть в открытых источниках,
агрегированы до уровня, где симпсоновский парадокс не воспроизвести.

**Сами данные в репозиторий не коммитятся** (решение 29): папка `raw/`
перечислена в корневом `.gitignore`. В git лежат генератор, скрипт
эталонов и один эталонный CSV.

## Файлы

| Файл | Роль | В git? |
|---|---|---|
| `generate_p2.py` | Генератор. Пишет `raw/signups.csv`, `raw/payments.csv`. `SEED = 20260821`. | да |
| `reference_p2.py` | Считает эталоны заново: дубли платежей, задвоенные аккаунты, помесячную конверсию по каналу (симпсоновский парадокс), обе трактовки тренда, ретеншн-грид. Пишет `ref_retention.csv`. | да |
| `reference_answers.md` | Контрольная точка и все эталонные числа, с определением каждого (решение 30). | да |
| `ref_retention.csv` | 12 строк — по одной на когорту месяца первого платежа, `m0`…`m5`. | да |
| `raw/signups.csv` | `user_id, email, signup_date, channel`. | **нет** — создаётся генератором |
| `raw/payments.csv` | `payment_id, user_id, payment_ts, amount` — включает дубли ретраев и продления подписки. | **нет** — создаётся генератором |

## Проверка строк

Числа сверяются `tools/check_consistency.py` с фактическими файлами.
Здесь только коммитящийся эталон; `raw/` в репозитории не лежит.

| Файл | Строк |
|---|---|
| `ref_retention.csv` | 12 |
