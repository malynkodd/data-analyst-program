# Эталонные ответы P3 (дашборд для закупівель категорії «продукти харчування»)

Считает `reference_p3.py` по снапшоту `data/snapshot/`, независимо от
внутренних списків скриптов выгрузки (правило двойного авторства).

## Контрольная точка снапшота

Дата выгрузки: **2026-08-21**. Источники: Prozorro API
(`public.api.openprocurement.org/api/2.5/tenders`), API НБУ
(`bank.gov.ua/NBU_Exchange/exchange_site`).

```
tenders_food.csv: 487 строк, sha256 71b860624eba13eec50e740661c2919c085a8a6923e057d84ef69227ddf56784
exchange_rates.csv: 109125 строк, sha256 c1d023e6561a41aba31cfda75ca886bad129c4a39865ef9c6a0c2365a7ea403d
```

Расхождение — учащийся смотрит не свой снапшот, а другой файл; сверять
дальше нечем, пока не подставлен именно этот.

## Разбор закрыт турникетом

Ответы, ранги, выбранные трактовки и разбор дефектов вынесены в
`answers.md` и закрыты турникетом (`tools/vault.py`, решения 51 и 53).
Причина названа независимой проверкой
`audit/independent-final-check-2026-08-24.md`, блокер 8.2: проект — это
единственное доказательство, что умения работают вместе, и приёмка, где
ответ лежит рядом открытым файлом, доказывает только умение читать.

Открывается после сохранённой попытки, проход пишется в
`research/attempts.md`:

```
> python tools\vault.py open program\P3\data\answers.md --attempt program\P3\work\<ваш файл>
```

Открытым здесь остаётся ровно то, без чего проект не начать: контрольная
точка датасета выше и два снапшота `ref_tenders_period_*.csv` — они
вход проекта, а не его ответ.
