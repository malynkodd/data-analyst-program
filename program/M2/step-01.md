# M2.1. Приводит дату и сумму заказа к рабочему виду на полной выгрузке

Умение: D1 (часть 1 из 3: очистка `order_date_raw` и `amount_raw` —
даты в двух форматах и число, записанное текстом, включая знак возврата;
часть 2 — очистка категорий, `step-02.md`; часть 3 — сводная с 3 срезами
за 90 минут с нуля, `step-03.md`)
Модуль: M2
Требуется до этого: M0 целиком (I2 — работа с файлами и терминалом;
`build_reference.py` запускается тем же способом, что скрипты M0)
Время: 5.5–7.0 ч (оценка проектная, не измерена; калибровка —
`research/self.md`)

## 1.1. Цель и связь с умением

После шага вы превращаете две "грязные" колонки полной выгрузки
(`order_date_raw` — 20 000 строк, два формата дат; `amount_raw` — сумма
как текст в 4 форматах, включая возвраты как отрицательные значения в той
же колонке) в три рабочие колонки: `order_date_clean` (настоящая дата),
`quarter` (квартал, выведенный из даты) и `amount_clean` (настоящее
число, со знаком). Это первая часть умения D1 из части 1 blueprint: «за
90 минут из грязной выгрузки на 20 000 строк делает сводную с 3
срезами» — здесь не 90 минут и не полный цикл, а отработка двух из
четырёх дефектов на не ограниченном по времени материале. Итоговая
проверка на время — в `step-03.md`.

## 1.2. Теория своими словами

В шаге две большие темы: дата, записанная двумя разными способами, и
сумма, которая на самом деле текст. Обе решаются одним приёмом — не
доверять автоматике, а разобрать строку руками. Дальше по порядку.

Формулы даны английскими именами функций (`IF`, `OR`, `SUBSTITUTE`,
`VALUE`, `NUMBERVALUE`, `LEFT`, `MID`, `RIGHT`, `DATE`, `SEARCH`, `TRIM`,
`YEAR`, `MONTH`, `ROUNDUP`). В русской и украинской локализации Excel те
же функции называются `ЕСЛИ`, `ИЛИ`, `ПОДСТАВИТЬ`, `ЗНАЧЕН`, `ЧЗНАЧ` и
так далее — меняются названия, логика остаётся. В ячейку каждая формула
вводится со знака `=`; ниже он опущен, чтобы слева стояло имя рабочей
колонки.

**Дата в двух форматах.** `order_date_raw` содержит либо `2025-09-21`
(ISO, разделитель `-`), либо `08.03.2025` (день.месяц.год, разделитель
`.`). Хорошая новость: в этом датасете форматы различимы по одному
признаку — по разделителю. Даты, которую можно прочитать двумя
способами, здесь не бывает.

Соблазн — отдать разбор функции `DATEVALUE` и не думать. Она читает
строку по региональным настройкам того компьютера, на котором открыт
файл, так что одна и та же строка на вашей машине и на машине коллеги
превращается в разные даты. Поэтому формула ниже автораспознаванием не
пользуется вовсе: она смотрит на разделитель, режет строку по позициям
символов и всегда собирает дату через `DATE(год, месяц, день)`:

```
order_date_clean =
IF(
  ISNUMBER(SEARCH("-", B2)),
  DATE(VALUE(LEFT(B2,4)), VALUE(MID(B2,6,2)), VALUE(RIGHT(B2,2))),
  DATE(VALUE(RIGHT(B2,4)), VALUE(MID(B2,4,2)), VALUE(LEFT(B2,2)))
)
```

Читать такую формулу проще снаружи внутрь. Снаружи `IF` — развилка по
формату. Условие развилки: `SEARCH("-", B2)` ищет дефис, а `ISNUMBER(...)`
превращает результат поиска в `ИСТИНА`/`ЛОЖЬ`. Тонкость в том, что при
отсутствии дефиса `SEARCH` не возвращает ноль, а падает с ошибкой, — и
`ISNUMBER` от ошибки даёт `ЛОЖЬ`. Дальше обе ветки делают одно и то же
разными руками: дефис есть — это ISO, `LEFT`/`MID`/`RIGHT` берут части в
порядке год-месяц-день; дефиса нет — это `ДД.ММ.ГГГГ`, и те же три
функции берут части в обратном порядке.

Квартал считается уже от чистой даты, отдельной формулой. Деление номера
месяца на три с округлением вверх и даёт номер квартала:

```
quarter = YEAR(I2) & "-Q" & ROUNDUP(MONTH(I2)/3, 0)
```

**Сумма как текст.** `amount_raw` выглядит как число, но им не является:
это строка, и приходит она в одном из 4 видов — `32847.57` (точка),
`35702,85` (запятая как десятичный разделитель), `50 738.43` (пробел как
разделитель тысяч), `32847.57 грн` (суффикс).

Отдельная история — возвраты. Они лежат в этой же колонке, и знак у них
записан двумя способами: явным минусом (`-3245.08`) или «бухгалтерской»
формой, где вместо минуса скобки (`(13539,52)`). Во втором случае минуса
в строке нет вообще — знак придётся вывести из наличия скобки.

Разбор делается не одной формулой, а тремя рабочими колонками. Так
видно, где именно всё сломалось, если сломается:

```
is_return       = OR(LEFT(TRIM(H2), 1) = "(", LEFT(TRIM(H2), 1) = "-")
amount_stripped = SUBSTITUTE(
                    SUBSTITUTE(
                      SUBSTITUTE(
                        SUBSTITUTE(
                          SUBSTITUTE(TRIM(H2), "грн", ""),
                          "(", ""
                        ),
                        ")", ""
                      ),
                      "-", ""
                    ),
                    ",", "."
                  )
amount_stripped_final = SUBSTITUTE(amount_stripped, " ", "")
amount_clean    = IF(
                    is_return,
                    -NUMBERVALUE(amount_stripped_final, "."),
                    NUMBERVALUE(amount_stripped_final, ".")
                  )
```

Логика такая. `amount_stripped` за один проход снимает всё лишнее:
суффикс `" грн"`, обе скобки и знак минуса. На выходе — всегда
положительная числовая строка, в каком бы из 4 форматов ни пришла
исходная запись. Знак вешается ровно один раз и снаружи, через
`is_return`.

Правило «знак ставится один раз» звучит как формальность, но именно в
нём здесь ломаются (см. 1.6). Оставьте минус внутри `amount_stripped` и
дополнительно умножьте на `-1` через `is_return` — строки с минусом
(`-3245.08`) получат знак дважды и станут положительными. Строки со
скобками (`(13539,52)`) при этом посчитаются верно: скобка сама по себе
минуса не несёт. Отсюда и коварство: сломана только часть возвратов, и по итогу,
округлённому до миллионов, этого не видно.

**Почему `NUMBERVALUE`, а не `VALUE`.** Обе функции превращают текст в
число, но `VALUE` читает строку по десятичному разделителю вашей
системы. В ru-ru и uk-ua этот разделитель — запятая, а `amount_stripped`
последним `SUBSTITUTE` специально заменяет запятую на точку. Точку такая
`VALUE` числом не считает: `VALUE("32847.57")` даёт `#ЗНАЧ!`. На этом
датасете так ломаются **все 20 000 строк из 20 000** (прогон в Excel
Microsoft 365 ru-ru через COM, 2026-09-05, — `Application.International(3)`
= `,`). `NUMBERVALUE(текст, ".")` разделитель принимает вторым
аргументом и от настроек системы не зависит: те же 20 000 строк дают 0
ошибок и сумму `530839498.36`. Та же зависимость от локали, из-за
которой выше отвергнут `DATEVALUE`, — только у `DATEVALUE` она даёт
молча другую дату, а у `VALUE` громкую ошибку во всей колонке.

Внутри `order_date_clean` `VALUE` осталась: там она получает
`LEFT(B2,4)`, `MID(B2,6,2)`, `RIGHT(B2,2)` — куски вида `2025`, `09`,
`21`, целые числа без разделителя вообще. Такую строку любая локаль
читает одинаково.

`OR` в Excel — функция, а не оператор между двумя сравнениями: писать
`A = "(" OR A = "-"` нельзя, формула не вводится. Отсюда форма
`OR(…, …)` в `is_return`.

**Справочник по функциям этого шага.** Формулы здесь даны английскими
именами; в русской и украинской локализации имена другие, логика та же.
Полный список аргументов — в справочнике Microsoft, а не в этом тексте:

- [`SUBSTITUTE` — замена подстроки](https://support.microsoft.com/en-us/office/substitute-function-6434944e-a904-4336-a9b0-1e58df3bc332)
- [Относительные, абсолютные и смешанные ссылки](https://support.microsoft.com/en-us/office/switch-between-relative-absolute-and-mixed-references-dfec08cd-ae65-4f56-839e-5f0d8d0baca9)

## 1.3. Разобранный пример на сквозном датасете

Датасет — `program/M2/data/sales_extract_raw.csv`, 20 000 строк, схема
объявлена в `step-00.md`.

**Сначала файл надо открыть правильно, и двойным щелчком это не
делается.** Двойной щелчок по этому csv в Windows с русским или
украинским языком системы даёт лист, на котором вся строка лежит в одной
ячейке колонки A, а `Побутова техніка` выглядит как
`РџРѕР±СѓС‚РѕРІР° С‚РµС…РЅС–РєР°`. Прогон на машине автора (Excel
Microsoft 365 ru-ru, COM, 2026-09-05): `UsedRange` = `$A$1:$A$20001`,
то есть 20 001 строка и **одна** колонка вместо восьми. Причин две, обе
из M0:

- **разделитель.** В файле поля разделены запятой, а Excel при двойном
  щелчке режет csv по «разделителю элементов списка» Windows, который в
  ru-ru и uk-ua — точка с запятой (`Application.International(5)` = `;`);
- **кодировка.** Файл в UTF-8 **без BOM** (первые байты — `6F 72 64 65`,
  то есть сразу `orde…` из слова `order_id`), а без BOM Excel читает его
  как cp1251 — тот же дефект, который разбирает `M0/step-01.md`.

Импорт делается через мастер, где обе вещи задаются руками, а тип двух
колонок — третьей. Путь по документации Microsoft для русского
интерфейса: вкладка **Данные** → группа **Получение и преобразование
данных** → **Из текстового/CSV-файла**
([справка Microsoft](https://support.microsoft.com/ru-ru/office/%D0%B8%D0%BC%D0%BF%D0%BE%D1%80%D1%82-%D0%B8%D0%BB%D0%B8-%D1%8D%D0%BA%D1%81%D0%BF%D0%BE%D1%80%D1%82-%D1%82%D0%B5%D0%BA%D1%81%D1%82%D0%BE%D0%B2%D1%8B%D1%85-%D1%84%D0%B0%D0%B9%D0%BB%D0%BE%D0%B2-%D1%81-%D1%80%D0%B0%D0%B7%D0%B4%D0%B5%D0%BB%D0%B8%D1%82%D0%B5%D0%BB%D1%8F%D0%BC%D0%B8-txt-%D0%B8%D0%BB%D0%B8-csv-5250ac4c-663c-47ce-937b-339e391393ba)).
В диалоге выставляются три вещи, и именно они, а не названия кнопок,
определяют результат:

1. **источник файла (кодировка)** — `65001: Юникод (UTF-8)`;
2. **разделитель** — запятая;
3. **тип колонок `order_date_raw` и `amount_raw`** — текстовый. Для
   этого в диалоге нажимается **Преобразовать данные**, в открывшемся
   редакторе запроса тип обеих колонок меняется на «Текст», и результат
   выгружается на лист.

Если названия кнопок в вашей сборке другие — ищите эти три настройки, а
не эти три надписи.

**Почему обе колонки обязаны прийти текстом.** Строку `2025-09-21`,
попавшую в ячейку общего формата, Excel превращает в настоящую дату:
`ISTEXT` даёт `ЛОЖЬ`, значение ячейки становится числом `45921`,
`LEFT(B2,4)` возвращает `4592`, а `SEARCH("-", B2)` не находит дефиса
(его в числе нет) — и формула `order_date_clean` уходит в ветку
`ДД.ММ.ГГГГ`, ничем не сообщая об этом. `amount_raw` по той же причине:
часть значений Excel прочитает числом и потеряет ведущий ноль, скобку
или суффикс. Прогон с правильным импортом (кодовая страница 65001,
запятая-разделитель, обе колонки текстовые) даёт `UsedRange` =
`$A$1:$H$20001`, `ISTEXT(B2)` = `ИСТИНА`, `LEFT(B2,4)` = `2025`.

Рабочий лист после импорта: колонки A–H — как в csv
(`order_id`…`amount_raw`), колонки I, J, K, L, M, N — рабочие
(`order_date_clean`, `quarter`, `is_return`, `amount_stripped`,
`amount_stripped_final`, `amount_clean`), строки 2–20001 (строка 1 —
заголовки). Рабочих колонок шесть — по числу формул из 1.2; `amount_clean`
попадает в **N**, а не в M.

Три показательные строки (полный список примеров — `data/README.md` и
`data/reference_answers.md`):

| `order_id` | `order_date_raw` | `order_date_clean` | `quarter` |
|---|---|---|---|
| `S00003` | `08.03.2025` | `2025-03-08` | `2025-Q1` |
| `S00013` | `2024-11-25` | `2024-11-25` | `2024-Q4` |
| `S00066` | `22.02.2024` | `2024-02-22` | `2024-Q1` |

| `order_id` | `amount_raw` | `is_return` | `amount_clean` |
|---|---|---|---|
| `S00001` | `32847.57 грн` | ЛОЖЬ | `32847.57` |
| `S00054` | `(13539,52)` | ИСТИНА | `-13539.52` |
| `S00036` | `-3245.08` | ИСТИНА | `-3245.08` |

Протянув все шесть формул (`order_date_clean`, `quarter`, `is_return`,
`amount_stripped`, `amount_stripped_final`, `amount_clean`) на все
20 000 строк и просуммировав колонку `amount_clean` целиком
(`=SUM(N2:N20001)`), результат должен совпасть с SQL-эталоном:

```
$ python3 build_reference.py
...
grand_total_amount: 530839498.36
```

`grand_total_amount` не зависит от очистки категорий (её ещё нет на этом
шаге) — это сумма по всей таблице, и она уже полностью определяется
только чистыми `order_date_clean`/`amount_clean`. Расхождение вашей суммы
с 530839498.36 после протягивания формул на все строки — сигнал ошибки в
одной из шести формул, а не повод скорректировать эталон.

## 1.4. Задание

На полном `sales_extract_raw.csv` (20 000 строк):

1. Импортируйте файл по инструкции 1.3: кодировка `65001: Юникод
   (UTF-8)`, разделитель — запятая, колонки `order_date_raw` и
   `amount_raw` — текстовые. Проверьте до формул: данные занимают
   колонки A–H (не одну A), `=ISTEXT(B2)` даёт `ИСТИНА`, категория в
   `C2` читается по-украински.
2. Постройте `order_date_clean` и `quarter` (формулы 1.2), протяните на
   все строки.
3. Постройте `is_return`, `amount_stripped`, `amount_stripped_final` и
   `amount_clean` (формулы 1.2), протяните на все строки. Рабочих
   колонок шесть: I, J, K, L, M, N.
4. Посчитайте `SUM(amount_clean)` — это колонка **N**, а не M
   (`=SUM(N2:N20001)`).
5. Сверьте результат с `data/reference_answers.md`
   (`grand_total_amount`) — расхождение должно быть 0.
6. Если не сходится: проверьте на 3 строках из таблицы примера в 1.3
   (`S00003`, `S00054`, `S00036`) — они дают известный правильный
   результат и локализуют, в какой из шести формул ошибка.

## 1.5. Критерий готовности

- После импорта данные занимают восемь колонок A–H (`UsedRange` —
  `$A$1:$H$20001`, не `$A$1:$A$20001`), `=ISTEXT(B2)` и `=ISTEXT(H2)`
  дают `ИСТИНА`, `=UNICODE(MID(C2,1,1))` даёт `1055` (буква `П`
  кириллицей), а не `208` (`Ð`, признак чтения UTF-8 как cp1251).
- `SUM(amount_clean)` по всем 20 000 строкам равен `530 839 498.36`
  (`data/reference_answers.md`), расхождение 0.
- `order_date_clean` для `S00003`, `S00013`, `S00066` совпадает со
  значениями таблицы 1.3 (расхождение — 0 дней).
- `amount_clean` для `S00001`, `S00054`, `S00036` совпадает со значениями
  таблицы 1.3 (расхождение 0, включая знак).
- Ни одна ячейка `order_date_clean` или `amount_clean` не содержит
  ошибки (`#VALUE!`, `#NUM!` и подобных) — проверяется `COUNTIF` диапазона
  на `ISERROR` (должно быть 0 ошибок из 20 000).

**Не сошлось — не переделывайте вслепую:** раздел 1.6 разбирает 6 ошибок
этого шага, каждую с признаком, по которому её видно в своём результате.
Своей ошибки среди них нет — идите наружу: поиск и вопрос ИИ
разрешены (`README.md`, раздел 4), но каждое такое обращение
пишется пометкой `[сторона] что искал` в `research/self.md`.

## 1.6. Типичные ошибки

**Ошибка:** снимаете запятую как разделитель разрядов
(`SUBSTITUTE(text,",","")`), а не заменяете её на точку
(`SUBSTITUTE(text,",",".")`).
**Почему выглядит правильно:** разряды в этой колонке **действительно**
отделяются — пробелом (`25 519.72`, 2822 строки), и пробел вы снимаете
именно удалением. Поступить с запятой так же выглядит последовательно.
Excel возражать не станет: `"35702,85"` превращается в `"3570285"`, и это
законное число — ошибки при переводе в число не даст.
**Почему неправильно:** в этой выгрузке запятая — **десятичный**
разделитель, а не разрядный, и таких строк 4103 из 20 000. Каждая из них
вырастает ровно в 100 раз: `35702,85` даёт 3 570 285.00 вместо 35 702.85.
Итог по всей таблице — **11 141 540 708.26** вместо **530 839 498.36**,
завышение на 10 610 701 209.90, почти в 21 раз (прогон на этом датасете
2026-08-28). Ни одна ячейка при этом не показывает `#VALUE!`: ошибка
видна только сверкой с эталоном.
**Как правильно:** разделитель разрядов и десятичный разделитель — два
разных символа с разной судьбой: первый удаляется, второй заменяется на
точку. Признак в своём результате: сумма отличается от
`grand_total_amount` не на проценты, а в разы, — значит дело в масштабе
отдельных чисел, а не в пропущенных строках.

**Ошибка:** проверяете `is_return` только по минусу (`LEFT(text,1)="-"`),
не учитывая скобки.
**Почему выглядит правильно:** пролистали первые строки, увидели минусы —
и правило готово. В примерах `data/README.md`, которые попадаются на
глаза первыми, возвраты действительно отмечены минусом.
**Почему неправильно:** из 952 строк-возвратов 507 (53%) записаны в
скобках, не минусом (`data/reference_answers.md`, «Счётчики дефектов»).
Формула, проверяющая только минус, найдёт 445 из 952 возвратов и оставит
507 положительными — итоговая сумма разойдётся с эталоном на несколько
десятков миллионов (наивный пересчёт без учёта возвратов в скобках даёт
разницу такого порядка, см. «Наивный пересчёт» в
`reference_answers.md`).
**Как правильно:** проверять оба признака — минус ИЛИ открывающая скобка.

**Ошибка:** строите `amount_stripped` без удаления знака минуса (снимает
только `"грн"`, скобки, пробелы), а затем всегда применяете `-VALUE(...)`
для строк, где `is_return` истинно.
**Почему выглядит правильно:** для возвратов в скобках (`(13539,52)`)
такая формула работает верно — скобки не несут знака сами по себе,
`VALUE` считает число как положительное, внешний минус превращает его в
отрицательное, итог правильный.
**Почему неправильно:** для возвратов с минусом (`-3245.08`, их 445 из
952) минус остаётся внутри строки, которую видит `VALUE`: `VALUE("-3245.08")`
уже возвращает `-3245.08` сам по себе. Внешний минус из `is_return`
применяется поверх этого и меняет знак второй раз: `-(-3245.08) =
3245.08` — положительное число вместо отрицательного. Ошибка исчезает
именно на скобочных возвратах и проявляется только на 445 строках из
952, поэтому расхождение с эталоном (530839498.36) не бросается в глаза
как «всё сломано» — оно выглядит как небольшое отклонение, если сверять
только округлённые до миллионов итоги.
**Как правильно:** снимать знак минуса в `amount_stripped` вместе со
скобками и суффиксом — тогда `amount_stripped` всегда положительная
строка, и знак применяется ровно один раз, снаружи, через `is_return`.

**Ошибка:** строите `order_date_clean` через `DATEVALUE(B2)` напрямую, не
проверяя формат.
**Почему выглядит правильно:** `DATEVALUE` — штатная функция «текст в
дату», и делает она ровно то, что нужно. Универсальной она и выглядит.
**Почему неправильно:** ответ `DATEVALUE` зависит от машины. Строку
`"08.03.2025"` одна локаль прочитает как 8 марта, другая — та, что ждёт
`ММ.ДД.ГГГГ`, — как 3 августа или как несуществующую дату. Файл при этом
выглядит одинаково, а числа получаются разные. Пересчёт на чужой машине
после `git clone` перестаёт сходиться — ровно то, чего вся программа
избегает (решение 13, `design/decisions.md`).
**Как правильно:** разбирать дату по позициям символов, как в формуле
1.2. Автораспознавание здесь не помощник.

**Ошибка:** открываете `sales_extract_raw.csv` двойным щелчком, а не
через мастер импорта.
**Почему выглядит правильно:** расширение `.csv` связано с Excel, файл
открывается в один клик и лист выглядит заполненным — строки на месте,
их видно.
**Почему неправильно:** колонок при этом одна, а не восемь.
`UsedRange` даёт `$A$1:$A$20001`: все восемь полей лежат в колонке A
одной строкой, потому что Excel режет csv по разделителю элементов
списка Windows (в ru-ru — `;`), а в файле разделитель — запятая. Плюс
кодировка: файл в UTF-8 без BOM, и `Побутова техніка` читается как
`РџРѕР±СѓС‚РѕРІР° С‚РµС…РЅС–РєР°`. Колонок `B2` и `H2`, на которые
ссылаются все шесть формул 1.2, в такой книге не существует вовсе
(прогон 2026-09-05, Excel Microsoft 365 ru-ru).
**Как правильно:** импортировать по инструкции 1.3, задав кодировку,
разделитель и текстовый тип двух колонок. Признак в своём результате:
до первой формулы посмотрите, есть ли в книге колонка H и читается ли
`C2` по-украински.

**Ошибка:** переводите очищенную строку в число функцией `VALUE`, а не
`NUMBERVALUE(…, ".")`.
**Почему выглядит правильно:** `VALUE` — штатная функция «текст в
число», она короче и в примерах из интернета встречается чаще.
**Почему неправильно:** `amount_stripped` заканчивается заменой запятой
на точку, а `VALUE` читает десятичный разделитель по настройкам
системы; в ru-ru и uk-ua это запятая. `VALUE("32847.57")` даёт
`#ЗНАЧ!` — и так на **20 000 строках из 20 000** (прогон 2026-09-05:
`SUMPRODUCT(--ISERROR(...))` по колонке с `VALUE` = 20000, по колонке с
`NUMBERVALUE` = 0). `SUM` по такой колонке не считается вовсе, а
критерий 1.5 требует 0 ошибок из 20 000.
**Как правильно:** `NUMBERVALUE(amount_stripped_final, ".")` — разделитель
задан вторым аргументом и от локали не зависит. Внутри
`order_date_clean` `VALUE` остаётся: там она получает целые числа без
разделителя.

## 1.7. Честная оценка времени

5.5–7.0 ч. Оценка проектная, не измерена ни на ком: `research/self.md`
пуст (blueprint, часть 6.1, строка M2 — «не требуется» относится к
модулю целиком при сравнении с [М], но не снимает потребности в
калибровке на уровне отдельных шагов). Из чего складывается:

| Слагаемое | Часы |
|---|---|
| чтение 1.2: два формата дат, четыре формата сумм, возвраты в скобках | 1–1,25 |
| разбор примера 1.3 на трёх строках (`S00003`, `S00054`, `S00036`) | 0,75 |
| задание 1: импорт csv — кодировка, разделитель, текстовый тип двух колонок | 0,5 |
| задание 2: `order_date_clean` и `quarter` на 20 000 строк | 0,75–1 |
| задание 3: четыре колонки очистки суммы | 1,25–1,5 |
| задания 4–5: контрольная сумма и сверка с `grand_total_amount` | 0,5 |
| задание 6: локализация ошибки по трём известным строкам | 0,75–1,5 |

Последняя строка — с самым широким разбросом, и это не небрежность
оценки: если шесть формул сошлись сразу, она стоит ноль, а если нет —
поиск идёт по 20 000 строк, и три контрольные строки из примера есть
единственный способ не искать вслепую.

## 1.8. Вопрос уровня собеседования

**Вопрос:** «В выгрузке из другой системы дата хранится текстом, и часть
строк не парсится стандартной функцией разбора дат. Что вы сделаете?»

**Эталон (ответ покрывает ≥3 из 4 пунктов):**
1. Проверяет, один ли формат дат во всём файле, или несколько вперемешку
   (открывает несколько строк из разных частей файла, а не только
   первые).
2. Ищет однозначный признак, по которому форматы можно различить
   (разделитель, длина строки, позиция года) — не полагается на
   автоматическое распознавание, которое зависит от локали.
3. Строит дату явно через компоненты (год/месяц/день), а не через
   функцию автораспознавания, если форматов больше одного.
4. Проверяет результат на строках, где легко перепутать день и месяц
   (число ≤12 в обеих позициях) — это частный случай, где ошибка разбора
   не проявляется как явная ошибка формулы, а даёт молча неверную дату.
