# M6.03. Находит операции со статистически аномальной суммой

Умение: B2
Модуль: M6
Требуется до этого: `step-01.md` этого модуля — `load_all.py` читает
`ledger.csv` верно
Новые файлы, объявляемые этим шагом: `program\M6\work\find_outliers.py`
— ваш код; `program\M6\work\outliers.csv` — 10 строк
Время: 3–4 ч

## 1.1. Цель и связь с умением

Продолжение умения B2 части 1 blueprint на задаче другого рода: не
потеря строки при соединении, а операция, которая формально ничего не
нарушает — счёт существует, сумма положительная, расчёт может даже
прийти как положено, — но статистически не похожа на то, что этот же
счёт обычно делает. Обоснование модуля — «investigate anomalies»
(Starlight Media, `research/market.md`).

## 1.2. Теория своими словами

**Выброс определяется относительно своей группы, не относительно всех.**
Счета в этом датасете ведут себя очень по-разному: у одних типичная
операция — 500 грн, у других — 3500. И операция на 5000 грн для первого
счёта — событие, а для второго — вторник. Порог без разбивки по счёту
сравнивает несравнимое.

**Медиана устойчивее среднего к тому же самому выбросу, который ищется.**
Возьмите пять значений: 500, 520, 480, 510, **9000**. Среднее — 2202,
медиана — 510. Порог «5× от среднего» (11 010) сам выброс не поймает.
Порог «5× от медианы» (2550) поймает. Свойство называется устойчивостью
(robustness), а суть его вот в чём: ни порог, ни точка отсчёта не должны
зависеть от того самого значения, которое проверяется.

**Справочник.**

- [pandas: `quantile`, параметр `interpolation`](https://pandas.pydata.org/docs/reference/api/pandas.Series.quantile.html)
  — от метода интерполяции зависит Q3, от Q3 — порог, от порога — число
  найденных строк. Умолчание `linear`, и оно же используется в эталоне.
- [pandas: `describe`](https://pandas.pydata.org/docs/reference/api/pandas.DataFrame.describe.html)
  — восемь чисел одной командой; в `step-05.md` двенадцать величин
  паспорта распределения строятся вокруг них.

## 1.3. Разобранный пример на сквозном датасете

Прогон на машине автора 2026-08-20.

```python
median_by_account = completed.groupby("account_id")["amount"].transform("median")
outliers = completed[completed["amount"] > median_by_account * 5]
```

```
статистических выбросов (>5× медианы своего счёта): 10
```

Первые две строки: `T000448` (`ACC-0039`) — 45 850.00 грн при медиане
счёта 3 727.50, **12.3×**; `T000556` (`ACC-0024`) — 40 250.00 грн при
медиане 3 570.00, **11.3×**.

**Порог без разбивки по счёту — не приближение, а другая, неверная
проверка.** Общая медиана по всем 2047 `completed`-операциям —
**1176.00** грн. Порог «5× от неё» (5880 грн) даёт совсем другой список:

```python
global_median = completed["amount"].median()
wrong = completed[completed["amount"] > global_median * 5]
```

```
найдено по общему порогу: 32
из них ложных (нормальны для своего счёта): 23
пропущено (аномальны для своего счёта, но ниже общего порога): 1
```

Пропущенный случай: `T002153` (`ACC-0028`) — 5000.00 грн при медиане
своего счёта 550.00 (**9.1×**, реальная аномалия), но ниже общего
порога 5880 — общий порог его не видит вовсе. 23 ложных — это в основном
операции счетов с высокой типичной суммой (медиана 2000–3500), которые
выше общего порога `5×1176=5880` не поднимаются сами по себе, но не
редкость для своего счёта — 23 «выброса» пропадают полностью после
перехода на медиану своего счёта.

## 1.4. Задание

1. Напишите `work\find_outliers.py`: для каждой `completed`-операции
   считает медиану по своему счёту (`groupby("account_id")`) и отмечает
   операцию выбросом, если её сумма больше медианы своего счёта в 5 раз.
2. Соберите `work\outliers.csv` — пять колонок, 10 строк, отсортированных
   по `txn_id`, кодировка UTF-8:

   ```
   txn_id,account_id,amount,медиана_счёта,во_сколько_раз
   ```

   Последняя колонка — строкой вида `12.3x` (одна цифра после запятой,
   латинская `x`).
3. Проверьте в самом скрипте: посчитайте те же выбросы порогом по общей
   медиане (без `groupby`) и напечатайте, сколько строк разошлось с
   версией по счёту — не для отчёта, а чтобы увидеть разницу своими
   глазами до сдачи шага.
4. Сверьте:

   ```
   > .venv\Scripts\python.exe program\M3\data\compare_csv.py program\M6\work\outliers.csv program\M6\data\ref_outliers.csv
   ```

5. Коммит:

   ```
   > git add program\M6\work\find_outliers.py program\M6\work\outliers.csv
   > git commit -m "M6.03: статистические выбросы по медиане счёта"
   ```

## 1.5. Критерий готовности

Четыре проверки, каждая — «да/нет» без второго человека.

| Проверка | Эталон | Допуск |
|---|---|---|
| `compare_csv.py` на `outliers.csv` | `data/ref_outliers.csv`, 10 строк | вывод `СОВПАДАЕТ`, код возврата 0 |
| порог считается по `groupby("account_id")` | медиана внутри группы, не общая | поиск `.median()` без предшествующего `groupby` в контексте, где вычисляется порог, — 0 попаданий вне явного сравнения с версией по общей медиане пункта 3 |
| `T002153` в результате | присутствует | отсутствие — признак порога по общей медиане, а не по счёту |
| строк, которые есть в общем пороге, но нет в результате | 23 | несовпадение — порог посчитан неверно в любую сторону |

**Не сошлось — не переделывайте вслепую:** раздел 1.6 разбирает 2 ошибки
этого шага, каждую с признаком, по которому её видно в своём результате.
Своей ошибки среди них нет — идите наружу: поиск и вопрос ИИ
разрешены (`README.md`, раздел 4), но каждое такое обращение
пишется пометкой `[сторона] что искал` в `research/self.md`.

## 1.6. Типичные ошибки

Обе измерены на этом датасете.

**Ошибка:** считаете порог по общей медиане всех `completed`-операций, а
не по медиане своего счёта.
**Почему выглядит правильно:** код короче — `groupby` не нужен, порог
считается, список выбросов не пустой, а число выглядит правдоподобно:
32 «выброса» на 2047 операций — 1.6%, на глаз разумная доля.
**Почему неправильно:** из 32 найденных **23 — ложные**: операции
счетов с высокой типичной суммой (медиана 2000–3500 грн), для которых
5000–8000 грн — обычное дело. И **1 реальный** пропущен: `T002153`,
9.1× своей медианы (550 грн), но ниже общего порога 5880 грн — общий
порог его не видит вовсе, потому что он меньше, чем «обычная» операция
крупного счёта.
**Как правильно:** `groupby("account_id")["amount"].transform("median")`
— порог обязан считаться внутри группы, а не по всей таблице разом.

**Ошибка:** считаете порог от среднего, а не от медианы своего счёта.
**Почему выглядит правильно:** на слух «среднее» и «медиана» — одно и то
же «типичное значение». А при большом числе операций на счёт (здесь их
около 50) единичный выброс сдвигает среднее счёта слишком слабо, чтобы
поменять итоговый список: на этом датасете оба способа дают одни и те же
10 строк.
**Почему неправильно:** совпадение результатов здесь — свойство именно
этого датасета (много операций на счёт), а не доказательство того, что
способ работает всегда. На счёте с малым числом операций один выброс
сдвигает среднее сильнее, чем медиану, — иллюстрация в 1.2 показывает
это на пяти числах. Полагаться на среднее — значит полагаться на
то, что слабое место просто не успело себя показать.
**Как правильно:** медиана — по определению, а не потому что «здесь
разницы не видно».

## 1.7. Честная оценка времени

3–4 ч. Оценка проектная, не измерена (`research/self.md` пуст). Из чего
складывается: `groupby`-порог и первая версия — 1–1,5 ч; сравнение с
общим порогом и разбор расхождения — 1–1,5 ч; сборка отчёта и сверка —
1 ч.

## 1.8. Вопрос уровня собеседования

**Вопрос:** «Вы ищете операции с аномально большой суммой. Коллега
предлагает порог „сумма выше 5000 грн“ — просто, быстро, покрывает и
маленькие, и крупные счета одним числом. Что вы возразите?»

**Эталон (ответ покрывает ≥3 из 4 пунктов):**
1. Называет главный риск фиксированного порога: он одинаков для счетов
   с разным типичным объёмом, значит либо пропускает реальные аномалии
   маленьких счетов, либо ложно помечает нормальные операции крупных.
2. Предлагает нормировать порог по самому счёту (медиана или похожая
   устойчивая мера), а не задавать число заранее.
3. Отмечает, что порог должен считаться устойчивой к выбросам величиной
   (медиана), а не средним — иначе сам выброс участвует в определении
   того, что считается выбросом.
4. Указывает, что фиксированное число придётся пересматривать вручную
   при росте оборотов, а порог, привязанный к текущим данным счёта,
   подстраивается сам.
