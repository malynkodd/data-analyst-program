# M10.02. Открывает Pull Request и сливает ветку через него

Умение: I1
Модуль: M10
Требуется до этого: `step-01.md` этого модуля — ветка с содержательными
коммитами запушена на GitHub
Новые файлы, объявляемые этим шагом: нет — работа целиком в
веб-интерфейсе GitHub (или `gh` CLI) над репозиторием из `step-01.md`
Время: 6–7 ч

## 1.1. Цель и связь с умением

Умение I1 части 1 blueprint, вторая половина: «...PR → merge». Критерий
части 1: «В истории: 0 коммитов с сообщением «fix»/«update», `.gitignore`
есть, файлов >50 МБ нет, **ветка смёржена через PR**» — последнее условие
шаг 01 не закрывает: ветка запушена, но ещё не слита.

## 1.2. Теория своими словами

**Pull Request — не техническая формальность, а точка проверки перед
тем, как код попадёт в `main`.** У одного человека без второго ревьюера
эта точка не пропадает — она превращается в самопроверку: PR заставляет
посмотреть на diff целиком, со стороны, а не построчно во время
написания.

**Чеклист самопроверки перед PR — то же самое решение 3, что для acceptance
criteria проектов.** Публикуется до слияния, а не после: список пунктов,
по которому проверяется собственный PR, — то, что отличает «код
компилируется» от «код готов».

**Конфликт слияния — не поломка, а вопрос, на который Git не может
ответить сам.** Он возникает ровно тогда, когда две ветки правят одни и
те же строки одного файла. Git видит две версии и не знает, какая
нужна; он останавливается и отдаёт решение вам. Ничего при этом не
потеряно и не сломано — состояние обеих веток на месте.

Конфликт помечен в файле тремя разделителями:

```
<<<<<<< HEAD
то, что уже в ветке, куда сливаете
=======
то, что приходит из сливаемой ветки
>>>>>>> имя-ветки
```

`git status --short` в этот момент печатает `UU <файл>` — **U**nmerged с
обеих сторон.

**Разрешить конфликт — значит написать итоговый текст руками**, а не
выбрать одну из двух половин кнопкой. Это и есть место, где теряют
данные: обе половины часто нужны частично, а самый быстрый способ убрать
красное — оставить свою и стереть чужую. Правильный порядок такой:

1. прочитать обе половины целиком и понять, что меняет каждая;
2. написать итог — иногда это одна из половин, чаще смесь;
3. удалить все три строки-разделителя;
4. `git add <файл>` — этим вы говорите «конфликт разрешён»;
5. `git commit` — завершает слияние.

Проверка после — не «выглядит правильно», а поиск: строк `<<<<<<<` в
файле должно остаться **0**. Забытый разделитель ломает файл молча —
Python и SQL на нём падают с синтаксической ошибкой в непонятном месте,
а Markdown и CSV не падают вовсе и просто несут мусор.

**`git revert` — отмена уже сделанного коммита новым коммитом.** Это не
то же, что `git restore` из `M0/step-03.md`: `restore` возвращает файл к
последнему коммиту и истории не касается, `revert` добавляет в историю
новый коммит, содержимое которого — обратные изменения указанного.
Исходный коммит при этом остаётся на месте.

Так и нужно: в ветке, которую кто-то уже забрал себе, история не
переписывается. `git reset`, стирающий коммиты, — инструмент для ветки,
которую никто не видел; после пуша он превращается в способ сломать
работу другим.

**Merge commit vs squash — не всё равно, но не критично для одного
человека.**
[About pull request merges](https://docs.github.com/en/pull-requests/collaborating-with-pull-requests/incorporating-changes-from-a-pull-request/about-pull-request-merges)
называет три способа: обычный merge commit (сохраняет все коммиты
ветки), squash (все коммиты ветки — один коммит в `main`), rebase
(коммиты ветки переносятся поверх `main` без merge commit). Для
одного человека без параллельной работы разница на уровне «дело вкуса, а
не корректности» — в этом шаге используется обычный merge commit, потому
что он оставляет историю ветки видимой, что удобнее для самопроверки
задним числом.

## 1.3. Разобранный пример на изолированном тестовом репозитории

Прогон на машине автора 2026-08-20, не на `data-analyst-program`.

```
> git checkout main
> git merge --no-ff feature/add-notes -m "Merge pull request #1 from origin/feature/add-notes"
```

```
Merge made by the 'ort' strategy.
 notes.txt | 1 +
 1 file changed, 1 insertion(+)
 create mode 100644 notes.txt
```

```
> git log --oneline --graph --all
*   6bf4b9d Merge pull request #1 from origin/feature/add-notes
|\
| * 823b74c add project notes file
|/
* b3aeefb init
```

Реальный Pull Request через веб-интерфейс делает то же самое действие
(создаёт merge-коммит с похожим сообщением) через кнопку «Merge pull
request», а не команду `git merge` — здесь показана механика, которую
GitHub выполняет за вас.

```
> git log --merges --oneline
6bf4b9d Merge pull request #1 from origin/feature/add-notes
```

`git log --merges` — способ проверить постфактум, что слияние
действительно прошло через merge-коммит (то есть через PR или его
локальный эквивалент), а не через `git rebase`/fast-forward, которые
merge-коммита не оставляют.

## 1.4. Задание

1. На GitHub откройте Pull Request из ветки `feature/<...>` в `main`
   (кнопка «Compare & pull request» появляется автоматически после пуша
   ветки шага 01; либо `gh pr create --base main --head
   feature/<...> --title "<...>" --body "<...>"`, если стоит `gh`).
2. В описании PR — что сделано и зачем, 2–4 предложения. Не «см.
   коммиты» — описание должно быть понятно без перехода к списку
   коммитов.
3. Пройдите по чеклисту самопроверки перед слиянием:
   - `.gitignore` в репозитории есть и покрывает то, что не должно
     коммититься (виртуальное окружение, кэш, локальные конфиги);
   - ни один файл в диффе не превышает 50 МиБ;
   - ни один коммит ветки не назван «fix»/«update» (та же проверка, что
     в `step-01.md`, — на этот раз по всей ветке, не по одному коммиту);
   - diff открыт и прочитан целиком хотя бы один раз, до слияния, а не
     после.
4. Слейте PR через кнопку «Merge pull request» (обычный merge, не squash
   и не rebase — 1.2) либо `gh pr merge --merge`.
5. Проверьте локально после `git pull`:

   ```
   > git checkout main
   > git pull
   > git log --oneline --graph --all
   > git log --merges --oneline
   ```

   Вторая команда обязана показать хотя бы одну строку — сам факт
   merge-коммита.

**Сценарий 2 — конфликт слияния.** Постройте его специально: у одного
человека в одном репозитории он сам не случится, а на работе случится
на первой неделе.

6. В `main` создайте файл `metrics.md` ровно из двух строк и
   закоммитьте:

   ```
   take_rate: 2.02%
   revenue: 159881549.91
   ```

   Числа взяты из `M5/step-07.md` — те же оборот и take rate, что вы
   считали в Python.
7. Из `main` создайте ветку `feature/take-rate`, поправьте в ней
   **первую** строку на `take_rate: 2.05%`, закоммитьте.
8. Вернитесь в `main` и создайте из него **вторую** ветку
   `feature/revenue` (именно из `main`, не из первой ветки), поправьте
   в ней **вторую** строку на `revenue: 161204880.10`, закоммитьте.
9. Слейте в `main` первую ветку — она сольётся без вопросов. Затем
   слейте вторую. Git остановится:

   ```
   > git merge --no-ff feature/revenue
   Auto-merging metrics.md
   CONFLICT (content): Merge conflict in metrics.md
   Automatic merge failed; fix conflicts and then commit the result.
   ```

10. **Не запуская никаких команд**, откройте `metrics.md` и запишите:
    какие две строки стоят под `HEAD` и какие — под именем ветки. Затем
    ответьте письменно, одним предложением: что будет потеряно, если
    оставить только половину `HEAD`, и что — если оставить только
    вторую.
11. Разрешите конфликт руками: итоговый файл обязан содержать **обе**
    правки — `take_rate: 2.05%` и `revenue: 161204880.10`. Удалите все
    три разделителя, `git add metrics.md`, `git commit`.
12. Проверьте историю:

    ```
    > git status --short
    > Select-String -Pattern '<<<<<<<' -Path metrics.md
    > git log --oneline --graph
    > git log --merges --oneline
    ```

**Сценарий 3 — отмена своего коммита.**

13. Допишите в `metrics.md` третью строку `churn: 0.31` и закоммитьте
    её отдельным коммитом.
14. Отмените этот коммит: `git revert --no-edit HEAD`.
15. Проверьте три вещи разом: содержимое `metrics.md`, число коммитов в
    `git log --oneline` и то, что исходный коммит из истории **не
    исчез**.

## 1.5. Критерий готовности

Четыре проверки, каждая — «да/нет» без второго человека.

| Проверка | Эталон | Допуск |
|---|---|---|
| `git log --merges --oneline` в `main` после `git pull` | ≥1 строка | 0 строк — слияние прошло squash'ем или rebase, а не через PR-merge; критерий I1 требует именно «смёржена через PR» |
| PR на GitHub закрыт статусом Merged | зелёная плашка «Merged», не «Closed» | «Closed» без merge — ветка не вошла в `main`, задание не выполнено |
| `git log --format=%s \| Select-String -Pattern '^(fix\|update)$'` в `main` после слияния | пусто | совпадение — сообщение коммита ветки не поймано на шаге 01, надо переписать до PR в следующий раз |
| описание PR | 2–4 предложения, не пустое и не «see commits» | пустое описание — самопроверка PR не читается позже без перехода в diff |
| `metrics.md` после задания 11 | ровно 2 строки: `take_rate: 2.05%` и `revenue: 161204880.10` | одна из двух правок отсутствует — конфликт разрешён выбором половины, а не слиянием; это и есть потеря данных, ради разбора которой сценарий построен |
| `Select-String -Pattern '<<<<<<<' -Path metrics.md` | пусто, 0 совпадений | любое совпадение — забытый разделитель; в `.md` он не вызовет ошибки и уедет в репозиторий как есть |
| `git status --short` после задания 11 | пусто | строка `UU` означает, что `git add` после разрешения не сделан и слияние не завершено |
| `git log --merges --oneline` после задания 12 | **2** строки | 1 строка — второе слияние не создало merge-коммита; 0 — не создало ни одно |
| письменный ответ задания 10 | называет обе потери конкретно: «останется 2.02% вместо 2.05%» и «останется 159881549.91 вместо 161204880.10» | ответ «потеряются изменения другой ветки» без чисел не показывает, что обе половины прочитаны |
| `metrics.md` после задания 14 | 2 строки, `churn` отсутствует | три строки — `revert` не применился |
| `git log --oneline` после задания 14 | коммит `Revert "add churn to metrics summary"` **и** коммит `add churn to metrics summary` оба на месте | исчезновение исходного коммита означает `reset` вместо `revert` — переписанную историю, а не отменённое изменение |

## 1.6. Типичные ошибки

Первые две измерены прогоном сценария 2 на машине автора 2026-08-28;
конфликт для того и построен, чтобы ошибки этого шага перестали быть
предположениями.

**Ошибка:** разрешаете конфликт, оставив половину `HEAD` и удалив
вторую, — «моя ветка новее, значит правильнее».
**Почему выглядит правильно:** файл после этого валиден, разделителей
нет, `git commit` проходит, слияние помечено завершённым, и в `main`
действительно лежит самая свежая из **двух** правок первой строки.
**Почему неправильно:** половина `HEAD` содержит **обе** строки — и
исправленный `take_rate: 2.05%`, и старый `revenue: 159881549.91`.
Оставив её целиком, вы вернули выручку к прежнему значению:
161 204 880.10 → 159 881 549.91, потеря **1 323 330.19** при полном
отсутствии признаков ошибки. Git не подсказывает: с его точки зрения
конфликт разрешён, вы так решили. То же зеркально при выборе второй
половины — `take_rate` вернётся с 2.05% на 2.02%.
**Как правильно:** конфликтуют не «ветки», а строки; из двух половин
берётся по строке, а итог пишется руками. Признак в своём результате:
после разрешения в файле обязаны стоять **оба** новых числа; если
изменилось только одно из двух, вы выбрали половину, а не слили.

**Ошибка:** удаляете строки `<<<<<<<` и `>>>>>>>`, а `=======`
оставляете — или наоборот.
**Почему выглядит правильно:** самые заметные два разделителя убраны,
цветная подсветка в редакторе гаснет, файл выглядит нормальным.
**Почему неправильно:** `metrics.md` — Markdown, и `=======` в нём не
ошибка, а разметка: строка из знаков `=` под текстом делает предыдущую
строку заголовком первого уровня. Файл не падает, коммитится и уезжает
в репозиторий, а `take_rate: 2.05%` тихо превращается в заголовок.
В `.py` и `.sql` тот же забытый разделитель дал бы синтаксическую
ошибку сразу; молча ломается именно то, что не исполняется, — Markdown,
CSV, конфиги.
**Как правильно:** искать, а не смотреть: `Select-String -Pattern
'<<<<<<<|=======|>>>>>>>' -Path metrics.md` обязана вернуть пусто.
Признак в своём результате: файл после слияния длиннее двух строк.

**Ошибка:** сливаете PR через «Squash and merge» вместо обычного merge.
**Почему выглядит правильно:** GitHub предлагает оба варианта как
равноправные кнопки, squash даёт чистую линейную историю `main` без
промежуточных коммитов ветки.
**Почему неправильно:** `git log --merges` после squash-слияния не
покажет merge-коммит вовсе — GitHub squash не создаёт merge-коммит,
переписывает всё в один обычный коммит поверх `main`. Критерий I1
(«ветка смёржена через PR») проверяется по наличию merge-коммита в этом
шаге, и squash его не оставляет, хотя PR формально был и слияние
формально прошло через него.
**Как правильно:** обычный merge (кнопка «Create a merge commit» или
`gh pr merge --merge`) — сохраняет и merge-коммит, и историю ветки
внутри `main`.

**Ошибка:** удаляете ветку сразу после слияния, не проверив
`git log --merges` — «GitHub же предложил удалить, значит всё в порядке».
**Почему выглядит правильно:** удаление ветки после успешного слияния —
стандартное предложение GitHub, кнопка «Delete branch» появляется сразу
после «Merged».
**Почему неправильно:** если слияние на самом деле прошло не так, как
ожидалось (например, случайно нажат squash вместо merge), ветку уже
не восстановить без `git reflog`, и разбираться придётся по локальной
копии, которая тоже может быть неактуальна.
**Как правильно:** сверить критерии из 1.5 **до** удаления ветки, не
после — удаление необратимо дёшево сделать раньше времени и дорого
исправить позже.

## 1.7. Честная оценка времени

6–7 ч. Оценка проектная, не измерена (`research/self.md` пуст). Из чего
складывается:

| Слагаемое | Часы |
|---|---|
| чтение 1.2: PR, чеклист, способы слияния | 0,75–1 |
| чтение 1.2: конфликт, порядок разрешения, `revert` против `restore` | 0,5 |
| задания 1–5: открытие PR, чеклист, чтение diff целиком, слияние | 2,5–3 |
| задания 6–9: построение конфликта тремя коммитами | 0,75 |
| задания 10–12: чтение обеих половин, разрешение руками, проверка истории | 1–1,25 |
| задания 13–15: `revert` и проверка, что исходный коммит на месте | 0,5 |

Вилка увеличена с 4–5 ч контентным аудитом 2026-08-28: до него в модуле
не было ни конфликта слияния (ноль вхождений слова «конфликт»), ни
отката коммита, то есть двух самых частых событий первых недель в чужом
репозитории. Норматив на задание раздела 1.4 — ≤30 мин на любое из
пятнадцати.

## 1.8. Вопрос уровня собеседования

**Вопрос:** «В чём разница между обычным merge, squash и rebase при
слиянии Pull Request — и когда каждый уместен?»

**Эталон (ответ покрывает ≥3 из 4 пунктов):**
1. Merge commit сохраняет всю историю ветки и явно фиксирует момент
   слияния отдельным коммитом с двумя родителями.
2. Squash сжимает все коммиты ветки в один — удобно, когда
   промежуточные коммиты ветки были черновыми и не несут ценности сами
   по себе.
3. Rebase переносит коммиты ветки поверх `main` без merge-коммита,
   давая линейную историю, но переписывает хэши коммитов — опасно для
   веток, которые уже кто-то склонировал или продолжает.
4. Называет практическое следствие: если процесс требует видеть факт
   слияния через PR (например, для отчётности или проверки, как в этом
   модуле), squash и rebase могут не оставить нужного следа — выбор
   способа слияния зависит от того, что должно быть видно в истории
   потом, а не только от чистоты лога.
