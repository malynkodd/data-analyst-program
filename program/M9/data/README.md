# Датасеты модуля M9 (40 ловушек статистики)

## Статус этого файла

Полная декларация — `program/M9/step-00.md`, раздел «Датасет — 40
независимых микро-датасетов». Этот файл — описание папки `data/`.

Данные синтетические, каждый скрипт детерминирован (`SEED` фиксирован
до первого обращения к `random`). Общей контрольной точки одной
таблицей нет — вместо неё `reference_answers.md` со всеми 40 числами.

## Файлы

| Файл | Роль | В git? |
|---|---|---|
| `generate_apparatus.py` | Генератор трёх микро-датасетов шага 01 (умение F5). Детерминирован, `SEED = 508`; пары подобраны так, чтобы `r ≈ 0.62`. Средние чеки разыгрываются последними — вставленные раньше, они сдвинули бы поток `random` и изменили `r`. | да |
| `ab_apparatus.csv` | Результат A/B-теста платёжной формы: 2 строки, пользователи и оплаты в каждой группе. | да |
| `pairs_apparatus.csv` | 20 месяцев: расходы на рекламу и число заявок. 20 строк. | да |
| `means_apparatus.csv` | Средний чек 40 оплат того же A/B-теста, по 20 на группу. Нужен для t-критерия: доли проверяются z, средние — t. 40 строк. | да |
| `reference_apparatus.py` | Эталон шага 01: 26 величин (доля, SE, ДИ, разница, z, p-value, нужный размер группы, хи-квадрат и его порог, средние чеки, t и df, r, r²), посчитанных развёрнутыми формулами на `statistics.NormalDist`, без `scipy` и без готовых `statistics.correlation`/`linear_regression`. Единственное невычисляемое число — порог t для df = 38 (2.0244) из таблицы квантилей. | да |
| `ref_apparatus.csv` | Те же 26 величин таблицей — эталон для `compare_csv.py`. 26 строк. | да |
| `traps_a.py` | Ловушки 1–10 (выборка, случайность, смещение) — `step-02.md`. Печатает вопрос/датасет/оба ответа каждой. | да |
| `traps_b.py` | Ловушки 11–20 (значимость, множественные сравнения) — `step-03.md`. | да |
| `traps_c.py` | Ловушки 21–30 (устройство A/B-экспериментов) — `step-04.md`. | да |
| `traps_d.py` | Ловушки 31–40 (корреляция, причинность, агрегация) — `step-05.md`. | да |
| `reference_answers.md` | Все 40 ключевых чисел, сверяемых при чтении шага. | да |
| `exam.py.enc` | Закрытый набор F2 — 10 вопросов для `step-06.md`. Закрыт турникетом `tools/vault.py` (решение 51): открывается командой `python tools\vault.py open <путь без .enc> --attempt <файл вашей попытки>` и записывает проход в `research/attempts.md`. |
| `reference_answers.md.enc` | Разобранные ответы на все 40 ловушек. Закрыт турникетом `tools/vault.py` (решение 51): открывается командой `python tools\vault.py open <путь без .enc> --attempt <файл вашей попытки>` и записывает проход в `research/attempts.md`. |
| `exam.py` | Закрытый набор F2 — 10 вопросов, не совпадающих с 40 ловушками, для `step-06.md`. Не читать до собственного ответа. | да |
| `ab_report_sample.md` | Отчёт A/B-теста с тремя встроенными дефектами (умение F3) для `step-06.md`. | да |

Три датасета шага 01 — исключение из правила ниже: они читаются
несколькими заданиями подряд и обязаны быть одинаковыми у автора и у
читателя, поэтому лежат в git как файлы, а не порождаются на лету.

Датасеты каждой **ловушки** не пишутся на диск отдельными файлами —
генерируются и используются внутри своего скрипта немедленно (15–2000
строк/точек, не требуют хранения). Ничего не коммитится и не
попадает в `.gitignore`: сохранять нечего.

## Как воспроизвести

```
$ .venv\Scripts\python.exe program\M9\data\traps_a.py
$ .venv\Scripts\python.exe program\M9\data\traps_b.py
$ .venv\Scripts\python.exe program\M9\data\traps_c.py
$ .venv\Scripts\python.exe program\M9\data\traps_d.py
$ .venv\Scripts\python.exe program\M9\data\exam.py
```

Каждая команда печатает десять ловушек целиком — вопрос, датасет,
неверный и правильный ответы, ключевое число. `exam.py` — закрытый
набор для `step-06.md`: не запускать до собственного ответа.

## Проверка строк

Числа сверяются `tools/check_consistency.py` с фактическими файлами.

| Файл | Строк |
|---|---|
| `ab_apparatus.csv` | 2 |
| `pairs_apparatus.csv` | 20 |
| `means_apparatus.csv` | 40 |
| `ref_apparatus.csv` | 26 |
