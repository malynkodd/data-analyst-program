# Эталонные ответы P6 («Заказчик передумал»)

Считает `reference_p6.py`, независимо от `fetch_stat.py`/`fetch_nbu.py`.
Прогон на машине автора 2026-08-22.

## Контрольная точка (снапшот)

```
> python program\P6\data\fetch_stat.py
> python program\P6\data\fetch_nbu.py
```

| Файл | Строк | sha256 |
|---|---|---|
| `snapshot/retail_turnover_by_region.csv` | 1050 | `ce521539d674e5f95b2d3b1ff58778857ec7b011793b0eaa0dc99d53378cf143` |
| `snapshot/footwear_imports.csv` | 110 | `1282f2afeda98b845a5a77df4ea378f30dfd3ea4cb114b224130e0bb1715ca38` |
| `snapshot/usd_uah.csv` | 4252 | `e405b2d9ac9f7af7924b6fa05d7ebacf6311420dcd095b45a5a726bc18127744` |

Дата фактической выгрузки — 2026-08-22. Держстат и НБУ — живые источники,
обновляются; при повторной выгрузке числа разойдутся — это ожидаемо
(правило части 5 blueprint для P3/P4/P6), расхождение считается и
объясняется, а не подгоняется.

## Разбор закрыт турникетом

Ответы, ранги, выбранные трактовки и разбор дефектов вынесены в
`answers.md` и закрыты турникетом (`tools/vault.py`, решения 51 и 53).
Причина названа независимой проверкой
`audit/independent-final-check-2026-08-24.md`, блокер 8.2: проект — это
единственное доказательство, что умения работают вместе, и приёмка, где
ответ лежит рядом открытым файлом, доказывает только умение читать.

Открывается после сохранённой попытки, проход пишется в
`research/attempts.md`:

```
> python tools\vault.py open program\P6\data\answers.md --attempt program\P6\work\<ваш файл>
```

Открытым здесь остаётся ровно то, без чего проект не начать: контрольная
точка датасета выше.
