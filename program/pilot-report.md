# Пилот Фазы 2 — отчёт о формате

Дата: 2026-07-30. Скилл curriculum-design загружен, применён к M1
целиком и к одному образцу шага M3.

## Написано

- `program/M1/data/` — `generate_clients.py`, `clients.csv` (240 строк,
  детерминирован, seed 20260701), `count_active.py`, `peek_clients.py`,
  `check_order.py`, `reference_answers.md`, `README.md`.
- `program/M1/step-00.md` (декларация модуля), `step-01.md`, `step-02.md`
  (на реальном `git log`, не на mtime), `step-03.md` — весь модуль M1
  (A5, H2), 8–10 ч.
- `program/M3/data/` — `schema.sql`, `seed.sql` (5 таблиц: `customers`,
  `orders`, `products`, `order_items`, `payments`), `README.md`.
- `program/M3/step-00.md` (декларация модуля, полная схема на 5 таблиц),
  `step-05.md` — один написанный шаг, умение A2 (LEFT JOIN, теряющий строки).
- `design/decisions.md` — решения 13 (кросс-платформенные команды), 14
  (Git в M0), 15 (SQL-движок SQLite→PostgreSQL), плюс 2 новых пункта в
  «Что осталось нерешённым».
- `design/blueprint.md` — части 1(косвенно, п.7), 2.1/2.2 (M0/M10 часы и
  умения), 3.1/3.2 (M0→M1 в карте зависимостей), 6.1 (пересчёт часов),
  7 (новый пункт про самопроверку концептуальных умений).
- `CLAUDE.md` — ядро/обвязка пересчитаны (252–359 / 110–137).
- `.claude/skills/curriculum-design/SKILL.md` — раздел 1.3: обязательный
  `step-00.md` до первого шага модуля; третья категория датасета для
  сквозных модулей.

Все числа в шагах (112/118/133/155/167 в M1; 7/9/12 строк, сумма 8600.00,
контрольные суммы order_items/payments в M3) пересчитаны выполнением
кода/SQL, а не придуманы.

## Чеклист из 12 пунктов — построчно по всем 4 файлам шагов

| № | Пункт | Итог |
|---|---|---|
| 1 | ID умения указан, шаг его закрывает | PASS. Частичное покрытие (A5 разбито на M1.1/M1.2, H2 закрыт не целиком — остаток в M12) — отмечено явно, это разрешено скиллом |
| 2 | 8 блоков по порядку | PASS во всех 4 |
| 3 | Пример/задание на сквозном датасете модуля | PASS для обоих модулей после раунда 2 — `step-00.md` теперь есть у M1 и у M3, схема M3 объявлена на все 5 таблиц, не только на нужды одного шага |
| 4 | Датасет не запрещён; синтетика — с генератором | PASS для M1 (питон-генератор с seed). **Частично подогнано для M3** — вместо генератора вручную написанные `INSERT` (12 клиентов/19 заказов/6 товаров/23 позиции/13 платежей); оправдано тем, что нужный точный эффект (ровно 5 из 12 клиентов теряются, суммы order_items=orders.amount) проще гарантировать вручную, чем случайной генерацией с ограничениями — но это всё ещё не генератор в духе правила |
| 5 | Критерий без второго человека, эталон+порог+допуск | PASS с оговоркой на трёх шагах A5/H2 — механизм "сверьте со списком эталонных пунктов, нужно ≥K из N" мягче числового diff (требует интерпретации "тот же ли это механизм"). Step-05 (SQL) — строгий числовой критерий, без оговорок |
| 6 | Каждое "почему неправильно" — с цифрой | PASS во всех 4 |
| 7 | Сумма часов модуля в вилке части 6.1 | PASS для M1: 2.5–3 + 3.5–4.5 + 2–2.5 = 8.0–10.0, ровно вилка 8–10, без запаса. **Не проверяемо для M3** — написан 1 шаг из полного модуля (60–80 ч) |
| 8 | Вопрос собеседования + эталон списком | PASS во всех 4 |
| 9–10 | Ссылки/рыночные утверждения | Неприменимо — шаги процедурные, рыночных утверждений и ссылок нет вовсе |
| 11 | Запрещённые слова | 0 совпадений во всех 4 (проверено grep) |
| 12 | Данные выгружает учащийся, агент не скачивал | Неприменимо к скачиванию — оба датасета синтетические, сгенерированы/написаны локально при написании шага (аналогично P1/P2/P5) |

## Исправлено после первого прохода (2026-07-30, замечание пользователя)

Первая версия `step-01.md`/`step-02.md`/`step-05.md` содержала
POSIX-специфичные команды: `grep`, `awk -F`, `stat --format=%Y`, `<`-
редирект в `sqlite3`, многострочный `echo` с бэкслеш-продолжением.
Ничего из этого не выполняется в стандартной Windows PowerShell без
WSL/Git Bash — то есть учащийся на голом Windows не смог бы пройти
шаги как написано. Зафиксировано как решение 13 в `design/decisions.md`:
все команды в шагах — только вызовы предоставленных кросс-платформенных
Python-скриптов, никаких `grep`/`awk`/`sed`/`stat`. Добавлены
`peek_clients.py` (замена `grep`+`awk`) и `check_order.py` (замена
`stat --format`). `step-05.md` переписан на `sqlite3 db ".read file.sql"`
вместо `< file.sql`. Побочный эффект правки: при повторной генерации
примера обнаружилась опечатка при ручном наборе одной строки данных
(`C0010`, дата входа) в исходной версии `step-01.md` — исправлена, число
теперь взято из фактического вывода скрипта, а не набрано вручную.

## Раунд 2 (2026-07-30) — разбор всех 6 расхождений раунда 1

Пользователь прошёл по всем 6 пунктам ниже и дал решение по каждому.
Статус после раунда 2:

1. **A5 «по истории коммитов» vs суррогат на mtime.** РЕШЕНО (не
   компромиссом, а структурно): `git init/add/commit` перенесены из M10
   в M0 — решение 14, `design/decisions.md`. `step-02.md` переписан:
   критерий теперь — реальный `git log`, а не `check_order.py`. M10
   сократился на тот же час (8–10→7–9), M0 вырос (6–8→7–9); ядро/обвязка
   в CLAUDE.md и части 6.1 blueprint пересчитаны (252–359 / 110–137).
   `check_order.py` остался в репозитории как пример инструмента решения
   13, в M1.2 больше не используется.
2. **M0 → M1 не в карте зависимостей.** РЕШЕНО: добавлена строка в
   часть 3.2 blueprint и явное ребро в диаграмме части 3.1.
3. **Скилл не знал категории датасета для сквозных модулей.** РЕШЕНО:
   `curriculum-design/SKILL.md`, раздел 1.3 — добавлена третья категория
   («сквозные модули вне проектной привязки — синтетика по умолчанию»).
   `program/M1/data/README.md` обновлён, ссылается на постоянное правило
   вместо разового решения пилота.
4. **Датасет M3 — 2 таблицы вместо 5, не полноценная декларация.**
   РЕШЕНО процессным правилом + фактическим расширением: скилл теперь
   требует `step-00.md` до первого содержательного шага модуля (раздел
   1.3 и чеклист). Написаны `program/M1/step-00.md` (задним числом) и
   `program/M3/step-00.md` (с самого начала). Схема M3 расширена до 5
   таблиц (`products`, `order_items`, `payments` добавлены к
   `customers`/`orders`); контрольные суммы и результаты `step-05.md`
   перепроверены после расширения — не изменились (7/12 строк, сумма
   8600.00).
5. **SQL-движок не зафиксирован.** РЕШЕНО: решение 15, `design/decisions.md`,
   по цифрам `research/market.md` (диалект назван явно в 6 из 123 вакансий,
   4.9%; из них 3 — семья PostgreSQL/Redshift, 2 — MS SQL/SQL Server, 1 —
   Snowflake). SQLite на входе модуля (проверено: поддерживает CTE и
   оконные функции, SQLite 3.50.4), переход на PostgreSQL к концу модуля.
   Фальсификатор — доля T-SQL-вакансий в трекере откликов учащегося.
6. **Критерии A5/H2 мягче, чем у SQL-шага.** Не починено — по решению
   пользователя это не баг: концептуальное умение нельзя проверить
   числовым diff'ом, это природа задачи. Зафиксировано честно в части 7
   blueprint (пункт 7) и в `design/decisions.md` («Что осталось
   нерешённым», пункт 5) — самопроверка части умений слабее, это
   ограничение самостоятельного обучения без ментора, не брак формата.

## Мелкие правки, отложенные (2026-07-30, замечание пользователя)

Не блокируют, не чинить отдельной сессией — попутно со следующей
правкой в затронутых файлах.

1. **`README.md` и `resources.md` в корне — 0 байт.** При публикации
   репозитория README — первое, что видит нанимающий; пустым оставлять
   нельзя.
2. **`program/M1/data/peek_clients.py` — `--status` обязательный
   аргумент.** Это первый скрипт, который учащийся запускает в M0/M1;
   без фильтра посмотреть файл целиком нельзя. Сделать `--status`
   необязательным — без него показывать первые `--limit` строк без
   фильтрации.

## Новое расхождение, найденное при исполнении раунда 2

При пересчёте части 6.1 blueprint построчная сумма всех модулей и
проектов дала 478–642 ч, а не 480–644, указанные в части 6.2 и в
CLAUDE.md. Разница +2 ч на обеих границах — не связана с переносом часа
Git (перенос сам по себе не меняет общую сумму) и не объяснена ни одним
источником Фазы 1. Зафиксировано в части 6.1 blueprint и в
`design/decisions.md` («Что осталось нерешённым», пункт 4), не
устранено — источник разницы не найден.

---

# Заход 3 (2026-07-30) — M0 целиком + M2 целиком (первый длинный модуль)

Скилл curriculum-design загружен и применён к двум модулям: M0
(онбординг: терминал, файлы, минимальный Git — I2 + фрагмент I1) и M2
(таблицы Excel/Sheets — D1, D2, D3, 30–40 ч, 8 шагов). M2 — первая
проверка формата на модуле длиннее 3 шагов; это и было главной целью
захода.

## Написано

- `program/M0/data/` — `generate_orders_log.py`, `orders_log.csv` (500
  строк, синтетика, seed `20260801`), `check_env.py`, `view_head.py`,
  `count_lines.py`, `find_row.py`, `check_utf8.py`, `README.md`,
  `reference_answers.md`.
- `program/M0/step-00.md`, `step-01.md` (терминал: `cd`, запуск скрипта,
  текстовый файл в UTF-8), `step-02.md` (первые 20 строк / счёт строк /
  поиск подстроки через скрипты, решение 13), `step-03.md` (`git
  init`/`add`/`commit`, решение 14) — модуль целиком, 7.0–9.0 ч.
- `program/M2/data/` — `generate_sales_extract.py`, `sales_extract_raw.csv`
  (20 000 строк, 4 встроенных дефекта: два формата дат, 5 категорий по 5
  вариантов регистра/пробелов, сумма текстом в 4 форматах с возвратами как
  отрицательными значениями в той же колонке, 4.19% строк со снятым с
  продажи товаром), `product_lookup.csv` (60 товаров), `build_reference.py`
  (SQL-эталон на sqlite), `README.md`, `reference_answers.md`.
- `program/M2/step-00.md` (полная схема на весь модуль — 8 колонок сырых
  данных + план очищенных колонок, объявлены до первого шага) и 8 шагов:
  `step-01`/`step-02`/`step-03` (D1 — очистка даты/суммы, категории и
  решение о трактовке возвратов, итоговая проверка за 90 минут), `step-04`
  … `step-07` (D2 — XLOOKUP/ИНДЕКС+ПОИСКПОЗ, СУММЕСЛИМН, ЕНД/ЕСЛИОШИБКА,
  текстовые функции + итоговая проверка 8/10), `step-08` (D3 — таблица
  против BI).

Все числа во всех 11 файлах шагов — из реального вывода
`build_reference.py` или точечных Python-проверок отдельных строк,
выполненных при написании (не придуманы и не введены вручную).

## Чеклист из 12 пунктов — по всем 11 файлам шагов (M0: 3, M2: 8)

| № | Пункт | Итог |
|---|---|---|
| 1 | ID умения указан, шаг его закрывает | PASS во всех 11. I2 разбит на 2 шага (M0.1/M0.2) + отдельный фрагмент I1 (M0.3); D1 — на 3 шага, D2 — на 4 шага с итоговой проверкой в последнем, D3 — 1 шаг. Частичное покрытие явно обозначено в шапке каждого шага |
| 2 | 8 блоков по порядку | PASS во всех 11 (проверено `grep -c '^## 1\.'`, везде 8) |
| 3 | Пример/задание на сквозном датасете, объявленном в `step-00.md` до первого шага | PASS для обоих модулей. M2 — первая проверка правила на модуле с несколькими встроенными дефектами и накапливающимися колонками-помощниками (7 к концу D1); полная схема сырых данных объявлена в `step-00.md` целиком до `step-01.md`, расширять задним числом не пришлось |
| 4 | Датасет не запрещён; синтетика — с генератором | PASS для обоих. M2 — улучшение по сравнению с M3 раунда 1 (там вручную написанные `INSERT`): датасет на 20 000 строк с 4 встроенными дефектами полностью получен генератором с фиксированным `SEED`, ни одна строка не вписана вручную |
| 5 | Критерий без второго человека, эталон+порог+допуск | PASS с той же оговоркой, что и раньше для концептуальных умений: D3 (`step-08.md`, «≥2 из 4 эталонных критериев») и часть решения о возвратах в `step-02.md` требуют интерпретационной сверки со списком, как A5/H2 в M1 — это уже зафиксированная, принятая особенность (blueprint, часть 7, пункт 7), не новая проблема. Все числовые критерии D1/D2/I2 — строгий числовой diff |
| 6 | Каждое "почему неправильно" — с цифрой | PASS во всех 11. Один пункт в `M2/step-01.md` описывает реальную ошибку, найденную при написании этого же шага (см. ниже) |
| 7 | Сумма часов шагов модуля в вилке части 6.1 | PASS для обоих, без запаса на обеих границах: M0 — 7.0–9.0 (вилка 7–9), M2 — 30.0–40.0 (вилка 30–40) — впервые проверено на 8 шагах вместо 2–3 |
| 8 | Вопрос собеседования + эталон списком | PASS во всех 11 |
| 9–10 | Ссылки / рыночные утверждения | M0 (`step-01`, `step-02`) и M2 (`step-08`) цитируют [М, «Мифы (б)»] (цитата про vim и про «Excel muscle») с явным тегом источника — тег для `step-08.md` изначально был пропущен и добавлен при самопроверке (см. ниже). Внешних ссылок ни в одном из 11 файлов нет — фетч-проверка неприменима, как и в M1/M3 |
| 11 | Запрещённые слова | 0 совпадений после правки. Один ложный `grep`-хит: `давайте` — часть слова «создавайте» в `M0/step-01.md`, не нарушение тона, но переформулировано, чтобы буквальный поиск давал 0 без исключений. `!` встречается только внутри нотации ошибок Excel (`#VALUE!`, `#REF!`, `#DIV/0!`) — не восклицание, оставлено как есть, см. ниже |
| 12 | Данные выгружает учащийся, агент не скачивал | Неприменимо к скачиванию — оба датасета синтетические, сгенерированы локально при написании шагов, как P1/P2/P5 и как M1 |

## Исправлено в процессе (не молча)

1. **Реальный баг в собственной формуле очистки суммы (`M2/step-01.md`).**
   Первая версия формулы `amount_clean` снимала знак минуса отдельно
   (`is_return` определялся по минусу или скобке), но `amount_stripped`
   не убирал сам символ `-` из строки перед `VALUE()`. Для возвратов,
   записанных в скобках, это не давало ошибки (скобки знака не несут), но
   для 445 из 952 возвратов, записанных через минус, знак применялся
   дважды: `VALUE("-3245.08")` уже даёт `-3245.08`, внешний `-` из
   `is_return` превращал это обратно в `+3245.08`. Обнаружено не на
   глаз, а потому что числа в шаге обязаны быть из реального вывода
   скрипта: симуляция формулы на Python дала расхождение с
   `grand_total_amount` (530839498.36 против неверной суммы), что и
   вскрыло ошибку до публикации шага, а не после. Исправлено: знак минуса
   снимается в `amount_stripped` вместе со скобками и суффиксом, знак
   применяется один раз, снаружи. Правильная версия проверена отдельным
   скриптом (`excel-formula-simulated total: 530839498.36`, совпадает с
   `build_reference.py`). Сам инцидент стал материалом для одного из
   пунктов «Типичные ошибки» этого шага — ошибка не гипотетическая, а та,
   что была реально сделана при авторинге.
2. **Пропущенный тег `[М]` у рыночного утверждения в `M2/step-08.md`.**
   Фраза про то, что Excel не вытесняется BI и что нанимающие менеджеры
   отмечают нехватку «Excel muscle» у джунов, была написана без явной
   пометки источника — добавлена ссылка `[М, «Мифы (б)»]` при
   самопроверке по чеклисту (пункт 10), до публикации.
3. **Ложное совпадение «давайте» внутри «создавайте»** в `M0/step-01.md`
   — не нарушение тона по существу (слово «давайте» как приглашение к
   совместному действию не использовалось), но переформулировано, чтобы
   буквальная grep-проверка по чеклисту (пункт 11) не давала лишних
   срабатываний, требующих ручного разбора при каждой проверке.

## М2 как первый длинный модуль (8 шагов) — что произошло с форматом

Пользователь отдельно просил зафиксировать именно это. Коротко: формат
не сломался, но два места показали трение, которого не было на M1/M3.

1. **Правило «`step-00.md` до первого шага» выдержало проверку на
   бо́льшем масштабе.** На M1/M3 (раунд 1) само правило появилось только
   потому, что его не было и схему пришлось расширять задним числом. На
   M2 схема (8 колонок сырых данных, 4 дефекта, план очищенных колонок)
   была объявлена в `step-00.md` целиком до `step-01.md` — ни разу не
   пришлось возвращаться и дополнять `step-00.md` по итогам более поздних
   шагов. Это первое прямое подтверждение, что правило, введённое по
   итогам прошлого раунда, действительно предотвращает тот же класс
   проблемы на более длинном модуле.

2. **Требование «числа только из реального вывода скрипта» на
   Excel-модуле обходится дороже, чем на SQL/терминальном.** В M1/M3
   скрипт, который запускает учащийся, — тот же скрипт, что даёт эталон:
   написал `count_active.py` — прогнал — получил число. В M2 учащийся
   работает в Excel/Sheets, которые агент не может запустить и
   проверить, поэтому для каждой формулы шага (`DATE`/`LEFT`/`MID`/
   `SUBSTITUTE`/`XLOOKUP`/`SUMIFS`/`SUMPRODUCT`) пришлось писать
   параллельную Python-симуляцию именно этой формулы, чтобы получить
   число для текста шага. Это двойное авторство одной и той же логики
   (один раз как формула Excel, второй раз как Python) — и именно оно
   поймало реальный баг (пункт 1 выше) до публикации, а не после. Итог:
   дороже писать, но правило подтвердило свою цену на практике, а не
   только в теории.

3. **Накопление колонок-помощников за пределами того, что явно объявляет
   `step-00.md`.** К концу `step-03.md` (конец D1) на листе уже 7 рабочих
   колонок (`order_date_clean`, `quarter`, `is_return`, `amount_stripped`,
   `amount_stripped_final`, `amount_clean`, `category_clean`), но
   `step-00.md` в разделе про датасет называет по имени только 4 из них
   (`category_clean`, `order_date_clean`, `quarter`, `amount_clean`) —
   те, на которые опираются шаги D2. Промежуточные scratch-колонки
   (`is_return`, `amount_stripped`, `amount_stripped_final`) сознательно
   не включены в декларацию модуля, потому что ни один шаг после
   `step-01.md` на них не ссылается напрямую. Решение защитимое (то же
   рассуждение, что для внутренних переменных функции — не всё, что
   существует внутри шага, обязано попадать в декларацию модуля), но это
   не проверялось явно скиллом раньше и стоит иметь в виду на следующем
   длинном модуле: если в будущем модуле промежуточная колонка всё же
   понадобится позже, привычка не декларировать её станет проблемой того
   же типа, что и в раунде 1 с M3.

4. **Требование «решение без единственного правильного ответа, начиная с
   шагов, ведущих к проектам» удовлетворено на уровне модуля, а не на
   уровне каждого шага.** Из 8 шагов M2 такое решение явно есть только в
   двух: `step-02.md` (включать ли возвраты в выручку — три определения,
   разный выбор для разных получателей отчёта) и `step-08.md` (сама суть
   умения D3). Четыре формульных шага D2 (`step-04`–`step-07`) —
   механические katas на конкретную функцию, без деловой неоднозначности:
   XLOOKUP находит то, что находит, `SUMIFS` считает то, что считает.
   Формулировка скилла («в задании есть хотя бы одно решение... начиная с
   шагов, ведущих к проектам») прочитана как «в модуле есть хотя бы одно
   такое решение», а не «в каждом шаге» — иначе 4 из 8 шагов потребовали
   бы придуманной, неестественной неоднозначности только ради формального
   соответствия. Это осознанная трактовка, не молчаливое пропускание
   пункта, и текст скилла стоит уточнить явно на этот счёт, если трактовка
   не совпадает с намерением.

5. **Конфликт «кириллица в данных» × «ASCII-вывод скрипта в консоли»
   впервые проявился по-настоящему на M2.** В M0 и M1 значения полей —
   латиница/ASCII по конструкции, конфликта не было. В M2 категории
   (`Ноутбуки`, `Смартфони`…) — по сути украинские, и `build_reference.py`
   должен был и работать с кириллицей (сравнение, группировка), и
   безопасно печатать агрегаты в консоль на Windows. Решение — печатать
   ASCII-коды категорий (`laptops`, `phones`…) в выводе скрипта, а
   каноническую кириллицу — только в markdown-файлах (не через print).
   Задокументировано в `program/M2/data/README.md`, но само правило пока
   нигде не закреплено на уровне скилла — оно последовательно с уже
   существующим доводом в `program/M1/data/count_active.py`, но явной
   строки в `curriculum-design/SKILL.md` для этого нет. Не менял скилл
   самостоятельно в рамках этого захода — это на усмотрение пользователя:
   решить, стоит ли закрепить конвенцию отдельной строкой, аналогично тому,
   как раунд 2 пилота M1/M3 закрепил правило `step-00.md`.

## Что не проверялось в этом заходе

- Фактическое время прохождения (`research/self.md` по-прежнему пуст) —
  оценки часов всех 11 шагов проектные, требуют калибровки.
- M2 не выполнялась вживую в реальном Excel/Google Sheets — формулы
  проверены логически и через параллельную Python-симуляцию, но не через
  фактический запуск в самом приложении (агент не имеет доступа к
  Excel/Sheets в этой среде).
