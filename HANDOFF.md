# Передача проекта

Сводка для входа в проект в новой среде. Составлена 2026-08-13, коммит
`78484cd`; актуализирована 2026-08-17 дважды (заход по решению 28: M4
написан целиком, вилка его часов приведена к сумме шагов; заход, начавший
M5) и 2026-08-19 дважды (заход по определениям чисел, из которого вышло
**решение 30: число приводится вместе с определением, которое его
порождает**; заход по шагу 04, умение B2); 2026-08-20 четырежды (заход по
шагу 05, умение B3; заход по шагу 06, умение B4; заход по шагу 07 —
умение B5, **M5 написан целиком** — и решению 31, вилка M5 приведена к
сумме шагов, 80–120 → 43–56; заход, написавший **M6 целиком** — B1/B2
повторно, сверка леджера с расчётом эквайера).
Состояние: **M5, M6, M7, M8 закрыты; следующее действие — M9
(Статистика-ловушки) или M10 (Git)**, оба готовы по карте зависимостей
(`M8 → M9`, `M0 → M10`) — порядок между ними не задан частью 3.1
blueprint, выбирать по своему усмотрению захода. M7 — первый модуль,
который автор не мог прогнать сам (нет доступа к консоли BigQuery);
числа в нём — гарантии конструкции датасета и документированного
поведения BigQuery, а не измерения, и это названо прямо в
`program/M7/step-00.md` и `DEFERRED.md`. Здесь нет содержания — только
где что лежит, в каком состоянии и что делать первым. Каждый пункт со
ссылкой на файл, который является источником истины; при расхождении
прав файл, а не эта сводка.

---

## 1. Что это

Программа обучения: от нуля до первой оплачиваемой работы, где анализ
данных — основная или существенная часть обязанностей, на украинском
рынке. Международный найм — цель второго горизонта, не результат
выпуска. Русский язык, markdown-репозиторий, никакого кода продукта:
артефакт проекта — это текст программы.

- Цель, читатель, семь незыблемых правил, порядок фаз — `CLAUDE.md`.
- Определение готовности (30 умений в топ-15, 36 ID всего), обратное
  проектирование модулей, портфолио из 6 проектов, график —
  `design/blueprint.md`.
- Обоснование каждого модуля рынком — `research/market.md`,
  `research/competitors.md`, `research/access.md`, `research/gap.md`.

**Объём:** 464–617 ч построчной суммой (`design/blueprint.md`, 6.1; было
478–642 до решения 28). При 25 ч/нед — 4–6 месяцев, при 10 ч/нед — 11–14.
Срок поиска работы сверх этого не обещается: данных нет.

**Ограничения (решение 21, `design/decisions.md`).** Читатель ровно один
— автор программы, и он начнёт её проходить после того, как она будет
дописана. Отсюда сужение: платформа только Windows, домен только
fintech, окружение — версии и пути автора, международный срез рынка не
расширяется. Не сужаются: проверяемые критерии, честные часы, числа
только из реального прогона, `tools/check_consistency.py`.

Всё отложенное сужением — `DEFERRED.md`, по строке на пункт, каждая
строка обязана называть существующий файл (проверяется скриптом).
Расширение под аудиторию должно быть проходом по этому списку, а не
поиском по репозиторию.

---

## 2. Где мы

**Фазы 0 и 1 закрыты.** Идёт Фаза 2 (контент). Впереди Фаза 3 (карьера)
и Фаза 4 (аудит). Порядок фаз не перескакивается — `CLAUDE.md`.

**Написано: 49 содержательных шагов в девяти модулях; M5, M6, M7, M8
закрыты целиком.**

| Модуль | Шагов | Состояние |
|---|---|---|
| M0 Онбординг, терминал | 3 | написан |
| M1 Постановка задачи | 3 | написан |
| M2 Таблицы | 8 | написан; ни разу не выполнен в реальном Excel/Sheets |
| M3 SQL | 12 | написан; SQLite + PostgreSQL |
| M4 Power BI | 5 | **написан целиком** — C1 (шаги 02–04) и C2 (`step-05.md`); шаги 01–04 пройдены руками в Desktop — гейт закрыт, 38 дефектов, из них 23 закрыто, 1 «не чинится», 14 открыто. Критерий C2 прогоном не проверен: `step-05.md` не пройден ни разу (строка в `DEFERRED.md`) |
| M5 Python | 7 из 7 — **закрыт** | начат 2026-08-17, закрыт 2026-08-20: `step-00.md` (разбивка B1–B5 на 7 шагов), датасет с генератором, эталоны и контрольная точка, `step-01.md` (инфраструктура), `step-02.md`–`step-03.md` (B1), `step-04.md` (B2), `step-05.md` (B3), `step-06.md` (B4), `step-07.md` (B5). Вилка часов приведена к сумме шагов решением 31 (43–56, было 80–120). Ни один шаг никто не проходил — единственное, что остаётся открытым |
| M6 Данные, которые ломаются | 4 из 4 — **закрыт** | написан 2026-08-20: `step-00.md` (B1/B2 повторно — сверка леджера с расчётом эквайера, а не парсинг), `step-01.md` (B1, первый проход сверки), `step-02.md` (B2, объяснённые причины, 0 необъяснённых), `step-03.md` (B2, статистические выбросы по медиане счёта), `step-04.md` (B2, нарушения бизнес-правил статуса). Часы 16–20 попали в вилку 6.1 (15–20) без правки. Ни один шаг никто не проходил |
| M7 BigQuery / DWH | 4 из 4 — **закрыт** | написан 2026-08-20: `step-00.md` (датасет — SQL-генератор внутри BigQuery, 4 386 000 строк, без файла на диске — «Отступление от конвенции модуля»), `step-01.md` (инфраструктура, sandbox без карты), `step-02.md`–`step-03.md` (E1, партиционирование/кластеризация, ≥5× гарантировано конструкцией — 731 равная партиция), `step-04.md` (E2, `SELECT *`/`LIMIT`, оба — верифицированные цитаты документации). **Автор не имел доступа к консоли BigQuery** — числа шагов не измерены прогоном, а гарантированы схемой и документацией; первый реальный прогон — `DEFERRED.md`, раздел «Проверка BigQuery» |
| M8 Метрики | 3 из 3 — **закрыт** | написан 2026-08-22: `step-00.md`, `step-01.md` (CPL/CPA по каналам, CAC смешанный — блендовый CAC ниже любого канального CPA), `step-02.md` (LTV/ROAS/ROMI/ROI, ROMI=ROAS−1 тождественно), `step-03.md` (Retention/Churn D30, незрелая когорта исключена, а не занулена — измерено занижение на 8,1 п.п. при ошибке). Часы 15–20 совпали с вилкой 6.1 по обеим границам — `[WARN]`, вилка не правилась с Фазы 1 |

Модули M9–M16 и проекты P1–P6 не начаты.

**Объём шага сокращён — решение 28 (2026-08-17).** Принцип: сокращаем
упаковку, не содержание. Теория 1.2 вдвое, типичные ошибки только
измеренные, шаги укрупняются (потолок 10 ч), навигация по GUI коротко,
полировка ради вида не делается. Не сокращается никогда: покрытие 30
умений, шесть проектов, критерий с эталоном/порогом/допуском, блок 1.8,
честные часы. Написанные 31 шаг задним числом не режутся — принцип
действует на M5–M16 и P1–P6. Правило в скилле — раздел 0.

**Покрытие умений.** Шап­ку `Умение: <ID>` имеют 21 ID из 36 (F1 —
теперь закрыт M8); 15 ID не имеют ни одного шага — это и есть
единственный `[FAIL]` скрипта, и он означает «модули не написаны», а не
дефект.

**Часы.** M0/M1/M2 сведены под вилку по обеим границам — признак подгонки
(решение 24). **Вилка M4 приведена к сумме шагов: 40–60 → 26–35**
(решение 28), потому что недостающего содержания больше нет. Отсюда новые
числа программы: итог **464–617 ч**, ядро **238–334**, календарь
**47–62 недели** (11–14 месяцев) при 10 ч/нед и **19–25 недель**
(4–6 месяцев) при 25 ч/нед. M4 теперь тоже даёт `[WARN]` о совпадении с
вилкой по обеим границам — форма та же, происхождение обратное: вилка
поставлена по сумме, а не сумма подведена под вилку (решение 28, часть 3;
`program/M4/step-00.md`). Сама вилка остаётся неизмеренной — калибровка
замером первого прохождения, строка в `DEFERRED.md`.

Различие двух `[WARN]` теперь печатает сам скрипт: строка части 6.1 может
нести пометку **«вилка по факту (решение 28)»**, и для таких модулей текст
предупреждения другой — «вилка выставлена по сумме шагов, прохождением не
подтверждена». Третья ветка ловит устаревшую пометку. Отрицательные
примеры на трёх состояниях файла — `research/tools-gate.md`, 5.12.

**M5 закрыт: сумма семи шагов — 43–56 ч, вилка правлена решением 31 под
эту же сумму** (была 80–120, оценка [М] для темы «Python» целиком).
Совпадение с проекцией `step-00.md` от 2026-08-17 — до часа: модуль не
подгонялся под известное число, число было названо раньше, чем написан
хоть один шаг. `check_module_hours()` печатает на M5 `[WARN]` «вилка
выставлена по сумме шагов, прохождением не подтверждена» — тот же второй
вид предупреждения, что у M4 после решения 28.

**Текущий вывод `tools/check_consistency.py`** (дословно, 2026-08-22,
конец захода, написавшего M8 целиком):

```
[OK]   6.1: распарсено 24 строк, построчная сумма 427–553
[OK]   CLAUDE.md: 427–553 совпадает с частью 6.1
[OK]   6.2 Итого: 427–553 совпадает с частью 6.1
[OK]   часть 2.1: 7 модулей, сумма 201–270 ч — совпадает с заголовком, строкой «Итого» и вилками части 6.1
[OK]   часть 2.2: 10 модулей, сумма 110–137 ч — совпадает с заголовком, строкой «Итого» и вилками части 6.1
[WARN] M0: сумма 3 шагов 7–9 ч при вилке 6.1 7–9 — совпадение по обеим границам: признак подгонки суммы под вилку, а не подтверждение оценок (решение 24)
[WARN] M1: сумма 3 шагов 8–10 ч при вилке 6.1 8–10 — совпадение по обеим границам: признак подгонки суммы под вилку, а не подтверждение оценок (решение 24)
[WARN] M2: сумма 8 шагов 30–40 ч при вилке 6.1 30–40 — совпадение по обеим границам: признак подгонки суммы под вилку, а не подтверждение оценок (решение 24)
[OK]   M3: сумма 12 шагов 60–77 ч при вилке 6.1 60–80
[WARN] M4: сумма 5 шагов 26–35 ч при вилке 6.1 26–35 — вилка выставлена по сумме шагов «вилка по факту (решение 28)», прохождением не подтверждена: калибровать её нечем, пока модуль никто не прошёл
[WARN] M5: сумма 7 шагов 43–56 ч при вилке 6.1 43–56 — вилка выставлена по сумме шагов «вилка по факту (решение 31)», прохождением не подтверждена: калибровать её нечем, пока модуль никто не прошёл
[OK]   M6: сумма 4 шагов 16–20 ч при вилке 6.1 15–20
[OK]   M7: сумма 4 шагов 19–26 ч при вилке 6.1 20–30
[WARN] M8: сумма 3 шагов 15–20 ч при вилке 6.1 15–20 — совпадение по обеим границам: признак подгонки суммы под вилку, а не подтверждение оценок (решение 24)
[OK]   часть 1: заявлено 30 умений, построчный подсчёт по секциям без пометки 'вне топ-15' совпадает
[OK]   часть 1: всего 36 ID умений во всех секциях (A1..K2), включая 'вне топ-15'
[FAIL] умения без шапки 'Умение: <ID>' ни в одном шаге program/**/*.md: C3, C4, F2, F3, G1, G2, G3, H1, H3, J1, J2, J3, J4, K1, K2
[OK]   program/M*/step-NN.md: все 49 шагов объявили вид — 45 с умением, 4 без; инфраструктурные: program/M3/step-09.md, program/M4/step-01.md, program/M5/step-01.md, program/M7/step-01.md
[OK]   design/decisions.md: 31 решений, нумерация сплошная 1..31
[OK]   research/tools-gate.md: 38 дефектов со статусом (R1..R37 и D1) — открыт: 14, закрыт: 23, не чинится: 1
[OK]   program/M*/data/: календарь покрывает дату расчёта, сверено папок: 2
[OK]   DEFERRED.md: 25 строк отложенного, все 57 путей существуют
[OK]   все 9 начатых модулей имеют step-00.md
[OK]   program/M*/data/README.md: сверено 35 строк из раздела «Проверка строк» во всех 9 файлах
[OK]   эталоны на состоянии базы своего шага: сверено 8 пар (CSV ↔ запрос) в модулях: M3. НЕ покрыто: эталоны PostgreSQL (step-09..step-12 — нужен поднятый сервер, числа проверены вручную одним прогоном); activity_log (не коммитится, миллионы строк — сверяется через sha256 контрольной точки); модули без SQL-эталонов: M0, M1, M2, M4, M5, M6, M7, M8
[OK]   program/M*/data/: проверено 78 файлов в 9 папках, ни одного не покрытого README/.gitignore не найдено
[OK]   program/**/*.md: ни одной из 6 надписей, измеренных как неверные (research\pbi-ui-labels.md, раздел 2)
[WARN] research\pbi-ui-labels.md: 4 надписи взяты не с экрана — подтвердить или опровергнуть на первом прогоне M4 (раздел 3)
[OK]   program/**/*.md: запрещённых слов и POSIX-команд в коде не найдено

Есть проблемы — см. [FAIL] выше.
```

Код возврата 1. Это ожидаемое состояние Фазы 2, а не регресс.

---

## 3. Как здесь работают

**Семь правил — `CLAUDE.md`.** Кратко, без пересказа: всё пишется в
файлы, в чат — отчёт до 10 строк; каждое утверждение о рынке со ссылкой
на источник; каждая ссылка проверена через fetch; ни одного модуля без
обоснования из `research/market.md`; ни одного шага без практики и
проверяемого критерия; часы честные; тон без воды и мотивационных
вставок.

**Скилл `curriculum-design`** (`.claude/skills/curriculum-design/SKILL.md`)
— обязателен при создании, дописывании, переписывании и ревью любого
учебного текста, включая правку одного абзаца или одной оценки часов. Он
задаёт восьмиблочную структуру шага (шапка, 1.1 цель — 1.8 вопрос
собеседования), тон, формат критерия готовности (эталон + порог +
допуск), требование, чтобы каждое «почему неправильно» содержало число,
полученное на датасете модуля, и чеклист перед сдачей шага. **Раздел 0 —
объём:** восемь блоков применяются в сокращённом виде (решение 28), и
читать его надо до раздела 1, иначе структура применится в полном.

**`tools/check_consistency.py`** — единственная механическая защита
репозитория. Проверяет: сумму часов построчно против blueprint и
CLAUDE.md, покрытие умений шапками шагов, сплошную нумерацию решений,
существование всех путей из `DEFERRED.md`, числа строк в
`data/README.md`, эталоны на состоянии базы своего шага, отсутствие
файлов вне README, запрещённые слова и POSIX-команды, **учёт дефектов
гейта** (`check_defect_status()`: ровно одна строка статуса на дефект,
три разрешённые формы, сплошная нумерация — решение 27), **надписи
интерфейса** (`check_ui_labels()`: ни одной формы, измеренной как
неверной, в `program/**/*.md` — реестр `research/pbi-ui-labels.md`),
**часы частей 2.1/2.2 против 6.1** (`check_part2_hours()`: вилка каждого
модуля, строка «Итого» и число в заголовке раздела — заведено 2026-08-17,
после того как эти итоги полтора месяца отставали на решение 14).
Гоняется в конце
каждого захода, вывод цитируется дословно. Правило, не проверяемое
скриптом, живёт до первого захода, в котором про него забудут (решение
20) — поэтому новое правило по возможности заводится вместе с проверкой.

**Отчёт о заходе** обязан содержать строку **«Решения: затронуты N, N;
заведены N»**. Если правка не следует ни из одного решения и нового не
заводит — писать «затронуты нет, заведены нет». Формат и обоснование —
раздел «Отчёт о заходе» скилла; примеры всех заходов — `program/pilot-report.md`.

**Решения.** `design/decisions.md`, 29 штук, нумерация сплошная. Отменять
решение можно только новым решением с пометкой в старом (образец —
решение 19, п. 3, отменённый решением 23). Ссылаться на решение по
номеру и дате.

**Git.** Работа в `master`. Артефакты коммитятся сразу, в конце сессии
`git status` не должен показывать untracked. **`git push` в конце каждой
сессии — обязателен**, он не делается автоматически.

**Прогон вместо чтения.** Из 8 заходов пилота дефекты, которые нельзя
было найти чтением, нашли реальный прогон, проверка пользователем фактов
(а не отчёта) и добавление скриптовой проверки — `audit/midphase-report.md`,
раздел «Одно замечание о самом процессе». Это же подтвердила Часть 3
гейта: **36 дефектов инструкций и дефект датасета на четырёх шагах из
четырёх**, причём десять на шаге 03 и девять на шаге 04. Шесть из
тридцати шести сидят в самих критериях готовности (R22, R30, R32, R33,
R36) — то есть проверка, которой шаг предлагает себя проверить, врёт в
обе стороны: пропускает несделанное и не пропускает верное.

---

## 4. Что открыто

### 4.1. Гейт закрыт — из 38 дефектов открыто 16

Решение 26 запрещало писать умение C2, пока написанные шаги M4 не
пройдены руками как учащимся. **Все четыре пройдены**, все пять пунктов
решения 26 отвечены. Запрет на C2 снят; снят и запрет на починку —
правки теперь лягут на проверенный текст, а не на предположения.

Единственный источник истины по прогону — `research/tools-gate.md`,
Часть 3. Не пересказывать, читать:

- **3.3** — 37 дефектов инструкций R1–R37, каждый с измерением и со
  строкой `Статус:` (учёт заведён решением 27, часть 2).
- **3.4** — что прогон **подтвердил**: все числа шагов 02–04 сошлись до
  копейки, `TOTALYTD` работает под культурой `"ru"`, разделитель
  аргументов DAX — запятая, три сверки экспорта из трёх, формат выгрузки
  ровно такой, как записало решение 22.
- **3.5** — итог по каждому шагу и итоговое состояние модели.
- **3.6** — что осталось незакрытым и что не убрано с диска.
- **3.7** — вердикт и **два класса дефектов**, ради которых гейт и
  заводился: молчаливые неверные исходы и критерии, которые не
  проверяют того, что обещают.
- **3.8** — **дефект датасета D1**: календарь был короче `settled_date`,
  30 операций на 47482,23 проваливались в пустую строку. **Закрыт
  2026-08-14** (`17d7bc6`), там же записано, что именно изменилось после
  пересборки.

Четыре самых тяжёлых, если читать некогда:

| | Что | Цена |
|---|---|---|
| R30 | `check_pbip.py --step 03` непроходим в **обоих** состояниях модели | машинный критерий шага 03 не выполняется никогда |
| R32 | `--step 04` печатает код 0 на шаге, где из восьми заданий сделаны два | критерий подтверждает несделанную работу |
| R35 | два экспорта одного визуала разошлись целиком из-за перекрёстной фильтрации, в файле следа фильтра нет | неверен сам артефакт сдачи шага |
| R36 | критерий задания 8 требует число, измеренное для другой подстановки | верный ответ засчитывается как неверный |

### 4.2. Корзина «до M4» — что осталось

Приоритеты заведены `audit/midphase-report.md`, раздел «ПРИОРИТЕТЫ».

| | Что | Состояние |
|---|---|---|
| A1 | `sqlite3` CLI: откуда взять | **закрыт** — дефект B1 гейта, `research/tools-gate.md`, раздел 4 |
| A2 | Прогнать M2 в реальном Excel | **открыт** — семь формул не введены ни разу; требует работы человека в GUI |
| A3 | Нулевая проверка M4, 6 пунктов | **закрыт** — Часть 2 гейта |
| A4 | Как жить с инфраструктурными шагами | **закрыт** — решение 23 |
| A5 | Перестать сводить часы под вилку | **закрыт** — решение 24 и `[WARN]` скрипта |
| A6 | Механизм проверки для M16 / Looker Studio | **открыт** — не трогалось |
| A7 | Разбивка A2: 5 против 7 | **закрыт** — решение 23 отменило п. 3 решения 19 |

### 4.3. Корзина B — со сроком, иначе не сделается

15 пунктов, `audit/midphase-report.md`, корзина B. Самые ранние по сроку:

- **B15** — пригодность источников P3 (Prozorro + НБУ): срок был «до
  `program/M4/step-00.md`», и он **просрочен** — `step-00.md` написан.
- **B8** — Git не появляется между `M0/step-03.md` и P1: 20 шагов без
  единого коммита; срок «до P1».
- **B1** — H1/H3/P2/P6 требуют второго человека; срок «до P2».
- **B2/B3/B13** — шесть файлов заявляют macOS/Linux/WSL после решения 21,
  `python3` против `python`, «Excel или Google Sheets»; срок «до конца
  Фазы 2».
- **B9** — `README.md` и `resources.md` в корне по 0 байт; стоит 20 минут.
- **B5** — инверсия блоков 1.2/1.3 в M2 решается вместе с M4: если та же
  инверсия повторится в DAX-шагах, править надо скилл, а не M2.

Корзина C («не чинить», 7 пунктов) — там же; каждое «не чинить» с
обоснованием.

### 4.4. Открыто по существу, а не по недосмотру

Четыре пункта без решения — `design/decisions.md`, раздел «Что осталось
нерешённым», и `design/blueprint.md`, часть 7. В `DEFERRED.md` сознательно
не перенесены: у них нет решения, а не нет времени.

- Командный кросс-функциональный проект не воспроизводится в одиночку.
- Ревью кода человеком заменяется pytest и воспроизводимостью лишь частично.
- Порог перехода на ступень 2 (международный найм) неизвестен.
- Самопроверка концептуальных умений (A5, H2, G2, G3, F2) мягче остальных.

### 4.5. Хвосты прогона — уже в истории git

Коммит `317d831` («gate: M4 manual run, 4 instruction defects (in
progress)») положил в репозиторий папки прогона целиком: 52 файла,
`program/M4/pbi/` и `program/M4/pbi-probe/`. Вместе с ними в историю
попало то, что решение 22 (раздел «Что коммитится») коммитить запрещает:

- `program/M4/pbi/m4.SemanticModel/.pbi/cache.abf` — 259 094 байта;
- `program/M4/pbi-probe/probe.SemanticModel/.pbi/cache.abf` — 12 135 байт;
- `.pbi/localSettings.json` в четырёх местах;
- `program/M4/pbi/m4.pbix` — 18 080 байт, след дефекта R3.

Это прямое следствие дефекта **R4**: `.gitignore` внутри репозитория
Power BI Desktop не создаёт, а корневой `.gitignore` строки про `.pbi/`
не содержит. Разбирать придётся вместе с починкой R4 — решить, что из
папки прогона остаётся в репозитории как артефакт гейта, что уходит в
`.gitignore`, и переписывать ли историю ради 271 КБ бинарного кэша.

Уборка самого прогона (`m4.pbix`, `pbi-probe`, `D:\pbi-probe2`) записана
в `research/tools-gate.md`, 3.6, и не сделана.

---

## 5. Следующее действие

**M8 закрыт целиком (3 из 3 шагов), 2026-08-22.** Следующие готовые по
карте зависимостей — `M9 Статистика-ловушки` (`M8 → M9`) и `M10 Git`
(`M0 → M10`, не зависит от M8). Оба не начаты; порядок между ними в
части 3.1 blueprint не задан — выбор следующего модуля свободный.

**M8 закрыт восемью метриками в три шага** (F1): `step-01.md` —
CPL/CPA по каналам и CAC смешанный (блендовый CAC ниже любого
канального CPA — измерено, 368.15 против 193–624; наивное среднее трёх
CPA даёт 458.36, тоже неверно); `step-02.md` — LTV D30/ROAS/ROMI/ROI
(LTV:CAC) (тождество `ROMI = ROAS − 1` проверяется в самом скрипте);
`step-03.md` — Retention/Churn D30 (незрелая когорта — 147 из 955
клиентов, у кого 30-й день ещё не наступил на дату выгрузки 2026-05-15
— исключается из расчёта, а не приравнивается к оттоку; ошибка даёт
44.71% вместо верных 52.85%, разница 8,1 п.п.). Датасет и дата выгрузки
спроектированы так, чтобы раскол на зрелые/незрелые когорты был
неизбежен, а не гипотетическим случаем.

**M7 — первый модуль без реального прогона.** Автор не имеет доступа к
консоли BigQuery; все числа шагов 02–04 (объём сканирования, кратность
падения, стоимость) — гарантии, вытекающие из конструкции датасета
(731 партиция ровно по 6000 строк) и из документированного поведения
BigQuery (partition pruning, колоночное хранение, `LIMIT` не сокращает
чтение), а не измерения. Это отличие от M0–M6 названо прямо в
`program/M7/step-00.md`, `data/reference_answers.md` и `DEFERRED.md`,
раздел «Проверка BigQuery» — тот же класс ограничения, что был у M2
(Excel не открывался) и у черновика M4 до гейта, только структурный, а
не временный: доступ к GCP-консоли у среды написания отсутствует в
принципе. Датасет создаётся SQL-запросом `CREATE TABLE ... AS SELECT` с
`GENERATE_ARRAY`/`UNNEST` прямо внутри BigQuery — решение обойти
неясность вокруг поддержки batch-загрузки CSV в sandbox-режиме
(документация не подтверждает и не запрещает её явно).

**M6 закрыт умениями B1/B2 повторно** — не описка, а осознанное
решение, объяснённое в `program/M6/step-00.md`: часть 1 blueprint не
заводит отдельного ID под «сверку двух источников» и «поиск аномалий»,
и обе задачи по механике — соединение с неидеальным ключом и объяснённая
потеря (то есть B2), только ключ двусторонний, а «потеря» — расхождение
между двумя версиями правды, а не пропавшая строка при одностороннем
join. Датасет — сверка внутреннего леджера платёжного сервиса с
расчётной выпиской эквайера (`program/M6/data/generate_m6.py`,
`SEED = 20260821`, 2324 операции леджера / 2204 записи расчёта).
Обе стороны читаются, сверяются, и все 51 несовпадение объясняются
целиком двумя причинами (окно отставания и дубль ретрая по бизнес-ключу
— **0 необъяснённых**), поверх этого — статистические выбросы (по
медиане счёта, не по общей: общий порог даёт 23 ложных и пропускает 1
реальный, измерено `step-03.md`) и нарушения двух бизнес-правил статуса
(`failed` не должен иметь расчёта — 8 нарушений; `reversed` должен иметь
компенсацию в 5 дней — 21 из 103).

**M7 — следующий модуль для написания.** Перед `program/M7/step-00.md`
— тот же порядок входа, что у M4/M5/M6: прочитать строку M7 в части 2.1
blueprint (E1, E2; 20–30 ч; топ-15 №7, 23.6%, BigQuery 8.9% против
Snowflake 7.3%), решить разбивку шагов, спроектировать датасет.
**Ограничение, которого не было у M5/M6:** BigQuery — облачный сервис, а
`sandbox` этого агента не имеет доступа к GCP-консоли и не может
подтвердить прогоном фактическое выполнение запроса или объём
просканированных байт (умение E1 требует «объём сканирования падает ≥5×
(байты до/после зафиксированы скриншотом)», E2 — «сходится с оценщиком
консоли ±10%»). Придётся решить на входе в модуль: писать SQL и логику
партиционирования/кластеризации с уверенностью в синтаксисе (BigQuery
Standard SQL — диалект, отличия от SQLite/PostgreSQL M3 надо будет
явно назвать), но без прогона в реальной консоли — по тому же принципу,
по которому M2 (Excel) и черновик M4 до гейта писались до прогона
человеком, с явной пометкой в `DEFERRED.md`, что первый реальный прогон
в консоли BigQuery ещё предстоит.

**Шаг 07 (B5, график matplotlib) закрыл M5.** Два подграфика с общей
осью X (оборот сверху, комиссия снизу, каждый в своём масштабе) — решение,
явно отвергающее `ax.twinx()`: программа уже запрещает двойные оси для
дашбордов Power BI (P3, критерий 13, `design/decisions.md`), и тот же
принцип применён здесь. Наивный вариант (один общий Y) измеримо непригоден:
ряд комиссии занимает 0.34% высоты графика. Проверка структуры графика
(число осей, подписи, единицы, легенда, тип осей) сделана механической —
читает те же объекты `Figure`/`Axes`, которыми график построен, — вместо
того чтобы полагаться на взгляд, честно признанный самым слабым классом
проверки в программе (`design/decisions.md`, о критериях BI-визуалов).

**Решение 31 (2026-08-20): вилка M5 приведена к сумме семи шагов — 43–56 ч,
было 80–120.** Совпадение с проекцией `program/M5/step-00.md` от
2026-08-17 — до часа, без подгонки задним числом. Программа целиком
сократилась на те же 37–64 ч: итог 464–617 → **427–553**, ядро 238–334 →
**201–270**, календарь 47–62 → **43–56 недель** при 10 ч/нед (19–25 →
**18–23** при 25 ч/нед), то есть 11–14 → **10–13 месяцев** (4–6 →
**4–5**). `tools/check_consistency.py` заодно обобщён: пометка «вилка по
факту» в части 6.1 раньше была захардкожена под решение 28 (`BY_FACT_MARK`
= ровно эта строка); теперь `BY_FACT_RE` разбирает номер решения из самой
строки, и M4 с M5 печатают верное сообщение каждый со своим номером.

**Находки шага 05 (в силе, не забыть при ревью M5).** Колонка
`fee.amount` после `json_normalize` в шаге 03 остаётся строкой (JSON
хранит сумму как `"2192.54"`) — впервые ломается в арифметике шага 05:
`ValueError: Unknown format code 'f' for object of type 'str'` при
попытке напечатать результат `.sum()` строкового столбца. Отдельно —
сумма валютных выплат без округления каждой строки до 2 знаков перед
суммированием даёт **159881549.92** вместо **159881549.91**, без единого
предупреждения. Обе — `step-05.md`, разделы 1.3 и 1.6.

Что уже измерено и не надо измерять заново — `program/M5/data/reference_answers.md`,
два последних раздела: шесть находок прогона pandas 3.0.5, включая две,
которые станут «типичными ошибками» с настоящими числами. Первая —
`pd.read_json` на вложенном файле падает. Вторая — наивное соединение со
справочником: **5571 строка из 3534, +57.6%**, и дальше по конвейеру
оборот 253 738 225.56 вместо 159 881 549.91 (+58.7%) без единого
сообщения об ошибке. **Число идёт в шаг только вместе с определением**,
а определение состоит из трёх независимых ответов: тип соединения
(`inner` по умолчанию), три дубля кода (оставлены) и пять записей без кода
(оставлены все пять). При других ответах то же действие даёт 3523, 3534,
3695, 3706, 5399, 5410 или 5582 — все восемь посчитаны в
`ref_naive_merge.csv`. Отдельно измерена ловушка: вариант «`inner`, дубли
сняты, записи без кода схлопнуты» даёт ровно эталонные **3523** строки
(эталон после join со справочником — именно 3523, а не 3534: одиннадцать
выплат с кодом вне справочника в отчёт не попадают и названы строкой
`ref_join_report.csv`). Партнёр при этом не тот у 414 строк из 3523 —
сверка по числу строк такое решение пропустит, поэтому критерий шага
сверяется `ref_partner_report.csv` целиком. Число «заглушка угадала 55 из
469» тоже идёт с определением: заглушка — первая по порядку строк запись
без кода (`ФОП «Барвінок Маркет»`), названия сравниваются по правилу 3
канона; при строгом равенстве было бы 36, при одной обрезке краёв — 43.

M4 закрыт: написан целиком, гейт пройден, корзины 0–5 решения 27 закрыты.
Единственное, что по нему остаётся, — **критерий C2 прогоном не
проверен**: `step-05.md` не пройден руками ни разу, и формат записи
параметра запроса в TMDL не измерен. Это строка в `DEFERRED.md` (раздел
«Проверка Power BI»), и она ждёт первого прохождения модуля, а не захода
написания.

Ниже — история починки M4, оставленная как протокол: что каким коммитом
закрыто и что осталось открытым.

**Починка M4 по итогам гейта — закончена.** Порядок был зафиксирован
**решением 27** (2026-08-14), там же —
три требования к работе: каждая правка называет номер закрываемого
дефекта и ставит ему `Статус: закрыт` в `research/tools-gate.md`; корзины
0, 1 и 4 обязаны оставить машинную проверку, а не только правку текста;
цена — 5–8 заходов, меряется по `program/pilot-report.md`.

**Корзина 0 закрыта 2026-08-14** — тем же заходом, что завёл решение 27.
Все 37 записей (`R1–R36`, `D1`) имеют строку `Статус: открыт`; формат
статуса задан жёстко тремя формами (решение 27, часть 2);
`check_defect_status()` в `tools/check_consistency.py` падает на
отсутствующем статусе, на статусе своими словами и на дыре в нумерации.
Отрицательный пример прогнан и записан дословно —
`research/tools-gate.md`, 5.5. Слово `Статус:` в начале строки в
`tools-gate.md` зарезервировано под учёт, шапка файла переписана на
`Состояние:`.

Дальше по зависимостям:

1. **D1 — датасет. Закрыто 2026-08-14, коммит `17d7bc6`.** В
   `generate_m4.py` заведена граница `CALENDAR_TAIL_DAYS = 7`, обе папки
   пересобраны прогоном. Изменился **только календарь**: 546 → 553 и
   577 → 584 строки с новыми sha256; операций с `settled_date` вне
   календаря 0 вместо 30 и 50. Три `ref_*.csv` пересобраны и совпали с
   прежними построчно — ни одно эталонное число не поехало, потому что
   все они считаются по `tx_date`. Инвариант стерегут две проверки:
   генератор до записи файлов и `check_calendar_covers_settled()` в
   `check_consistency.py`. Отрицательный пример — `tools-gate.md`, 5.6.
2. **Критерии. Закрыто 2026-08-14**, коммиты `a581725` и `62b9b85`.
   R22, R30, R32, R33, R36 переписаны вместе с `check_pbip.py`:
   `--step 03` проходит в **обоих** состояниях модели и падает на
   состоянии конца шага 02; `--step 04` требует артефакт каждого задания
   поимённо и даёт 11 `[FAIL]` там, где прежняя редакция печатала восемь
   `[OK]` и код 0. Опора критерия перенесена с экрана на файл: заведён
   `ref_totals.csv` и визуал итогов. Отрицательные примеры — состояния
   проекта из истории git (`tools-gate.md`, 5.7). Прогон исправленного
   шага 04 нашёл **R37** (визуал итогов задан без порядка полей) и
   повторил **R35** на другом визуале — оба записаны.
3. **Молчаливые неверные исходы. Закрыто 2026-08-14**, коммиты
   `ce8469b` и `7c2d9fd`. R13, R14, R15, R17 переписаны через проверку
   после действия: карточка убрана оттуда, где число сверяется с
   эталоном; подставленная кратность не считается подтверждением пары
   колонок; диалог «многие ко многим» приведён дословно. R9 и R11 ушли
   вместе с переписанным заданием 7 шага 02. **R35 закрыт как «не
   чинится» 2026-08-16** (`tools-gate.md`, 5.8): перекрёстную фильтрацию
   ловит сверка четырёх выгрузок с эталонами — это `[FAIL]` на числах, а
   не тихий пропуск; профилактика осталась в тексте заданий 3, 6 и 9, а
   машинная проверка раскладки по страницам снята.
4. **Навигация по GUI. Закрыто 2026-08-16**, коммит `9f918c6`. R2, R9,
   R10, R16, R18, R28, R31 — один класс, и починен он тремя вещами, а не
   семью правками: реестр `research/pbi-ui-labels.md` (измеренные
   надписи, запрещённые формы, отдельный раздел для надписей, взятых не
   с экрана), правило в скилле о записи пути (вид → вкладка → группа →
   элемент, контекстная вкладка — с условием появления) и
   `check_ui_labels()` в `tools/check_consistency.py`. Отрицательный
   пример взят без порчи файлов: проверка родилась красной на
   действительном состоянии шагов — `tools-gate.md`, 5.9.
5. **R4 и уборка. Закрыто 2026-08-16**, коммит `9f918c6`. Корневой
   `.gitignore` закрывает локальное состояние проектов Power BI и
   `.pbix`; `check_pbip.py` больше не требует `.gitignore` именно в
   папке проекта — Desktop его туда не кладёт, и это измерение R4, а не
   догадка. Убраны `m4.pbix`, `pbi-probe/`, `D:\pbi-probe2`,
   `D:\m4-run`. `D:\m4-gate` оставлен: на нём стоят измерения Части 2.
   Пять файлов выгрузки в `program\M4\data\csv\` оставлены — они
   доказательная база **открытого** R34. Бинарные хвосты в истории
   (803 КБ) отложены строкой в `DEFERRED.md`: чистка требует
   `push --force`, а коммиты здесь уже терялись дважды.
6. **Умение C2. Написано 2026-08-16**, `program/M4/step-05.md`.
   Подмена источника — сменой значения параметра запроса `DataFolder`, а
   не правкой пяти запросов; доказательство переезда — три выгрузки
   против `ref_next_*.csv`, а не отсутствие ошибок при Refresh (первое
   условие C2 отменено решением 22 на измерении). `reference_m4.py`
   получил режим `--next`: эталоны обоих периодов считает один и тот же
   код, и четыре базовых `ref_*.csv` после переписывания скрипта остались
   побайтово теми же. `check_pbip.py --step 05` показал обе половины
   примера, но положительную — на сборке автора, а не на проекте из
   Desktop: формат записи параметра в TMDL прогоном не измерен
   (`tools-gate.md`, 5.11). **Сам шаг ни разу не пройден руками** — это
   первый шаг модуля, написанный после гейта.

7. **Вилка часов M4. Закрыто 2026-08-17**, решение 28: 40–60 → 26–35 по
   построчной сумме пяти шагов. Содержание дописано целиком, закрывать
   разрыв больше нечем, кроме правки вилки (решение 24 задаёт именно это
   направление). Производные числа программы пересчитаны прогоном —
   раздел 2, «Часы».

---

## 6. Долги, которые прогоном M4 не закрываются

Ни один из трёх не имеет срока и не всплывёт сам: скрипт их не видит,
а работа над M4 их не касается.

- **M2 ни разу не открывался в реальном Excel** — пункт A2, раздел 4.2.
  Класс риска тот же, что дал двенадцать дефектов в M4: локализованные
  имена функций и разделитель аргументов ломают шаг молча, чтением это
  не находится.
- **`research/self.md` пуст** — одна запись от 2026-07-29, и та помечена
  «не учёба». Программу не проходит никто, включая автора, поэтому 14 из
  24 часовых оценок `design/blueprint.md`, §6.1 стоят с пометкой
  «требует калибровки» и калибровать их нечем. Долг растёт с каждым
  написанным модулем.
- **Фальсификаторы решений не срабатывают.** Часть фальсификаторов в
  `design/decisions.md` зависит от того, что кто-то проходит программу,
  и потому не сработала ни разу. Сработали только те, которые
  проверяются без учащегося — чтением собственных файлов и реальным
  прогоном инструмента. Вывод сформулирован в самом решении 19: признак,
  проверяемый чтением файлов, срабатывает раньше того, который ждёт
  прохождения; это стоит помнить, назначая следующие фальсификаторы.

---

## 7. Что ломалось и почему

- Дважды скрипт, написанный по документации, падал при первом реальном
  запуске: `compare_csv.py` и инструкция по `sqlite3`. Отсюда правило
  «прогон вместо чтения» в разделе 3 — оно выведено из этих двух
  случаев, а не принято заранее.
- Коммиты `061c6c1` и `4b40196` отвязались от `master` — вероятно,
  распаковка архива поверх рабочей папки. Сохранены тегами `lost-audit`
  и `lost-round9`. Отсюда обязательный `git push` (раздел 3) и правило
  распаковывать архивы только в отдельную директорию.