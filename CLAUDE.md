# Проект: Data Analyst Program

## Цель программы
Довести человека без опыта до первой оплачиваемой работы, где
анализ данных — основная или существенная часть обязанностей,
на украинском рынке (продукт, аутсорс, консалтинг, локальный бизнес).
Отдельным блоком — подготовка к международному найму (remote/B2B/
релокация) как к цели второго горизонта, через несколько лет работы,
а не как к результату выпуска.

Обучение: 531–672 ч (ядро 250–323, обвязка 136–167, портфолио 108–136,
сборка резюме/профиля/трекера 8–11 и возвратный контроль 29–35 — две
последние строки не входят ни в одну из трёх категорий).
Ядро/обвязка сдвинуты на 1 ч решением 14 (`design/decisions.md`,
2026-07-30): git init/add/commit перенесены из M10 в M0. Ядро уменьшено на
14–25 ч решением 28 (2026-08-17): вилка M4 приведена к построчной сумме
пяти написанных шагов, 40–60 → 26–35; итог был 478–642. Ядро уменьшено ещё
на 37–64 ч решением 31 (2026-08-20): вилка M5 приведена к построчной сумме
семи написанных шагов, 80–120 → 43–56; итог был 464–617. Блок «Выход»
увеличен на 1 ч решением 37 (2026-08-23): вилка сборки резюме, профиля и
трекера приведена к построчной сумме трёх написанных шагов, 8–10 → 8–11;
итог был 427–553. Программа увеличена на 13–9 ч решением 41 (2026-08-23):
в шаги M2, M3, M4, M5, M8 и M15 дописаны конструкции, встречавшиеся в
репозитории ноль раз (`CASE WHEN`, `FULL OUTER JOIN`, self-join,
`RANK`/`DENSE_RANK`, разбор чужой схемы, `$A$1`, `value_counts`,
`try/except`, `pivot_table`, перцентили, `ALL`/`VAR` и контекст фильтра,
измерения и меры, LOD и порядок фильтров); вилки шести модулей приведены
к построчной сумме, итог был 427–554. Программа увеличена на 18–23 ч
решением 49 (2026-08-24): заведён блок `program/review/` — пять точек
возвратного контроля R1–R5 на границах этапов, 53 возвратных задания,
новых умений не несут; итог был 442–565. Программа увеличена на 47–58 ч
решением 50 (2026-08-24): по внешнему аудиту
`audit/independent-audit-2026-08-24.md` дописаны десять шагов под десять
новых умений части 1 blueprint — язык Python (B6), потоковое чтение (B7),
выбор формы диаграммы (D4), описательная статистика и порог выброса (F4),
аппарат вывода (F5), продуктовые метрики (F6), RFM и юнит-экономика (F7),
сезонность и прогноз (F8), подзапросы и наборы SQL (A6), грязный
текстовый ключ (A7); вилки шести модулей приведены к построчной сумме,
итог был 460–588. Программа увеличена на 4 ч решением 52 (2026-08-25):
по независимой финальной проверке
`audit/independent-final-check-2026-08-24.md` в блок `program/review/`
дописаны восемь возвратных заданий под умения решения 50 (A6, A7, B7,
F4, F6, F7, F8), которые до этого не перепроверялись нигде, и выпускное
задание на F1, которое шапка R5 заявляла, а раздел 3 не содержал; новых
умений не несут, итог был 507–646. Программа увеличена на 18–20 ч
решением 54 (2026-08-28): по контентному аудиту `AUDIT_REPORT.md`
(средний балл 78.3/100) закрыты дефекты, которые правятся объёмом, —
M0 получил отмену изменений и файловые операции (7–9 → 8–10), M1 —
шаг «какое решение изменится от ответа» и вторые кейсы (8–10 → 13–16,
плотность практики была самой низкой в программе), M9 — вывод порога
хи-квадрата из `z²` и t-критерий (27–33 → 28–34), M10 — конфликт
слияния и `git revert` (7–9 → 9–11), M11 — привязка определений к
IFRS 9 и акту НБУ, разделение G2/G3 и вторые задачи на срезах
(20–25 → 22–27), блок `program/review/` — четырнадцать возвратных
заданий под тринадцать умений, которые не перепроверялись нигде
(22–27 → 29–35). Итог был 511–650. Программа увеличена на 2 ч
решением 55 (2026-09-03): по аудиту глазами студента
`audit/student-eyes-2026-09-03.md` в M0 дописана установка Python и Git
(`step-01.md`, 1.3 «Шаг 0»; 8–10 → 9–11), а в M5 — пять прогонных
примеров под конструкции языка, которых `step-02.md` требовал заданиями
и не показывал кодом нигде (`while`, `sorted(key=)` по одному и по
составному ключу, сортировка дат `дд.мм.рррр`, `try`/`except
ValueError`; 57–71 → 58–72). Тем же решением M7 переехал из этапа 4 в
этап 3 части 6.2: M16 стоял этапом раньше таблицы BigQuery, на которой
строится. Итог был 529–670. Значение 480–644 —
арифметическая опечатка, исправлена 2026-07-30 (`design/decisions.md`,
решение 16). Все числа — прогоном `tools/check_consistency.py` и разбором
таблицы 6.1, не пересчётом в уме.
При 25 ч/нед — 22–27 недель (5–6 месяцев). При 10 ч/нед — 54–68 недель
(12–15 месяцев). Обе пары считаются от официального итога и сверяются
`check_calendar_arithmetic()` (решение 55).
Срок поиска работы сверх этого: данных нет, не обещаем.

Русский язык. Markdown-репозиторий.

## Кто читатель
Взрослый человек без опыта в данных, учится сам, без ментора,
10–25 часов в неделю. Не студент. Ему нужна работа, а не диплом.

Читатель на сегодня ровно один — автор программы, и он начнёт её
проходить после того, как она будет дописана. Отсюда сужение
(решение 21, `design/decisions.md`, 2026-07-30): платформа — Windows,
домен — только fintech, окружение — версии и пути автора. Не сужаются
проверяемые критерии, честные часы, числа из реального прогона и
`tools/check_consistency.py`. Всё отложенное — построчно в `DEFERRED.md`
в корне, с указанием, где в репозитории оно заложено.

## Незыблемые правила
1. Всё пишется в файлы. В чат — только отчёты до 10 строк.
2. Каждое утверждение о рынке — со ссылкой на источник.
   Нет источника — нет утверждения.
3. Каждая ссылка на ресурс проверена через fetch. Битые — удалять.
4. Ни одного модуля без обоснования данными из research/market.md.
5. Ни одного шага без практики и проверяемого критерия готовности.
6. Оценки времени честные. 40 часов — значит пиши 40.
7. Тон: без воды, без мотивационных вставок, без
   "давайте вместе погрузимся в увлекательный мир данных".

## Порядок фаз
0 исследование → 1 проектирование → 2 контент → 3 карьера → 4 аудит
Не перескакивать. После каждой фазы — стоп и отчёт.

## Текущий статус
На 2026-08-24 закрыты фазы 0, 1, 2, 3 и 4. Числа ниже — прогоном
`tools/check_consistency.py` и подсчётом по репозиторию, не по памяти.

- **Фазы 0 и 1.** research/{market,competitors,access,gap}.md,
  research/sources-gate.md, design/{blueprint,decisions}.md.
  Решений — 49, нумерация сплошная.
- **Фаза 2 (контент) написана целиком.** 17 модулей M0–M16 и 6 проектов
  P1–P6: 23 файла `step-00.md` (декларации) и 83 содержательных шага.
  Все 46 ID умений части 1 blueprint закрыты шапкой `Умение:` хотя бы
  одного шага.
- **Гейт M4 закрыт до конца.** Ручной прогон 01–04 в Power BI Desktop —
  2026-08-14 (36 дефектов R1–R36, решения 26, 27); умение C2
  (`step-05.md`) прогнано руками 2026-08-23: десять `[OK]` и код 0 на
  настоящем проекте, ноль изменённых файлов таблиц после подмены
  значения параметра (`research/tools-gate.md`, 5.11).
- **Фаза 3 (карьера).** `program/career/` — блок «Выход», 8–11 ч по
  части 6.1 blueprint: резюме, GitHub-профиль портфолио, режим поиска.
  Решения 37 и 38.
- **Возвратный контроль: `program/review/`** (решение 49, 2026-08-24).
  Пять точек R1–R5 на границах этапов, 75 возвратных заданий, 29–35 ч
  (решение 54, 2026-08-28: было 61 задание и 22–27 ч; решение 52,
  2026-08-25: было 53 задания и 18–23 ч). Пороги сдачи — 80/50 % для
  R1–R4 и 19/15 заданий из 22 для R5 (`program/review/step-00.md`).
  Умений не несут и не могут нести — только перепроверяют закрытые, их
  по-прежнему 46. Закрывает дефект 6 внешнего аудита (механизма возврата
  к пройденному не было ни одного) и отсутствие сквозной проверки на
  выпуске. Каталог назван не `M17` по той же причине, что `career`:
  часы сверяет `check_review_hours()`.
- **Фаза 4 (аудит).** `audit/phase4-report.md` — сквозная проверка всей
  программы: согласованность, живость ссылок, честность часов, полнота
  критериев готовности.
- **Доработка по внешнему аудиту (решения 50 и 51, 2026-08-24).**
  `audit/independent-audit-2026-08-24.md` нашёл четыре критических и
  четыре высоких пробела; дописаны десять шагов под десять новых умений
  (A6, A7, B6, B7, D4, F4–F8), заведён турникет закрытых материалов
  `tools/vault.py` и машинная проверка H1. Результат — в
  `audit/second-audit-2026-08-24.md` (таблица CG-1…CG-14 со статусом
  каждого) и `audit/final-exam-2026-08-24.md` (экзамен на 32 вопроса с
  картой «вопрос → умение → где учат → где практикуют → где проверяют»).
  Машинная сверка выросла с 33 шагов из 73 до 55 из 88.
- **Независимая финальная проверка (решения 52 и 53, 2026-08-25).**
  `audit/independent-final-check-2026-08-24.md` — проверка с нуля, без
  доверия предыдущим аудитам: своя матрица компетенций по 46 умениям
  (сформировано 27, частично 7, однократно 12), свой экзамен из 18
  заданий с эталонами, посчитанными прогоном, и пересмотр CG-1…CG-13
  по более строгому критерию — навык считается закрытым, только если он
  ещё и переиспользован. Вердикт NOT READY с пятью блокерами. Закрыты
  три: 8.1 (семь умений решения 50 не перепроверялись нигде — восемь
  возвратных заданий, решение 52), 8.3 (R5 заявлял 15 умений при 14
  заданиях — задание на F1, решение 52), 8.2 (эталоны шести проектов
  лежали открытыми до попытки — 16 файлов за турникет, решение 53).
  Открыты два, и оба правкой текста не закрываются: 8.4 (Tableau и
  Looker Studio не прогнаны) и 8.5 (банк вакансий M13 протухает по
  календарю).
- **Прохождение: приложение `app/`** (решение 40, 2026-08-23). Локальное
  веб-приложение: читает те же `program/**/step-*.md`, запускает те же
  `check_*.py`, ведёт таймер и дописывает строку в `research/self.md`.
  Содержания в себе не держит (`app/PLAN.md`, раздел 0), часов программе
  не добавляет. Вопрос встроенному ИИ-ассистенту считается «обращением на
  сторону» по правилу 6 `research/self.md` наравне с внешним поиском;
  вердикт ИИ по уже написанному ответу — нет, это самопроверка, пометка
  `[проверка]`, и в фальсификатор решения 28 она не входит (решение 45,
  2026-08-24). Редизайн интерфейса — решение 56 (2026-09-03): шесть
  проектов `project.md` стали открываемыми (до этого P1–P6 стояли в
  дереве «0/0» и не показывались нигде), числа программы ушли из
  разметки в API (в `index.html` стояли «36 умений» при 46 и «73 шага»
  при 90), заведена визуальная система (`app/PLAN.md`, раздел 9) и
  `app/tests/test_static.py` — статику до этого не проверяло ничто.
  Проект открывается как шаг, но содержательным шагом не считается:
  90 части 6.5 остаются 90, проекты считаются отдельно.

**Что не закрыто и агентом не закрывается.** `research/self.md` пуст:
программу пока никто не проходил. Поэтому 22 из 25 часовых вилок части
6.1 blueprint стоят с пометкой «требуется калибровка», и снять её может
только замер фактического времени при первом прохождении — не вторая
оценка и не работа над текстом. Число сверяется
`check_calibration_count()` в `tools/check_consistency.py` во всех трёх
местах, где оно живёт (эта строка, часть 7 blueprint, таблица 6.1) —
решение 42, 2026-08-24; до этого строка здесь не читалась скриптом и
простояла на «17» после решения 41. Пять точек возвратного контроля
добавили к некалиброванному не только часы: пороги сдачи (80/50 для
R1–R4, 12/9 для R5) назначены, а не измерены. Остальное отложенное —
построчно в `DEFERRED.md`.

**Объём практики — 564 новых задания и 75 возвратных, 639 всего, в 90
шагах** (часть 6.5 blueprint, прогон `tools/count_tasks.py`, сверка
`check_practice_volume()`). Вторая величина объёма рядом с часами,
метрика Karpov [К, 3.4, п. 9]; в отличие от часов проверяется без
прохождения. Два числа не складываются в одно: возвратное задание меряет
удержание, а не охват, и наружу публикуется «564» (решения 49, 50, 52 и
54). Отдельно
от часов не публикуется вовсе: задание на 5 минут и на 2 часа считаются
одинаково (решение 47).

**Точка входа для студента — корневой `README.md`** (решение 55,
2026-09-03). До этого файл был нулевого размера, а порядок прохождения,
правила журнала, команда турникета и список того, что надо поставить,
жили в `design/blueprint.md`, `research/self.md` и `tools/vault.py` —
документах, написанных для проектировщика. `README.md` их не дублирует,
а адресует; часов программе не добавляет.

**Лицо в шагах — «вы».** 1.1–1.3 и 1.6 — прямое обращение, 1.4 —
повелительное, 1.5 — безлично (чек-лист), третье лицо только там, где
подлежащее не человек. Слова «учащийся», «студент», «читатель» о самом
читателе в шагах не употребляются (решение 46, таблица — в скилле
`curriculum-design`, раздел 2).

**Часы шага против норматива заданий.** Вилка шага (`Время:`) и
норматив времени на задания в разделе 1.5 — разные величины, и между
ними разрыв в разы (M3.04: 5–6 ч при ≤55 мин заданий). Раздел 1.7
каждого шага обязан раскладывать вилку на слагаемые — образец
`program/M5/step-03.md`. `research/self.md` пишет два числа на шаг:
время чтения и время заданий, иначе первый прогон разрыв не разрешит
(решение 44, 2026-08-24).
