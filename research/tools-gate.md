# Ручной гейт инструментов: консольный `sqlite3` и нулевая проверка Power BI

Даты прогона: 2026-07-30 (Часть 1), 2026-07-31 (Часть 2),
2026-07-31 … 2026-08-14, пять заходов (Часть 3).
Состояние: **все три части закрыты.** Часть 3 — ручной прогон M4 по
решению 26 — прошла все четыре написанных шага модуля; итог в 3.5–3.8,
порядок починки по её результатам — решение 27.

Слово `Статус:` в начале строки в этом файле зарезервировано под учётную
строку дефекта (решение 27, часть 2) и больше ни для чего не
используется: на нём стоит машинная проверка.

Гейт выполнялся вручную: все действия в GUI и в терминале делал автор на
своей машине, агент давал инструкции, сверял вывод и записывал результат.
Агент ничего не устанавливал и ничего не чинил. Все числа ниже получены
прогоном, не из документации; там, где источником была документация, это
названо явно.

Назначение: проверить исполнимость `program/M3/step-01.md` на живой
машине (Часть 1), ответить на шесть пунктов нулевой проверки решения 22
до написания `program/M4/step-01.md` (Часть 2) и пройти написанные шаги
M4 как учащийся — гейт перед умением C2, решение 26 (Часть 3).

---

## 1. ЧАСТЬ 1 — консольный клиент `sqlite3`. Закрыта

### 1.1. Окружение на момент прогона

| Параметр | Значение |
|---|---|
| ОС | Windows 11 Pro 10.0.26200 |
| Оболочка | Windows PowerShell (не PowerShell 7) |
| Python | 3.11.9 |
| `sqlite3` в `PATH` до гейта | отсутствует |
| `sqlite3` после гейта | 3.53.4 2026-07-24 19:02:57 `bf7c7f30031888f4e796e429ab3978879485813aaca6f641c7b33e4e09459bcc` (64-bit) |

### 1.2. Что произошло на первой команде модуля

`program/M3/step-01.md`, раздел 1.3 — первая команда 60–80-часового
модуля:

```
$ sqlite3 m3.db ".read schema.sql"
```

Фактический результат:

```
sqlite3 : Имя "sqlite3" не распознано как имя командлета, функции, файла
сценария или выполняемой программы.
    + CategoryInfo          : ObjectNotFound: (sqlite3:String) [], CommandNotFoundException
```

Обходного пути через Python на этой машине нет: встроенный REPL
`python -m sqlite3` появился только в Python 3.12, у автора 3.11.9.

### 1.3. Дефекты

**B1 — блокер. Модуль неисполним с первой команды; инструкции по
установке клиента нет ни в одном файле репозитория.**

Ни `program/M3/step-01.md`, ни `program/M3/step-00.md` не содержат
указаний, как получить консольный клиент. `step-00.md`, раздел
«SQL-движок», утверждает обратное: «модуль начинается на SQLite (**0
действий по установке**…)». Источник ошибки — решение 15
`design/decisions.md`: «M3 начинается на SQLite: ставится в 0 действий
(**входит в стандартную библиотеку Python**, файл базы — обычный файл)».

Утверждение верно для **библиотеки** `sqlite3` из стандартной поставки
Python — ею пользуются скрипты программы. Шаги M3 написаны против
**консольного клиента** `sqlite3`. Это разные вещи, и клиент в поставку
Python не входит. Проверено прогоном (1.2).

Затронуто: решение 15 (формулировка «0 действий»), `step-00.md` раздел
«SQL-движок», `step-01.md` раздел 1.3.

**B2 — критерий сверки с эталонным CSV невыполним из коробки.**

Клиент 3.53.4 по умолчанию печатает результат рамками с заголовками:

```
╭──────────┬───────────┬────────╮
│ order_id │  status   │ amount │
╞══════════╪═══════════╪════════╡
│      111 │ pending   │  390.0 │
╰──────────┴───────────┴────────╯
```

Эталоны модуля — CSV с запятой и заголовком
(`program/M3/data/a1_task1_running_total.csv`):

```
customer_id,order_id,order_date,amount,running_total
1,101,2026-05-02,1200.0,1200.0
```

Критерий `step-06.md`, 1.5 — «вывод собственного запроса **построчно**
(все колонки, все строки, тот же порядок) совпадает с соответствующим
эталонным CSV» — механически не выполняется: сравниваются рамочная
таблица и CSV. Ни один шаг M3 не задаёт `.mode csv` / `.headers on` и не
запускает клиент как `sqlite3 -csv -header`. Проверено поиском по
`program/M3/step-*.md`: совпадений `.mode` — ноль.

Затронуто: `step-06.md` (6 эталонов A1), `step-07.md` (2 эталона A3),
`step-10.md`, `step-11.md`.

**B3 — не назван рабочий каталог.** `step-01.md` даёт `sqlite3 m3.db
".read schema.sql"` без указания, откуда запускать. `schema.sql` и
`seed.sql` лежат в `program/M3/data/`. Из корня репозитория команда не
работает.

**B4 — не описан повторный прогон.** `.read schema.sql` поверх уже
существующей `m3.db` падает на `table already exists`. Шаг не говорит ни
удалить файл базы, ни что делать при повторе. В прогоне это обошли
`Remove-Item m3.db` — команда добавлена агентом, в тексте шага её нет.

**B5 — скрытая зависимость критерия от данных.** Критерий `step-01.md`,
1.5: «сломанный запрос возвращает 13 строк — ровно все `completed`-заказы
независимо от суммы». Это верно только потому, что ни один
`pending`-заказ не проходит `amount > 500`: их два, 111 (390.0) и 116
(260.0). В шаге это не названо. Если будущая правка `seed.sql` добавит
`pending` с суммой выше 500, критерий сломается, и причина будет
неочевидна.

**B6 — проверенная и снятая гипотеза.** `sqlite3.exe -csv > файл` на
Windows даёт двойной возврат каретки. Обнаружено 2026-07-31 при
подготовке данных для Части 2, воспроизведено прогоном:

```
$ sqlite3.exe -csv -header m3.db "SELECT order_id, amount FROM orders
  ORDER BY order_id LIMIT 2;" > b6_test.csv
$ od -c b6_test.csv
o r d e r _ i d , a m o u n t \r \r \n 1 0 1 , 1 2 0 0 . 0 \r \r \n ...
```

В CSV-режиме SQLite сам завершает строку `\r\n`, а stdout в текстовом
режиме Windows переводит `\n` ещё раз — получается `\r\r\n`.

Область поражения ограничена, это проверено, а не предположено:

- Восемь эталонов `program/M3/data/a*.csv` чистые — CRLF, ноль двойных
  `\r` (сгенерированы Python, не редиректом).
- `compare_csv.py` двойной `\r` переживает: `csv.reader` с `newline=""`,
  `.strip()` на ячейках и отсев пустых строк его снимают — проверено
  запуском на битом файле, разбор корректный.

**Дефекта нет — репозиторий это уже знает и лечит.** Проверено после
находки, до записи вывода:

- Восемь эталонов `program/M3/data/a*.csv` чистые — CRLF, ноль двойных
  `\r` (сгенерированы Python, не редиректом).
- `compare_csv.py` двойной `\r` переживает: `csv.reader` с `newline=""`,
  `.strip()` на ячейках и отсев пустых строк его снимают — проверено
  запуском на битом файле, разбор корректный.
- Явление описано в docstring `compare_csv.py` (пункт 2 «Перевод
  строки») со ссылкой на решение 13, и оттуда выведено правило шага.
- Правило действительно есть в тексте: `step-01.md` строки 167–175
  предписывают `.once <файл>` и прямо запрещают перенаправление `>`;
  `step-06.md:162` и `step-07.md:158` на него ссылаются;
  `data/README.md:88` повторяет.

Запись оставлена как след проверки: гипотеза о дефекте выдвигалась и
была отклонена прогоном. Собственная ошибка агента (данные для Части 2
были сгенерированы редиректом, а не `.once`, и вышли с `\r\r\n`) —
подтверждение того, что правило шага нужное.

### 1.4. Что подтвердилось прогоном

Содержание шага верно целиком. Все обещанные числа сошлись, расхождений
ноль.

| Проверка | Обещано в шаге | Факт |
|---|---|---|
| Всего заказов | 19 | 19 |
| `completed` | 13 | 13 |
| Пример 1.3, сломанный запрос | 4 строки: 111/390.0, 116/260.0, 117/320.0, 118/410.0 | совпало построчно |
| Пример 1.3, исправленный | 3 строки (без 116) | совпало |
| Задание 1.4, сломанный | 13 строк, все `completed` | совпало |
| Нарушители `amount > 500` | 103 (430), 112 (455), 114 (330) | совпало |
| Задание 1.4, исправленный | 10 строк | 10 |

Вывод F (исправленный запрос задания 1.4), дословно:

```
order_id,status,amount
101,completed,1200.0
102,completed,850.0
104,completed,600.0
105,completed,720.0
107,completed,980.0
108,completed,540.0
109,completed,610.0
113,completed,700.0
115,completed,510.0
119,completed,675.0
```

### 1.5. Фактическая цена установки клиента — измерена

Страница [Precompiled Binaries for Windows](https://www.sqlite.org/download.html)
(fetch 2026-07-30) отдаёт `sqlite-tools-win-x64-3530400.zip`, 6 557 792
байта. Распаковка даёт четыре `.exe` плоско, без вложенной папки и без
установщика:

| Файл | Байт |
|---|---|
| `sqlite3.exe` | 4 022 272 |
| `sqldiff.exe` | 3 243 520 |
| `sqlite3_analyzer.exe` | 4 484 608 |
| `sqlite3_rsync.exe` | 3 179 520 |

Прав администратора не требуется. `PATH` дописан на уровне пользователя
(`[Environment]::SetEnvironmentVariable("Path", …, "User")`), новое окно
терминала подхватывает. Это тот же путь без админских прав, который
решение 19 п. 2 выбрало для PostgreSQL.

Итого честная цена: **скачать 6.6 МБ, распаковать, дописать `PATH`** —
не «0 действий», но и не установка сервера.

### 1.6. Побочное замечание по прогону

`PATH` на машине автора содержит `D:\sqlite-tools` дважды: команда
дописывания выполнена повторно. Ни на что не влияет, но стоит вычистить.

---

## 2. ЧАСТЬ 2 — нулевая проверка Power BI (решение 22). Закрыта

### 2.1. Окружение

| Параметр | Значение |
|---|---|
| Power BI Desktop | 2.156.951.0, **версия из Microsoft Store** |
| Способ установки | Store (`https://aka.ms/pbidesktopstore`), прав администратора не требует |
| Язык интерфейса | русский (Store-версия берёт язык Windows) |
| Экран | 1920×1080, масштаб 100% |
| Ключ реестра `HKLM:\SOFTWARE\Microsoft\Microsoft Power BI Desktop` | **отсутствует** |

Способы установки сверены с
[Download Power BI Desktop](https://learn.microsoft.com/en-us/power-bi/fundamentals/desktop-get-the-desktop)
(fetch 2026-07-30): Store — без прав администратора, обновляется
автоматически; Download Center (`PBIDesktopSetup_x64.exe`) — требует прав
администратора.

### 2.2. Находка P1 — автоопределение кодировки зависит от содержимого файла; C2 ломается тихо

**Что сделано.** Два CSV выгружены из `m3.db` в `D:\m4-gate\`
(вне репозитория) запросами:

```sql
SELECT customer_id, name, city FROM customers ORDER BY customer_id;
SELECT order_id, customer_id, status, amount,
       substr(order_date,1,7) AS period_ym
FROM orders ORDER BY order_id;
```

Оба записаны в UTF-8 **без BOM**. `customers.csv` содержит украинскую
кириллицу (`Коваленко О.`, `Київ`), `orders.csv` — только латиницу и
цифры.

**Что показал Power BI** в окне предпросмотра «Текстовый или CSV-файл»:

| Файл | Содержимое | «Источник файла», подставлен автоматически |
|---|---|---|
| `customers.csv` | кириллица UTF-8 | `65001: Юникод (UTF-8)` — кириллица отображена верно |
| `orders.csv` | только ASCII | **`1251: Кириллическая (Windows)`** |

Разделитель в обоих случаях определён верно («Запятая»). Определение
типов — «на основе первых 200 строк».

**Почему это дефект, а не курьёз.** Кодировка не определяется заново при
каждом обновлении — она зашивается в код запроса на момент импорта.
Файл, состоящий сегодня только из ASCII, получает 1251. Как только в
очередной выгрузке появится кириллица — название категории, город,
контрагент, статус — тот же запрос прочитает её как 1251 и молча
испортит, без единого сообщения об ошибке.

Это прямое попадание в умение C2, критерий которого сформулирован в
части 1 blueprint как «подмена исходного файла на файл следующего месяца
+ Refresh → 0 ошибок, 0 ручных правок». Он провалится, и провалится
тихо — худший из режимов отказа.

**Следствие для M4.** Шаг обязан требовать явной установки `65001` в
«Источнике файла», а не полагаться на автоопределение. Это не оговорка в
1.6, а часть задания и часть критерия. Отдельно: данные программы
кириллические начиная с M2 (решение 17), то есть проблема не
гипотетическая для этого домена.

### 2.3. Находка P2 — смягчение решения 22 по датам не работает

**Что утверждает решение 22.** Единственная документированная
невоспроизводимость экспорта — формат `datetime` по локали машины. И
далее дословно: «Снимается конструкцией задания: в экспортируемых
визуалах столбцов `datetime` нет, период выражается текстом `YYYY-MM`
или целочисленным ключом даты».

**Что произошло.** Колонка `period_ym` выгружена ровно так, как
предписывает решение — текстом `2026-05`. В предпросмотре Power BI она
показана как **`01.05.2026`**: определение типов распознало строку
`2026-05` как дату и отформатировало по локали машины.

**Почему это важно.** Смягчение из решения 22 само по себе не
срабатывает. «Выразить период текстом» недостаточно — Power BI
преобразует такой текст в дату автоматически, и на выходе получается
ровно то, чего решение избегало: значение, зависящее от локали. Чтобы
смягчение работало, шаг обязан дополнительно **явно задать типу колонки
«Текст»** в Power Query и не полагаться на то, что текст останется
текстом.

Формулировку решения 22 нужно поправить: не «период выражается текстом»,
а «период выражается текстом **и тип колонки принудительно установлен в
Текст**, потому что автоопределение типов превращает `YYYY-MM` в дату».

### 2.4. Остальные находки Части 2

**P3 — родного коннектора к SQLite нет; к PostgreSQL есть.** Поиск
`SQLite` в полном диалоге «Получить данные» не даёт ничего. В списке баз
данных присутствуют: SQL Server, SQL Server Analysis Services, MySQL,
**PostgreSQL**, SQL, SQL Azure, Azure Synapse Analytics, Azure для
PostgreSQL, MongoDB Atlas SQL.

Проектное следствие для M4, лучше исходного замысла: M3 заканчивается
переносом данных на PostgreSQL (`step-09.md`), значит M4 подключается к
той же базе `m3` **напрямую**, а не к промежуточному CSV. Тогда критерий
C1 («числа сходятся с эталонным SQL») сверяет визуал и эталонный запрос
на одних и тех же данных, без экспортной прослойки. Путь через CSV
остаётся только там, где нужен именно навык импорта файла (C2).

**P4 — признак установленности из решения 22 ложноотрицателен для
Store-версии.** Решение 22 фиксирует отсутствие Power BI по трём
признакам, один из которых — ключ реестра `HKLM:\SOFTWARE\Microsoft\
Microsoft Power BI Desktop`. У установленной Store-версии этого ключа
нет. Проверять надо `Get-AppxPackage *PowerBI*`.

**P5 — PBIP и TMDL всё ещё preview; дополнительно включён PBIR.** В
разделе «Функции в предварительной версии» версии 2.156.951.0
присутствуют и включены:

- «Параметр сохранения проекта Power BI (PBIP)» — включено
- «└ Сохранить семантическую модель в формате TMDL» — включено
- «└ Хранение отчетов с использованием расширенного формата метаданных
  (PBIR)» — включено

Первые два подтверждают слой 2 решения 22 (обе функции по-прежнему
preview, обе требуют ручного включения). Третьего решение 22 не знало:
при включённом PBIR текстом сохраняется не только семантическая модель,
но и сам отчёт — включая типы визуалов. Это потенциально переводит часть
слоя 3 («нет 3D, пончиков и двойных осей» — P3, критерий 13) из чеклиста
автора в машинную проверку. **Не проверено прогоном** — гипотеза,
требующая подтверждения на сохранённом проекте.

**P6 — локаль интерфейса.** Интерфейс русифицированный. Решение 21 п. 4
требует «одна локаль»; значит шаги M4 обязаны ссылаться на русские
названия пунктов меню, и это надо объявить в `program/M4/step-00.md`
явно, иначе шаг будет называть английские подписи, которых автор не
увидит.

**P7 — денежная колонка молча уходит в текст при русской локали.**
Прогон 2026-07-31. Импорт `D:\m4-gate\orders.csv` (значения `amount` вида
`1200.0`) штатным коннектором «Текст или CSV» без ручных правок дал в
M-коде:

```
Table.TransformColumnTypes(#"Повышенные заголовки", {{"order_id",
Int64.Type}, {"customer_id", Int64.Type}, {"status", type text},
{"amount", type text}, {"period_ym", type date}}, "ru")
```

Культура `"ru"` подставлена автоматически. В ней десятичный разделитель —
запятая, поэтому `1200.0` не распознаётся как число, и Power Query кладёт
колонку в **текст**, не выдавая ни ошибки, ни предупреждения. Мера
`SUM(orders[amount])` на такой модели не считается.

Тяжелее P2: P2 портит подпись периода, P7 обнуляет само число. Критерии
C1–C4 решения 22 («числа визуала сходятся с эталонным SQL») на модели по
умолчанию невыполнимы — эталон 8600.00 нечем сверять, пока тип не
исправлен вручную. Обход, проверенный прогоном: культура `"en-US"` в
`Table.TransformColumnTypes` — после замены `amount` встал числом,
`period_ym` текстом `2026-05`.

Источник расхождения — не CSV: SQLite пишет `1200.0` с точкой всегда.
Значит любой CSV-эталон проекта, попав в Power BI на машине автора,
получает эту проблему. M4 обязан задавать культуру явно.

**P8 — явный выбор кодировки в GUI не всегда попадает в файл проекта.**
Расширение P1. Прогон 2026-07-31, два файла, одинаковые действия («Источник
файла» = `65001` в диалоге импорта):

| Файл | Содержимое | `Encoding` в `Csv.Document` |
|---|---|---|
| `orders.csv` | чистый ASCII | **параметр отсутствует** |
| `customers.csv` | кириллица | `Encoding = 65001` |

Power BI записывает `Encoding` только когда выбранное значение расходится
с автоопределением. На ASCII-файле выбор совпал с определением, параметр
не записан — кодировка в проекте **не зафиксирована**. Следствие: файл,
сегодня ASCII, а завтра с кириллицей (перегенерация эталона, новые
города), переопределится молча, и C2 сломается без единой правки в
проекте. Прибить кодировку через GUI нельзя — только правкой M-кода.

Побочно: коннектор ставит `QuoteStyle = QuoteStyle.None`, то есть кавычки
не считаются ограничителем поля. Любая запятая внутри значения (название
компании, адрес) разъедет строку по колонкам. Тоже лечится только правкой
M-кода — `QuoteStyle.Csv`.

### 2.5. Модель, собранная для прогона

Собрана 2026-07-31, GUI-действия автора, инструкции агента.

- Два запроса Power Query из `D:\m4-gate\*.csv`, M-код задан явно
  (`Encoding = 65001`, `QuoteStyle = QuoteStyle.Csv`, культура `en-US`,
  `period_ym` принудительно `type text`) — обход P1, P2, P7, P8.
- Связь **создана Power BI автоматически**, руками не проводилась:
  `orders[customer_id]` → `customers[customer_id]`, кардинальность
  **«Многие к одному (\*:1)»**, кросс-фильтрация **«Простое»**, связь
  активна, «Предполагать целостность данных» снято.
- Связь пережила переименование таблицы (`orders csv` → `orders`) без
  разрыва — проверено удалением и пересозданием запроса.
- Мера на `orders`:
  `Total Completed = CALCULATE(SUM(orders[amount]), orders[status] = "completed")`,
  формат задан на мере («Десятичное число», 2 разряда).
- Визуал «Таблица»: `customers[city]` + мера, сортировка по мере по
  убыванию.

**Двойное авторство формулы сошлось — построчно, не только на итоге.**
Визуал против эталонного SQL на той же `m3.db`:

| Город | Визуал Power BI | Эталонный SQL |
|---|---|---|
| Київ | 2480,00 | 2480.00 |
| Одеса | 2465,00 | 2465.00 |
| Львів | 1995,00 | 1995.00 |
| Дніпро | 1150,00 | 1150.00 |
| Харків | 510,00 | 510.00 |
| **Всего** | **8600,00** | **8600.00** |

Эталон итога взят из `program/M3/step-00.md` (`SUM(orders.amount) WHERE
status='completed'` = 8600.00), разбивка по городам посчитана прогоном:
`SELECT c.city, SUM(o.amount) ... JOIN ... WHERE o.status='completed'
GROUP BY c.city ORDER BY SUM(o.amount) DESC`. Совпали состав строк, все
значения и порядок.

### 2.6. Шесть пунктов нулевой проверки решения 22 — ответы

Экспорт визуала выполнен дважды подряд, без единого изменения между
экспортами: `D:\m4-gate\export1.csv`, `export2.csv`, оба по 136 байт.

| # | Пункт решения 22 | Ответ |
|---|---|---|
| 1 | Кодировка экспортируемого `.csv` | **UTF-8 с BOM** (`EF BB BF`) |
| 2 | Разделитель полей / десятичный | поля — **запятая**; десятичный — **запятая** (локаль `ru`), из-за чего числа заключены **в кавычки** |
| 3 | Строка «Applied filters» | **отсутствует**; первая строка файла — заголовок |
| 4 | Детерминированность | **побайтово идентичны**, SHA-256 совпал |
| 5 | Порядок строк | **совпал с сортировкой визуала** (Київ → Харків) |
| 6 | Кардинальность и направление в `relationships.tmdl` | **не проверено** — требует сохранения `.pbip` |

SHA-256 обоих файлов:
`bcc691fccbf2501d2ef68b41e07d7ca05befb9a94fd48019f195930415208ba6`.

Байты `export1.csv` целиком:

```
EF BB BF c i t y , T o t a l   C o m p l e t e d \r \n
Київ , " 2 4 8 0 , 0 0 " \r \n
Одеса , " 2 4 6 5 , 0 0 " \r \n
Львів , " 1 9 9 5 , 0 0 " \r \n
Дніпро , " 1 1 5 0 , 0 0 " \r \n
Харків , " 5 1 0 , 0 0 " \r \n
```

**Пункт 4 — главный — закрыт положительно.** Экспорт детерминирован.
Механизм проверки решения 22 (экспорт визуала → сверка скриптом) в
принципе работает. Развилка «ЕСЛИ ЧАСТЬ 2 ПРОВАЛЕНА» **не срабатывает**.

### 2.7. Находка P9 — экспортируются форматные строки, и C1 на них падает

Ответ на вопрос задания «форматные строки против сырых чисел»: в экспорт
попадает **отформатированное значение**, а не сырое число. `2480,00` —
это `formatString` меры (2 разряда) плюс десятичная запятая русской
локали, а не `2480`.

Следствие проверено запуском, а не рассуждением. Эталон, сгенерированный
из `m3.db` с тем же заголовком и порядком строк, против `export1.csv`:

```
$ python compare_csv.py export1.csv ref_city.csv
РАСХОЖДЕНИЕ в строке 1, колонка 'Total Completed':
  у вас '2480,00', в эталоне '2480.00'
  ваша строка:   Київ,2480,00
  строка эталона: Київ,2480.00
код возврата: 1
```

Данные совпадают полностью. Расходится запись числа. `same_value()` в
`compare_csv.py` сначала сравнивает строки, потом пробует `float(got)`;
`float("2480,00")` бросает `ValueError`, и функция возвращает `False`.

BOM скрипт переживает (`utf-8-sig`), кавычки тоже (разбор по правилам
CSV), кириллица прочиталась верно. Ломает ровно десятичная запятая.

Значит **C1 нельзя сдать существующим `compare_csv.py`**. Три выхода, в
порядке предпочтения:

1. Научить `same_value()` заменять запятую на точку перед `float()` —
   правка одной строки, лечит и десятичный разделитель, и любой другой
   визуал M4. Риск: в CSV с запятой-разделителем значение `1 234,56`
   пришло бы в кавычках с неразрывным пробелом — нужна и чистка пробелов.
2. Требовать в шаге M4 задавать мере формат без разрядов и сверять целые
   числа. Сужает задание.
3. Отдельный скрипт сверки для M4. Дублирование, хуже первого.

Побочно: строка «Всего» (grand total) визуала **в экспорт не попала** —
экспортируются только строки данных. Критерий, сверяющий итог, должен
получать его суммированием строк, а не искать в файле.

**Закрыто 2026-07-31 первым выходом из трёх.** `normalize_number()` в
`compare_csv.py` снимает разделитель разрядов (обычный, неразрывный и
узкий неразрывный пробел) и десятичную запятую перед `float()`. Тот же
вызов, что печатал расхождение:

```
$ python compare_csv.py D:/m4-gate/export1.csv D:/m4-gate/ref_city.csv
СОВПАДАЕТ: 5 строк, 2 колонок, расхождение 0.
код возврата: 0
```

Восемь эталонных пар M3 прогнаны после правки — числа в них с точкой, и
правка их не задела: 8 из 8 `СОВПАДАЕТ`, расхождений 0. Смягчение
сравнения проверено с обратной стороны `test_compare_csv.py` (26 случаев,
13 из них — расхождения, которые обязаны остаться расхождениями): `2480,00`
против `2490.00` — расхождение; `1,234` против `1234` — тоже расхождение,
одна запятая читается как десятичная, а не как разделитель разрядов.

### 2.8. `.pbip` сохранён и прочитан с диска

`D:\m4-gate\pbi\gate.pbip`, версия 2.156.951.0, обе preview-функции
(PBIP, TMDL) и PBIR включены. **23 файла, весь вес — 50 КБ**, самый
крупный (20 КБ) — файл темы. Слой 2 решения 22 подтверждён: проект
сохраняется текстом.

Power BI сам кладёт в корень проекта `.gitignore`:

```
**/.pbi/localSettings.json
**/.pbi/cache.abf
```

То есть единственный бинарник (`cache.abf`, 20 КБ) из-под версионирования
выводится автоматически — проект коммитится в репозиторий как есть,
отдельного правила заводить не нужно.

**Пункт 6 нулевой проверки — ответ.** `definition/relationships.tmdl`
целиком:

```
relationship AutoDetected_32360c20-c06a-42dd-b478-fa006adf4b99
	fromColumn: orders.customer_id
	toColumn: customers.customer_id
```

Два следствия, оба важны для формулировки критериев:

1. **Кардинальности и направления фильтрации в файле нет.** GUI
   показывает «Многие к одному (\*:1)» и «Простое» — в TMDL не записано
   ничего. Вероятная причина: TMDL опускает значения по умолчанию, и
   тогда критерий надо строить наоборот — не «найди строку
   `crossFilteringBehavior: bothDirections`», а «убедись, что её нет».
   **Проверено отдельным прогоном** — см. 2.9.
2. **Имя связи содержит GUID** (`AutoDetected_<uuid>`), и `lineageTag`
   с GUID стоит у каждой таблицы, колонки и меры. Значит **побайтовое
   сравнение TMDL с эталоном невозможно в принципе** — критерии M4 обязаны
   разбирать текст (поиск строки, regex), а не сличать файлы.

**Мера и типы читаются текстом.** `definition/tables/orders.tmdl`:

```
measure 'Total Completed' = CALCULATE(SUM(orders[amount]), orders[status] = "completed")
	formatString: 0.00
...
column amount
	dataType: double
column period_ym
	dataType: string
```

Плюс — в том же файле лежит **весь M-код целиком**, в разделе
`partition orders = m`, включая `Encoding = 65001`, `QuoteStyle =
QuoteStyle.Csv` и культуру `"en-US"`. То есть обходы P1, P7 и P8
проверяются машинно по тексту проекта: критерий может потребовать
наличия этих подстрок.

**Гипотеза P5 подтверждена, и шире, чем предполагалось.** При включённом
PBIR каждый визуал лежит отдельным JSON:
`gate.Report/definition/pages/<id>/visuals/<id>/visual.json`. В нём:

```
"visual": { "visualType": "tableEx",
  "query": { "queryState": { "Values": { "projections": [ ... "queryRef": "customers.city" ...
                                                          "queryRef": "orders.Total Completed" ... ] } },
             "sortDefinition": { "sort": [ { ... "Property": "Total Completed" },
                                             "direction": "Descending" ] } } }
```

Машинно читаются: **тип визуала** (`visualType`), **какие поля на нём**
(`queryRef`), **сортировка и её направление** (`sortDefinition`). Это
переводит в машинную проверку сразу три вещи, числившиеся за чеклистом
автора: запрет на 3D/пончики/двойные оси (слой 3 решения 22, критерий 13),
состав полей визуала и пункт 5 нулевой проверки — порядок строк
проверяется по объявленной сортировке, без экспорта.

Цена — зависимость от трёх preview-функций сразу (PBIP, TMDL, PBIR), из
которых PBIR решение 22 не знало. Все три включаются вручную и могут
измениться в следующих версиях. Это надо записать в предусловие M4 и в
само решение.

### 2.9. TMDL пишет только отклонения от умолчания — проверено прогоном

Гипотеза из 2.8 не оставлена предположением. В GUI изменено одно
свойство связи: «Направление перекрестной фильтрации» с «Простое» на
**«Оба»**, проект пересохранён тем же `.pbip`. `relationships.tmdl` стал:

```
relationship AutoDetected_32360c20-c06a-42dd-b478-fa006adf4b99
	crossFilteringBehavior: bothDirections
	fromColumn: orders.customer_id
	toColumn: customers.customer_id
```

Строка появилась ровно там и ровно тогда, когда значение разошлось с
умолчанием: файл — 174 байта.

После проверки значение возвращено на «Простое» и проект пересохранён.
`relationships.tmdl` вернулся к исходным трём строкам, 133 байта, следов
отменённой правки не осталось — TMDL не копит мусор от откатов.

**Правило для критериев M4, выведенное прогоном.** Свойство при значении
по умолчанию в TMDL отсутствует. Значит проверка формулируется как
утверждение об отсутствии:

- «в `relationships.tmdl` нет строки `crossFilteringBehavior:`» —
  равносильно «направление фильтрации одинарное»;
- «нет строк `fromCardinality:` / `toCardinality:`» — равносильно
  «многие-к-одному»;
- «нет строки `isActive: false`» — равносильно «связь активна».

Формулировка «убедись, что строка `crossFilteringBehavior: oneDirection`
присутствует» была бы невыполнима: такой строки не бывает.

Та же логика распространяется на всю модель, и это надо держать в уме при
написании C3/C4: отсутствие строки в TMDL — не признак того, что автор
чего-то не сделал, а признак значения по умолчанию.

### 2.10. Вердикт Части 2

**Часть 2 пройдена. Развилка «ЕСЛИ ЧАСТЬ 2 ПРОВАЛЕНА» не срабатывает.**
Решение о том, что M4 не пишется, не принимается; часы M4 в части 6.1
blueprint не пересматриваются в сторону сокращения.

Основание — все шесть пунктов нулевой проверки отвечены прогоном, и
критический (пункт 4, детерминированность) отвечен положительно: два
экспорта подряд побайтово идентичны. Механизм решения 22 «визуал →
экспорт → сверка скриптом» работоспособен.

Что при этом изменилось против того, каким решение 22 себе это
представляло — четыре пункта, каждый упирается в находку прогона:

| Что | Как было в решении 22 | Что показал прогон |
|---|---|---|
| Подготовка данных | считалась бесплатной, обход по датам — конструкцией задания | P2, P7, P8: модель по умолчанию непригодна, нужен явный M-код |
| C1 (числа сходятся) | сверка `compare_csv.py` | P9: скрипт падает на десятичной запятой, нужна правка `same_value()` |
| Слой 3 (вид визуала) | чеклист автора | P5: `visualType`, поля и сортировка читаются машинно из PBIR |
| Проверка модели | «прочитать TMDL» | 2.9: только через отсутствие строк; побайтовое сравнение невозможно |

Итог по C1–C4: **все четыре остаются машинными**, ни один не уходит в
чеклист автора. Более того, часть слоя 3 из чеклиста в машинную проверку
переходит. Цена — не в отказе от критериев, а в трёх дополнительных
обязательствах M4:

1. Шаг M4 обязан задавать M-код явно (кодировка, `QuoteStyle`, культура,
   типы), а не полагаться на диалог импорта. Это отдельный подшаг с
   разобранным примером — часов добавляет, а не убавляет.
2. `compare_csv.py` требует правки в `same_value()`: нормализация
   десятичного разделителя перед `float()`. Без неё C1 не сдаётся.
3. Предусловие M4 — три включённые preview-функции (PBIP, TMDL, PBIR), а
   не две, как знало решение 22.

Пересмотр решения 22 и уточнение части 6.1 blueprint делаются отдельной
правкой, вне этого файла: гейт фиксирует измеренное, решения принимаются
в `design/decisions.md`.

---

### 2.11. Разбор: P1 и P8 — противоречие или разные вещи (прогон 2026-07-31)

Вопрос задан после свода: P1 утверждает, что кодировка **зашивается в код
запроса** на момент импорта; P8 — что кодировка **в файле проекта не
фиксируется**. Разобрано прогоном, а не сопоставлением формулировок.

**Прогон.** Прочитан с диска сохранённый проект гейта,
`D:\m4-gate\pbi\gate.SemanticModel\definition\tables\*.tmdl` — то самое
место, где живёт M-код (2.8). Обе таблицы, дословно, раздел `partition`:

```
Источник = Csv.Document(File.Contents("D:\m4-gate\customers.csv"),
  [Delimiter = ",", Columns = 3, Encoding = 65001, QuoteStyle = QuoteStyle.Csv])
Источник = Csv.Document(File.Contents("D:\m4-gate\orders.csv"),
  [Delimiter = ",", Columns = 5, Encoding = 65001, QuoteStyle = QuoteStyle.Csv])
```

`orders.csv` — чистый ASCII, и `Encoding = 65001` у него **есть**. Это
третий факт, которого до сих пор не было ни в P1, ни в P8.

**Ответ: противоречия между P1 и P8 нет — они про разные моменты.**
P1 про то, **когда и из чего** принимается решение о кодировке (в диалоге
импорта, по содержимому файла). P8 про то, **попадает ли принятое решение
в текст проекта**. Одно не отменяет другого.

**Противоречие есть внутри самого P8 — его объяснение опровергается
измерением P1.** P8 объясняет наблюдение правилом «Power BI записывает
`Encoding` только когда выбранное значение расходится с автоопределением».
Подставляем измеренное:

| Файл | Автоопределение (P1) | Выбрано автором (P8) | Правило P8 предсказывает | Измерено (P8) |
|---|---|---|---|---|
| `orders.csv` | 1251 | 65001 | расходится → **записать** | параметра нет |
| `customers.csv` | 65001 | 65001 | совпало → **не писать** | `Encoding = 65001` |

Правило предсказало противоположное по обеим строкам. Оно вычёркивается;
измерение P8 остаётся в силе. Механизм, по которому GUI решает писать
параметр или нет, гейтом **не установлен** и для конструкции M4 не нужен —
см. ниже.

**Что установлено измерением, без гипотез о механизме:**

1. ASCII-файл в диалоге импорта получает `1251` (P1).
2. Явный выбор `65001` в диалоге для ASCII-файла в проект **не попал** (P8).
   Прибить кодировку через GUI нельзя.
3. `Encoding = 65001`, вписанный в M-код **руками**, лежит в `.tmdl` и
   переживает сохранение проекта — для обоих файлов, включая ASCII
   (прогон выше). Это и есть рабочий путь.

**Что осталось непроверенным и почему это больше не важно.** Открыт один
вопрос: переопределяется ли кодировка при Refresh у запроса, в котором
параметра `Encoding` нет вовсе. Ответ требует GUI автора: подменить
`orders.csv` на файл с кириллицей и нажать «Обновить». Прогон не
выполнялся, гипотеза не выдвигается. На конструкцию M4 ответ не влияет:
шаг всё равно обязан вписывать `Encoding` руками — по P7 (культура) и по
P8 (`QuoteStyle`) это уже требуется, и вопрос «что делает запрос без
параметра» в задании не возникает. Он остаётся значимым ровно для одной
вещи — понимания, насколько тихо ломается модель, собранная кликами.

**Следствие для C2 — критерий в текущей формулировке не работает, и
причина тяжелее, чем предполагал вопрос.** Формулировка части 1
blueprint — «подмена исходного файла на файл следующего месяца + Refresh →
0 ошибок, 0 ручных правок». P1 измерил режим отказа, при котором **оба
условия выполняются на испорченных данных**: кодировка прочитана как 1251,
кириллица превращена в мусор, Power BI не выдал ни ошибки, ни требования
правки. То есть критерий не «невыполним» — он выполним одинаково на
исправном и на сломанном результате, а значит не проверяет ничего.
Переформулирован решением 22 (раздел «C2»).

---

## 3. ЧАСТЬ 3 — ручной прогон M4 в Power BI (решение 26). Закрыта

Прогон начат 2026-07-31 (`step-01.md`), продолжен 2026-08-04 и
2026-08-13 (`step-02.md` целиком), 2026-08-14 (`step-03.md` целиком) и
2026-08-14, вторым заходом того же дня (`step-04.md` целиком) — даты по
времени записи файлов проекта `program\M4\pbi`. Порядок тот же, что в Частях 1 и 2: все действия в GUI
делал автор, агент давал инструкции, сверял вывод и записывал. Ничего не
чинилось по ходу — правки содержания в этот прогон не вносились вовсе.

Отличие от Части 2: там проверялся **инструмент** на модели, собранной
для проверки. Здесь проверяются **написанные шаги модуля** — агент даёт
их учащемуся дословно, а расхождение между написанным и происходящим
записывается как дефект инструкции. Нумерация дефектов продолжает ряды
`B` (Часть 1) и `P` (Часть 2) префиксом `R`.

Прогон **доведён до конца**: все четыре написанных шага M4 пройдены
руками. Машинный критерий шага 02 сошёлся, шага 03 — не сходится ни в
одном из двух состояний модели (R30), шага 04 — сошёлся, но проходит и
на невыполненном шаге (R32). Итог по шагам — раздел 3.5, что осталось
незакрытым — 3.6, вердикт — 3.7, дефект датасета — 3.8.

### 3.1. Окружение

| Параметр | Значение |
|---|---|
| ОС | Windows 11 Pro 10.0.26200 |
| Power BI Desktop | 2.156.951.0, установка из Microsoft Store |
| Язык интерфейса | русский |
| Python | 3.11.9 |
| Три preview-функции (PBIP, TMDL, PBIR) | включены до начала прогона |

Версия Desktop — та же, что в Части 2. Обновления Store между частями не
было, и объяснять расхождения сменой версии нечем.

### 3.2. Контрольная точка датасета — сошлась

`generate_m4.py` прогнан до первой задачи любого шага. Все десять sha256
(`csv/` и `csv_next/`, по пять файлов) совпали с таблицами
`program/M4/data/reference_answers.md`, расхождений 0. Числа строк
6582 / 24 / 6 / 432 / 546 и 7316 / 25 / 6 / 457 / 577 подтверждены.

Это единственная часть модуля, которая на прогоне не дала ни одного
расхождения.

### 3.3. Дефекты инструкций

**R1 — включение preview-функций не идемпотентно.**
Статус: открыт
`step-01.md`, задание 3, велит включить три функции в предварительной
версии безусловно. Ветки «если уже включены» нет, а признака
включённости шаг не даёт. На прогоне автор искал в «Параметрах» пункт,
которого там уже не было, потому что функции были включены в предыдущей
сессии. Все три — глобальные настройки приложения и переживают
перезапуск. Учащийся, вернувшийся к модулю через неделю, упирается в ту
же стену. Признак включённости, который работает, прогон установил
попутно: наличие папок `definition/` и файлов `.tmdl` в сохранённом
проекте.

**R2 — визуалы названы, но ни разу не объяснены.**
Статус: открыт
`step-01.md`, задание 5: «Положите на страницу визуал „Таблица“ и
перетащите в него `city_uk`». Названо, что должно получиться, а не что
нажать. На прогоне вопрос был буквально «что это значит? как это
сделать?». Дефект не одиночный: положить визуал требуют также
`step-02.md` (задание 7, пять карточек) и `step-04.md` (три визуала), и
ни в одном месте модуля панель «Визуализации» не разобрана ни разу. Это
первое действие учащегося в главном окне Power BI, и модуль проходит
его молча.

**R3 — проект сохранён как `.pbix`, и шаг этому не мешает.**
Статус: открыт
`step-01.md`, задание 6, называет формат («сохраните как Power BI
Project»), но не говорит, что формат выбирается из выпадающего списка
«Тип файла», где по умолчанию стоит `.pbix`. На прогоне автор сохранил
`m4.pbix` (18 080 байт), и `check_pbip.py --step 01` дал пять `[FAIL]`,
код возврата 1.

Отдельно от дефекта — **подтверждение**: третья типичная ошибка
`step-01.md` описывает ровно этот случай, и её заявленное последствие
(«не проходит ни одна из семи проверок») оказалось верным. Ошибка,
предсказанная автором, случилась с самим автором на первой же попытке.

**R4 — `.gitignore` внутри репозитория Power BI Desktop не создаёт.**
Статус: открыт
`step-01.md` (1.2 и четвёртая типичная ошибка, «Как правильно: ничего не
делать»), решение 22 (раздел «Что коммитится») и `step-00.md`
(предусловие, п. 4) опираются на то, что `.gitignore` проекта создаёт
сам Desktop. Прогон измерил четыре случая вместо того, чтобы строить
гипотезу:

| Куда сохраняли | Папка | `.gitignore` |
|---|---|---|
| `D:\m4-gate\pbi` (гейт, вне репозитория) | новая | создан, 45 байт |
| `program\M4\pbi` (внутри репозитория, был `m4.pbix`) | непустая | **не создан** |
| `program\M4\pbi-probe` (внутри репозитория) | новая пустая | **не создан** |
| `D:\pbi-probe2` (вне репозитория) | новая пустая | создан, 45 байт |

Содержимое созданного файла в обоих случаях одинаково:

```
**/.pbi/localSettings.json
**/.pbi/cache.abf
```

Правило держится везде, кроме единственного места, где программа его
применяет. Цена: `m4.SemanticModel\.pbi\cache.abf` — 12 110 байт, и
корневой `.gitignore` репозитория его не покрывает (в нём
`.claude/settings.local.json`, `*.db`, `__pycache__/`, `*.pyc`,
`program/M4/data/csv/`, `program/M4/data/csv_next/` — строки про `.pbi/`
нет).

Что осталось неразделённым и названо вслух: триггер — наличие git-репозитория
или наличие родительского `.gitignore`. Пробы это не различили. На
починку не влияет: писать `.gitignore` проекта придётся руками в любом
случае.

**R5 — два из четырёх «молчаливых умолчаний» диалога импорта не
воспроизвелись.**
Статус: открыт
`step-02.md`, 1.2, перечисляет четыре решения, которые диалог импорта
принимает молча. Прогон подал диалогу настоящие файлы датасета и
записал, что он сделал:

| Умолчание по 1.2 | Что было на прогоне |
|---|---|
| культура `"ru"` в `Table.TransformColumnTypes` | **сработало** — оба файла |
| `2026-05` прочитан как дата | **сработало** — `transactions[period_ym]` стал датой, `01.01.2025` |
| кодировка определяется по содержимому, латиница получает `1251` | **не сработало** — диалог написал `Encoding = 65001` сам |
| `QuoteStyle = QuoteStyle.None` | **не сработало** — диалог написал `QuoteStyle = QuoteStyle.Csv` сам |

M-код `merchants.csv`, который написал диалог, дословно:

```
let
  Источник = Csv.Document(File.Contents("D:\data-analyst-program\program\M4\data\csv\merchants.csv"), [Delimiter = ",", Columns = 4, Encoding = 65001, QuoteStyle = QuoteStyle.Csv]),
  #"Повышенные заголовки" = Table.PromoteHeaders(Источник, [PromoteAllScalars = true]),
  #"Измененный тип столбца" = Table.TransformColumnTypes(#"Повышенные заголовки", {{"merchant_id", Int64.Type}, {"merchant_name", type text}, {"city", type text}, {"address", type text}}, "ru")
in
  #"Измененный тип столбца"
```

`merchants` при этом дал **24 строки** без единой правки: многострочные
адреса разобраны верно.

Четыре следствия:

1. Утверждение «`QuoteStyle.None` рвёт значение по переводу строки»
   остаётся **непроверенным**: диалог сам такого условия не создаёт, а
   вручную его на прогоне не ставили.
2. Четвёртая типичная ошибка `step-02.md` (26 строк вместо 24) на этом
   датасете **невоспроизводима**. Её число получено рассуждением, а не
   прогоном, — это нарушение раздела 1.6 скилла `curriculum-design`.
3. Вторая типичная ошибка `step-02.md` («явный выбор 65001 в диалог не
   доходит») прогону **противоречит**. Находка P1 измерена на файле из
   одной латиницы, а шаг подаёт её как общее правило.
4. Довод «почему это отдельный шаг, а не двадцать минут `step-03.md`»
   держался на четырёх причинах, из которых прогон подтвердил две.

Мелочь оттуда же: реальное имя шага — `#"Измененный тип столбца"`, в
1.3 написано `#"Изменённый тип"`.

**R6 — колонка сумм не «объявлена текстом», а отсутствует в списке
типов.**
Статус: открыт
`step-02.md`, 1.3, показывает результат диалога как `{"amount", type
text}` — то есть учит искать в списке строку с явным `type text`. На
настоящем `transactions.csv` `amount_uah` в `Table.TransformColumnTypes`
**отсутствует вовсе**: перечислены `tx_id`, `merchant_id`, `tx_date`,
`settled_date`, `mcc`, `status`. Итог тот же (колонка текстовая, `SUM`
не считается), причина другая: не «объявлена текстом», а не объявлена
никак и осталась тем, чем вышла из `Csv.Document`. Разобранный пример
учит искать строку, которой в файле не будет.

**R7 — проверка числа строк стоит раньше средства проверки.**
Статус: открыт
`step-02.md`, задание 6: «Примените изменения и проверьте число строк
каждой таблицы». Где смотреть — не сказано. Счётчик в строке состояния
редактора Power Query по умолчанию считает по первой 1000 строк и
печатает `999+`, а не число; приписка «Профилирование столбца на основе
первых строк (1000)» об этом сообщает, но шаг о ней молчит и
переключения на весь набор не упоминает ни разу. Учащийся по этой
инструкции сверит `999+` с 6582.

Честное число строк даёт задание **7** (карточки) — то есть проверка из
задания 6 требует инструмента, который появляется заданием позже.

**R8 — «Сохраните проект» стоит одним пунктом в конце 5–7-часового
шага.**
Статус: открыт
`step-02.md`, задание 8. Между «загрузил первую таблицу» и «сохранил»
по инструкции проходит весь шаг: пять загрузок, правка M-кода в пяти
запросах, сверка строк, шесть визуалов. Шаг оценён в 5–7 ч, то есть
заведомо не в один присест, и о промежуточном сохранении не говорит.
Измерено: после первой сессии прогона на диске лежал результат
`step-01.md` (одна таблица `smoke`, запись 2026-07-31 16:47), а обе
загруженные таблицы `step-02.md` отсутствовали.

**R9 — задание построено на несуществующем элементе интерфейса.**
Статус: открыт
`step-02.md`, задание 7: «Положите на страницу пять визуалов „Карточка“:
`Количество строк` по каждой таблице». Пункта «Количество строк» в
панели «Данные» Power BI Desktop нет. Проверено визуально на всех пяти
таблицах модуля: панель показывает только колонки
(`merchants` — `address`, `city`, `merchant_id`, `merchant_name`;
`transactions` — `amount_uah`, `mcc`, `merchant_id`, `period_ym`,
`settled_date`, `status`, `tx_date`, `tx_id`), служебных полей нет.

Тяжелее R2: там элемент интерфейса назван, но не объяснён, здесь —
назван тот, которого не существует. Учащийся упирается в тупик и не
может закрыть первый пункт критерия готовности (пять чисел строк),
потому что единственный названный шагом способ их получить отсутствует.

Строка `Количество строк` встречается в репозитории **ровно один раз** —
`program/M4/step-02.md:189`. В `step-04.md` карточки используются под
меры, а меры к тому шагу заведены, и там дефекта нет.

Способы, которыми число строк получить можно (оба измерены на прогоне
как доступные, ни один в модуле не описан): завести меру
`COUNTROWS(<таблица>)` — но мер до `step-04.md` в модуле нет; либо
положить в карточку ключевую колонку и сменить агрегат на «Количество».
Второй способ требует оговорки, которой в шаге тоже нет: «Количество» —
это `COUNT`, он считает непустые значения колонки, а не строки таблицы,
и совпадает с числом строк только на колонке без пропусков.

**Чем R9 обошли, чтобы прогон продолжился (заход 2026-08-13).** Взят
второй способ, на всех пяти таблицах одинаково: ключевая колонка в
карточку, затем правый щелчок по полю в колодце «Значение» →
`Количество`. Колонки: `transactions.tx_id`, `merchants.merchant_id`,
`mcc_categories.code`, `merchant_plan.merchant_ref`,
`calendar.date_key`. Все пять — без пропусков, поэтому `COUNT` совпал с
числом строк: 6582 / 24 / 6 / 432 / 546, эталон сошёлся. Обход рабочий,
но он не восстанавливает шаг: учащемуся, читающему `step-02.md`, взять
его неоткуда — в тексте нет ни слова «Количество» как агрегата, ни
оговорки про `COUNT`. На `calendar.date_key` обход дополнительно упёрся
в R12 и R13 — подробности там.

**R10 — переход из редактора Power Query в главное окно не назван.**
Статус: открыт
Между заданием 5 (`step-02.md`, типы колонок) и заданием 7 (визуалы)
учащийся обязан выйти из редактора Power Query командой
**Главная → Закрыть и применить**. Задание 6 говорит «Примените
изменения» — называет действие, но не место и не кнопку. Панелей
«Визуализации» и «Данные», нужных заданию 7, в редакторе Power Query нет
вовсе, так что учащийся, оставшийся в редакторе, ищет их там, где их не
бывает. Тот же класс, что R2.

**R11 — не сказано, что визуалов шесть и что они отдельные.**
Статус: открыт
`step-02.md`, задание 7, перечисляет «пять визуалов „Карточка“… и один —
`Сумма amount_uah`». Механика интерфейса при этом такова, что галочка у
второго поля добавляет его **в тот же выделенный визуал**, а не создаёт
новый: чтобы получить шесть карточек, перед каждой нужно снять
выделение щелчком по пустому месту страницы. Ни числа «шесть» одним
словом, ни требования снять выделение в задании нет.

**R12 — «Автоматические дата и время» ломают критерий `step-03.md`.**
Статус: открыт
Самая тяжёлая находка прогона, и единственная, где дефект не в
формулировке, а в том, что модуль не знает про поведение инструмента.

Настройка «Автоматические дата и время» включена в Power BI Desktop по
умолчанию. Для каждой колонки типа «дата» она заводит скрытую таблицу
календаря и скрытую связь к ней. В модуле три таких колонки
(`transactions[tx_date]`, `transactions[settled_date]`,
`calendar[date_key]`), и после загрузки пяти таблиц — до того, как
учащийся провёл хоть одну связь, — в сохранённом проекте лежит:

```
tables/DateTableTemplate_86f19579-70ff-49a6-8881-5beb50aeace3.tmdl
tables/LocalDateTable_2f7a081d-b37a-42c7-b28b-adb886f072f7.tmdl
tables/LocalDateTable_5b20ce05-b1bf-4a77-bc8c-7f961ec3ce49.tmdl
tables/LocalDateTable_9357b11a-ed5b-4bba-8be2-353803f0f10c.tmdl
```

и `relationships.tmdl` на 717 байт с четырьмя связями:

```
relationship 842e5041-c21c-481d-99f3-985e6df0fd6d
	joinOnDateBehavior: datePartOnly
	fromColumn: transactions.tx_date
	toColumn: LocalDateTable_5b20ce05-b1bf-4a77-bc8c-7f961ec3ce49.Date

relationship 6d1a944e-df8a-4f39-905a-bb9843e56889
	joinOnDateBehavior: datePartOnly
	fromColumn: transactions.settled_date
	toColumn: LocalDateTable_2f7a081d-b37a-42c7-b28b-adb886f072f7.Date

relationship AutoDetected_97997382-8214-4816-8bab-4fa5b610b188
	fromColumn: transactions.merchant_id
	toColumn: merchants.merchant_id

relationship 29036238-b5c4-4cae-8110-b9686bcb0a15
	joinOnDateBehavior: datePartOnly
	fromColumn: calendar.date_key
	toColumn: LocalDateTable_9357b11a-ed5b-4bba-8be2-353803f0f10c.Date
```

Три следствия, все проверяемые по этому же файлу:

1. **Критерий `step-03.md` непроходим на настройках по умолчанию.**
   `check_pbip.py`, `check_step03()`, требует ровно пяти связей
   (`len(blocks) == 5`). После четырёх связей, которые проводит
   учащийся, в файле окажется восемь: три скрытых, одна
   `AutoDetected_`, четыре проведённых руками. Проверка `[FAIL]`.
   Остальные проверки шага при этом проходят: связей не-`AutoDetected`
   станет семь при требуемых «не менее четырёх», строка
   `isActive: false` останется одна, а `joinOnDateBehavior:` в списке
   `DEFAULTS_MUST_BE_ABSENT` не значится.
2. **Число таблиц в модели не пять, а девять.** `reference_answers.md`
   («Что должно получиться в модели после `step-02.md`») перечисляет
   пять. `check_pbip.py --step 01` печатает число таблиц справочно и
   `[FAIL]` из-за этого не даёт, но эталон расходится с моделью.
3. **Дефект интерфейса, который видит учащийся** — это тот же дефект.
   Колонка даты приходит в панель «Данные» не колонкой, а иерархией
   Год/Квартал/Месяц/День, и чтобы положить в визуал саму колонку,
   надо переключаться на неё явно. Модуль об этом не говорит нигде.

Что **не установлено** и требуется до починки: точный путь к настройке в
русском интерфейсе и её область действия (текущий файл или все новые).
На прогоне настройку не искали и не трогали — прогон измеряет, а не
чинит. Отсюда же открытый вопрос, которого у модуля не было: выключать
её в `step-01.md` или строить критерий `step-03.md` так, чтобы скрытые
связи не мешали.

**Уточнение R12 (2026-08-13, при возобновлении прогона).** Проект открыт
заново после недельного перерыва; файлы никем не правились (запись
2026-08-04). Прочитаны `model.tmdl` и `relationships.tmdl` целиком —
добавилось три факта, которых в записи выше не было:

1. **У настройки есть след в тексте модели.**
   `m4.SemanticModel\definition\model.tmdl:10` содержит
   `annotation __PBI_TimeIntelligenceEnabled = 1`. Это значит, что
   автодата хранится **в самом файле проекта**, а не только в настройках
   приложения, и что состояние проверяется поиском подстроки — тем же
   способом, что и остальные проверки решения 22, а не осмотром
   диалога. Часть вопроса «область действия» этим закрыта: у файла своё
   значение. Путь к пункту в русском интерфейсе по-прежнему не
   установлен — настройку не искали.
2. **Девять таблиц подтверждены вторым файлом.** `model.tmdl:16–24`
   перечисляет `ref table` девять раз: пять таблиц модуля и четыре
   служебных (`DateTableTemplate_86f19579…` и три `LocalDateTable_…`).
   Раньше это было видно только по содержимому папки `tables/`.
3. **Скрытых связей три, автоопределённая — четвёртая, и все четыре
   лежат в файле до единого клика учащегося.** `relationships.tmdl`
   содержит ровно четыре блока: три с `joinOnDateBehavior: datePartOnly`
   к `LocalDateTable_*` и `AutoDetected_97997382-…`
   (`transactions.merchant_id` → `merchants.merchant_id`). Арифметика
   `check_step03()` от этого не меняется (8 = 3 + 1 + 4, из них
   не-`AutoDetected` семь при требуемых «не менее четырёх»), но
   меняется формулировка для починки: **до `step-03.md` в модели уже
   лежит четыре связи, а не три**, и одна из них — та самая связь 1,
   которую `reference_answers.md` засчитывает учащемуся. Учащийся
   проводит руками четыре связи из пяти ожидаемых, а видит в модели
   восемь.

4. **Следствие 3 измерено на карточке и оказалось тяжелее записанного.**
   Выше сказано «колонка даты приходит в панель „Данные“ не колонкой, а
   иерархией». На прогоне 2026-08-13 галочка у `calendar[date_key]`
   положила в карточку **пять чипов сразу** — `date_key`, `Год`,
   `Квартал`, `Месяц`, `День`, — и все пять ушли в колодец «Категории».
   Карточка напечатала склейку уровней (`2 тыс. Квартал 1 Январь 1`,
   `2 тыс. Квартал 1 Январь 2`, … со скроллом). То есть учащийся,
   выполняющий задание 7 буквально, получает на пятой карточке не число
   и не ошибку, а список строк.

5. **Скрытые связи невидимы там, куда шаг посылает смотреть.** Измерено
   2026-08-14, первое действие `step-03.md`: вид **«Модель»** показывает
   **пять** таблиц модуля и **одну** линию — `merchants` — `transactions`
   («\*» со стороны `transactions`, «1» со стороны `merchants`, стрелка в
   сторону `transactions`). Таблиц автодаты на схеме **нет**, и в панели
   «Данные» их тоже нет: ни `DateTableTemplate_*`, ни `LocalDateTable_*`.
   То есть в `relationships.tmdl` четыре связи, а глазами доступна одна.
   Пункт критерия «связей в модели ровно 5» через вид «Модель» — а это
   единственное место, куда `step-03.md` посылает учащегося смотреть на
   связи, — **не проверяется в принципе**: расхождение между схемой и
   файлом не 8 против 5, а 1 против 4 ещё до первого клика. Учащийся,
   сверивший схему, увидит ровно то, что обещает шаг, и получит `[FAIL]`
   скрипта без единой видимой причины.

6. **Скрытая таблица снимается ровно с той колонки, которую связали
   вручную, и арифметика выше оказалась неверной.** Измерено 2026-08-14,
   после связей 3 и 4 `step-03.md`
   (`transactions[tx_date]` → `calendar[date_key]` и
   `transactions[settled_date]` → `calendar[date_key]`, проект
   сохранён). В `relationships.tmdl` **пять** блоков, а не семь:

   ```
   AutoDetected_97997382-…   transactions.merchant_id → merchants.merchant_id
   29036238-…                calendar.date_key → LocalDateTable_9357b11a-… (joinOnDateBehavior: datePartOnly)
   aa2ba565-…                transactions.mcc → mcc_categories.code
   cbd08f10-…                transactions.tx_date → calendar.date_key
   84950e57-…                isActive: false; transactions.settled_date → calendar.date_key
   ```

   Исчезли **ровно два** блока автодаты — по `transactions.tx_date` и
   `transactions.settled_date`, то есть по тем двум колонкам, которые
   были связаны с `calendar` вручную. Вместе с ними ушли их таблицы:
   `model.tmdl` перечисляет теперь **семь** `ref table` вместо девяти, в
   папке `tables/` осталась одна `LocalDateTable_9357b11a-….tmdl`.

   Что **не** убралось и убираться не собирается:
   `LocalDateTable_9357b11a-…` вместе со своей связью
   (`calendar.date_key` → `…Date`) и `DateTableTemplate_86f19579-…` на
   месте. Механизм не «автодата убирает за собой», а точечный: скрытая
   таблица снимается с той колонки, у которой появилась настоящая связь
   с таблицей дат, и только с неё. У `calendar[date_key]` такой связи не
   будет никогда — это колонка даты внутри самого календаря.

   Отсюда прогноз, записанный в пунктах 3 и в разделе 4.1 `HANDOFF.md`
   («скрипт увидит восемь»), **неверен**. После связи 5 в файле будет
   **шесть** блоков: пять связей модуля плюс одна автодатная — та, что
   принадлежит `calendar[date_key]`, колонке даты внутри самой таблицы
   календаря, у которой настоящей связи с другим календарём быть не
   может. Критерий `step-03.md` («связей в модели ровно 5») всё равно не
   проходит, но лишняя связь одна, а не три, и причина у неё
   единственная и точечная.

Состояние переживает закрытие и повторное открытие проекта: те же
четыре связи и те же девять таблиц через девять дней после записи
(измерение до связей 3–4; после них — пункт 6).

**R13 — «положите визуал „Карточка“» не говорит, в какой колодец кладётся
поле; по умолчанию оно уходит в «Категории», и карточка печатает список
значений вместо числа.**
Статус: открыт
`step-02.md`, задание 7. На прогоне 2026-08-13 галочка у
`merchants[merchant_id]` в панели «Данные» посадила поле в колодец
**«Категории»**, и карточка показала **список `101, 102, 103` со
скроллом** — не число и не сообщение об ошибке. Первый пункт критерия
готовности (24 строки в `merchants`) через такую карточку не
закрывается.

Обход, которым прогон продолжен: контекстное меню поля → **«Переместить
в» → «Значение»**, затем агрегат **«Количество»**. Дал 24.

Тот же класс, что R2 и R10: назван результат («положите визуал»), а не
последовательность действий. Разница в том, что здесь у действия есть
**молчаливый неверный исход**: учащийся видит заполненную карточку и
может засчитать её как выполненное задание. Явный порядок, которого в
шаге нет: сначала иконка «Карточка» в панели «Визуализации», затем поле
в колодец **«Значение»**, затем агрегат.

**Открытый вопрос закрыт тем же прогоном, и ответ меняет починку.** На
пятой карточке (`calendar`) порядок соблюдён буквально: сначала иконка
«Карточка» в панели «Визуализации», визуал создан и остался выделенным,
и только потом галочка в панели «Данные». Поле всё равно село в
**«Категории»**, «Значение» осталось пустым. Значит порядок «сначала
иконка, потом галочка» дефекта **не лечит**: галочка кладёт поле в
«Категории» независимо от того, как создан визуал. Починка шага обязана
называть **колодец** («Переместить в» → «Значение» либо перетаскивание
поля прямо в колодец), а не последовательность действий.

Чем три карточки сессии 2026-08-04 получили «Значение» — не измерено. В
их `visual.json` поле лежит в колодце `Data`
(`queryState.Data.projections`, `Aggregation.Function: 0`), но каким
действием оно туда попало, прогон не записал; правдоподобная гипотеза
(перетаскивание вместо галочки) остаётся гипотезой.

**R13 шире, чем карточки: то же повторилось на «Таблице» в `step-03.md`
(2026-08-14).** Первая раскладка задания 4 требует визуал с двумя полями
(`mcc_categories[category_name]` и `Сумма amount_uah`). Галочка у второго
поля при **снятом выделении** создала **второй визуал** вместо
добавления поля в первый: на странице оказались список категорий отдельно
и одинокий итог отдельно. Ни в `step-02.md`, ни в `step-03.md` нигде не
сказано, что перед добавлением второго поля визуал надо **выделить**.

Значит дефект не про карточки, а про способ, которым модуль вообще велит
собирать визуалы: пока не назван ни колодец, ни требование выделить
визуал, каждое «положите визуал с полями X и Y» имеет молчаливый неверный
исход. Починка одна на все такие задания, и её место — `step-02.md`,
первое появление визуала.

Мелочь оттуда же, для полноты: столбцы «Таблицы» встали в порядке
`Сумма amount_uah`, затем `category_name` — обратном тому, в каком поля
названы в задании. На сверку чисел не влияет.

**R14 — числа на карточках приходят с автоформатом, а критерий шага
сверяет с точной записью.**
Статус: открыт
`step-02.md`, 1.5, пункт первый требует сверить число строк с эталоном
(`6582`), пункт второй — сумму `10236902.82` с допуском «расхождение 0 до
второго знака». Измерено: карточка с `Количество tx_id` печатает
**`6,582 тыс.`**. Эталон `6582` на экране в таком виде не появляется, и
«расхождение 0» глазами по нему не проверяется — сверяются две разные
записи одного числа.

Для суммы это тяжелее, чем для строк, и **измерено**: карточка
`Сумма amount_uah` печатает **`10,24 млн`** при эталоне `10236902.82`.
Допуск критерия — «расхождение 0 до второго знака»; на экране число
показано до второго знака **миллионов**, то есть с ценой деления 10 000.
Пункт 2 критерия `step-02.md` через карточку не проверяется вовсе — не
«неудобно», а нечем: любая ошибка меньше пяти тысяч гривен даст ту же
запись `10,24 млн`.

Итог по всем шести числам задания 7, как они выглядят на экране против
эталона:

| Карточка | Эталон | На экране | Сверяется глазами |
|---|---|---|---|
| `Количество tx_id` | 6582 | `6,582 тыс.` | нет — другая запись |
| `Количество merchant_id` | 24 | `24` | да |
| `Количество code` | 6 | `6` | да |
| `Количество merchant_ref` | 432 | `432` | да |
| `Количество date_key` | 546 | `546` | да |
| `Сумма amount_uah` | 10236902.82 | `10,24 млн` | нет — точность ±10 000 |

Автоформат срабатывает на числах от тысячи, поэтому четыре из шести
проверок критерия работают, а две — обе, ради которых шаг написан
(6582 строк и сумма после правки культуры), — не работают.

**Граница дефекта, измеренная 2026-08-14: R14 — про визуал «Карточка», а
не про модуль.** Первая раскладка `step-03.md` в визуале **«Таблица»**
напечатала суммы полностью, с копейками (`3955730,62`, `2112420,45`, …,
итог `10236902,82`) и сошлась с `ref_by_category.csv` до копейки.
Значит автоформат — свойство карточки, а не форматной строки колонки, и
чинить его надо в заданиях с карточками. Критерии, которые сверяются по
«Таблице», проверяются глазами и в починке не нуждаются.

Дефект не в задании, а в **критерии**: 1.5 написан так, будто карточка
показывает то же, что лежит в модели. Починка — либо сказать в шаге,
какой вид числа ожидать, либо добавить отключение автоформата, либо
перенести сверку точных чисел на `check_pbip.py` и экспорт, оставив
карточкам порядок величины.

**Автоформат применяется и там, где число не величина, а метка.** На
карточке `calendar` уровень иерархии `Год` напечатан как **`2 тыс.`**
вместо `2025`. Это тот же механизм, что дал `6,582 тыс.`, но последствие
другое: сумму в сокращённой записи хотя бы можно опознать, а год — нет,
на экране получается мусор. Значит вариант починки «сказать в шаге, какой
вид числа ожидать» покрывает не все случаи: для меток сокращённая запись
не имеет правильного вида вовсе, и остаётся только отключение
автоформата.

**R15 — там, где связь по паре колонок построить нельзя, диалог молча
предлагает валидную с виду связь «многие ко многим» с двунаправленной
фильтрацией.**
Статус: открыт
`step-03.md`, 1.2 («связь соединяет одну колонку с одной») и 1.3, связь 5.
Прогон 2026-08-13 подал диалогу ровно тот сценарий, который шаг называет
невозможным: **Управление связями → Создать**, `merchant_plan` и
`transactions`, выделены `merchant_ref` и `merchant_id`, затем
Ctrl+щелчок по `period_ym` в обеих таблицах.

Измерено: Ctrl+щелчок **переносит** выделение, а не добавляет —
утверждение документации подтверждено (раздел 3.4). Но отказа диалог не
показывает. Он принял последнее выделение (`period_ym` ↔ `period_ym`),
сам выставил **«Количество элементов: Многие ко многим (\*:\*)»** и
**«Направление перекрёстной фильтрации: Оба»** и напечатал плашку
дословно:

> Эта связь имеет кратность "многие ко многим". Используйте такую
> кратность, только если ожидаете, что ни в одном из столбцов
> (period_ym и period_ym) не будет уникальных значений, и понимаете
> существенные отличия ее принципа работы.

Учащийся, попробовавший собрать составной ключ кликами, получает не
запрет, а готовую к сохранению связь. Сохранив её, он получает лишнюю
шестую связь, двунаправленную фильтрацию и раздутый итог. Шаг об этом не
предупреждает нигде: слово «многие-ко-многим» встречается в `step-03.md`
**один раз** (строка 172) и описывает последствие связи по одному
`merchant_ref`, а не поведение диалога; «Оба» упомянуто один раз
(строка 74) и только как след в TMDL.

Класс тот же, что у R13: у действия есть молчаливый неверный исход,
выглядящий как выполненное задание. Цена выше — ломается модель, а не
одна карточка.

**R16 — термины шага не совпадают с терминами русского интерфейса.**
Статус: открыт
`step-03.md` говорит **«кардинальность»** (7 вхождений), диалог связей —
**«Кратность»** и **«Количество элементов»**; строки «кардинальность» в
интерфейсе нет вовсе. Кнопку подтверждения шаг не называет ни разу; в
диалоге она — **«Сохранить»**.

Сам по себе дефект не блокирующий, но это тот же класс, что R2 и R10:
учащийся ищет в интерфейсе слово, которого там нет. Отличие от R2/R10 —
здесь термин не пропущен, а назван **другим словом**, и это хуже: поиск
по подсказке даёт ложную уверенность, что нужный элемент не найден.

Третье расхождение, измеренное там же: `step-03.md:112–113` велит «снять
флажок **„Сделать эту связь активной“**» — в диалоге флажок называется
**«Активировать связь»**. Это уже не термин из теории, а прямая
инструкция по клику: учащийся ищет подпись, которой в диалоге нет, и
сверить ему не с чем.

**R17 — «Power BI подставит кардинальность сам» подано в шаге как
признак правильной связи, а подстановка не смотрит в данные.**
Статус: открыт
`step-03.md:99–100`, связь 2: «Кардинальность Power BI подставит сам —
„Многие к одному“, потому что `code` уникален (6 значений на 6 строк)».
Учащийся из этого выводит, что подставленная кардинальность подтверждает
правильность выбранной пары колонок.

Измерено на паре `merchants[merchant_id]` → `transactions[mcc]`, у
которой **нет ни одного общего значения** (проверено по файлам датасета:
`mcc` — 4121, 5411, 5541, 5812, 5912, 6012; `merchant_id` — 101…124;
пересечение пусто):

- «Количество элементов»: **«Один ко многим (1:\*)»**;
- «Направление перекрёстной фильтрации»: **«Простое»**;
- «Активировать связь»: флажок стоит и **доступен**;
- кнопка **«Сохранить» активна**;
- предупреждений нет ни одного.

То есть кардинальность выводится из **уникальности значений внутри
каждой колонки** и ничего не говорит о том, что колонки вообще
пересекаются. Бессмысленная связь выглядит в диалоге в точности как
правильная связь 2. Единственный признак, которому шаг учит доверять на
этом месте, ложный.

Контраст, измеренный в том же заходе (побочно, на ошибочно выбранной
паре `transactions[merchant_id]` → `merchants[merchant_id]`): дубликат
**по той же паре колонок** блокируется наглухо — красное сообщение
«Между этими двумя столбцами уже существует связь», все поля серые,
«Сохранить» неактивна. Значит проверки в диалоге есть, просто они
устроены про структуру связей, а не про содержимое колонок.

**R18 — путь «Моделирование → Управление связями» ведёт во вкладку,
которой в текущем виде нет.**
Статус: открыт
`step-03.md`, 1.3, связь 2. Шаг сначала посылает в вид **«Модель»**
(связь 1), а следом даёт путь **«Моделирование → Управление связями →
Создать»**. Измерено 2026-08-14:

- вкладки **«Моделирование» в виде «Модель» нет**; её нет и в виде
  «Данные». Она есть только в виде **«Отчёт»**;
- в виде «Данные» кнопка «Управление связями» живёт на **контекстной**
  вкладке **«Средства работы с таблицами»**, группа «Связи», и вкладка
  появляется, только когда выделена таблица.

То есть учащийся, шедший по шагу подряд, стоит в виде «Модель» и ищет
вкладку, которой в этом виде не существует. Класс тот же, что у R2 и
R10 (назван результат, не место), но с отягчением: место **названо**, и
названо неверно для того вида, в который шаг сам же и привёл. Ошибка
такого рода дороже пропуска — учащийся ищет дольше, потому что уверен,
что искомое есть.

**Побочное измерение к R9 и R14 (2026-08-14).** В виде «Данные» строка
состояния печатает `Таблица: calendar (строк: 546)` — то самое число
строк, ради которого задание 7 `step-02.md` строит карточку. Это готовый
обход сразу двух дефектов: число берётся без карточки (R9) и без
автоформата (R14). В шаге этот путь не назван ни разу. Годность как
починки не проверялась — обход записан, решение о нём принимается после
прогона шага 04, по требованию решения 26.

**R19 — «Создать» при выделенной строке списка попадает в «Изменить», и
существующая связь переписывается молча.**
Статус: открыт
`step-03.md`, 1.3, связь 2: «**Управление связями → Создать**». Измерено
2026-08-14, прогон и контрольное воспроизведение:

Диалог «Управление связями» держит пять кнопок — **«Новая связь»**,
«Автоматическое обнаружение», **«Изменить»**, «Удалить», «Фильтр».
Строка списка выделяется щелчком, и тогда «Изменить» активна. Окно,
которое открывается по «Изменить», называется **«Изменение связи»** и от
окна **«Новая связь»** отличается одним словом в заголовке; поля,
раскладка и кнопка «Сохранить» те же.

На прогоне при выделенной строке `AutoDetected` открылось окно
изменения, обе стороны были переписаны на `transactions[mcc]` →
`mcc_categories[code]` и сохранены. Результат: в списке **одна** строка,
связи к `merchants` нет ни в списке, ни на схеме, ни в
`relationships.tmdl` (файл проверен). Ни предупреждения, ни вопроса, ни
следа в интерфейсе — связь не была удалена, она **перестала быть связью
к `merchants`**.

Сложение с R12 даёт то, ради чего это записано: у учащегося на схеме
видна ровно одна линия (пункт 5 уточнения R12), и она же — единственная
выделяемая строка списка. Промах по кнопке стоит ему той самой связи 1,
которую шаг велел не трогать («Автосозданную не трогайте», задание 1), а
критерий шага покажет это как «связей четыре вместо пяти» без единой
объяснимой причины. Класс тот же, что у R13, R15 и R17 — молчаливый
неверный исход, — но здесь он ещё и **разрушающий**: теряется уже
сделанная работа.

Шаг не называет ни кнопку «Новая связь», ни существование «Изменить»,
ни требование снять выделение. Слова «Создать» в этом диалоге нет вовсе
(ср. R16).

**Состояние двух утверждений о связях (порядок решения 26).**

| Утверждение | Состояние после заходов 2026-08-13 и 2026-08-14 |
|---|---|
| «связь соединяет одну колонку с одной» | **подтверждено** — Ctrl+щелчок переносит выделение, а не добавляет; пара колонок не собирается (R15) |
| «активной между двумя таблицами может быть одна связь» | **подтверждено целиком** — см. ниже |

**Как закрыта вторая половина (2026-08-14).** Контрольный прогон: строка
`AutoDetected` открыта на изменение, у неё сменена только нижняя сторона
на `mcc_categories[code]`, связь сохранена. В списке стало **две**
строки между одной и той же парой таблиц:

```
transactions (mcc)          -> mcc_categories (code)   Активный
transactions (merchant_id)  -> mcc_categories (code)   Неактивн
```

То есть Power BI **сам погасил вторую связь** в момент сохранения, а не
отказал. Сообщения нет вообще — ни в диалоге, ни после. Единственный
признак, который вообще виден: флажок **«Активировать связь» снялся сам**
в момент смены таблицы, до нажатия «Сохранить».

Это ровно то, что `step-03.md:111–112` обещает про связи 3 и 4 («Вторую
Power BI сделает неактивной автоматически»), — утверждение документации
подтверждено на настоящей модели, и обещание шага верно. Цена
подтверждения: единственный признак происходящего — самопроизвольно
снявшийся флажок, который учащийся заметит только если смотрел на него.

**Повторено на штатной паре шага (связи 3 и 4, 2026-08-14).**
`transactions[tx_date]` → `calendar[date_key]` создана первой и осталась
активной; при создании `transactions[settled_date]` → `calendar[date_key]`
флажок «Активировать связь» **снялся сам в момент выбора пары**, до
нажатия «Сохранить», без единого сообщения. После сохранения список:

```
transactions (mcc)           *--<--1  mcc_categories (code)    Активный
transactions (merchant_id)   *--<--1  merchants (merchant_id)  Активный
transactions (settled_date)  *--<--1  calendar (date_key)      Неактивн
transactions (tx_date)       *--<--1  calendar (date_key)      Активный
```

Неактивная связь нарисована на схеме **пунктиром**. В файле она получила
строку `isActive: false` — ровно ту единственную, которой требует
критерий шага. Ручное снятие флажка, предусмотренное `step-03.md:112–113`
как второй вариант, не понадобилось.

**R20 — какая из двух связей к календарю останется активной, решает
порядок создания, а шаг порядка не задаёт.**
Статус: открыт
`step-03.md`, 1.3: «Проведите обе связи… Вторую Power BI сделает
неактивной автоматически», и следом: «Активной остаётся связь по
`tx_date`». Измерено: активной осталась именно та, что создана
**первой**. Шаг не говорит, что связь по `tx_date` надо проводить
первой, — он перечисляет `tx_date` и `settled_date` как «связи 3 и 4» в
описательном порядке.

Учащийся, начавший с `settled_date`, получит активной её. Критерий шага
этого не ловит: `check_pbip.py --step 03` требует ровно одну строку
`isActive: false` и не смотрит, у какой связи она стоит. Не поймают и
числа — раскладка по месяцам просто даст 557105.73 вместо 597013.11 за
август, то есть внешне такой же правильный отчёт. Ошибка обнаруживается
только сверкой конкретного числа с эталоном, а `step-03.md` требует эту
сверку **после** построения связей, когда причина расхождения уже не
видна.

**Четвёртое утверждение из документации — интерфейс о нём молчит.**
Ни при создании связи по `tx_date`, ни при создании связи по
`settled_date` Desktop не показал **ни одного** сообщения, подсказки или
предупреждения про дату, время или типы. Единственный признак типа во
всём диалоге — значок календаря слева от `calendar (date_key)` в списке
связей. Утверждение «`Date` — надстройка над `DateTime`» через интерфейс
связей не проверяется никак; если оно держит требование `type date` в
M-коде, проверять его придётся не здесь.

Файл говорит о том же и точнее: строка `joinOnDateBehavior: datePartOnly`
стоит **только** у автодатной связи. У связей 3 и 4, проведённых руками
между двумя колонками типа «дата», её нет. То есть различение «дата
против даты со временем» — свойство механизма автодаты, а не связей
вообще, и в модели утверждение не подтверждается в том виде, в каком
подано в шаге.

Попутно измерено про формат: **неактивность пишется явно** —
`isActive: false`, а не отсутствием строки. Правило 2.9 («TMDL пишет
только отклонения от умолчаний») этим не нарушено: умолчание — активная
связь, отклонение записано. Критерий `step-03.md` («ровно одна строка
`isActive: false`») построен верно.

**R17 подтверждён с сохранением, а не только в диалоге.** Связь
`transactions[merchant_id]` → `mcc_categories[code]` — пустое пересечение
значений (101…124 против 4121…6012) — сохранилась без единого
предупреждения и легла в список как обычная связь.

Половина, которая была проверена ранее: **до сохранения** диалог о
единственной активной связи не предупреждает вовсе — на паре, где активная связь
между теми же таблицами уже есть, флажок «Активировать связь» стоит,
доступен и никак не помечен.

Вместе эти две половины дают полную картину поведения: **до** сохранения
интерфейс молчит и показывает вторую связь как обычную активную, **в
момент** сохранения гасит её сам, тоже молча. Ни на одном из двух этапов
учащемуся не сказано, что произошло; знать, какая из двух связей осталась
активной, он может только открыв список после сохранения.

**Третья грань, измеренная на задании 5 (R25).** Поведение зависит от
того, как появляется вторая активная связь: при **создании** Desktop
гасит её сам и молча, при **ручном включении** уже существующей —
отказывает с красной ошибкой и неактивной кнопкой «Сохранить». Правило
из документации одно, а исполнений у него два, и второе шагом не
описано.

**R21 — связь 5 создаётся автоопределением, а шаг велит создавать её
руками и ведёт в красную ошибку.**
Статус: открыт
`step-03.md`, 1.3, связь 5: «После этого **Управление связями →
Создать**: `transactions[plan_key]` → `merchant_plan[plan_key]`».
Измерено 2026-08-14: после **Закрыть и применить** связь **уже есть**.
Попытка создать её по инструкции даёт красное сообщение «Между этими
двумя столбцами уже существует связь», список показывает пять строк, и
пятая — `transactions (plan_key)` → `merchant_plan (plan_key)`,
активная. В файле она названа `AutoDetected_21fa637e-2a55-4b6f-a0aa-…`,
то есть создана автоопределением, а не учащимся.

Причина — в самом шаге, в 1.2: «autodetect would have automatically
created the relationship for you when you loaded the data if both columns
had the same name». Составной ключ собирается в обеих таблицах под
**одним и тем же именем** `plan_key` — условие автоопределения выполнено
ровно тем действием, которое шаг предписывает. Шаг цитирует правило и
тут же его нарушает: 1.1 утверждает «три связи из пяти автоопределение не
строит — их проводит учащийся», а построено оказывается две из пяти.

Дефект не косметический, потому что задевает содержание умения. Связь 5
— единственное место, где учащийся собирает ключ и связывает по нему; на
прогоне вся его работа свелась к двум настраиваемым столбцам, а связь
появилась сама. Формулировка «Датасет модуля построен так, что
автоматика справляется ровно с одной связью из пяти» (1.1) на этом
датасете неверна.

**R22 — критерий шага пропускает подмену: проверка «созданных вручную
≥4» даёт `[OK]` там, где вручную создано три.**
Статус: открыт
`check_pbip.py`, `check_step03()`, строки 194–199: вручную считается всё,
что не начинается с `AutoDetected`. Измерено на настоящей модели: шесть
блоков, из них два `AutoDetected*` (связь 1 и связь 5 — R21), значит
«вручную» насчитывается **четыре**, порог пройден. На деле учащийся
провёл руками **три** связи: `mcc` → `code`, `tx_date` → `date_key`,
`settled_date` → `date_key`. Четвёртой в этот счёт попала автодатная
`calendar.date_key` → `LocalDateTable_9357b11a-…` (R12), которую
учащийся не создавал и не видит.

Два дефекта гасят друг друга в счётчике: R21 отнимает одну ручную связь,
R12 добавляет одну невидимую — и проверка, написанная ровно для того,
чтобы поймать «обошёл вместо того, чтобы построить», печатает `[OK]`.
Это хуже, чем `[FAIL]`: `[FAIL]` заставляет разбираться, `[OK]` закрывает
вопрос.

**R23 — задание требует сверить раскладку по месяцу с эталоном, которого
в `reference_answers.md` нет.**
Статус: открыт
`step-03.md`, задание 4: «Проверьте раскладку суммы по трём измерениям:
по категории (6 строк), по тарифу (3 строки), **по месяцу (18 строк)**.
Сравните с эталонными числами из `data/reference_answers.md`». Сверено с
файлом 2026-08-14:

- по категории — есть, `ref_by_category.csv`, шесть строк, сошлось;
- по тарифу — есть, раздел «Числа для критерия `step-03.md`», три
  строки, сошлось;
- **по месяцу — нет**. Восемнадцать строк в `reference_answers.md`
  встречаются один раз, в `ref_month_ytd.csv`, и это визуал 2 **шага
  04**: там `Settled Amount` (только успешные операции) и `Settled YTD`,
  а не `Сумма amount_uah` по всем статусам. Числа другие по построению.

**R24 — числовые колонки измерения приходят в визуал с агрегатом «Сумма»,
и раскладка по месяцу без правки агрегата не строится.**
Статус: открыт
`step-03.md`, задание 4, третья раскладка. Измерено 2026-08-14:
`calendar[year]` и `calendar[month_no]` — колонки типа «целое число», и
Power BI кладёт их в визуал как **`Сумма year`** и **`Сумма month_no`**.
Пока по каждой не выбрано **«Не суммировать»**, восемнадцати строк не
получается. Иерархия дат здесь ни при чём: колонки пришли обычными,
мешал только агрегат по умолчанию (это отличает случай от R12/R13, где
подменялось само поле).

После правки агрегата раскладка сошлась полностью: 18 строк, `2025 / 8`
= **597013,11**, итог **10236902,82**, все 18 чисел совпали с пересчётом
по `tx_date`.

Класс тот же, что у R9 и R13: шаг называет поля, но не называет, что с
ними придётся сделать, чтобы получилось обещанное. Отличие в том, что
здесь неверный исход **не** молчаливый — вместо 18 строк учащийся видит
одну, и это заметно сразу.

Критерий 1.5 это подтверждает от противного: во «Втором» пункте
перечислены категория, тариф и август 2025 — раскладки по месяцу там
нет. То есть задание требует сверки, которой критерий не требует и для
которой нет эталона. Учащийся либо сверит с `ref_month_ytd.csv` и
получит расхождение на ровном месте, либо построит визуал и не сверит
ничего.

**R25 — «временное переключение активной связи» подано как одно
действие, а прямое его исполнение блокируется ошибкой.**
Статус: открыт
`step-03.md`, задание 5: «Второй способ получите временным
переключением активной связи, затем верните как было». Измерено
2026-08-14: флажок «Активировать связь» поставлен на связи по
`settled_date`, связь по `tx_date` не тронута. Результат — красное
сообщение, кнопка **«Сохранить» неактивна**:

> Существуют неоднозначные пути между "transactions" и "calendar":
> 'transactions'->'calendar' и 'transactions'->'calendar'

То есть включить вторую связь, не погасив первую, нельзя. Переключение
— это **два** действия в строгом порядке: сначала снять активность у
`tx_date`, потом поставить у `settled_date`, и столько же обратно.
Порядок в шаге не задан вообще, а слово «переключением» подсказывает
одно действие. Учащийся упирается в ошибку, из которой не следует, что
делать.

Отягчение: правится это через кнопку **«Изменить»** — ту самую, которая
в R19 молча переписывает связь. Единственное место шага, где «Изменить»
нужна по назначению, шаг не называет, а место, где она разрушительна,
не предупреждает.

**R26 — сообщение об ошибке не называет колонки, и по нему нельзя понять,
какие пути неоднозначны.**
Статус: открыт
Текст выше печатает оба пути **одинаковой** строкой —
`'transactions'->'calendar'` и `'transactions'->'calendar'`. Различаются
эти пути только колонками (`tx_date` и `settled_date`), и ровно их в
сообщении нет. Учащемуся, который видит это впервые, сообщение не
сообщает ничего: оно называет проблему, но не даёт признака, по которому
её найти в списке из шести связей.

Это дефект Power BI, а не текста модуля, и починить его нельзя — но шаг
обязан его предвосхитить, потому что задание 5 приводит в это сообщение
любого, кто выполнит его буквально.

**R27 — задание 7 посылает читать файл, содержимое которого шаг описал
неверно.**
Статус: открыт
`step-03.md`, задание 7: «Откройте `relationships.tmdl` и прочитайте его
целиком. **Он короткий.** Запишите в `research/self.md` одной строкой,
какие из заданных вами свойств в нём отсутствуют и почему». Критерий
1.5, третий пункт, ждёт ответа про две группы: кардинальность и
направление фильтрации, обе отсутствуют как умолчания.

Файл на прогоне (2026-08-14, после всех пяти связей) содержит **шесть**
блоков, и три из них учащийся не заводил:

- `AutoDetected_97997382-…` — связь 1, про неё шаг говорит (1.3);
- `AutoDetected_21fa637e-…` — связь 5, которую шаг велел создать руками
  (R21), и учащийся не поймёт, почему у неё имя автоопределения;
- `29036238-…` `calendar.date_key` → `LocalDateTable_9357b11a-…` со
  строкой **`joinOnDateBehavior: datePartOnly`** — таблицы с таким
  именем нет ни в панели «Данные», ни на схеме (R12, пункт 5), а
  свойства `joinOnDateBehavior` нет ни в шаге, ни в решении 22.

То есть задание про «какие свойства отсутствуют» приводит учащегося к
файлу, где половина непонятного — это **присутствующее** лишнее, а не
отсутствующее умолчание. Формулировка «он короткий» задаёт ожидание
пяти одинаковых блоков по три строки; на деле блоков шесть, один из них
ссылается на невидимую таблицу. Разбор этого в шаге не предусмотрен
вовсе.

**R28 — путь «Таблица → Пометить как таблицу дат» ведёт во вкладку,
которой нет, и называет кнопку не так, как она называется.**
Статус: открыт
`step-04.md`, 1.3, единственное действие перед мерами. Измерено
2026-08-14:

| Что сказано в шаге | Что в интерфейсе |
|---|---|
| вкладка «Таблица» | «Средства работы с таблицами» — контекстная, появляется только при выделенной таблице в виде «Данные» |
| — | группа «Календари» |
| кнопка «Пометить как таблицу дат» | «Отметить как таблицу дат» |
| — | подтверждение — кнопка «Сохранить», не «ОК» |

Третий случай одного и того же класса после R2/R10 (назван результат, не
место) и R18 (место названо и названо неверно). Здесь дефект тот же, что
в R18, дословно: вкладка, которой в нужном виде не существует. Два
случая в двух соседних шагах — это уже не описка в одном месте, а
систематическая привычка текста называть вкладку по смыслу действия, а
не по надписи на ленте. Чинится не правкой двух строк, а правилом скилла
о том, как записывается путь в GUI.

Отдельно: диалог печатает зелёное **«Проверка успешно пройдена»** и не
говорит, что проверял. Утверждение 1.2 («Power BI проверяет колонку:
уникальные значения, отсутствие пустых, непрерывный ряд дат») прогоном
**не опровергнуто и не подтверждено** — интерфейс факта проверки не
раскрывает, а источник утверждения — документация.

**R29 — `[FAIL]` критерия `step-03.md` снимается действием, которое
живёт в `step-04.md`.**
Статус: открыт
Пометка `calendar` таблицей дат печатает предупреждение (дословно):

> Учтите, что все встроенные таблицы дат, уже связанные с этой таблицей,
> будут удалены. Визуальные элементы или выражения DAX, ссылающиеся на
> них, могут нарушиться.

Оно исполняется буквально. Состояние на диске до и после (сверено по
файлам проекта, 2026-08-14):

| | до пометки | после |
|---|---|---|
| таблиц в `definition/tables/` | 7 (пять модуля + `DateTableTemplate_*` + `LocalDateTable_9357b11a-…`) | **5**, ровно модульные |
| блоков в `relationships.tmdl` | 6 | **5** |
| автодатная связь `calendar.date_key` → `LocalDateTable_…` | есть, с `joinOnDateBehavior: datePartOnly` | **нет** |

То есть единственный `[FAIL]` шага 03 — «связей в модели: 6, ожидалось
5» (R12) — вылечен первым заданием следующего шага:

```
[OK]   связей в модели: 5
```

Следствие сильнее самого R12: машинный критерий `step-03.md` **не может
быть выполнен в тот момент, когда шаг его требует**, и становится
выполнимым только после действия, которое учащийся сделает шагом позже и
по другому поводу — ради `TOTALYTD`, а не ради связей. Учащийся,
дошедший до конца шага 03 и упёршийся в `[FAIL]`, не имеет в тексте
шага ни одного указания, что делать: причина не названа, а лекарство
лежит в ненаписанном для него будущем.

**R30 — `check_pbip.py --step 03` непроходим в обоих состояниях модели, и
ложный `[OK]` R22 держался на автодатной связи.**
Статус: открыт
Тот же прогон, сразу после пометки (2026-08-14), дословно:

```
Проект: D:\data-analyst-program\program\M4\pbi  шаг: 03

[OK]   связей в модели: 5
[FAIL] связей, созданных вручную: 3, ожидалось не менее 4 — три случая ограничения C1 автоопределением не строятся
[OK]   ровно одна связь неактивна (вторая колонка-кандидат по дате)
[OK]   отклонений от умолчаний нет: кардинальность *:1, фильтрация односторонняя, связи активны (кроме одной явно неактивной)
[OK]   составной ключ: связь использует transactions.plan_key
[OK]   составной ключ: связь использует merchant_plan.plan_key

НЕ СОШЛОСЬ: 1 проверок. Шаг не закрыт.
код возврата: 1
```

`[FAIL]` переехал с первой проверки на вторую. Это и есть подтверждение
R22 измерением: пока автодатная связь была в файле, она подставлялась в
счётчик «созданных вручную» и добивала три до четырёх. Убрали её —
проверка показала истинное число, и оно `3`, потому что связь 5
построило автоопределение (R21).

Итог по шагу 03 в этом окружении:

| Состояние модели | Что печатает `--step 03` |
|---|---|
| автодата на месте (конец шага 03) | `[FAIL]` связей 6 при 5 (R12) |
| автодата снята (первое задание шага 04) | `[FAIL]` вручную 3 при ≥4 (R21) |

Прохода нет ни в одном. Критерий, который решение 26 назначало главной
машинной проверкой умения C1 по связям, на настройках Power BI Desktop по
умолчанию не выполняется никогда — ни до, ни после. Чинить придётся не
одну из двух проверок, а обе, вместе с R12 и R21.

**R31 — вкладка с полем «Формат» названа в шаге не так, как в
интерфейсе.**
Статус: открыт
`step-04.md`, 1.3: «вкладка "Инструменты для мер", поле "Формат"».
Измерено 2026-08-14: вкладка называется **«Средства работы с мерами»**,
поле «Формат» на ней есть и названо верно. Третий случай подряд после
R18 и R28; здесь дефект мельче — вкладка существует и находится, ошибочна
только надпись. Записан отдельным номером не ради веса, а ради счёта:
три промаха по надписям на ленте в двух шагах — это метрика для правила
скилла, а не три независимых описки.

Всё остальное в задании 2 прошло без единого расхождения, и это стоит
записать отдельно, потому что подозрений было два и оба сняты:
**разделитель аргументов — запятая**, точки с запятой русская локаль не
потребовала; `RELATED` и `TOTALYTD` введены без ошибок и без красной
подсветки; кнопка «Создать меру» на вкладке «Главная» — там, где её
называет шаг. Шесть мер легли в `transactions.tmdl` с формат-строками
`0.00`, `0`, `0.0000` ровно по таблице форматов из 1.3.

**R32 — машинный критерий `step-04.md` печатает восемь `[OK]` и код 0 на
модели, где не построен ни один визуал этого шага.**
Статус: открыт
Измерено 2026-08-14, сразу после ввода шести мер и **до** заданий 4–6
(визуалы шага 04 не строились вовсе, на странице лежат девять визуалов
шага 03):

```
Проект: D:\data-analyst-program\program\M4\pbi  шаг: 04

[OK]   мера Total Amount: выражение содержит SUM(
[OK]   мера Settled Amount: выражение содержит CALCULATE(
[OK]   мера Tx Count: выражение содержит COUNTROWS(
[OK]   мера Decline Rate: выражение содержит DIVIDE(
[OK]   мера Commission: выражение содержит SUMX(, RELATED(
[OK]   мера Settled YTD: выражение содержит TOTALYTD(
[OK]   визуалов на диске: 9
[OK]   визуалов с объявленной сортировкой: 8

ВСЁ СОШЛОСЬ: расхождений 0.
код возврата: 0
```

Критерий 1.5, пункт «Четвёртое», требует дословно «восемь строк `[OK]`,
ни одной `[FAIL]`, код возврата 0» — и он **уже выполнен**, хотя из
восьми заданий шага сделаны два. Причина в самих проверках:
`check_step04()` считает визуалы на диске (`>= 3`) и визуалы со строкой
`sortDefinition` (`>= 3`), не глядя, какие это визуалы и какие в них
поля. Девять визуалов шага 03 проходят оба порога с запасом, а запрет на
круговые/кольцевые/ленточные не срабатывает, потому что таких там нет.

Класс тот же, что у R22, и следствие то же: `[OK]` закрывает вопрос.
Учащийся, который написал шесть мер и остановился, получает от
единственной машинной проверки шага полное подтверждение, что шаг сдан.
Отличие от R22 в том, что там `[OK]` был ложным из-за стечения двух
других дефектов, а здесь проверка ровно так и написана: она проверяет
наличие визуалов, а критерий выдаёт её за проверку **тех самых трёх**
визуалов. Пороги `>= 3` на странице, где визуалы копятся от шага к шагу,
не различают «построил» и «было раньше».

**R33 — второй пункт критерия `step-04.md` не проверяется на карточках:
карточка округляет до трёх значащих и переопределяет формат меры.**
Статус: открыт
Критерий 1.5, «Второе»: «шесть значений мер на карточках без разрезов
совпадают с таблицей из 1.3 **до второго знака** (`Decline Rate` — до
четвёртого). Расхождение 0». Измерено 2026-08-14 — шесть карточек
построены по заданию 3, фильтров нет, карточки растянуты:

| Мера | Формат-строка в модели | Карточка печатает | Требует критерий |
|---|---|---|---|
| `Total Amount` | `0.00` | `10,24 млн` | 10236902.82 |
| `Settled Amount` | `0.00` | `8,70 млн` | 8703122.77 |
| `Tx Count` | `0` | **`7 тыс.`** | 6582 |
| `Settled YTD` | `0.00` | `2,80 млн` | 2798508.43 |

Ни одно из четырёх значений не сверяется с эталоном «до второго знака»:
на карточке этих знаков нет. `Tx Count` — самый наглядный случай: в
модели у меры стоит формат-строка `0`, целое число без единиц, а карточка
печатает `7 тыс.` вместо `6582`, то есть промахивается на 418 единиц и
выглядит при этом аккуратно.

Отсюда два следствия, и второе тяжелее первого:

1. Это R14 из `step-02.md`, повторившийся в `step-04.md`. Там он ломал
   один пункт критерия (сумма `amount_uah`), здесь — весь пункт целиком,
   все шесть значений сразу. Дефект не в шаге 02 и не в шаге 04, а в
   привычке обоих шагов класть на карточку число, которое потом надо
   сверять с эталоном.
2. **Утверждение 1.3 «Форматы задаются на мере, а не на визуале» на
   карточке не выполняется.** Формат-строка в модели стоит и записана в
   `transactions.tmdl` дословно по таблице форматов; карточка её
   переопределяет своими единицами измерения. Шаг подаёт формат меры как
   способ управлять тем, что окажется в выгрузке (1.2, и это про экспорт
   верно), но учащийся прочитает это как «формат задан один раз и виден
   везде» — и на первом же визуале получит обратное.

**Обход, которым пункт критерия закрыт по существу.** Те же шесть мер,
положенные в визуал **«Таблица»** без разрезов, печатаются полностью, и
все шесть сошлись с таблицей 1.3 **до последнего знака**:

```
Total Amount     10236902.82
Settled Amount    8703122.77
Tx Count               6582
Decline Rate         0.1077
Commission         169676.11
Settled YTD        2798508.43
```

Обход тот же по классу, что найденный в R18 (строка состояния вместо
карточки): нужное число берётся визуалом, который не сокращает. В шаге
он не назван — задание 3 говорит «на карточках» прямо.

**R34 — шаг не говорит, куда сохранять экспорт, а диалог предлагает папку
датасета модуля.**
Статус: открыт
`step-04.md`, 1.3 («Сохраните три файла и сверьте») и задания 5–6. Место
сохранения не названо нигде, а пример команды сверки написан так, будто
файл лежит в корне репозитория:
`python program\M3\data\compare_csv.py export_by_category.csv …`.
Измерено 2026-08-14: диалог сохранения открывается в папке, из которой
шаг 02 импортировал данные, и три файла легли в
**`program\M4\data\csv\`** — вместе с пятью исходными CSV модуля:

```
calendar.csv                18523  31.07.2026
export_by_category.csv        489  14.08.2026
export_month_ytd.csv          638  14.08.2026
export_plan_commission.csv     84  14.08.2026
mcc_categories.csv            238  31.07.2026
merchant_plan.csv           11593  31.07.2026
merchants.csv                2575  31.07.2026
transactions.csv           412473  31.07.2026
```

Исходные пять не пострадали — имена не совпали. Но это везение, а не
защита: в той же папке лежат файлы, которые генерирует
`generate_m4.py`, и учащемуся предложено сохранять туда результаты
работы.

**Репозиторий этого не видит — проверено, а не предположено.**
`tools/check_consistency.py` проверяет `program/M*/data/` на файлы, не
покрытые README и `.gitignore`; прогнан с пятью экспортами в папке
(2026-08-14) и напечатал:

```
[OK]   program/M*/data/: проверено 46 файлов в 5 папках, ни одного не покрытого README/.gitignore не найдено
```

Число файлов то же, что до прогона. Причина — `program/M4/data/csv/`
целиком в корневом `.gitignore` (решение 22, «Что коммитится»: в
репозитории генератор и контрольная точка, а не данные). То есть
единственная механическая защита репозитория к этой папке слепа по
устройству, и слепа правильно — данные там и не должны отслеживаться.

Следствие ровно обратное тому, которое напрашивалось: экспорт не
засоряет репозиторий, он **оседает рядом с датасетом невидимо**. Ни
`git status`, ни `check_consistency.py` о нём не скажут, а
`generate_m4.py` при пересборке перезапишет пять своих файлов и чужие
оставит. Через несколько прогонов в папке датасета лежат файлы
неизвестного происхождения, и отличить их от датасета можно только
по README.

Класс тот же, что у R4 (шаг не сказал, где живёт артефакт, и артефакт лёг
в репозиторий): дефект не в интерфейсе, а в том, что шаг молчит о месте.

**R35 — два экспорта одного визуала разошлись целиком, потому что между
ними визуал был перекрёстно отфильтрован щелчком по другому визуалу; в
файле следа фильтра нет.**
Статус: открыт
Задание 6: «Экспортируйте визуал 1 второй раз, в другой файл, и сверьте
два экспорта между собой тем же скриптом». Критерий 1.5, «Третье»:
«Гейт измерил, что два экспорта подряд побайтово идентичны (совпал
SHA-256)». Измерено 2026-08-14 — два экспорта визуала 1, между ними
работа с визуалом 3 и ни одной правки визуала 1:

```
export_by_category.csv     489 байт
export_by_category_2.csv   465 байт

$ python program\M3\data\compare_csv.py export_by_category_2.csv export_by_category.csv
РАСХОЖДЕНИЕ в строке 1, колонка 'Total Amount': у вас '236498,44', в эталоне '3955730,62'
код возврата: 1
```

Второй файл — не сбой и не обрезок. Это полноценная таблица с теми же
шестью категориями, теми же пятью колонками и правдоподобными числами:

```
category_name,Total Amount,Settled Amount,Tx Count,Decline Rate
Фінансові послуги,"236498,44","211017,45",46,"0,1087"
Продуктові магазини,"147205,44","122637,74",115,"0,1043"
...
```

Что это за срез, установлено арифметикой по самому файлу: сумма колонки
`Settled Amount` равна `211017,45 + 122637,74 + 92184,58 + 70985,23 +
30531,27 + 8057,02 = 535413,29`, а это дословно строка `2025,9`
эталона `ref_month_ytd.csv`. Визуал 1 был отфильтрован **сентябрём
2025** — перекрёстной фильтрацией от щелчка по строке визуала 2.

Почему это дефект шага, а не оплошность:

1. **Файл не сообщает, что он отфильтрован.** Шаг сам записал в 1.2:
   «строки "Applied filters" нет, первая строка — заголовок». В Части 2
   это записано как удобство формата; здесь то же свойство означает, что
   отфильтрованный экспорт неотличим от полного **по содержимому файла**.
   Признак один — числа, а сверять числа учащийся как раз и собирался.
2. **Критерий 1.5 обещает обратное тому, что происходит.** «Два экспорта
   подряд побайтово идентичны» верно только при неизменном состоянии
   отчёта, а состояние меняется щелчком по любой строке любой таблицы на
   странице — то есть ровно тем действием, которым учащийся ходит по
   собранным им же визуалам. Слова «подряд» и «ничего не трогая» в
   задании 6 нет.
3. **Страница к этому подводит.** Три визуала шага 04 лежат на той же
   странице, что девять визуалов шага 03, и все они кросс-фильтруют друг
   друга по умолчанию. Ни `step-03.md`, ни `step-04.md` не говорят ни про
   перекрёстную фильтрацию, ни про то, что визуалы стоит развести по
   страницам.

Класс — молчаливый неверный исход, тот же, что R13/R15/R17 (3.7), и
самый дорогой его случай за весь прогон: неверен здесь не элемент
интерфейса, а **артефакт сдачи шага**. Если бы отфильтрованным оказался
первый экспорт, а не второй, учащийся получил бы `РАСХОЖДЕНИЕ` против
эталона и пошёл искать ошибку в мерах и связях, которых там нет.

**R36 — задание 8 спрашивает про одну подстановку, а критерий требует
число, измеренное для другой.**
Статус: открыт
`step-04.md`, задание 8: «почему `Commission` нельзя заменить на
`[Settled Amount] * <средняя ставка>`. Число из `step-03.md` в ответе
обязательно». Критерий 1.5, «Пятое»: «письменный ответ содержит число
**516.96** или **0.30%**».

Число в `step-03.md` действительно есть — строки 245–246 и
`reference_answers.md`, строки 115–116. Но измерено оно для другого
счёта: «комиссия, если взять **последний тариф мерчанта на всю
историю**» = 169159.15. Подстановка средней ставки, про которую
спрашивает задание, — это другая арифметика. Посчитана на тех же CSV
`program/M4/data/csv/` (2026-08-14):

| Подстановка | Результат | Расхождение с 169676.11 |
|---|---|---|
| последний тариф мерчанта на всю историю (то, что в `step-03.md`) | 169159.15 | **516.96**, 0.30% |
| `[Settled Amount] * 0.019` (ставка, названная в 1.6 «средней») | 165359.33 | 4316.78, 2.54% |
| `[Settled Amount] *` эффективная средняя ставка (169676.11 / 8703122.77 = **1.9496%**) | 169676.11 | **0.00**, 0.000% |

Три следствия, по нарастанию тяжести:

1. Учащийся, который честно посчитает то, о чём его спросили, получит
   4316.78 или 0.00 — и **ни одно из этих чисел критерий не примет**.
   Принимается только 516.96, то есть ответ на незаданный вопрос.
2. Блок «Ошибка» в 1.6 неверен внутри себя: он называет 0.019 «средней
   ставкой» (эффективная — 1.9496%) и тут же подтверждает её числом
   516.96, которое к ней не относится.
3. **Правильный ответ на заданный вопрос критерию противоречит.** При
   эффективной средней ставке итог сходится **точно, до копейки** — иначе
   и быть не может, эта ставка из него и получена. Ломается не итог, а
   раскладка, и ломается крупно (посчитано там же):

   | Тариф | `Commission` правильно | При плоской средней ставке | Расхождение |
   |---|---|---|---|
   | `basic` | 74344.12 | 57976.53 | **−16367.59** (−22%) |
   | `standard` | 57459.71 | 58959.72 | +1500.01 |
   | `premium` | 37872.28 | 52739.86 | **+14867.58** (+39%) |

   Это и есть содержательный ответ: замена не видна в итоге вообще и
   даёт до 39% ошибки в разрезе — том самом, который строит визуал 3.
   Учащийся, который до этого дойдёт, будет по критерию **не прав**,
   потому что не назовёт 516.96.

Класс новый для прогона и хуже R23 (там критерий требовал сверки с
несуществующим эталоном): здесь эталон существует, но отвечает на другой
вопрос, и критерий наказывает за верный ответ. Чинится либо
переформулировкой задания под 516.96 («последний тариф вместо
помесячного»), либо новым измерением под среднюю ставку — но не правкой одной
цифры.

### 3.4. Что прогон подтвердил

**Датасет.** Контрольная точка сошлась целиком — раздел 3.2.

**`check_pbip.py` работает на настоящем проекте, а не только на макете.**
Оговорка в докстринге скрипта («прогонялся на макете структуры проекта…
если маска не сойдётся с реальным именем папки, правится маска») снята:
маски `*.SemanticModel/definition`, `*.Report` и
`definition/pages/*/visuals/*/visual.json` сошлись с тем, что Power BI
Desktop записал на диск. На корректно сохранённом `.pbip` шаг 01 дал
шесть `[OK]` из семи; единственный `[FAIL]` — `.gitignore`, то есть
дефект R4, а не дефект скрипта.

**Дискриминатор PBIR сформулирован верно.** `report.json` в проекте
есть, но лежит в `<имя>.Report/definition/report.json`; `step-01.md`
требует его отсутствия именно в **корне** `*.Report`, и это условие
различает PBIR и PBIR-Legacy правильно.

**P7 подтверждён на настоящих данных и закрыт явным M-кодом.**
`amount_uah` при культуре `"ru"` уходит в текст (R6 — про механизм).
После правки культуры на `"en-US"` колонка в модели числовая: в панели
«Данные» стоит знак `Σ`.

**P2 подтверждён на настоящих данных.** `period_ym` без явного типа
становится датой и показывает `01.01.2025` вместо `2025-01`. Обход,
который решение 22 считало достаточным в первой редакции, здесь не
работает — это уже было записано Частью 2, прогон подтвердил на датасете
модуля.

**Автоопределение связи 1 работает.** В `relationships.tmdl` лежит
`AutoDetected_97997382-8214-4816-8bab-4fa5b610b188`,
`transactions.merchant_id` → `merchants.merchant_id`. Это подтверждает
`step-03.md`, 1.3 («Связь 1 — уже есть») и строку 1 таблицы связей в
`reference_answers.md`.

Отдельно, потому что по ходу прогона была высказана обратная гипотеза:
команда **«Обнаружение связей»** ответила «Связи не обнаружены», и это
было прочитано как «автоматика на датасете M4 бездействует». Файл
проекта показал обратное — обнаруживать было нечего, потому что связь
уже стояла с загрузки. Гипотеза снята измерением; в текст модуля она не
попала.

**`QuoteStyle` по умолчанию разобрал многострочные адреса верно** —
`merchants` дал 24 строки. Это подтверждает эталон
`reference_answers.md` (24, а не 26) и одновременно означает, что
режима отказа с 26 строками на прогоне не было (R5).

**Пять таблиц загружены, проект сохранён как `.pbip`.**

**Добавлено заходом по `step-04.md` (2026-08-14).** Здесь подтверждений
больше, чем в любом другом шаге прогона, и это стоит записать так же
подробно, как дефекты.

**`TOTALYTD` под культурой `"ru"` работает — пункт 5 решения 26 закрыт.**
Последнее из четырёх утверждений, взятых в M4 из документации и не
проверенных прогоном (3.6), проверено. После пометки `calendar` таблицей
дат мера `TOTALYTD([Settled Amount], 'calendar'[date_key])` дала все
восемнадцать строк эталона, включая сброс накопления в январе 2026:

```
year,month_no,Settled Amount,Settled YTD
2025,1,"495704,58","495704,58"
...
2026,1,"468889,81","468889,81"
2026,6,"495266,84","2798508,43"
```

Ни русская культура модели, ни русский интерфейс на классическую
time-intelligence не повлияли.

**Разделитель аргументов DAX — запятая.** Подозрение было прямым: M2 и
M4 уже дали дюжину дефектов на локализации (R6, P1, P7), и точка с
запятой в формулах была бы четырнадцатым. Все шесть мер введены с
запятой, без ошибок и без красной подсветки, включая `RELATED` и
`TOTALYTD`. Excel-подобной подмены разделителя по локали в DAX нет.

**Экспорт: пункт 4 решения 26 закрыт, три сверки из трёх.**
`compare_csv.py` прогнан на настоящих файлах, выгруженных Power BI
Desktop, а не на имитации:

```
export_by_category.csv      СОВПАДАЕТ: 6 строк, 5 колонок, расхождение 0.   код 0
export_month_ytd.csv        СОВПАДАЕТ: 18 строк, 4 колонок, расхождение 0.  код 0
export_plan_commission.csv  СОВПАДАЕТ: 3 строк, 2 колонок, расхождение 0.   код 0
```

Это второе независимое авторство формулы, ради которого писался
`reference_m4.py`: числа Power BI и числа Python сошлись до копейки на
всех трёх разрезах, включая комиссию по составному ключу
(`SUMX` + `RELATED`, 169676.11) и раскладку, где порядок по комиссии
обратен порядку по обороту.

**Свойства формата экспорта из 1.2 подтверждены на реальных файлах.**
Первые три байта `export_month_ytd.csv` — `EF BB BF`, то есть UTF-8 с
BOM; десятичный разделитель и разделитель полей оба запятая, из-за чего
числа в кавычках (`"495704,58"`); строки итогов в файле нет; строки
«Applied filters» нет, первая строка — заголовок. Всё, что решение 22
записало слоем 1, выдержало проверку настоящей выгрузкой.

**Два экспорта подряд при неизменном состоянии отчёта побайтово
идентичны.** Измерение Части 2 воспроизведено на настоящем визуале:
`export_by_category.csv` и `export_by_category_3.csv` — по 489 байт,
SHA-256 совпал (`0ED437AD…3057`). Ценность этого факта выросла после
R35: именно он и позволяет утверждать, что разошедшийся второй экспорт
разошёлся не от нестабильности выгрузки, а от изменённого состояния
страницы.

**«Не суммировать» снимает R24 полностью.** После выбора «Не
суммировать» для `calendar[year]` и `calendar[month_no]` в заголовке
выгрузки стоит `year,month_no,…`, а не `Сумма year` — то есть типичная
ошибка, описанная в 1.6, и лечится ровно тем, чем шаг велит её лечить.

**Шесть значений мер сошлись до последнего знака** — таблица в R33.
Содержание шага 04 прогон подтвердил целиком; всё найденное — про
инструкции, критерии и одно число в задании 8.

### 3.5. Итог по шагам

**`step-02.md` пройден целиком (заход 2026-08-13).** Задание 7 закрыто
обходом R9 (см. там же), все шесть чисел получены, критерий 1.5 сверен:

| Пункт критерия 1.5 | Состояние |
|---|---|
| пять чисел строк = 6582 / 24 / 6 / 432 / 546 | сошлись, но две карточки читаются с автоформатом (R14) |
| сумма `amount_uah` = 10236902.82, расхождение 0 | **глазами не проверяется** — карточка печатает `10,24 млн` (R14) |
| `check_pbip.py --step 02` — десять `[OK]`, ноль `[FAIL]`, код 0 | сошлось дословно |

Вывод скрипта на настоящем проекте:

```
Проект: D:\data-analyst-program\program\M4\pbi  шаг: 02

[OK]   пять таблиц модуля на месте: calendar, mcc_categories, merchant_plan, merchants, transactions
[OK]   calendar.tmdl: кодировка, QuoteStyle и культура заданы явно
[OK]   mcc_categories.tmdl: кодировка, QuoteStyle и культура заданы явно
[OK]   merchant_plan.tmdl: кодировка, QuoteStyle и культура заданы явно
[OK]   merchant_plan.commission_pct: тип double
[OK]   merchant_plan.period_ym: тип string
[OK]   merchants.tmdl: кодировка, QuoteStyle и культура заданы явно
[OK]   transactions.tmdl: кодировка, QuoteStyle и культура заданы явно
[OK]   transactions.amount_uah: тип double
[OK]   transactions.period_ym: тип string

ВСЁ СОШЛОСЬ: расхождений 0.
код возврата: 0
```

Оба утверждения о связях, которые решение 26 велит проверить до
`step-03.md`, **подтверждены** — раздел 3.3, таблица после R19. Второе
закрыто уже внутри шага 03, на связях к `mcc_categories`.

**`step-03.md` пройден (заход 2026-08-14).** Пять связей построены, три
раскладки сверены, оба числа за август 2025 получены. Что дал критерий:

| Пункт критерия 1.5 | Состояние |
|---|---|
| `check_pbip.py --step 03` — шесть `[OK]`, код 0 | **не сошлось**: один `[FAIL]` (связей 6 вместо 5 — R12), код 1; один `[OK]` ложный (R22) |
| по категории — 6 строк, суммы различаются | сошлось до копейки с `ref_by_category.csv` |
| по тарифу — 3602951.97 / 3503332.16 / 3130618.69, итог 10236902.82 | сошлось |
| август 2025: 597013.11 по `tx_date`, 557105.73 по `settled_date` | оба сошлись; второе — только в два действия (R25) |
| по месяцу — 18 строк | 18 по `tx_date`; по `settled_date` 19, девятнадцатая пустая (D1) |
| записать в `research/self.md`, каких свойств нет | см. 3.6 |

Вывод скрипта на настоящем проекте:

```
Проект: D:\data-analyst-program\program\M4\pbi  шаг: 03

[FAIL] связей в модели: 6, ожидалось 5
[OK]   связей, созданных вручную: 4 (автоопределением: 2)
[OK]   ровно одна связь неактивна (вторая колонка-кандидат по дате)
[OK]   отклонений от умолчаний нет: кардинальность *:1, фильтрация односторонняя, связи активны (кроме одной явно неактивной)
[OK]   составной ключ: связь использует transactions.plan_key
[OK]   составной ключ: связь использует merchant_plan.plan_key

НЕ СОШЛОСЬ: 1 проверок. Шаг не закрыт.
код возврата: 1
```

**`step-04.md` пройден (заход 2026-08-14, второй за день).** Шесть мер
написаны, три визуала собраны, пять выгрузок сделаны. Что дал критерий:

| Пункт критерия 1.5 | Состояние |
|---|---|
| три сверки экспорта с эталонами, три из трёх | **сошлось**, расхождение 0 во всех трёх (3.4) |
| шесть значений мер на карточках, до 2-го знака (`Decline Rate` — 4-го) | **на карточках не проверяется вовсе** (R33); через визуал «Таблица» сошлись до последнего знака |
| два экспорта одного визуала совпадают между собой | со второй попытки: первый прогон разошёлся целиком (R35), после снятия перекрёстного фильтра SHA-256 совпал |
| `check_pbip.py --step 04` — восемь `[OK]`, код 0 | сошлось; **и сходилось до постройки визуалов** (R32) |
| письменный ответ содержит 516.96 или 0.30% | **не проверялся**: критерий требует число от другой подстановки (R36) |

Итоговое состояние модели: **пять** таблиц (автодатные снялись при
пометке таблицы дат, R29); в `relationships.tmdl` **пять** связей,
активная по `tx_date`, `isActive: false` у `settled_date`; `calendar`
помечен таблицей дат (`dataCategory: Time`); шесть мер в
`transactions.tmdl` с формат-строками `0.00`/`0`/`0.0000`; на странице
девятнадцать визуалов (девять со шага 03, шесть карточек, три таблицы
шага 04 и одна таблица-обход из R33); проект сохранён как `.pbip`.

Прогон Части 3 на этом закончен: все четыре написанных шага M4 пройдены
руками.

### 3.6. Что осталось незакрытым

**По `step-02.md`:**

- утверждение «`QuoteStyle.None` рвёт значение по переводу строки»
  осталось непроверенным (R5) — режима отказа на прогоне не было.

**По `step-03.md`:**

- третий пункт критерия (записать в `research/self.md`, каких свойств в
  `relationships.tmdl` нет и почему) на прогоне **не проверялся**: это
  запись учащегося в собственный дневник, а прогон делает не учащийся.
  Дефектов в самом пункте не найдено, кроме R27 — того, что читать
  придётся файл, описанный шагом неверно.

**По `step-04.md`:**

- задание 8 (письменный ответ про `Commission`) на прогоне **не
  выполнялось** — это ответ учащегося, а прогон делает не учащийся. Сам
  вопрос и его критерий разобраны по файлам и оказались дефектными
  (R36);
- утверждение 1.2 «скрытые на визуале поля в экспорт не попадают»
  осталось непроверенным: скрытых полей на трёх визуалах не было.

**Систематический пробел гейта — задания, чей артефакт есть письменный
ответ учащегося.** Их два, и оба пропущены по одному и тому же
основанию: `step-03.md`, задание 7 (запись в `research/self.md`, каких
свойств нет в `relationships.tmdl`) и `step-04.md`, задание 8 (почему
`Commission` нельзя заменить средней ставкой). Решение принято дважды —
заходом по шагу 03 и заходом по шагу 04, оба раза вслух: прогон
проверяет инструкции и критерии, а учится по программе никто.

Цена пробела измерима на нём же. Дефекты в обоих пропущенных заданиях
всё-таки нашлись — **R27** и **R36**, — но найдены чтением файлов и
пересчётом на CSV, а не выполнением. R36 (критерий требует число от
другой подстановки и наказывает за верный ответ) вскрылся только потому,
что подстановку посчитали отдельно; выполнением задания он вскрылся бы
сразу. Сколько ещё таких сидит в заданиях, которые прогон не делал,
гейт не знает и знать не может.

Отдельным следствием: `research/self.md` не менялся с 2026-07-29, и
долг, записанный в `HANDOFF.md`, §6 (14 из 24 часовых оценок
`design/blueprint.md`, §6.1 стоят с пометкой «требует калибровки», а
калибровать нечем), прогоном M4 не уменьшился ни на строку.

**По решению 26 — пройдены все пять пунктов:**

| Пункт решения 26 | Состояние |
|---|---|
| 1. Клики по связям: все пять, включая выбор активной | **пройден** — с четырьмя дефектами: R19, R21, R25, R26 |
| 2. `check_pbip.py --step 03` и `--step 04` на настоящем `.pbip` | оба прогнаны. `--step 03` **не сходится ни в одном состоянии модели** (R12, R21, R30); `--step 04` печатает код 0, но проходит и на невыполненном шаге (R32) |
| 3. Числа 597013.11 / 557105.73 / 10236902.82 | **пройден** — все три сошлись, плюс раскладки по категории и тарифу |
| 4. Сверка экспорта через `compare_csv.py` на реальном файле | **пройден** — три сверки из трёх, расхождение 0 (3.4) |
| 5. `TOTALYTD` на культуре `"ru"` | **пройден** — восемнадцать строк, сброс накопления в январе 2026 (3.4) |

Отдельно: пункт 2 решения 26 ждал «в том числе счётчик `AutoDetected_*`
(ожидается ровно 1)». Их **два** — R21. Ожидание записано неверно, и
проверка, которая должна была это поймать, дала `[OK]` (R22).

**Четыре утверждения, взятые в M4 из документации:**

1. «связь соединяет одну колонку с одной» — **подтверждено** прогоном
   (R15, раздел 3.4);
2. «активной может быть одна связь» — **подтверждено** прогоном
   (раздел 3.3, блок после R19): вторая связь между теми же таблицами
   сохраняется и гасится Desktop'ом самостоятельно, без сообщения;
3. поведение `TOTALYTD` — **подтверждено** прогоном шага 04 (3.4):
   восемнадцать строк, накопление сбрасывается в январе, культура `"ru"`
   не мешает;
4. предупреждение про `Date` как надстройку над `DateTime` —
   **не подтверждается интерфейсом**: при создании обеих связей по
   датам Desktop не показал ни одного сообщения о типах, а
   `joinOnDateBehavior: datePartOnly` стоит только у автодатной связи
   (раздел 3.3). Если это утверждение держит требование `type date` в
   M-коде, проверять его придётся не через связи.

**Уборка после прогона не сделана.** На диске остались:
`program\M4\pbi\m4.pbix` (18 080 байт, след R3),
`program\M4\pbi-probe` и `D:\pbi-probe2` (пробы R4), пустая папка
`D:\m4-run` (заводилась под выгрузки шага 04, диалог экспорта в неё не
пошёл — R34) и пять файлов выгрузки в `program\M4\data\csv\`:
`export_by_category.csv`, `export_by_category_2.csv` (отфильтрованный,
R35), `export_by_category_3.csv`, `export_month_ytd.csv`,
`export_plan_commission.csv`. Последние пять — доказательная база R34 и
R35, и удалять их до починки нельзя.

### 3.7. Вердикт Части 3

Часть 3 **закрыта**. Решение 26 требовало прогнать написанные шаги M4
руками прежде, чем писать умение C2; все четыре пройдены, все пять
пунктов решения 26 отвечены (3.6). Запрет на C2 снят, запрет на починку
— тоже: правки по итогам гейта теперь ложатся на проверенный текст.

Установлено: **тридцать шесть** дефектов инструкций (R1–R36) и **один
дефект датасета** (D1, 3.8). Тяжёлые:

- **R9** — задание 7 `step-02.md` опирается на несуществующий пункт
  интерфейса;
- **R12** — автодата ломает машинный критерий `step-03.md`: скрипт
  видит шесть связей при пяти построенных, и это единственный `[FAIL]`
  шага;
- **R14** — пункт 2 критерия `step-02.md` через карточку не
  проверяется вовсе;
- **R19** — промах по кнопке в диалоге связей молча уничтожает уже
  сделанную связь;
- **R21** — связь 5, ради которой написана половина шага 03, строится
  автоопределением, а инструкция ведёт в красную ошибку;
- **R22** — проверка «созданных вручную ≥4» даёт ложный `[OK]`;
- **D1** — календарь короче `settled_date`, 0,46% оборота уходит в
  пустую строку на том самом разрезе, который требует задание 5;
- **R30** — `check_pbip.py --step 03` непроходим **в обоих** состояниях
  модели: с автодатой валится по числу связей, без неё — по числу
  созданных вручную;
- **R32** — критерий `step-04.md` печатает код 0 на шаге, из восьми
  заданий которого сделаны два;
- **R35** — два экспорта одного визуала разошлись целиком из-за
  перекрёстной фильтрации, и в файле нет ни следа фильтра: неверным
  оказывается сам артефакт сдачи шага;
- **R36** — критерий задания 8 требует число, измеренное для другой
  подстановки, и наказывает за верный ответ.

Плотность за четыре шага: 36 дефектов инструкций на 4 из 4 шагов, из
них **десять (R18–R27) на шаге 03** и **девять (R28–R36) на шаге 04**,
плюс дефект датасета D1. Ни один шаг модуля не прошёл прогон чисто.

**Что изменилось в понимании к концу прогона.** Первые два шага дали
дефекты навигации: шаг не сказал, где кнопка. Шаги 03 и 04 дали два
класса потяжелее, и оба не находятся чтением:

1. **Молчаливые неверные исходы** (R13, R15, R17, R33, R35) — интерфейс
   не отказывает, а выдаёт правдоподобный результат: поле садится не в
   тот колодец, карточка печатает `7 тыс.` вместо `6582`,
   отфильтрованная выгрузка выглядит как полная.
2. **Критерии, которые не проверяют того, что обещают** (R22, R30, R32,
   R33, R36) — пять штук на два шага. Проверка либо даёт `[OK]` там, где
   работа не сделана, либо непроходима вовсе, либо требует чужого числа.
   Это дороже дефектов навигации: критерий — единственное, чем учащийся
   без ментора отличает сделанное от несделанного, и здесь он врёт в обе
   стороны.

Первый класс чинится добавлением проверки после действия, второй —
переписыванием самих критериев и `check_pbip.py`. Ни то, ни другое не
сводится к правке формулировок.

Отдельно стоит записать, что **прогон подтвердил и содержание тоже**:
все числа шагов 02, 03 и 04 сошлись с эталонами до копейки — шесть
чисел строк, три раскладки, оба числа за август 2025, шесть значений
мер и все три экспорта против `reference_m4.py`. Тридцать шесть дефектов
— про инструкции и критерии, а не про то, что модуль учит неверному.
Это же и оправдывает цену гейта: содержание, которое он подтвердил,
чтением не подтверждалось никак.

Отдельно от дефектов инструкций прогон нашёл **дефект датасета** — 3.8.
Он чинится не текстом шага, а `generate_m4.py`, и тянет за собой
пересборку эталонов.

### 3.8. Дефект датасета D1 — календарь короче, чем `settled_date`

**D1 — календарь короче, чем `settled_date`.**
Статус: закрыт 2026-08-14, 17d7bc6, program/M4/data/generate_m4.py CALENDAR_TAIL_DAYS

Нумерация отдельная (`D`), потому что чинится не текст шага, а
`program/M4/data/generate_m4.py`, и починка тянет пересборку эталонов.

Найден 2026-08-14 на задании 5 `step-03.md`, при переключении активной
связи на `settled_date`. В таблице появилась **верхняя строка с пустыми
`year` и `month_no` на сумму 47482,23**. Причина проверена по файлам
датасета скриптом:

```
calendar:     2025-01-01 .. 2026-06-30, 546 дней
tx_date:      2025-01-01 .. 2026-06-30
settled_date: 2025-01-02 .. 2026-07-03
операций с settled_date вне календаря: 30
их сумма: 47482.23   доля оборота: 0.4638%
даты: 2026-07-01 (14), 2026-07-02 (11), 2026-07-03 (5)
```

Механизм — в `generate_m4.py`, строки 267–270: оба файла режутся по
`BASE_END`, но **разными полями**. Календарь фильтруется по
`date_key <= BASE_END`, транзакции — по `tx_date <= BASE_END`, а
`settled_date` сдвинут относительно `tx_date` на 1–3 дня вперёд и
перенесён с выходных на понедельник. Тридцать операций последних дней
июня получают дату расчёта уже в июле, и в календаре таких дней нет.

**То же есть и в `csv_next/`**, то есть дефект переживёт подмену файлов
в C2: календарь там `2025-01-01 .. 2026-07-31` (577 дней), максимальный
`settled_date` — `2026-08-03`, вне календаря **50 операций на
41581,44**. Обрезки нет вовсе (`calendar_all` пишется целиком), но
граница календаря всё равно проходит по `NEXT_END`, а дата расчёта её
переступает.

**Почему это дефект, а не свойство данных.** Пока активна связь по
`tx_date`, всё сходится день в день, и дефект невидим — `tx_date`
кончается ровно `2026-06-30`. Он проявляется только на том разрезе,
который `step-03.md` сам же и требует построить (задание 5), и
проявляется тихо: 0,46% оборота уходит в строку с пустым измерением.
Учащийся, который сверяет итог, ничего не заметит — итог сходится,
10236902,82 на месте.

**Что от него ломается в написанном тексте:**

- обещание «по месяцу (18 строк)» (задание 4) верно только для
  `tx_date`; по `settled_date` строк **19**, и девятнадцатая пустая.
  Шаг про это не говорит нигде — `settled_date` упомянут в
  `step-03.md` четыре раза (строки 109, 121, 184, 253), ни одно из
  упоминаний не про число строк;
- `ref_month_ytd.csv` — 18 строк данных, последняя `2026,6`. Для сверки
  разреза по дате расчёта он не годится (и не для того написан, см.
  R23);
- мера `Settled Amount` и `Settled YTD` шага 04 считаются по календарю,
  которого не хватает на июль 2026 — при разрезе по дате расчёта они
  унаследуют ту же дыру.

**Проверено на прогоне шага 04 (2026-08-14): при активной связи по
`tx_date` дыра не проявляется.** Визуал 2 дал ровно 18 строк, без
девятнадцатой пустой, и сошёлся с `ref_month_ytd.csv` до копейки, а
`Settled YTD` на 2026-06 равен эталонным 2798508.43. То есть D1 сидит
тихо и в шаге 04 тоже: он ждёт первого же переключения активной связи на
`settled_date` — того самого действия, которое задание 5 `step-03.md`
требует выполнить, а шаг 04 не запрещает. Дефект от этого не легче: мера
`Settled Amount` названа по дате расчёта, а считается по дате операции,
и учащийся, который сделает разрез «по-честному», получит другие числа,
чем эталон.

**Починка — сделана 2026-08-14, корзина 1 решения 27.** В
`generate_m4.py` заведена отдельная граница календаря
`CALENDAR_TAIL_DAYS = 7`: календарь строится до `NEXT_END + 7`, а срез
`csv/` — до `BASE_END + 7`. Семь дней не на глаз: `business_shift` даёт
`+1…3` дня, перенос с выходных добавляет до `+2`, максимум `+5`, и ещё 2
дня запаса. Цифре не доверяют — инвариант
`max(settled_date) <= max(date_key)` проверяется дважды: генератором до
записи файлов и `check_calendar_covers_settled()` в
`tools/check_consistency.py` по обеим папкам.

Что изменилось после пересборки (прогон, а не пересчёт руками):

| | было | стало |
|---|---|---|
| `csv/calendar.csv` | 546 строк, `3bd6f1cb…` | **553** строки, `550e22eb…` |
| `csv_next/calendar.csv` | 577 строк, `478d63d7…` | **584** строки, `614cc2c7…` |
| операций с `settled_date` вне календаря, `csv/` | 30 на 47482.23 | **0** |
| операций с `settled_date` вне календаря, `csv_next/` | 50 на 41581.44 | **0** |
| месяцев в разрезе по `settled_date`, `csv/` | 18 + пустая строка | **19**, июль 2026 на 47482.23 подписан |

Остальные восемь файлов не изменились ни на байт — sha256 `merchants`,
`mcc_categories`, `merchant_plan` и `transactions` в обеих папках те же,
что были. Не изменилось и **ни одно** эталонное число:
`ref_by_category.csv`, `ref_month_ytd.csv` и `ref_plan_commission.csv`
пересобраны `reference_m4.py` и совпали с прежними построчно, 6 / 18 / 3
строки. Это ожидаемо и стоит записать: календарь удлинился в той части,
где нет операций по `tx_date`, а все эталоны шага 04 считаются по
активной связи, то есть по `tx_date`.

Правки текста — только число дней календаря, пять мест: `step-00.md`
(таблица таблиц, плюс явно названный период календаря), `step-02.md`
(критерий числа строк), `step-03.md` (1.3, состав модели), `step-04.md`
(1.2, «подряд идущих дней» для пометки таблицы дат) и
`reference_answers.md` (две таблицы контрольной точки и таблица модели).
Ни одно число оборота, комиссии или количества операций не тронуто.

---

## 4. Что гейт оставил репозиторию

Части 1 и 2 закрыты, Часть 3 идёт. Ни одна правка репозитория в ходе
гейта **не делалась** — гейт измеряет, а не чинит.

**Дефекты Части 1 закрыты вне этой сессии.** Между записью Части 1
(коммит `0699344`, 2026-07-30) и продолжением гейта 2026-07-31 в
репозиторий легли коммиты `18bf501` («пять дефектов гейта») и `4b4ea6c`.
Сверено с диском построчно, не по сообщению коммита:

| Дефект | Где закрыт |
|---|---|
| B1 | `design/decisions.md:590` — «Поправка 2026-07-31: „0 действий“ было неверно»; `program/M3/step-01.md:81–88` — скачивание zip, распаковка, `PATH` на уровне пользователя |
| B2 | `program/M3/step-01.md:165` — `.mode csv` задан до первой команды, дающей CSV |
| B3 | `program/M3/step-01.md:49` — появился «Шаг 0. Рабочий каталог» с указанием `program/M3/data` |
| B4 | `program/M3/step-01.md:117` — разобран `table customers already exists` |
| B5 | `program/M3/step-01.md:273–275` — названо явно: «`pending`-заказов два — 111 (390.0) и 116 (260.0)» |

B6 дефектом не был (см. 1.3).

**Очередь Части 2 — пять пунктов из шести закрыты 2026-07-31 сводом в
решение 22, шестой открыт:**

| # | Что | Где | Из чего | Статус |
|---|---|---|---|---|
| 1 | Нормализовать десятичный разделитель в `same_value()` перед `float()` | `program/M3/data/compare_csv.py` | P9 | **закрыт** — `normalize_number()`, 8/8 пар M3 сошлись, `test_compare_csv.py` (2.7) |
| 2 | Переписать признак установленности Power BI на `Get-AppxPackage *PowerBI*` | `design/decisions.md`, решение 22 | P4 | **закрыт** — решение 22, «Признак установленности» |
| 3 | Снять смягчение «период текстом `YYYY-MM` спасает от дат»; заменить требованием явного M-кода (кодировка, `QuoteStyle`, культура, типы) | `design/decisions.md`, решение 22 | P2, P7, P8 | **закрыт** — смягчение помечено отменённым, введён слой 0 |
| 4 | Добавить PBIR третьей preview-функцией в предусловие; перевести `visualType`, состав полей и сортировку из чеклиста в машинную проверку | `design/decisions.md`, решение 22 | P5 | **закрыт** — слой 3 переписан |
| 5 | Записать правило «в TMDL проверяется отсутствие строки, а не наличие»; запретить побайтовое сравнение TMDL (GUID в `lineageTag`) | `design/decisions.md`, решение 22 | 2.8, 2.9 | **закрыт** — слой 2, «Проверка через отсутствие строки» |
| 6 | Учесть подшаг «явный M-код» в часах M4 | `design/blueprint.md`, часть 6.1 | 2.10 | **открыт** — правится при написании `program/M4/step-00.md`, когда состав шагов известен и вилку есть с чем сверять |

Сверх этих шести свод добавил два, которых очередь не знала: ограничение
критерия C1 по автосозданию связей (2.5) и переформулировку C2 (2.11).
Оба записаны в решение 22.

**Что гейт оставил на диске** (вне репозитория, воспроизводимо):

- `D:\m4-gate\customers.csv` — 436 байт, UTF-8 без BOM, CRLF, 12 строк
- `D:\m4-gate\orders.csv` — 635 байт, ASCII, CRLF, 19 строк
- `D:\m4-gate\export1.csv`, `export2.csv` — по 136 байт, идентичны
- `D:\m4-gate\pbi\` — проект `gate.pbip`, 23 файла, 50 КБ

Оба CSV сгенерированы Python (`csv.writer`, `lineterminator="\r\n"`), а
не редиректом `sqlite3.exe` — первая попытка дала `\r\r\n`, см. B6.
Скрипт-генератор одноразовый, в репозиторий не кладётся: те же данные
воспроизводятся запросами из 2.2 через `.once`.

---

## 5. Прогон `tools/check_consistency.py`

### 5.1. Прогон 2026-07-31

Выполнен 2026-07-31 в конце сессии, на состоянии репозитория после
коммитов `18bf501` и `4b4ea6c`. Вывод дословно:

```
[OK]   6.1: распарсено 24 строк, построчная сумма 478–642
[OK]   CLAUDE.md: 478–642 совпадает с частью 6.1
[OK]   6.2 Итого: 478–642 совпадает с частью 6.1
[WARN] M0: сумма 3 шагов 7–9 ч при вилке 6.1 7–9 — совпадение по обеим границам: признак подгонки суммы под вилку, а не подтверждение оценок (решение 24)
[WARN] M1: сумма 3 шагов 8–10 ч при вилке 6.1 8–10 — совпадение по обеим границам: признак подгонки суммы под вилку, а не подтверждение оценок (решение 24)
[WARN] M2: сумма 8 шагов 30–40 ч при вилке 6.1 30–40 — совпадение по обеим границам: признак подгонки суммы под вилку, а не подтверждение оценок (решение 24)
[OK]   M3: сумма 12 шагов 60–77 ч при вилке 6.1 60–80
[OK]   часть 1: заявлено 30 умений, построчный подсчёт по секциям без пометки 'вне топ-15' совпадает
[OK]   часть 1: всего 36 ID умений во всех секциях (A1..K2), включая 'вне топ-15'
[FAIL] умения без шапки 'Умение: <ID>' ни в одном шаге program/**/*.md: B1, B2, B3, B4, B5, C1, C2, C3, C4, E1, E2, F1, F2, F3, G1, G2, G3, H1, H3, J1, J2, J3, J4, K1, K2
[OK]   program/M*/step-NN.md: все 26 шагов объявили вид — 25 с умением, 1 без; инфраструктурные: program/M3/step-09.md
[OK]   design/decisions.md: 24 решений, нумерация сплошная 1..24
[OK]   DEFERRED.md: 15 строк отложенного, все 36 путей существуют
[OK]   все 4 начатых модулей имеют step-00.md
[OK]   program/M*/data/README.md: сверено 11 строк из раздела «Проверка строк» во всех 4 файлах
[OK]   эталоны на состоянии базы своего шага: сверено 8 пар (CSV ↔ запрос) в модулях: M3. НЕ покрыто: эталоны PostgreSQL (step-09..step-12 — нужен поднятый сервер, числа проверены вручную одним прогоном); activity_log (не коммитится, миллионы строк — сверяется через sha256 контрольной точки); модули без SQL-эталонов: M0, M1, M2
[OK]   program/M*/data/: проверено 38 файлов в 4 папках, ни одного не покрытого README/.gitignore не найдено
[OK]   program/**/*.md: запрещённых слов и POSIX-команд в коде не найдено

Есть проблемы — см. [FAIL] выше.
```

Единственный `[FAIL]` — умения ненаписанных модулей (M4 и далее);
состояние существовало до гейта и этим гейтом не вызвано. C1–C4 в этом
списке — ровно те умения Power BI, ради которых гейт и проводился.

Три `[WARN]` по M0–M2 — новая проверка решения 24, к гейту отношения не
имеет.

### 5.2. Прогон 2026-08-14 (конец захода по Части 3)

Выполнен в конце захода 2026-08-13 → 2026-08-14, до коммита. Вывод
дословно:

```
[OK]   6.1: распарсено 24 строк, построчная сумма 478–642
[OK]   CLAUDE.md: 478–642 совпадает с частью 6.1
[OK]   6.2 Итого: 478–642 совпадает с частью 6.1
[WARN] M0: сумма 3 шагов 7–9 ч при вилке 6.1 7–9 — совпадение по обеим границам: признак подгонки суммы под вилку, а не подтверждение оценок (решение 24)
[WARN] M1: сумма 3 шагов 8–10 ч при вилке 6.1 8–10 — совпадение по обеим границам: признак подгонки суммы под вилку, а не подтверждение оценок (решение 24)
[WARN] M2: сумма 8 шагов 30–40 ч при вилке 6.1 30–40 — совпадение по обеим границам: признак подгонки суммы под вилку, а не подтверждение оценок (решение 24)
[OK]   M3: сумма 12 шагов 60–77 ч при вилке 6.1 60–80
[WARN] M4: сумма 4 шагов 20–27 ч при вилке 6.1 40–60 — сумма целиком вне вилки: сначала искать недостающее содержание, потом править вилку в blueprint (решение 24)
[OK]   часть 1: заявлено 30 умений, построчный подсчёт по секциям без пометки 'вне топ-15' совпадает
[OK]   часть 1: всего 36 ID умений во всех секциях (A1..K2), включая 'вне топ-15'
[FAIL] умения без шапки 'Умение: <ID>' ни в одном шаге program/**/*.md: B1, B2, B3, B4, B5, C2, C3, C4, E1, E2, F1, F2, F3, G1, G2, G3, H1, H3, J1, J2, J3, J4, K1, K2
[OK]   program/M*/step-NN.md: все 30 шагов объявили вид — 28 с умением, 2 без; инфраструктурные: program/M3/step-09.md, program/M4/step-01.md
[OK]   design/decisions.md: 26 решений, нумерация сплошная 1..26
[OK]   DEFERRED.md: 15 строк отложенного, все 36 путей существуют
[OK]   все 5 начатых модулей имеют step-00.md
[OK]   program/M*/data/README.md: сверено 14 строк из раздела «Проверка строк» во всех 5 файлах
[OK]   эталоны на состоянии базы своего шага: сверено 8 пар (CSV ↔ запрос) в модулях: M3. НЕ покрыто: эталоны PostgreSQL (step-09..step-12 — нужен поднятый сервер, числа проверены вручную одним прогоном); activity_log (не коммитится, миллионы строк — сверяется через sha256 контрольной точки); модули без SQL-эталонов: M0, M1, M2, M4
[OK]   program/M*/data/: проверено 46 файлов в 5 папках, ни одного не покрытого README/.gitignore не найдено
[OK]   program/**/*.md: запрещённых слов и POSIX-команд в коде не найдено

Есть проблемы — см. [FAIL] выше.
код возврата: 1
```

Отличия от 5.1, все объясняются работой между заходами, а не этим
заходом (в нём правился только `research/tools-gate.md`):

- `[FAIL]` короче на один ID — **C1** ушёл из списка: шаг с этим умением
  написан. Остальные 24 ID на месте;
- добавился `[WARN] M4: сумма 4 шагов 20–27 ч при вилке 40–60` — это
  ровно пункт 6 открытой очереди раздела 4 (часы M4, решение 22 / 2.10),
  и теперь он виден машинно, а не только по записи в гейте;
- 30 шагов вместо 26, 26 решений вместо 24, 5 модулей вместо 4 — M4
  написан до `step-04.md`.

`[FAIL]` — по-прежнему умения ненаписанных модулей; заходом не вызван и
им не чинится.

### 5.3. Прогон 2026-08-14, конец захода по `step-03.md`

Выполнен после записи R18–R27 и D1. Вывод **побайтово тот же, что в
5.2**: 15 `[OK]`, 4 `[WARN]` (M0, M1, M2, M4), один `[FAIL]` — те же 24
ID умений ненаписанных модулей, код возврата 1.

Это ожидаемо и само по себе измерение: заход правил только
`research/tools-gate.md` и `HANDOFF.md`, а `check_consistency.py` не
проверяет ни то, ни другое. Ни один из 27 дефектов гейта скрипту не
виден — включая D1, дефект самого датасета, при том что проверка
«эталоны на состоянии базы своего шага» существует и печатает `[OK]`:
она сверяет CSV с запросом внутри M3 и до M4 не достаёт (в её же строке
`НЕ покрыто: … модули без SQL-эталонов: M0, M1, M2, M4`).

Записано на будущее, к починке: дыра «календарь короче даты расчёта»
проверяется машинно одной строкой — `max(settled_date) <= max(date_key)`
по каждой паре файлов датасета. Правило, не проверяемое скриптом, живёт
до первого захода, в котором про него забудут (решение 20).

### 5.4. Прогон 2026-08-14, конец захода по `step-04.md`

Выполнен после записи R28–R36 и обновления 3.4–3.7. Вывод **снова
побайтово тот же**: 15 `[OK]`, 4 `[WARN]`, один `[FAIL]` (те же 24 ID
умений ненаписанных модулей), код возврата 1.

Третий раз подряд одно и то же — и это уже не констатация, а результат.
Заход, который закрыл Часть 3 гейта и нашёл девять дефектов, включая
непроходимый критерий шага 03 и лживый критерий шага 04, не сдвинул в
скрипте ни одного символа. **Ни один из 36 дефектов гейта и ни один из
шести дефектов, найденных прямо в критериях, скрипту не виден.**

Отдельно измерено на этом заходе: в `program/M4/data/csv/` во время
прогона лежало пять посторонних файлов — выгрузки визуалов (R34), — и
проверка «файлы вне README/.gitignore» напечатала свой обычный
`[OK] проверено 46 файлов в 5 папках`. Папка целиком в `.gitignore`, и
это правильно по решению 22, но означает, что чужие файлы в папке
датасета не видит вообще ничто: ни `git status`, ни скрипт.

К починке отсюда два кандидата на машинную проверку, оба в одну строку:
`max(settled_date) <= max(date_key)` по каждой паре файлов датасета
(записано в 5.3, D1) и сверка требований критерия шага с тем, что
`check_pbip.py` действительно проверяет (R32, R30).

### 5.5. Отрицательный пример к `check_defect_status()` (2026-08-14)

Проверка учёта дефектов (решение 27, часть 2) заведена вместе с
отрицательным примером — по требованию части 5 того же решения. Зелёный
прогон доказательством не считается: он одинаково выглядит и когда
проверка работает, и когда она всегда говорит «хорошо» (R32).

Сломано по одному случаю на каждую ветку проверки, после каждого файл
восстанавливался из копии. Вывод дословно:

```
### 1. снят статус у R19
[FAIL] research/tools-gate.md: дефект R19 без строки 'Статус:' (решение 27)
код возврата: 1

### 2. статус написан своими словами у R1
[FAIL] research/tools-gate.md: дефект R1 — статус не по формату решения 27: 'Статус: пока открыт, чинится позже'. Разрешено: 'Статус: открыт', 'Статус: закрыт ГГГГ-ММ-ДД, <коммит>, <файл:раздел>', 'Статус: не чинится — <обоснование>'
код возврата: 1

### 3. дыра в нумерации: R20 переименован в R37
[FAIL] research/tools-gate.md: нумерация дефектов R не сплошная — пропущены: [20], повторяются: нет
код возврата: 1

### 4. восстановлено
[OK]   research/tools-gate.md: 37 дефектов со статусом (R1..R36 и D1) — открыт: 37, закрыт: 0, не чинится: 0
код возврата: 1
```

Код возврата 1 в четвёртом прогоне — это **не** сбой восстановления: в
репозитории по-прежнему один `[FAIL]`, тот самый про 24 ID умений
ненаписанных модулей, и он ожидаемое состояние Фазы 2. Строка про
`tools-gate.md` в нём `[OK]`.

Целостность файла после трёх поломок сверена отдельно: 37 строк
`Статус: открыт`, заголовки R1, R19 и R20 на месте, число `[FAIL]` во
всём прогоне вернулось к одному.

Четвёртая ветка — «строк `Статус:` вне блоков дефектов» — сработала не
на подстроенном примере, а на настоящем тексте: шапка файла начиналась
со строки `Статус: **Части 1 и 2 закрыты…**`, проверка её поймала, и
шапка переписана на `Состояние:`. Слово `Статус:` в начале строки в этом
файле теперь зарезервировано под учёт.

### 5.6. Отрицательный пример к `check_calendar_covers_settled()` (2026-08-14)

Проверка инварианта `max(settled_date) <= max(date_key)` заведена
корзиной 1 решения 27 и, по части 5 того же решения, введена вместе с
отрицательным примером. Примера получилось **два**, и первый пошёл не
так, как задумано, — это стоит записать полностью.

**Попытка 1: вернуть `CALENDAR_TAIL_DAYS = 0` и пересобрать датасет.**
Замысел был прямой: воспроизвести состояние, в котором D1 существовал, и
посмотреть, поймает ли его скрипт. Не получилось:

```
SEED = 20260731
csv/: календарь кончается 2026-06-30, а максимальная settled_date 2026-07-03 — это дефект D1. Увеличьте CALENDAR_TAIL_DAYS, а не границу периода операций.

[OK]   program/M*/data/: календарь покрывает дату расчёта, сверено папок: 2
```

Генератор отказался писать битые файлы: его собственная проверка стоит
**до** записи, поэтому `csv/` осталась правильной, а
`check_consistency.py` продолжил печатать `[OK]`. Инвариант генератора
этим доказан, но ветка `[FAIL]` скрипта — нет. Ровно тот случай, ради
которого часть 5 решения 27 и написана: зелёный вывод здесь означал бы
«проверка не сработала», а выглядел бы как «всё хорошо».

**Попытка 2: обрезать `calendar.csv` руками, генератор не запускать.**

```
строк было 553, стало 546
[FAIL] program\M4\data\csv: календарь кончается 2026-06-30, а max(settled_date) = 2026-07-03 — 30 операций на 47482.23 уйдут в строку с пустым измерением (дефект D1)
код возврата: 1
```

Числа в сообщении — **те же самые**, что записаны в 3.8 по прогону
2026-08-14 до починки: 30 операций, 47482.23. То есть проверка не просто
падает, а называет ровно тот дефект, ради которого заведена.

**Восстановление** — прогоном генератора, не правкой файла:

```
csv/: max settled_date 2026-07-03 <= max date_key 2026-07-07
csv_next/: max settled_date 2026-08-03 <= max date_key 2026-08-07
  calendar.csv: 553 строк, sha256 550e22ebf09ea571021dfeb06f99f0297621e6561f96d6c5d48e6a8d4ba88e04
  calendar.csv: 584 строк, sha256 614cc2c735dcdd60f89decdfb7d78a1a4e5061bd48cdd824c7c0b8f2b168b3e9
[OK]   program/M*/data/: календарь покрывает дату расчёта, сверено папок: 2
```

Sha256 совпали с контрольной точкой `reference_answers.md` — файл
восстановлен побайтово, а не «примерно».

**Что этот случай добавляет к части 5 решения 27.** Отрицательный пример
надо подавать в ту проверку, которую вводишь, а не в ближайшую похожую.
Здесь две проверки стерегут один инвариант на разных рубежах —
генератор до записи, скрипт после, — и поломка через генератор
доказывает только первый. Правило на будущее: ломать ровно тот вход,
который читает новая проверка.

### 5.7. Отрицательные примеры к переписанным критериям (2026-08-14)

Корзина 2 решения 27 переписала `check_step03()` и `check_step04()`.
Отрицательный пример к каждой взят **недоделанным шагом, а не порчей
файла**: в истории репозитория лежат проекты после каждого шага прогона,
и это готовые состояния «задание не выполнено».

| Состояние | Коммит | Что в нём |
|---|---|---|
| A | `5d00ea3` | конец шага 02: одна видимая связь, три скрытых автодатных, мер нет |
| B | `29181c2` | конец шага 03: пять видимых связей + одна автодатная, мер нет, визуалов шага 04 нет |
| C | рабочее дерево | конец шага 04: автодата снята пометкой таблицы дат, шесть мер, четыре визуала, выгрузки |

**`--step 03`, состояние A — шаг не сделан, проверка падает:**

```
       справочно: скрытых связей автодаты 3 — на критерий не влияют, в панели «Данные» их не видно
[FAIL] видимых связей в модели: 1, ожидалось 5
[OK]   связь на месте: transactions.merchant_id → merchants.merchant_id
[FAIL] связи transactions.mcc → mcc_categories.code в модели нет
[FAIL] связи transactions.tx_date → calendar.date_key в модели нет
[FAIL] связи transactions.settled_date → calendar.date_key в модели нет
[FAIL] связи transactions.plan_key → merchant_plan.plan_key в модели нет
[FAIL] неактивных видимых связей: 0, ожидалась ровно одна (вторая колонка-кандидат по дате)

НЕ СОШЛОСЬ: 6 проверок. Шаг не закрыт.
код возврата: 1
```

**`--step 03`, состояние B — автодата жива, в файле шесть связей:**

```
       справочно: скрытых связей автодаты 1 — на критерий не влияют, в панели «Данные» их не видно
[OK]   видимых связей в модели: 5
[OK]   связь на месте: transactions.merchant_id → merchants.merchant_id
...
[OK]   неактивна ровно одна связь, и это связь по дате расчёта

ВСЁ СОШЛОСЬ: расхождений 0.
код возврата: 0
```

**`--step 03`, состояние C** — тот же вывод, код 0.

Это и есть закрытие **R30**: одна и та же проверка проходит в **обоих**
состояниях модели — и с живой автодатой, и без неё, — и падает там, где
работа действительно не сделана. Прежняя редакция падала в обоих:
с автодатой по числу связей, без неё по числу «созданных вручную».
Вместе с этим закрыт **R22**: счётчика происхождения связи больше нет,
подменить построенную связь автоопределённой теперь нельзя не потому,
что это ловится, а потому, что для критерия это одно и то же — предметом
проверки стала модель, а не история кликов.

**`--step 04`, состояние B — шаг 04 не начат:**

```
[FAIL] меры Total Amount в модели нет
[FAIL] меры Settled Amount в модели нет
[FAIL] меры Tx Count в модели нет
[FAIL] меры Decline Rate в модели нет
[FAIL] меры Commission в модели нет
[FAIL] меры Settled YTD в модели нет
[FAIL] визуал 1 (категории): на странице нет визуала с полями Decline Rate, Settled Amount, Total Amount, Tx Count, mcc_categories.category_name
[FAIL] визуал 2 (месяцы): на странице нет визуала с полями Settled Amount, Settled YTD, calendar.month_no, calendar.year
[FAIL] визуал 3 (тарифы): на странице нет визуала с полями Commission, merchant_plan.plan_code
[FAIL] визуал итогов: на странице нет визуала с полями Commission, Decline Rate, Settled Amount, Settled YTD, Total Amount, Tx Count
[FAIL] папки выгрузок ...\program\M4\pbi\export нет — задания 5–6 не выполнены: экспорт визуала и есть артефакт этого шага

НЕ СОШЛОСЬ: 11 проверок. Шаг не закрыт.
код возврата: 1
```

Одиннадцать `[FAIL]` на состоянии, где прежняя редакция печатала
**восемь `[OK]` и код 0**. Это закрытие **R32**: девять визуалов шага 03
больше не засчитываются за три визуала шага 04, потому что проверяется
не число визуалов на диске, а состав полей каждого; отсутствие папки
выгрузок само по себе роняет шаг.

**Что осталось недоказанным на момент записи.** На состоянии C
`--step 04` печатает тринадцать `[OK]` и один `[FAIL]`:

```
[FAIL] выгрузки export_totals.csv нет в program\M4\pbi\export — визуал не экспортирован
```

Это не дефект проверки, а честный пробел прогона: визуал итогов на
прогоне 2026-08-14 **строили, но не выгружали** — шесть чисел были
переписаны с экрана в чат вместо экспорта (см. 3.3, R33, «обход»).
Проверка требует файл, файла нет, и она правильно об этом говорит.
Полный зелёный прогон `--step 04` закроется одной выгрузкой.

---

## 6. Решения

**Затронуты: 15, 22, 26. Заведены: 27.**

Гейт фиксировал и не правил ничего до самого конца; заходом, закрывшим
`step-04.md`, решение 26 помечено исполненным, а порядок починки по его
итогам заведён решением 27.

- **Решение 15** — затронуто Частью 1, поправка **уже внесена** вне этой
  сессии (`design/decisions.md:590`, коммит `18bf501`). Сверено с диском.
  Со стороны гейта закрыто.
- **Решение 22** — затронуто Частью 2, **не правлено**. Подлежит правке
  по шести пунктам открытой очереди раздела 3: признак установленности
  (P4), смягчение по датам и требование явного M-кода (P2, P7, P8),
  PBIR третьей preview-функцией и перенос части слоя 3 в машинную
  проверку (P5), правило «в TMDL проверяется отсутствие строки» (2.9),
  нормализация десятичного разделителя в `compare_csv.py` (P9), часы M4
  в части 6.1 (2.10).
- **Решение 26** — затронуто Частью 3, **исполнено 2026-08-14**. Порядок
  («пройти все четыре шага, прежде чем что-либо править») соблюдён на
  всех пяти заходах прогона: заведены 36 дефектов инструкций и дефект
  датасета D1, дважды уточнён R12, не починен ни один. Пройдены все
  четыре шага, отвечены все пять пунктов (3.6). Три поправки к самому
  решению записаны в нём же: ожидание «`AutoDetected_*` ровно 1»
  опровергнуто (их два, R21), цена занижена впятеро, а 36 дефектов
  нашлись мимо списка «что прогон обязан закрыть».
- **Решение 27** — **заведено** этим заходом: порядок починки из пяти
  корзин плюс нулевая корзина учёта, обязательный номер закрываемого
  дефекта при каждой правке, три корзины обязаны оставить машинную
  проверку, цена 5–8 заходов с измерением по `program/pilot-report.md`.
  Основание — измерение, записанное в 5.4: три захода подряд
  `check_consistency.py` даёт побайтово тот же вывод, пока находятся 36
  дефектов, и ни один ему не виден.

Судьба C1–C4 определена и записана в 2.10: **все четыре остаются
машинными**, ни один не уходит в чеклист автора.

Запись в `program/pilot-report.md` не делается: заход не правил ни одного
шага. Она появится вместе с правками, следующими из этого гейта.
