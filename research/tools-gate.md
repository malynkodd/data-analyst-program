# Ручной гейт инструментов: консольный `sqlite3` и нулевая проверка Power BI

Даты прогона: 2026-07-30 (Часть 1), 2026-07-31 (Часть 2),
2026-07-31 … 2026-08-04 (Часть 3).
Статус: **Части 1 и 2 закрыты. Часть 3 — ручной прогон M4 по решению 26 —
остановлена на задании 7 шага `step-02.md`, разделы 3.5–3.7.**

Гейт выполнялся вручную: все действия в GUI и в терминале делал автор на
своей машине, агент давал инструкции, сверял вывод и записывал результат.
Агент ничего не устанавливал и ничего не чинил. Все числа ниже получены
прогоном, не из документации; там, где источником была документация, это
названо явно.

Назначение: проверить исполнимость `program/M3/step-01.md` на живой
машине (Часть 1), ответить на шесть пунктов нулевой проверки решения 22
до написания `program/M4/step-01.md` (Часть 2) и пройти написанные шаги
M4 как учащийся — гейт перед умением C2, решение 26 (Часть 3).

---

## 1. ЧАСТЬ 1 — консольный клиент `sqlite3`. Закрыта

### 1.1. Окружение на момент прогона

| Параметр | Значение |
|---|---|
| ОС | Windows 11 Pro 10.0.26200 |
| Оболочка | Windows PowerShell (не PowerShell 7) |
| Python | 3.11.9 |
| `sqlite3` в `PATH` до гейта | отсутствует |
| `sqlite3` после гейта | 3.53.4 2026-07-24 19:02:57 `bf7c7f30031888f4e796e429ab3978879485813aaca6f641c7b33e4e09459bcc` (64-bit) |

### 1.2. Что произошло на первой команде модуля

`program/M3/step-01.md`, раздел 1.3 — первая команда 60–80-часового
модуля:

```
$ sqlite3 m3.db ".read schema.sql"
```

Фактический результат:

```
sqlite3 : Имя "sqlite3" не распознано как имя командлета, функции, файла
сценария или выполняемой программы.
    + CategoryInfo          : ObjectNotFound: (sqlite3:String) [], CommandNotFoundException
```

Обходного пути через Python на этой машине нет: встроенный REPL
`python -m sqlite3` появился только в Python 3.12, у автора 3.11.9.

### 1.3. Дефекты

**B1 — блокер. Модуль неисполним с первой команды; инструкции по
установке клиента нет ни в одном файле репозитория.**

Ни `program/M3/step-01.md`, ни `program/M3/step-00.md` не содержат
указаний, как получить консольный клиент. `step-00.md`, раздел
«SQL-движок», утверждает обратное: «модуль начинается на SQLite (**0
действий по установке**…)». Источник ошибки — решение 15
`design/decisions.md`: «M3 начинается на SQLite: ставится в 0 действий
(**входит в стандартную библиотеку Python**, файл базы — обычный файл)».

Утверждение верно для **библиотеки** `sqlite3` из стандартной поставки
Python — ею пользуются скрипты программы. Шаги M3 написаны против
**консольного клиента** `sqlite3`. Это разные вещи, и клиент в поставку
Python не входит. Проверено прогоном (1.2).

Затронуто: решение 15 (формулировка «0 действий»), `step-00.md` раздел
«SQL-движок», `step-01.md` раздел 1.3.

**B2 — критерий сверки с эталонным CSV невыполним из коробки.**

Клиент 3.53.4 по умолчанию печатает результат рамками с заголовками:

```
╭──────────┬───────────┬────────╮
│ order_id │  status   │ amount │
╞══════════╪═══════════╪════════╡
│      111 │ pending   │  390.0 │
╰──────────┴───────────┴────────╯
```

Эталоны модуля — CSV с запятой и заголовком
(`program/M3/data/a1_task1_running_total.csv`):

```
customer_id,order_id,order_date,amount,running_total
1,101,2026-05-02,1200.0,1200.0
```

Критерий `step-06.md`, 1.5 — «вывод собственного запроса **построчно**
(все колонки, все строки, тот же порядок) совпадает с соответствующим
эталонным CSV» — механически не выполняется: сравниваются рамочная
таблица и CSV. Ни один шаг M3 не задаёт `.mode csv` / `.headers on` и не
запускает клиент как `sqlite3 -csv -header`. Проверено поиском по
`program/M3/step-*.md`: совпадений `.mode` — ноль.

Затронуто: `step-06.md` (6 эталонов A1), `step-07.md` (2 эталона A3),
`step-10.md`, `step-11.md`.

**B3 — не назван рабочий каталог.** `step-01.md` даёт `sqlite3 m3.db
".read schema.sql"` без указания, откуда запускать. `schema.sql` и
`seed.sql` лежат в `program/M3/data/`. Из корня репозитория команда не
работает.

**B4 — не описан повторный прогон.** `.read schema.sql` поверх уже
существующей `m3.db` падает на `table already exists`. Шаг не говорит ни
удалить файл базы, ни что делать при повторе. В прогоне это обошли
`Remove-Item m3.db` — команда добавлена агентом, в тексте шага её нет.

**B5 — скрытая зависимость критерия от данных.** Критерий `step-01.md`,
1.5: «сломанный запрос возвращает 13 строк — ровно все `completed`-заказы
независимо от суммы». Это верно только потому, что ни один
`pending`-заказ не проходит `amount > 500`: их два, 111 (390.0) и 116
(260.0). В шаге это не названо. Если будущая правка `seed.sql` добавит
`pending` с суммой выше 500, критерий сломается, и причина будет
неочевидна.

**B6 — проверенная и снятая гипотеза.** `sqlite3.exe -csv > файл` на
Windows даёт двойной возврат каретки. Обнаружено 2026-07-31 при
подготовке данных для Части 2, воспроизведено прогоном:

```
$ sqlite3.exe -csv -header m3.db "SELECT order_id, amount FROM orders
  ORDER BY order_id LIMIT 2;" > b6_test.csv
$ od -c b6_test.csv
o r d e r _ i d , a m o u n t \r \r \n 1 0 1 , 1 2 0 0 . 0 \r \r \n ...
```

В CSV-режиме SQLite сам завершает строку `\r\n`, а stdout в текстовом
режиме Windows переводит `\n` ещё раз — получается `\r\r\n`.

Область поражения ограничена, это проверено, а не предположено:

- Восемь эталонов `program/M3/data/a*.csv` чистые — CRLF, ноль двойных
  `\r` (сгенерированы Python, не редиректом).
- `compare_csv.py` двойной `\r` переживает: `csv.reader` с `newline=""`,
  `.strip()` на ячейках и отсев пустых строк его снимают — проверено
  запуском на битом файле, разбор корректный.

**Дефекта нет — репозиторий это уже знает и лечит.** Проверено после
находки, до записи вывода:

- Восемь эталонов `program/M3/data/a*.csv` чистые — CRLF, ноль двойных
  `\r` (сгенерированы Python, не редиректом).
- `compare_csv.py` двойной `\r` переживает: `csv.reader` с `newline=""`,
  `.strip()` на ячейках и отсев пустых строк его снимают — проверено
  запуском на битом файле, разбор корректный.
- Явление описано в docstring `compare_csv.py` (пункт 2 «Перевод
  строки») со ссылкой на решение 13, и оттуда выведено правило шага.
- Правило действительно есть в тексте: `step-01.md` строки 167–175
  предписывают `.once <файл>` и прямо запрещают перенаправление `>`;
  `step-06.md:162` и `step-07.md:158` на него ссылаются;
  `data/README.md:88` повторяет.

Запись оставлена как след проверки: гипотеза о дефекте выдвигалась и
была отклонена прогоном. Собственная ошибка агента (данные для Части 2
были сгенерированы редиректом, а не `.once`, и вышли с `\r\r\n`) —
подтверждение того, что правило шага нужное.

### 1.4. Что подтвердилось прогоном

Содержание шага верно целиком. Все обещанные числа сошлись, расхождений
ноль.

| Проверка | Обещано в шаге | Факт |
|---|---|---|
| Всего заказов | 19 | 19 |
| `completed` | 13 | 13 |
| Пример 1.3, сломанный запрос | 4 строки: 111/390.0, 116/260.0, 117/320.0, 118/410.0 | совпало построчно |
| Пример 1.3, исправленный | 3 строки (без 116) | совпало |
| Задание 1.4, сломанный | 13 строк, все `completed` | совпало |
| Нарушители `amount > 500` | 103 (430), 112 (455), 114 (330) | совпало |
| Задание 1.4, исправленный | 10 строк | 10 |

Вывод F (исправленный запрос задания 1.4), дословно:

```
order_id,status,amount
101,completed,1200.0
102,completed,850.0
104,completed,600.0
105,completed,720.0
107,completed,980.0
108,completed,540.0
109,completed,610.0
113,completed,700.0
115,completed,510.0
119,completed,675.0
```

### 1.5. Фактическая цена установки клиента — измерена

Страница [Precompiled Binaries for Windows](https://www.sqlite.org/download.html)
(fetch 2026-07-30) отдаёт `sqlite-tools-win-x64-3530400.zip`, 6 557 792
байта. Распаковка даёт четыре `.exe` плоско, без вложенной папки и без
установщика:

| Файл | Байт |
|---|---|
| `sqlite3.exe` | 4 022 272 |
| `sqldiff.exe` | 3 243 520 |
| `sqlite3_analyzer.exe` | 4 484 608 |
| `sqlite3_rsync.exe` | 3 179 520 |

Прав администратора не требуется. `PATH` дописан на уровне пользователя
(`[Environment]::SetEnvironmentVariable("Path", …, "User")`), новое окно
терминала подхватывает. Это тот же путь без админских прав, который
решение 19 п. 2 выбрало для PostgreSQL.

Итого честная цена: **скачать 6.6 МБ, распаковать, дописать `PATH`** —
не «0 действий», но и не установка сервера.

### 1.6. Побочное замечание по прогону

`PATH` на машине автора содержит `D:\sqlite-tools` дважды: команда
дописывания выполнена повторно. Ни на что не влияет, но стоит вычистить.

---

## 2. ЧАСТЬ 2 — нулевая проверка Power BI (решение 22). Закрыта

### 2.1. Окружение

| Параметр | Значение |
|---|---|
| Power BI Desktop | 2.156.951.0, **версия из Microsoft Store** |
| Способ установки | Store (`https://aka.ms/pbidesktopstore`), прав администратора не требует |
| Язык интерфейса | русский (Store-версия берёт язык Windows) |
| Экран | 1920×1080, масштаб 100% |
| Ключ реестра `HKLM:\SOFTWARE\Microsoft\Microsoft Power BI Desktop` | **отсутствует** |

Способы установки сверены с
[Download Power BI Desktop](https://learn.microsoft.com/en-us/power-bi/fundamentals/desktop-get-the-desktop)
(fetch 2026-07-30): Store — без прав администратора, обновляется
автоматически; Download Center (`PBIDesktopSetup_x64.exe`) — требует прав
администратора.

### 2.2. Находка P1 — автоопределение кодировки зависит от содержимого файла; C2 ломается тихо

**Что сделано.** Два CSV выгружены из `m3.db` в `D:\m4-gate\`
(вне репозитория) запросами:

```sql
SELECT customer_id, name, city FROM customers ORDER BY customer_id;
SELECT order_id, customer_id, status, amount,
       substr(order_date,1,7) AS period_ym
FROM orders ORDER BY order_id;
```

Оба записаны в UTF-8 **без BOM**. `customers.csv` содержит украинскую
кириллицу (`Коваленко О.`, `Київ`), `orders.csv` — только латиницу и
цифры.

**Что показал Power BI** в окне предпросмотра «Текстовый или CSV-файл»:

| Файл | Содержимое | «Источник файла», подставлен автоматически |
|---|---|---|
| `customers.csv` | кириллица UTF-8 | `65001: Юникод (UTF-8)` — кириллица отображена верно |
| `orders.csv` | только ASCII | **`1251: Кириллическая (Windows)`** |

Разделитель в обоих случаях определён верно («Запятая»). Определение
типов — «на основе первых 200 строк».

**Почему это дефект, а не курьёз.** Кодировка не определяется заново при
каждом обновлении — она зашивается в код запроса на момент импорта.
Файл, состоящий сегодня только из ASCII, получает 1251. Как только в
очередной выгрузке появится кириллица — название категории, город,
контрагент, статус — тот же запрос прочитает её как 1251 и молча
испортит, без единого сообщения об ошибке.

Это прямое попадание в умение C2, критерий которого сформулирован в
части 1 blueprint как «подмена исходного файла на файл следующего месяца
+ Refresh → 0 ошибок, 0 ручных правок». Он провалится, и провалится
тихо — худший из режимов отказа.

**Следствие для M4.** Шаг обязан требовать явной установки `65001` в
«Источнике файла», а не полагаться на автоопределение. Это не оговорка в
1.6, а часть задания и часть критерия. Отдельно: данные программы
кириллические начиная с M2 (решение 17), то есть проблема не
гипотетическая для этого домена.

### 2.3. Находка P2 — смягчение решения 22 по датам не работает

**Что утверждает решение 22.** Единственная документированная
невоспроизводимость экспорта — формат `datetime` по локали машины. И
далее дословно: «Снимается конструкцией задания: в экспортируемых
визуалах столбцов `datetime` нет, период выражается текстом `YYYY-MM`
или целочисленным ключом даты».

**Что произошло.** Колонка `period_ym` выгружена ровно так, как
предписывает решение — текстом `2026-05`. В предпросмотре Power BI она
показана как **`01.05.2026`**: определение типов распознало строку
`2026-05` как дату и отформатировало по локали машины.

**Почему это важно.** Смягчение из решения 22 само по себе не
срабатывает. «Выразить период текстом» недостаточно — Power BI
преобразует такой текст в дату автоматически, и на выходе получается
ровно то, чего решение избегало: значение, зависящее от локали. Чтобы
смягчение работало, шаг обязан дополнительно **явно задать типу колонки
«Текст»** в Power Query и не полагаться на то, что текст останется
текстом.

Формулировку решения 22 нужно поправить: не «период выражается текстом»,
а «период выражается текстом **и тип колонки принудительно установлен в
Текст**, потому что автоопределение типов превращает `YYYY-MM` в дату».

### 2.4. Остальные находки Части 2

**P3 — родного коннектора к SQLite нет; к PostgreSQL есть.** Поиск
`SQLite` в полном диалоге «Получить данные» не даёт ничего. В списке баз
данных присутствуют: SQL Server, SQL Server Analysis Services, MySQL,
**PostgreSQL**, SQL, SQL Azure, Azure Synapse Analytics, Azure для
PostgreSQL, MongoDB Atlas SQL.

Проектное следствие для M4, лучше исходного замысла: M3 заканчивается
переносом данных на PostgreSQL (`step-09.md`), значит M4 подключается к
той же базе `m3` **напрямую**, а не к промежуточному CSV. Тогда критерий
C1 («числа сходятся с эталонным SQL») сверяет визуал и эталонный запрос
на одних и тех же данных, без экспортной прослойки. Путь через CSV
остаётся только там, где нужен именно навык импорта файла (C2).

**P4 — признак установленности из решения 22 ложноотрицателен для
Store-версии.** Решение 22 фиксирует отсутствие Power BI по трём
признакам, один из которых — ключ реестра `HKLM:\SOFTWARE\Microsoft\
Microsoft Power BI Desktop`. У установленной Store-версии этого ключа
нет. Проверять надо `Get-AppxPackage *PowerBI*`.

**P5 — PBIP и TMDL всё ещё preview; дополнительно включён PBIR.** В
разделе «Функции в предварительной версии» версии 2.156.951.0
присутствуют и включены:

- «Параметр сохранения проекта Power BI (PBIP)» — включено
- «└ Сохранить семантическую модель в формате TMDL» — включено
- «└ Хранение отчетов с использованием расширенного формата метаданных
  (PBIR)» — включено

Первые два подтверждают слой 2 решения 22 (обе функции по-прежнему
preview, обе требуют ручного включения). Третьего решение 22 не знало:
при включённом PBIR текстом сохраняется не только семантическая модель,
но и сам отчёт — включая типы визуалов. Это потенциально переводит часть
слоя 3 («нет 3D, пончиков и двойных осей» — P3, критерий 13) из чеклиста
автора в машинную проверку. **Не проверено прогоном** — гипотеза,
требующая подтверждения на сохранённом проекте.

**P6 — локаль интерфейса.** Интерфейс русифицированный. Решение 21 п. 4
требует «одна локаль»; значит шаги M4 обязаны ссылаться на русские
названия пунктов меню, и это надо объявить в `program/M4/step-00.md`
явно, иначе шаг будет называть английские подписи, которых автор не
увидит.

**P7 — денежная колонка молча уходит в текст при русской локали.**
Прогон 2026-07-31. Импорт `D:\m4-gate\orders.csv` (значения `amount` вида
`1200.0`) штатным коннектором «Текст или CSV» без ручных правок дал в
M-коде:

```
Table.TransformColumnTypes(#"Повышенные заголовки", {{"order_id",
Int64.Type}, {"customer_id", Int64.Type}, {"status", type text},
{"amount", type text}, {"period_ym", type date}}, "ru")
```

Культура `"ru"` подставлена автоматически. В ней десятичный разделитель —
запятая, поэтому `1200.0` не распознаётся как число, и Power Query кладёт
колонку в **текст**, не выдавая ни ошибки, ни предупреждения. Мера
`SUM(orders[amount])` на такой модели не считается.

Тяжелее P2: P2 портит подпись периода, P7 обнуляет само число. Критерии
C1–C4 решения 22 («числа визуала сходятся с эталонным SQL») на модели по
умолчанию невыполнимы — эталон 8600.00 нечем сверять, пока тип не
исправлен вручную. Обход, проверенный прогоном: культура `"en-US"` в
`Table.TransformColumnTypes` — после замены `amount` встал числом,
`period_ym` текстом `2026-05`.

Источник расхождения — не CSV: SQLite пишет `1200.0` с точкой всегда.
Значит любой CSV-эталон проекта, попав в Power BI на машине автора,
получает эту проблему. M4 обязан задавать культуру явно.

**P8 — явный выбор кодировки в GUI не всегда попадает в файл проекта.**
Расширение P1. Прогон 2026-07-31, два файла, одинаковые действия («Источник
файла» = `65001` в диалоге импорта):

| Файл | Содержимое | `Encoding` в `Csv.Document` |
|---|---|---|
| `orders.csv` | чистый ASCII | **параметр отсутствует** |
| `customers.csv` | кириллица | `Encoding = 65001` |

Power BI записывает `Encoding` только когда выбранное значение расходится
с автоопределением. На ASCII-файле выбор совпал с определением, параметр
не записан — кодировка в проекте **не зафиксирована**. Следствие: файл,
сегодня ASCII, а завтра с кириллицей (перегенерация эталона, новые
города), переопределится молча, и C2 сломается без единой правки в
проекте. Прибить кодировку через GUI нельзя — только правкой M-кода.

Побочно: коннектор ставит `QuoteStyle = QuoteStyle.None`, то есть кавычки
не считаются ограничителем поля. Любая запятая внутри значения (название
компании, адрес) разъедет строку по колонкам. Тоже лечится только правкой
M-кода — `QuoteStyle.Csv`.

### 2.5. Модель, собранная для прогона

Собрана 2026-07-31, GUI-действия автора, инструкции агента.

- Два запроса Power Query из `D:\m4-gate\*.csv`, M-код задан явно
  (`Encoding = 65001`, `QuoteStyle = QuoteStyle.Csv`, культура `en-US`,
  `period_ym` принудительно `type text`) — обход P1, P2, P7, P8.
- Связь **создана Power BI автоматически**, руками не проводилась:
  `orders[customer_id]` → `customers[customer_id]`, кардинальность
  **«Многие к одному (\*:1)»**, кросс-фильтрация **«Простое»**, связь
  активна, «Предполагать целостность данных» снято.
- Связь пережила переименование таблицы (`orders csv` → `orders`) без
  разрыва — проверено удалением и пересозданием запроса.
- Мера на `orders`:
  `Total Completed = CALCULATE(SUM(orders[amount]), orders[status] = "completed")`,
  формат задан на мере («Десятичное число», 2 разряда).
- Визуал «Таблица»: `customers[city]` + мера, сортировка по мере по
  убыванию.

**Двойное авторство формулы сошлось — построчно, не только на итоге.**
Визуал против эталонного SQL на той же `m3.db`:

| Город | Визуал Power BI | Эталонный SQL |
|---|---|---|
| Київ | 2480,00 | 2480.00 |
| Одеса | 2465,00 | 2465.00 |
| Львів | 1995,00 | 1995.00 |
| Дніпро | 1150,00 | 1150.00 |
| Харків | 510,00 | 510.00 |
| **Всего** | **8600,00** | **8600.00** |

Эталон итога взят из `program/M3/step-00.md` (`SUM(orders.amount) WHERE
status='completed'` = 8600.00), разбивка по городам посчитана прогоном:
`SELECT c.city, SUM(o.amount) ... JOIN ... WHERE o.status='completed'
GROUP BY c.city ORDER BY SUM(o.amount) DESC`. Совпали состав строк, все
значения и порядок.

### 2.6. Шесть пунктов нулевой проверки решения 22 — ответы

Экспорт визуала выполнен дважды подряд, без единого изменения между
экспортами: `D:\m4-gate\export1.csv`, `export2.csv`, оба по 136 байт.

| # | Пункт решения 22 | Ответ |
|---|---|---|
| 1 | Кодировка экспортируемого `.csv` | **UTF-8 с BOM** (`EF BB BF`) |
| 2 | Разделитель полей / десятичный | поля — **запятая**; десятичный — **запятая** (локаль `ru`), из-за чего числа заключены **в кавычки** |
| 3 | Строка «Applied filters» | **отсутствует**; первая строка файла — заголовок |
| 4 | Детерминированность | **побайтово идентичны**, SHA-256 совпал |
| 5 | Порядок строк | **совпал с сортировкой визуала** (Київ → Харків) |
| 6 | Кардинальность и направление в `relationships.tmdl` | **не проверено** — требует сохранения `.pbip` |

SHA-256 обоих файлов:
`bcc691fccbf2501d2ef68b41e07d7ca05befb9a94fd48019f195930415208ba6`.

Байты `export1.csv` целиком:

```
EF BB BF c i t y , T o t a l   C o m p l e t e d \r \n
Київ , " 2 4 8 0 , 0 0 " \r \n
Одеса , " 2 4 6 5 , 0 0 " \r \n
Львів , " 1 9 9 5 , 0 0 " \r \n
Дніпро , " 1 1 5 0 , 0 0 " \r \n
Харків , " 5 1 0 , 0 0 " \r \n
```

**Пункт 4 — главный — закрыт положительно.** Экспорт детерминирован.
Механизм проверки решения 22 (экспорт визуала → сверка скриптом) в
принципе работает. Развилка «ЕСЛИ ЧАСТЬ 2 ПРОВАЛЕНА» **не срабатывает**.

### 2.7. Находка P9 — экспортируются форматные строки, и C1 на них падает

Ответ на вопрос задания «форматные строки против сырых чисел»: в экспорт
попадает **отформатированное значение**, а не сырое число. `2480,00` —
это `formatString` меры (2 разряда) плюс десятичная запятая русской
локали, а не `2480`.

Следствие проверено запуском, а не рассуждением. Эталон, сгенерированный
из `m3.db` с тем же заголовком и порядком строк, против `export1.csv`:

```
$ python compare_csv.py export1.csv ref_city.csv
РАСХОЖДЕНИЕ в строке 1, колонка 'Total Completed':
  у вас '2480,00', в эталоне '2480.00'
  ваша строка:   Київ,2480,00
  строка эталона: Київ,2480.00
код возврата: 1
```

Данные совпадают полностью. Расходится запись числа. `same_value()` в
`compare_csv.py` сначала сравнивает строки, потом пробует `float(got)`;
`float("2480,00")` бросает `ValueError`, и функция возвращает `False`.

BOM скрипт переживает (`utf-8-sig`), кавычки тоже (разбор по правилам
CSV), кириллица прочиталась верно. Ломает ровно десятичная запятая.

Значит **C1 нельзя сдать существующим `compare_csv.py`**. Три выхода, в
порядке предпочтения:

1. Научить `same_value()` заменять запятую на точку перед `float()` —
   правка одной строки, лечит и десятичный разделитель, и любой другой
   визуал M4. Риск: в CSV с запятой-разделителем значение `1 234,56`
   пришло бы в кавычках с неразрывным пробелом — нужна и чистка пробелов.
2. Требовать в шаге M4 задавать мере формат без разрядов и сверять целые
   числа. Сужает задание.
3. Отдельный скрипт сверки для M4. Дублирование, хуже первого.

Побочно: строка «Всего» (grand total) визуала **в экспорт не попала** —
экспортируются только строки данных. Критерий, сверяющий итог, должен
получать его суммированием строк, а не искать в файле.

**Закрыто 2026-07-31 первым выходом из трёх.** `normalize_number()` в
`compare_csv.py` снимает разделитель разрядов (обычный, неразрывный и
узкий неразрывный пробел) и десятичную запятую перед `float()`. Тот же
вызов, что печатал расхождение:

```
$ python compare_csv.py D:/m4-gate/export1.csv D:/m4-gate/ref_city.csv
СОВПАДАЕТ: 5 строк, 2 колонок, расхождение 0.
код возврата: 0
```

Восемь эталонных пар M3 прогнаны после правки — числа в них с точкой, и
правка их не задела: 8 из 8 `СОВПАДАЕТ`, расхождений 0. Смягчение
сравнения проверено с обратной стороны `test_compare_csv.py` (26 случаев,
13 из них — расхождения, которые обязаны остаться расхождениями): `2480,00`
против `2490.00` — расхождение; `1,234` против `1234` — тоже расхождение,
одна запятая читается как десятичная, а не как разделитель разрядов.

### 2.8. `.pbip` сохранён и прочитан с диска

`D:\m4-gate\pbi\gate.pbip`, версия 2.156.951.0, обе preview-функции
(PBIP, TMDL) и PBIR включены. **23 файла, весь вес — 50 КБ**, самый
крупный (20 КБ) — файл темы. Слой 2 решения 22 подтверждён: проект
сохраняется текстом.

Power BI сам кладёт в корень проекта `.gitignore`:

```
**/.pbi/localSettings.json
**/.pbi/cache.abf
```

То есть единственный бинарник (`cache.abf`, 20 КБ) из-под версионирования
выводится автоматически — проект коммитится в репозиторий как есть,
отдельного правила заводить не нужно.

**Пункт 6 нулевой проверки — ответ.** `definition/relationships.tmdl`
целиком:

```
relationship AutoDetected_32360c20-c06a-42dd-b478-fa006adf4b99
	fromColumn: orders.customer_id
	toColumn: customers.customer_id
```

Два следствия, оба важны для формулировки критериев:

1. **Кардинальности и направления фильтрации в файле нет.** GUI
   показывает «Многие к одному (\*:1)» и «Простое» — в TMDL не записано
   ничего. Вероятная причина: TMDL опускает значения по умолчанию, и
   тогда критерий надо строить наоборот — не «найди строку
   `crossFilteringBehavior: bothDirections`», а «убедись, что её нет».
   **Проверено отдельным прогоном** — см. 2.9.
2. **Имя связи содержит GUID** (`AutoDetected_<uuid>`), и `lineageTag`
   с GUID стоит у каждой таблицы, колонки и меры. Значит **побайтовое
   сравнение TMDL с эталоном невозможно в принципе** — критерии M4 обязаны
   разбирать текст (поиск строки, regex), а не сличать файлы.

**Мера и типы читаются текстом.** `definition/tables/orders.tmdl`:

```
measure 'Total Completed' = CALCULATE(SUM(orders[amount]), orders[status] = "completed")
	formatString: 0.00
...
column amount
	dataType: double
column period_ym
	dataType: string
```

Плюс — в том же файле лежит **весь M-код целиком**, в разделе
`partition orders = m`, включая `Encoding = 65001`, `QuoteStyle =
QuoteStyle.Csv` и культуру `"en-US"`. То есть обходы P1, P7 и P8
проверяются машинно по тексту проекта: критерий может потребовать
наличия этих подстрок.

**Гипотеза P5 подтверждена, и шире, чем предполагалось.** При включённом
PBIR каждый визуал лежит отдельным JSON:
`gate.Report/definition/pages/<id>/visuals/<id>/visual.json`. В нём:

```
"visual": { "visualType": "tableEx",
  "query": { "queryState": { "Values": { "projections": [ ... "queryRef": "customers.city" ...
                                                          "queryRef": "orders.Total Completed" ... ] } },
             "sortDefinition": { "sort": [ { ... "Property": "Total Completed" },
                                             "direction": "Descending" ] } } }
```

Машинно читаются: **тип визуала** (`visualType`), **какие поля на нём**
(`queryRef`), **сортировка и её направление** (`sortDefinition`). Это
переводит в машинную проверку сразу три вещи, числившиеся за чеклистом
автора: запрет на 3D/пончики/двойные оси (слой 3 решения 22, критерий 13),
состав полей визуала и пункт 5 нулевой проверки — порядок строк
проверяется по объявленной сортировке, без экспорта.

Цена — зависимость от трёх preview-функций сразу (PBIP, TMDL, PBIR), из
которых PBIR решение 22 не знало. Все три включаются вручную и могут
измениться в следующих версиях. Это надо записать в предусловие M4 и в
само решение.

### 2.9. TMDL пишет только отклонения от умолчания — проверено прогоном

Гипотеза из 2.8 не оставлена предположением. В GUI изменено одно
свойство связи: «Направление перекрестной фильтрации» с «Простое» на
**«Оба»**, проект пересохранён тем же `.pbip`. `relationships.tmdl` стал:

```
relationship AutoDetected_32360c20-c06a-42dd-b478-fa006adf4b99
	crossFilteringBehavior: bothDirections
	fromColumn: orders.customer_id
	toColumn: customers.customer_id
```

Строка появилась ровно там и ровно тогда, когда значение разошлось с
умолчанием: файл — 174 байта.

После проверки значение возвращено на «Простое» и проект пересохранён.
`relationships.tmdl` вернулся к исходным трём строкам, 133 байта, следов
отменённой правки не осталось — TMDL не копит мусор от откатов.

**Правило для критериев M4, выведенное прогоном.** Свойство при значении
по умолчанию в TMDL отсутствует. Значит проверка формулируется как
утверждение об отсутствии:

- «в `relationships.tmdl` нет строки `crossFilteringBehavior:`» —
  равносильно «направление фильтрации одинарное»;
- «нет строк `fromCardinality:` / `toCardinality:`» — равносильно
  «многие-к-одному»;
- «нет строки `isActive: false`» — равносильно «связь активна».

Формулировка «убедись, что строка `crossFilteringBehavior: oneDirection`
присутствует» была бы невыполнима: такой строки не бывает.

Та же логика распространяется на всю модель, и это надо держать в уме при
написании C3/C4: отсутствие строки в TMDL — не признак того, что автор
чего-то не сделал, а признак значения по умолчанию.

### 2.10. Вердикт Части 2

**Часть 2 пройдена. Развилка «ЕСЛИ ЧАСТЬ 2 ПРОВАЛЕНА» не срабатывает.**
Решение о том, что M4 не пишется, не принимается; часы M4 в части 6.1
blueprint не пересматриваются в сторону сокращения.

Основание — все шесть пунктов нулевой проверки отвечены прогоном, и
критический (пункт 4, детерминированность) отвечен положительно: два
экспорта подряд побайтово идентичны. Механизм решения 22 «визуал →
экспорт → сверка скриптом» работоспособен.

Что при этом изменилось против того, каким решение 22 себе это
представляло — четыре пункта, каждый упирается в находку прогона:

| Что | Как было в решении 22 | Что показал прогон |
|---|---|---|
| Подготовка данных | считалась бесплатной, обход по датам — конструкцией задания | P2, P7, P8: модель по умолчанию непригодна, нужен явный M-код |
| C1 (числа сходятся) | сверка `compare_csv.py` | P9: скрипт падает на десятичной запятой, нужна правка `same_value()` |
| Слой 3 (вид визуала) | чеклист автора | P5: `visualType`, поля и сортировка читаются машинно из PBIR |
| Проверка модели | «прочитать TMDL» | 2.9: только через отсутствие строк; побайтовое сравнение невозможно |

Итог по C1–C4: **все четыре остаются машинными**, ни один не уходит в
чеклист автора. Более того, часть слоя 3 из чеклиста в машинную проверку
переходит. Цена — не в отказе от критериев, а в трёх дополнительных
обязательствах M4:

1. Шаг M4 обязан задавать M-код явно (кодировка, `QuoteStyle`, культура,
   типы), а не полагаться на диалог импорта. Это отдельный подшаг с
   разобранным примером — часов добавляет, а не убавляет.
2. `compare_csv.py` требует правки в `same_value()`: нормализация
   десятичного разделителя перед `float()`. Без неё C1 не сдаётся.
3. Предусловие M4 — три включённые preview-функции (PBIP, TMDL, PBIR), а
   не две, как знало решение 22.

Пересмотр решения 22 и уточнение части 6.1 blueprint делаются отдельной
правкой, вне этого файла: гейт фиксирует измеренное, решения принимаются
в `design/decisions.md`.

---

### 2.11. Разбор: P1 и P8 — противоречие или разные вещи (прогон 2026-07-31)

Вопрос задан после свода: P1 утверждает, что кодировка **зашивается в код
запроса** на момент импорта; P8 — что кодировка **в файле проекта не
фиксируется**. Разобрано прогоном, а не сопоставлением формулировок.

**Прогон.** Прочитан с диска сохранённый проект гейта,
`D:\m4-gate\pbi\gate.SemanticModel\definition\tables\*.tmdl` — то самое
место, где живёт M-код (2.8). Обе таблицы, дословно, раздел `partition`:

```
Источник = Csv.Document(File.Contents("D:\m4-gate\customers.csv"),
  [Delimiter = ",", Columns = 3, Encoding = 65001, QuoteStyle = QuoteStyle.Csv])
Источник = Csv.Document(File.Contents("D:\m4-gate\orders.csv"),
  [Delimiter = ",", Columns = 5, Encoding = 65001, QuoteStyle = QuoteStyle.Csv])
```

`orders.csv` — чистый ASCII, и `Encoding = 65001` у него **есть**. Это
третий факт, которого до сих пор не было ни в P1, ни в P8.

**Ответ: противоречия между P1 и P8 нет — они про разные моменты.**
P1 про то, **когда и из чего** принимается решение о кодировке (в диалоге
импорта, по содержимому файла). P8 про то, **попадает ли принятое решение
в текст проекта**. Одно не отменяет другого.

**Противоречие есть внутри самого P8 — его объяснение опровергается
измерением P1.** P8 объясняет наблюдение правилом «Power BI записывает
`Encoding` только когда выбранное значение расходится с автоопределением».
Подставляем измеренное:

| Файл | Автоопределение (P1) | Выбрано автором (P8) | Правило P8 предсказывает | Измерено (P8) |
|---|---|---|---|---|
| `orders.csv` | 1251 | 65001 | расходится → **записать** | параметра нет |
| `customers.csv` | 65001 | 65001 | совпало → **не писать** | `Encoding = 65001` |

Правило предсказало противоположное по обеим строкам. Оно вычёркивается;
измерение P8 остаётся в силе. Механизм, по которому GUI решает писать
параметр или нет, гейтом **не установлен** и для конструкции M4 не нужен —
см. ниже.

**Что установлено измерением, без гипотез о механизме:**

1. ASCII-файл в диалоге импорта получает `1251` (P1).
2. Явный выбор `65001` в диалоге для ASCII-файла в проект **не попал** (P8).
   Прибить кодировку через GUI нельзя.
3. `Encoding = 65001`, вписанный в M-код **руками**, лежит в `.tmdl` и
   переживает сохранение проекта — для обоих файлов, включая ASCII
   (прогон выше). Это и есть рабочий путь.

**Что осталось непроверенным и почему это больше не важно.** Открыт один
вопрос: переопределяется ли кодировка при Refresh у запроса, в котором
параметра `Encoding` нет вовсе. Ответ требует GUI автора: подменить
`orders.csv` на файл с кириллицей и нажать «Обновить». Прогон не
выполнялся, гипотеза не выдвигается. На конструкцию M4 ответ не влияет:
шаг всё равно обязан вписывать `Encoding` руками — по P7 (культура) и по
P8 (`QuoteStyle`) это уже требуется, и вопрос «что делает запрос без
параметра» в задании не возникает. Он остаётся значимым ровно для одной
вещи — понимания, насколько тихо ломается модель, собранная кликами.

**Следствие для C2 — критерий в текущей формулировке не работает, и
причина тяжелее, чем предполагал вопрос.** Формулировка части 1
blueprint — «подмена исходного файла на файл следующего месяца + Refresh →
0 ошибок, 0 ручных правок». P1 измерил режим отказа, при котором **оба
условия выполняются на испорченных данных**: кодировка прочитана как 1251,
кириллица превращена в мусор, Power BI не выдал ни ошибки, ни требования
правки. То есть критерий не «невыполним» — он выполним одинаково на
исправном и на сломанном результате, а значит не проверяет ничего.
Переформулирован решением 22 (раздел «C2»).

---

## 3. ЧАСТЬ 3 — ручной прогон M4 в Power BI (решение 26). Не закрыта

Прогон начат 2026-07-31 (`step-01.md`), продолжен 2026-08-04 и
2026-08-13 (`step-02.md` целиком) — даты по времени записи файлов проекта
`program\M4\pbi`. Порядок тот же, что в Частях 1 и 2: все действия в GUI
делал автор, агент давал инструкции, сверял вывод и записывал. Ничего не
чинилось по ходу — правки содержания в этот прогон не вносились вовсе.

Отличие от Части 2: там проверялся **инструмент** на модели, собранной
для проверки. Здесь проверяются **написанные шаги модуля** — агент даёт
их учащемуся дословно, а расхождение между написанным и происходящим
записывается как дефект инструкции. Нумерация дефектов продолжает ряды
`B` (Часть 1) и `P` (Часть 2) префиксом `R`.

Прогон **остановлен на границе перед `step-03.md`**: шаги 01 и 02
пройдены, машинный критерий шага 02 сошёлся. Где именно и в каком
состоянии — раздел 3.5, что не закрыто — 3.6.

### 3.1. Окружение

| Параметр | Значение |
|---|---|
| ОС | Windows 11 Pro 10.0.26200 |
| Power BI Desktop | 2.156.951.0, установка из Microsoft Store |
| Язык интерфейса | русский |
| Python | 3.11.9 |
| Три preview-функции (PBIP, TMDL, PBIR) | включены до начала прогона |

Версия Desktop — та же, что в Части 2. Обновления Store между частями не
было, и объяснять расхождения сменой версии нечем.

### 3.2. Контрольная точка датасета — сошлась

`generate_m4.py` прогнан до первой задачи любого шага. Все десять sha256
(`csv/` и `csv_next/`, по пять файлов) совпали с таблицами
`program/M4/data/reference_answers.md`, расхождений 0. Числа строк
6582 / 24 / 6 / 432 / 546 и 7316 / 25 / 6 / 457 / 577 подтверждены.

Это единственная часть модуля, которая на прогоне не дала ни одного
расхождения.

### 3.3. Дефекты инструкций

**R1 — включение preview-функций не идемпотентно.**
`step-01.md`, задание 3, велит включить три функции в предварительной
версии безусловно. Ветки «если уже включены» нет, а признака
включённости шаг не даёт. На прогоне автор искал в «Параметрах» пункт,
которого там уже не было, потому что функции были включены в предыдущей
сессии. Все три — глобальные настройки приложения и переживают
перезапуск. Учащийся, вернувшийся к модулю через неделю, упирается в ту
же стену. Признак включённости, который работает, прогон установил
попутно: наличие папок `definition/` и файлов `.tmdl` в сохранённом
проекте.

**R2 — визуалы названы, но ни разу не объяснены.**
`step-01.md`, задание 5: «Положите на страницу визуал „Таблица“ и
перетащите в него `city_uk`». Названо, что должно получиться, а не что
нажать. На прогоне вопрос был буквально «что это значит? как это
сделать?». Дефект не одиночный: положить визуал требуют также
`step-02.md` (задание 7, пять карточек) и `step-04.md` (три визуала), и
ни в одном месте модуля панель «Визуализации» не разобрана ни разу. Это
первое действие учащегося в главном окне Power BI, и модуль проходит
его молча.

**R3 — проект сохранён как `.pbix`, и шаг этому не мешает.**
`step-01.md`, задание 6, называет формат («сохраните как Power BI
Project»), но не говорит, что формат выбирается из выпадающего списка
«Тип файла», где по умолчанию стоит `.pbix`. На прогоне автор сохранил
`m4.pbix` (18 080 байт), и `check_pbip.py --step 01` дал пять `[FAIL]`,
код возврата 1.

Отдельно от дефекта — **подтверждение**: третья типичная ошибка
`step-01.md` описывает ровно этот случай, и её заявленное последствие
(«не проходит ни одна из семи проверок») оказалось верным. Ошибка,
предсказанная автором, случилась с самим автором на первой же попытке.

**R4 — `.gitignore` внутри репозитория Power BI Desktop не создаёт.**
`step-01.md` (1.2 и четвёртая типичная ошибка, «Как правильно: ничего не
делать»), решение 22 (раздел «Что коммитится») и `step-00.md`
(предусловие, п. 4) опираются на то, что `.gitignore` проекта создаёт
сам Desktop. Прогон измерил четыре случая вместо того, чтобы строить
гипотезу:

| Куда сохраняли | Папка | `.gitignore` |
|---|---|---|
| `D:\m4-gate\pbi` (гейт, вне репозитория) | новая | создан, 45 байт |
| `program\M4\pbi` (внутри репозитория, был `m4.pbix`) | непустая | **не создан** |
| `program\M4\pbi-probe` (внутри репозитория) | новая пустая | **не создан** |
| `D:\pbi-probe2` (вне репозитория) | новая пустая | создан, 45 байт |

Содержимое созданного файла в обоих случаях одинаково:

```
**/.pbi/localSettings.json
**/.pbi/cache.abf
```

Правило держится везде, кроме единственного места, где программа его
применяет. Цена: `m4.SemanticModel\.pbi\cache.abf` — 12 110 байт, и
корневой `.gitignore` репозитория его не покрывает (в нём
`.claude/settings.local.json`, `*.db`, `__pycache__/`, `*.pyc`,
`program/M4/data/csv/`, `program/M4/data/csv_next/` — строки про `.pbi/`
нет).

Что осталось неразделённым и названо вслух: триггер — наличие git-репозитория
или наличие родительского `.gitignore`. Пробы это не различили. На
починку не влияет: писать `.gitignore` проекта придётся руками в любом
случае.

**R5 — два из четырёх «молчаливых умолчаний» диалога импорта не
воспроизвелись.**
`step-02.md`, 1.2, перечисляет четыре решения, которые диалог импорта
принимает молча. Прогон подал диалогу настоящие файлы датасета и
записал, что он сделал:

| Умолчание по 1.2 | Что было на прогоне |
|---|---|
| культура `"ru"` в `Table.TransformColumnTypes` | **сработало** — оба файла |
| `2026-05` прочитан как дата | **сработало** — `transactions[period_ym]` стал датой, `01.01.2025` |
| кодировка определяется по содержимому, латиница получает `1251` | **не сработало** — диалог написал `Encoding = 65001` сам |
| `QuoteStyle = QuoteStyle.None` | **не сработало** — диалог написал `QuoteStyle = QuoteStyle.Csv` сам |

M-код `merchants.csv`, который написал диалог, дословно:

```
let
  Источник = Csv.Document(File.Contents("D:\data-analyst-program\program\M4\data\csv\merchants.csv"), [Delimiter = ",", Columns = 4, Encoding = 65001, QuoteStyle = QuoteStyle.Csv]),
  #"Повышенные заголовки" = Table.PromoteHeaders(Источник, [PromoteAllScalars = true]),
  #"Измененный тип столбца" = Table.TransformColumnTypes(#"Повышенные заголовки", {{"merchant_id", Int64.Type}, {"merchant_name", type text}, {"city", type text}, {"address", type text}}, "ru")
in
  #"Измененный тип столбца"
```

`merchants` при этом дал **24 строки** без единой правки: многострочные
адреса разобраны верно.

Четыре следствия:

1. Утверждение «`QuoteStyle.None` рвёт значение по переводу строки»
   остаётся **непроверенным**: диалог сам такого условия не создаёт, а
   вручную его на прогоне не ставили.
2. Четвёртая типичная ошибка `step-02.md` (26 строк вместо 24) на этом
   датасете **невоспроизводима**. Её число получено рассуждением, а не
   прогоном, — это нарушение раздела 1.6 скилла `curriculum-design`.
3. Вторая типичная ошибка `step-02.md` («явный выбор 65001 в диалог не
   доходит») прогону **противоречит**. Находка P1 измерена на файле из
   одной латиницы, а шаг подаёт её как общее правило.
4. Довод «почему это отдельный шаг, а не двадцать минут `step-03.md`»
   держался на четырёх причинах, из которых прогон подтвердил две.

Мелочь оттуда же: реальное имя шага — `#"Измененный тип столбца"`, в
1.3 написано `#"Изменённый тип"`.

**R6 — колонка сумм не «объявлена текстом», а отсутствует в списке
типов.**
`step-02.md`, 1.3, показывает результат диалога как `{"amount", type
text}` — то есть учит искать в списке строку с явным `type text`. На
настоящем `transactions.csv` `amount_uah` в `Table.TransformColumnTypes`
**отсутствует вовсе**: перечислены `tx_id`, `merchant_id`, `tx_date`,
`settled_date`, `mcc`, `status`. Итог тот же (колонка текстовая, `SUM`
не считается), причина другая: не «объявлена текстом», а не объявлена
никак и осталась тем, чем вышла из `Csv.Document`. Разобранный пример
учит искать строку, которой в файле не будет.

**R7 — проверка числа строк стоит раньше средства проверки.**
`step-02.md`, задание 6: «Примените изменения и проверьте число строк
каждой таблицы». Где смотреть — не сказано. Счётчик в строке состояния
редактора Power Query по умолчанию считает по первой 1000 строк и
печатает `999+`, а не число; приписка «Профилирование столбца на основе
первых строк (1000)» об этом сообщает, но шаг о ней молчит и
переключения на весь набор не упоминает ни разу. Учащийся по этой
инструкции сверит `999+` с 6582.

Честное число строк даёт задание **7** (карточки) — то есть проверка из
задания 6 требует инструмента, который появляется заданием позже.

**R8 — «Сохраните проект» стоит одним пунктом в конце 5–7-часового
шага.**
`step-02.md`, задание 8. Между «загрузил первую таблицу» и «сохранил»
по инструкции проходит весь шаг: пять загрузок, правка M-кода в пяти
запросах, сверка строк, шесть визуалов. Шаг оценён в 5–7 ч, то есть
заведомо не в один присест, и о промежуточном сохранении не говорит.
Измерено: после первой сессии прогона на диске лежал результат
`step-01.md` (одна таблица `smoke`, запись 2026-07-31 16:47), а обе
загруженные таблицы `step-02.md` отсутствовали.

**R9 — задание построено на несуществующем элементе интерфейса.**
`step-02.md`, задание 7: «Положите на страницу пять визуалов „Карточка“:
`Количество строк` по каждой таблице». Пункта «Количество строк» в
панели «Данные» Power BI Desktop нет. Проверено визуально на всех пяти
таблицах модуля: панель показывает только колонки
(`merchants` — `address`, `city`, `merchant_id`, `merchant_name`;
`transactions` — `amount_uah`, `mcc`, `merchant_id`, `period_ym`,
`settled_date`, `status`, `tx_date`, `tx_id`), служебных полей нет.

Тяжелее R2: там элемент интерфейса назван, но не объяснён, здесь —
назван тот, которого не существует. Учащийся упирается в тупик и не
может закрыть первый пункт критерия готовности (пять чисел строк),
потому что единственный названный шагом способ их получить отсутствует.

Строка `Количество строк` встречается в репозитории **ровно один раз** —
`program/M4/step-02.md:189`. В `step-04.md` карточки используются под
меры, а меры к тому шагу заведены, и там дефекта нет.

Способы, которыми число строк получить можно (оба измерены на прогоне
как доступные, ни один в модуле не описан): завести меру
`COUNTROWS(<таблица>)` — но мер до `step-04.md` в модуле нет; либо
положить в карточку ключевую колонку и сменить агрегат на «Количество».
Второй способ требует оговорки, которой в шаге тоже нет: «Количество» —
это `COUNT`, он считает непустые значения колонки, а не строки таблицы,
и совпадает с числом строк только на колонке без пропусков.

**Чем R9 обошли, чтобы прогон продолжился (заход 2026-08-13).** Взят
второй способ, на всех пяти таблицах одинаково: ключевая колонка в
карточку, затем правый щелчок по полю в колодце «Значение» →
`Количество`. Колонки: `transactions.tx_id`, `merchants.merchant_id`,
`mcc_categories.code`, `merchant_plan.merchant_ref`,
`calendar.date_key`. Все пять — без пропусков, поэтому `COUNT` совпал с
числом строк: 6582 / 24 / 6 / 432 / 546, эталон сошёлся. Обход рабочий,
но он не восстанавливает шаг: учащемуся, читающему `step-02.md`, взять
его неоткуда — в тексте нет ни слова «Количество» как агрегата, ни
оговорки про `COUNT`. На `calendar.date_key` обход дополнительно упёрся
в R12 и R13 — подробности там.

**R10 — переход из редактора Power Query в главное окно не назван.**
Между заданием 5 (`step-02.md`, типы колонок) и заданием 7 (визуалы)
учащийся обязан выйти из редактора Power Query командой
**Главная → Закрыть и применить**. Задание 6 говорит «Примените
изменения» — называет действие, но не место и не кнопку. Панелей
«Визуализации» и «Данные», нужных заданию 7, в редакторе Power Query нет
вовсе, так что учащийся, оставшийся в редакторе, ищет их там, где их не
бывает. Тот же класс, что R2.

**R11 — не сказано, что визуалов шесть и что они отдельные.**
`step-02.md`, задание 7, перечисляет «пять визуалов „Карточка“… и один —
`Сумма amount_uah`». Механика интерфейса при этом такова, что галочка у
второго поля добавляет его **в тот же выделенный визуал**, а не создаёт
новый: чтобы получить шесть карточек, перед каждой нужно снять
выделение щелчком по пустому месту страницы. Ни числа «шесть» одним
словом, ни требования снять выделение в задании нет.

**R12 — «Автоматические дата и время» ломают критерий `step-03.md`.**
Самая тяжёлая находка прогона, и единственная, где дефект не в
формулировке, а в том, что модуль не знает про поведение инструмента.

Настройка «Автоматические дата и время» включена в Power BI Desktop по
умолчанию. Для каждой колонки типа «дата» она заводит скрытую таблицу
календаря и скрытую связь к ней. В модуле три таких колонки
(`transactions[tx_date]`, `transactions[settled_date]`,
`calendar[date_key]`), и после загрузки пяти таблиц — до того, как
учащийся провёл хоть одну связь, — в сохранённом проекте лежит:

```
tables/DateTableTemplate_86f19579-70ff-49a6-8881-5beb50aeace3.tmdl
tables/LocalDateTable_2f7a081d-b37a-42c7-b28b-adb886f072f7.tmdl
tables/LocalDateTable_5b20ce05-b1bf-4a77-bc8c-7f961ec3ce49.tmdl
tables/LocalDateTable_9357b11a-ed5b-4bba-8be2-353803f0f10c.tmdl
```

и `relationships.tmdl` на 717 байт с четырьмя связями:

```
relationship 842e5041-c21c-481d-99f3-985e6df0fd6d
	joinOnDateBehavior: datePartOnly
	fromColumn: transactions.tx_date
	toColumn: LocalDateTable_5b20ce05-b1bf-4a77-bc8c-7f961ec3ce49.Date

relationship 6d1a944e-df8a-4f39-905a-bb9843e56889
	joinOnDateBehavior: datePartOnly
	fromColumn: transactions.settled_date
	toColumn: LocalDateTable_2f7a081d-b37a-42c7-b28b-adb886f072f7.Date

relationship AutoDetected_97997382-8214-4816-8bab-4fa5b610b188
	fromColumn: transactions.merchant_id
	toColumn: merchants.merchant_id

relationship 29036238-b5c4-4cae-8110-b9686bcb0a15
	joinOnDateBehavior: datePartOnly
	fromColumn: calendar.date_key
	toColumn: LocalDateTable_9357b11a-ed5b-4bba-8be2-353803f0f10c.Date
```

Три следствия, все проверяемые по этому же файлу:

1. **Критерий `step-03.md` непроходим на настройках по умолчанию.**
   `check_pbip.py`, `check_step03()`, требует ровно пяти связей
   (`len(blocks) == 5`). После четырёх связей, которые проводит
   учащийся, в файле окажется восемь: три скрытых, одна
   `AutoDetected_`, четыре проведённых руками. Проверка `[FAIL]`.
   Остальные проверки шага при этом проходят: связей не-`AutoDetected`
   станет семь при требуемых «не менее четырёх», строка
   `isActive: false` останется одна, а `joinOnDateBehavior:` в списке
   `DEFAULTS_MUST_BE_ABSENT` не значится.
2. **Число таблиц в модели не пять, а девять.** `reference_answers.md`
   («Что должно получиться в модели после `step-02.md`») перечисляет
   пять. `check_pbip.py --step 01` печатает число таблиц справочно и
   `[FAIL]` из-за этого не даёт, но эталон расходится с моделью.
3. **Дефект интерфейса, который видит учащийся** — это тот же дефект.
   Колонка даты приходит в панель «Данные» не колонкой, а иерархией
   Год/Квартал/Месяц/День, и чтобы положить в визуал саму колонку,
   надо переключаться на неё явно. Модуль об этом не говорит нигде.

Что **не установлено** и требуется до починки: точный путь к настройке в
русском интерфейсе и её область действия (текущий файл или все новые).
На прогоне настройку не искали и не трогали — прогон измеряет, а не
чинит. Отсюда же открытый вопрос, которого у модуля не было: выключать
её в `step-01.md` или строить критерий `step-03.md` так, чтобы скрытые
связи не мешали.

**Уточнение R12 (2026-08-13, при возобновлении прогона).** Проект открыт
заново после недельного перерыва; файлы никем не правились (запись
2026-08-04). Прочитаны `model.tmdl` и `relationships.tmdl` целиком —
добавилось три факта, которых в записи выше не было:

1. **У настройки есть след в тексте модели.**
   `m4.SemanticModel\definition\model.tmdl:10` содержит
   `annotation __PBI_TimeIntelligenceEnabled = 1`. Это значит, что
   автодата хранится **в самом файле проекта**, а не только в настройках
   приложения, и что состояние проверяется поиском подстроки — тем же
   способом, что и остальные проверки решения 22, а не осмотром
   диалога. Часть вопроса «область действия» этим закрыта: у файла своё
   значение. Путь к пункту в русском интерфейсе по-прежнему не
   установлен — настройку не искали.
2. **Девять таблиц подтверждены вторым файлом.** `model.tmdl:16–24`
   перечисляет `ref table` девять раз: пять таблиц модуля и четыре
   служебных (`DateTableTemplate_86f19579…` и три `LocalDateTable_…`).
   Раньше это было видно только по содержимому папки `tables/`.
3. **Скрытых связей три, автоопределённая — четвёртая, и все четыре
   лежат в файле до единого клика учащегося.** `relationships.tmdl`
   содержит ровно четыре блока: три с `joinOnDateBehavior: datePartOnly`
   к `LocalDateTable_*` и `AutoDetected_97997382-…`
   (`transactions.merchant_id` → `merchants.merchant_id`). Арифметика
   `check_step03()` от этого не меняется (8 = 3 + 1 + 4, из них
   не-`AutoDetected` семь при требуемых «не менее четырёх»), но
   меняется формулировка для починки: **до `step-03.md` в модели уже
   лежит четыре связи, а не три**, и одна из них — та самая связь 1,
   которую `reference_answers.md` засчитывает учащемуся. Учащийся
   проводит руками четыре связи из пяти ожидаемых, а видит в модели
   восемь.

4. **Следствие 3 измерено на карточке и оказалось тяжелее записанного.**
   Выше сказано «колонка даты приходит в панель „Данные“ не колонкой, а
   иерархией». На прогоне 2026-08-13 галочка у `calendar[date_key]`
   положила в карточку **пять чипов сразу** — `date_key`, `Год`,
   `Квартал`, `Месяц`, `День`, — и все пять ушли в колодец «Категории».
   Карточка напечатала склейку уровней (`2 тыс. Квартал 1 Январь 1`,
   `2 тыс. Квартал 1 Январь 2`, … со скроллом). То есть учащийся,
   выполняющий задание 7 буквально, получает на пятой карточке не число
   и не ошибку, а список строк.

Состояние переживает закрытие и повторное открытие проекта: те же
четыре связи и те же девять таблиц через девять дней после записи.

**R13 — «положите визуал „Карточка“» не говорит, в какой колодец кладётся
поле; по умолчанию оно уходит в «Категории», и карточка печатает список
значений вместо числа.**
`step-02.md`, задание 7. На прогоне 2026-08-13 галочка у
`merchants[merchant_id]` в панели «Данные» посадила поле в колодец
**«Категории»**, и карточка показала **список `101, 102, 103` со
скроллом** — не число и не сообщение об ошибке. Первый пункт критерия
готовности (24 строки в `merchants`) через такую карточку не
закрывается.

Обход, которым прогон продолжен: контекстное меню поля → **«Переместить
в» → «Значение»**, затем агрегат **«Количество»**. Дал 24.

Тот же класс, что R2 и R10: назван результат («положите визуал»), а не
последовательность действий. Разница в том, что здесь у действия есть
**молчаливый неверный исход**: учащийся видит заполненную карточку и
может засчитать её как выполненное задание. Явный порядок, которого в
шаге нет: сначала иконка «Карточка» в панели «Визуализации», затем поле
в колодец **«Значение»**, затем агрегат.

**Открытый вопрос закрыт тем же прогоном, и ответ меняет починку.** На
пятой карточке (`calendar`) порядок соблюдён буквально: сначала иконка
«Карточка» в панели «Визуализации», визуал создан и остался выделенным,
и только потом галочка в панели «Данные». Поле всё равно село в
**«Категории»**, «Значение» осталось пустым. Значит порядок «сначала
иконка, потом галочка» дефекта **не лечит**: галочка кладёт поле в
«Категории» независимо от того, как создан визуал. Починка шага обязана
называть **колодец** («Переместить в» → «Значение» либо перетаскивание
поля прямо в колодец), а не последовательность действий.

Чем три карточки сессии 2026-08-04 получили «Значение» — не измерено. В
их `visual.json` поле лежит в колодце `Data`
(`queryState.Data.projections`, `Aggregation.Function: 0`), но каким
действием оно туда попало, прогон не записал; правдоподобная гипотеза
(перетаскивание вместо галочки) остаётся гипотезой.

**R14 — числа на карточках приходят с автоформатом, а критерий шага
сверяет с точной записью.**
`step-02.md`, 1.5, пункт первый требует сверить число строк с эталоном
(`6582`), пункт второй — сумму `10236902.82` с допуском «расхождение 0 до
второго знака». Измерено: карточка с `Количество tx_id` печатает
**`6,582 тыс.`**. Эталон `6582` на экране в таком виде не появляется, и
«расхождение 0» глазами по нему не проверяется — сверяются две разные
записи одного числа.

Для суммы это тяжелее, чем для строк, и **измерено**: карточка
`Сумма amount_uah` печатает **`10,24 млн`** при эталоне `10236902.82`.
Допуск критерия — «расхождение 0 до второго знака»; на экране число
показано до второго знака **миллионов**, то есть с ценой деления 10 000.
Пункт 2 критерия `step-02.md` через карточку не проверяется вовсе — не
«неудобно», а нечем: любая ошибка меньше пяти тысяч гривен даст ту же
запись `10,24 млн`.

Итог по всем шести числам задания 7, как они выглядят на экране против
эталона:

| Карточка | Эталон | На экране | Сверяется глазами |
|---|---|---|---|
| `Количество tx_id` | 6582 | `6,582 тыс.` | нет — другая запись |
| `Количество merchant_id` | 24 | `24` | да |
| `Количество code` | 6 | `6` | да |
| `Количество merchant_ref` | 432 | `432` | да |
| `Количество date_key` | 546 | `546` | да |
| `Сумма amount_uah` | 10236902.82 | `10,24 млн` | нет — точность ±10 000 |

Автоформат срабатывает на числах от тысячи, поэтому четыре из шести
проверок критерия работают, а две — обе, ради которых шаг написан
(6582 строк и сумма после правки культуры), — не работают.

Дефект не в задании, а в **критерии**: 1.5 написан так, будто карточка
показывает то же, что лежит в модели. Починка — либо сказать в шаге,
какой вид числа ожидать, либо добавить отключение автоформата, либо
перенести сверку точных чисел на `check_pbip.py` и экспорт, оставив
карточкам порядок величины.

**Автоформат применяется и там, где число не величина, а метка.** На
карточке `calendar` уровень иерархии `Год` напечатан как **`2 тыс.`**
вместо `2025`. Это тот же механизм, что дал `6,582 тыс.`, но последствие
другое: сумму в сокращённой записи хотя бы можно опознать, а год — нет,
на экране получается мусор. Значит вариант починки «сказать в шаге, какой
вид числа ожидать» покрывает не все случаи: для меток сокращённая запись
не имеет правильного вида вовсе, и остаётся только отключение
автоформата.

**R15 — там, где связь по паре колонок построить нельзя, диалог молча
предлагает валидную с виду связь «многие ко многим» с двунаправленной
фильтрацией.**
`step-03.md`, 1.2 («связь соединяет одну колонку с одной») и 1.3, связь 5.
Прогон 2026-08-13 подал диалогу ровно тот сценарий, который шаг называет
невозможным: **Управление связями → Создать**, `merchant_plan` и
`transactions`, выделены `merchant_ref` и `merchant_id`, затем
Ctrl+щелчок по `period_ym` в обеих таблицах.

Измерено: Ctrl+щелчок **переносит** выделение, а не добавляет —
утверждение документации подтверждено (раздел 3.4). Но отказа диалог не
показывает. Он принял последнее выделение (`period_ym` ↔ `period_ym`),
сам выставил **«Количество элементов: Многие ко многим (\*:\*)»** и
**«Направление перекрёстной фильтрации: Оба»** и напечатал плашку
дословно:

> Эта связь имеет кратность "многие ко многим". Используйте такую
> кратность, только если ожидаете, что ни в одном из столбцов
> (period_ym и period_ym) не будет уникальных значений, и понимаете
> существенные отличия ее принципа работы.

Учащийся, попробовавший собрать составной ключ кликами, получает не
запрет, а готовую к сохранению связь. Сохранив её, он получает лишнюю
шестую связь, двунаправленную фильтрацию и раздутый итог. Шаг об этом не
предупреждает нигде: слово «многие-ко-многим» встречается в `step-03.md`
**один раз** (строка 172) и описывает последствие связи по одному
`merchant_ref`, а не поведение диалога; «Оба» упомянуто один раз
(строка 74) и только как след в TMDL.

Класс тот же, что у R13: у действия есть молчаливый неверный исход,
выглядящий как выполненное задание. Цена выше — ломается модель, а не
одна карточка.

**R16 — термины шага не совпадают с терминами русского интерфейса.**
`step-03.md` говорит **«кардинальность»** (7 вхождений), диалог связей —
**«Кратность»** и **«Количество элементов»**; строки «кардинальность» в
интерфейсе нет вовсе. Кнопку подтверждения шаг не называет ни разу; в
диалоге она — **«Сохранить»**.

Сам по себе дефект не блокирующий, но это тот же класс, что R2 и R10:
учащийся ищет в интерфейсе слово, которого там нет. Отличие от R2/R10 —
здесь термин не пропущен, а назван **другим словом**, и это хуже: поиск
по подсказке даёт ложную уверенность, что нужный элемент не найден.

Третье расхождение, измеренное там же: `step-03.md:112–113` велит «снять
флажок **„Сделать эту связь активной“**» — в диалоге флажок называется
**«Активировать связь»**. Это уже не термин из теории, а прямая
инструкция по клику: учащийся ищет подпись, которой в диалоге нет, и
сверить ему не с чем.

**R17 — «Power BI подставит кардинальность сам» подано в шаге как
признак правильной связи, а подстановка не смотрит в данные.**
`step-03.md:99–100`, связь 2: «Кардинальность Power BI подставит сам —
„Многие к одному“, потому что `code` уникален (6 значений на 6 строк)».
Учащийся из этого выводит, что подставленная кардинальность подтверждает
правильность выбранной пары колонок.

Измерено на паре `merchants[merchant_id]` → `transactions[mcc]`, у
которой **нет ни одного общего значения** (проверено по файлам датасета:
`mcc` — 4121, 5411, 5541, 5812, 5912, 6012; `merchant_id` — 101…124;
пересечение пусто):

- «Количество элементов»: **«Один ко многим (1:\*)»**;
- «Направление перекрёстной фильтрации»: **«Простое»**;
- «Активировать связь»: флажок стоит и **доступен**;
- кнопка **«Сохранить» активна**;
- предупреждений нет ни одного.

То есть кардинальность выводится из **уникальности значений внутри
каждой колонки** и ничего не говорит о том, что колонки вообще
пересекаются. Бессмысленная связь выглядит в диалоге в точности как
правильная связь 2. Единственный признак, которому шаг учит доверять на
этом месте, ложный.

Контраст, измеренный в том же заходе (побочно, на ошибочно выбранной
паре `transactions[merchant_id]` → `merchants[merchant_id]`): дубликат
**по той же паре колонок** блокируется наглухо — красное сообщение
«Между этими двумя столбцами уже существует связь», все поля серые,
«Сохранить» неактивна. Значит проверки в диалоге есть, просто они
устроены про структуру связей, а не про содержимое колонок.

**Состояние двух утверждений о связях (порядок решения 26).**

| Утверждение | Состояние после захода 2026-08-13 |
|---|---|
| «связь соединяет одну колонку с одной» | **подтверждено** — Ctrl+щелчок переносит выделение, а не добавляет; пара колонок не собирается (R15) |
| «активной между двумя таблицами может быть одна связь» | **проверено наполовину** |

Половина, которая проверена: **до сохранения** диалог о единственной
активной связи не предупреждает вовсе — на паре, где активная связь
между теми же таблицами уже есть, флажок «Активировать связь» стоит,
доступен и никак не помечен.

Половина, которая **не проверена**: что Power BI делает **в момент
сохранения** второй связи между теми же таблицами — гасит её
автоматически (как обещает `step-03.md:111–112`) или отказывает. На
прогоне связь не сохранялась. Закрывается это только построением связей
3 и 4 `step-03.md`, то есть уже внутри следующего шага.

### 3.4. Что прогон подтвердил

**Датасет.** Контрольная точка сошлась целиком — раздел 3.2.

**`check_pbip.py` работает на настоящем проекте, а не только на макете.**
Оговорка в докстринге скрипта («прогонялся на макете структуры проекта…
если маска не сойдётся с реальным именем папки, правится маска») снята:
маски `*.SemanticModel/definition`, `*.Report` и
`definition/pages/*/visuals/*/visual.json` сошлись с тем, что Power BI
Desktop записал на диск. На корректно сохранённом `.pbip` шаг 01 дал
шесть `[OK]` из семи; единственный `[FAIL]` — `.gitignore`, то есть
дефект R4, а не дефект скрипта.

**Дискриминатор PBIR сформулирован верно.** `report.json` в проекте
есть, но лежит в `<имя>.Report/definition/report.json`; `step-01.md`
требует его отсутствия именно в **корне** `*.Report`, и это условие
различает PBIR и PBIR-Legacy правильно.

**P7 подтверждён на настоящих данных и закрыт явным M-кодом.**
`amount_uah` при культуре `"ru"` уходит в текст (R6 — про механизм).
После правки культуры на `"en-US"` колонка в модели числовая: в панели
«Данные» стоит знак `Σ`.

**P2 подтверждён на настоящих данных.** `period_ym` без явного типа
становится датой и показывает `01.01.2025` вместо `2025-01`. Обход,
который решение 22 считало достаточным в первой редакции, здесь не
работает — это уже было записано Частью 2, прогон подтвердил на датасете
модуля.

**Автоопределение связи 1 работает.** В `relationships.tmdl` лежит
`AutoDetected_97997382-8214-4816-8bab-4fa5b610b188`,
`transactions.merchant_id` → `merchants.merchant_id`. Это подтверждает
`step-03.md`, 1.3 («Связь 1 — уже есть») и строку 1 таблицы связей в
`reference_answers.md`.

Отдельно, потому что по ходу прогона была высказана обратная гипотеза:
команда **«Обнаружение связей»** ответила «Связи не обнаружены», и это
было прочитано как «автоматика на датасете M4 бездействует». Файл
проекта показал обратное — обнаруживать было нечего, потому что связь
уже стояла с загрузки. Гипотеза снята измерением; в текст модуля она не
попала.

**`QuoteStyle` по умолчанию разобрал многострочные адреса верно** —
`merchants` дал 24 строки. Это подтверждает эталон
`reference_answers.md` (24, а не 26) и одновременно означает, что
режима отказа с 26 строками на прогоне не было (R5).

**Пять таблиц загружены, проект сохранён как `.pbip`.**

### 3.5. Где прогон остановлен

**`step-02.md` пройден целиком (заход 2026-08-13).** Задание 7 закрыто
обходом R9 (см. там же), все шесть чисел получены, критерий 1.5 сверен:

| Пункт критерия 1.5 | Состояние |
|---|---|
| пять чисел строк = 6582 / 24 / 6 / 432 / 546 | сошлись, но две карточки читаются с автоформатом (R14) |
| сумма `amount_uah` = 10236902.82, расхождение 0 | **глазами не проверяется** — карточка печатает `10,24 млн` (R14) |
| `check_pbip.py --step 02` — десять `[OK]`, ноль `[FAIL]`, код 0 | сошлось дословно |

Вывод скрипта на настоящем проекте:

```
Проект: D:\data-analyst-program\program\M4\pbi  шаг: 02

[OK]   пять таблиц модуля на месте: calendar, mcc_categories, merchant_plan, merchants, transactions
[OK]   calendar.tmdl: кодировка, QuoteStyle и культура заданы явно
[OK]   mcc_categories.tmdl: кодировка, QuoteStyle и культура заданы явно
[OK]   merchant_plan.tmdl: кодировка, QuoteStyle и культура заданы явно
[OK]   merchant_plan.commission_pct: тип double
[OK]   merchant_plan.period_ym: тип string
[OK]   merchants.tmdl: кодировка, QuoteStyle и культура заданы явно
[OK]   transactions.tmdl: кодировка, QuoteStyle и культура заданы явно
[OK]   transactions.amount_uah: тип double
[OK]   transactions.period_ym: тип string

ВСЁ СОШЛОСЬ: расхождений 0.
код возврата: 0
```

Прогон остановлен **на границе перед `step-03.md`**, после проверки двух
утверждений о связях (порядок решения 26): первое подтверждено, второе
проверено наполовину — раздел 3.3, таблица после R17. Связи 1–5 не
строились, кроме автоопределённой связи 1, которая стояла с загрузки.

Состояние модели на момент остановки: пять таблиц модуля плюс
`DateTableTemplate_*` и три `LocalDateTable_*` от автодаты; в
`relationships.tmdl` четыре связи (три от автодаты + `AutoDetected`);
шесть карточек на странице; проект сохранён как `.pbip`.

### 3.6. Что осталось незакрытым

**По `step-02.md`:**

- утверждение «`QuoteStyle.None` рвёт значение по переводу строки»
  осталось непроверенным (R5) — режима отказа на прогоне не было.

**По решению 26 — пункт 1 пройден частично, пункты 2–5 не пройдены:**

| Пункт решения 26 | Состояние |
|---|---|
| 1. Клики по связям: все пять, включая выбор активной | не пройден; диалог связей открывался, связи не сохранялись |
| 2. `check_pbip.py --step 03` и `--step 04` на настоящем `.pbip` | не пройден (`--step 02` пройден) |
| 3. Числа 597013.11 / 557105.73 / 10236902.82 | не пройден |
| 4. Сверка экспорта через `compare_csv.py` на реальном файле | не пройден |
| 5. `TOTALYTD` на культуре `"ru"` | не пройден |

**Четыре утверждения, взятые в M4 из документации:**

1. «связь соединяет одну колонку с одной» — **подтверждено** прогоном
   (R15, раздел 3.4);
2. «активной может быть одна связь» — **проверено наполовину**: до
   сохранения диалог не предупреждает; поведение в момент сохранения
   второй связи между теми же таблицами не измерено;
3. поведение `TOTALYTD` — не проверено, держит меру `Settled YTD`;
4. предупреждение про `Date` как надстройку над `DateTime` — не
   проверено, держит требование `type date` в M-коде.

**Уборка после прогона не сделана.** На диске остались:
`program\M4\pbi\m4.pbix` (18 080 байт, след R3),
`program\M4\pbi-probe` и `D:\pbi-probe2` (пробы R4).

### 3.7. Вердикт Части 3

Часть 3 **не закрыта**. Решение 26 требовало прогнать M4 целиком до
того, как писать C2; прогон дошёл до конца второго шага из четырёх.

Что уже установлено, менять придётся независимо от того, чем кончится
остаток прогона: семнадцать дефектов инструкций, из которых R9 делает
задание 7 невыполнимым по написанному тексту, R12 делает непроходимым
машинный критерий `step-03.md`, а R14 — непроверяемым пункт 2 критерия
`step-02.md`. Все три найдены на первых двух шагах; плотность такова,
что оценивать оставшиеся два шага по написанному тексту нельзя.

Отдельно про класс дефектов, который прогон вскрыл только к концу: R13,
R15 и R17 — не пропуски, а **молчаливые неверные исходы**. Интерфейс не
отказывает и не ругается, он выдаёт результат, похожий на правильный:
поле садится не в тот колодец, диалог предлагает валидную с виду связь
`*:*`, кардинальность подставляется на непересекающихся колонках. Шаги
M4 написаны так, что учащемуся нечем отличить такой исход от
выполненного задания. Это исправляется не уточнением формулировок, а
добавлением проверок после действия.

---

## 4. Что гейт оставил репозиторию

Части 1 и 2 закрыты, Часть 3 идёт. Ни одна правка репозитория в ходе
гейта **не делалась** — гейт измеряет, а не чинит.

**Дефекты Части 1 закрыты вне этой сессии.** Между записью Части 1
(коммит `0699344`, 2026-07-30) и продолжением гейта 2026-07-31 в
репозиторий легли коммиты `18bf501` («пять дефектов гейта») и `4b4ea6c`.
Сверено с диском построчно, не по сообщению коммита:

| Дефект | Где закрыт |
|---|---|
| B1 | `design/decisions.md:590` — «Поправка 2026-07-31: „0 действий“ было неверно»; `program/M3/step-01.md:81–88` — скачивание zip, распаковка, `PATH` на уровне пользователя |
| B2 | `program/M3/step-01.md:165` — `.mode csv` задан до первой команды, дающей CSV |
| B3 | `program/M3/step-01.md:49` — появился «Шаг 0. Рабочий каталог» с указанием `program/M3/data` |
| B4 | `program/M3/step-01.md:117` — разобран `table customers already exists` |
| B5 | `program/M3/step-01.md:273–275` — названо явно: «`pending`-заказов два — 111 (390.0) и 116 (260.0)» |

B6 дефектом не был (см. 1.3).

**Очередь Части 2 — пять пунктов из шести закрыты 2026-07-31 сводом в
решение 22, шестой открыт:**

| # | Что | Где | Из чего | Статус |
|---|---|---|---|---|
| 1 | Нормализовать десятичный разделитель в `same_value()` перед `float()` | `program/M3/data/compare_csv.py` | P9 | **закрыт** — `normalize_number()`, 8/8 пар M3 сошлись, `test_compare_csv.py` (2.7) |
| 2 | Переписать признак установленности Power BI на `Get-AppxPackage *PowerBI*` | `design/decisions.md`, решение 22 | P4 | **закрыт** — решение 22, «Признак установленности» |
| 3 | Снять смягчение «период текстом `YYYY-MM` спасает от дат»; заменить требованием явного M-кода (кодировка, `QuoteStyle`, культура, типы) | `design/decisions.md`, решение 22 | P2, P7, P8 | **закрыт** — смягчение помечено отменённым, введён слой 0 |
| 4 | Добавить PBIR третьей preview-функцией в предусловие; перевести `visualType`, состав полей и сортировку из чеклиста в машинную проверку | `design/decisions.md`, решение 22 | P5 | **закрыт** — слой 3 переписан |
| 5 | Записать правило «в TMDL проверяется отсутствие строки, а не наличие»; запретить побайтовое сравнение TMDL (GUID в `lineageTag`) | `design/decisions.md`, решение 22 | 2.8, 2.9 | **закрыт** — слой 2, «Проверка через отсутствие строки» |
| 6 | Учесть подшаг «явный M-код» в часах M4 | `design/blueprint.md`, часть 6.1 | 2.10 | **открыт** — правится при написании `program/M4/step-00.md`, когда состав шагов известен и вилку есть с чем сверять |

Сверх этих шести свод добавил два, которых очередь не знала: ограничение
критерия C1 по автосозданию связей (2.5) и переформулировку C2 (2.11).
Оба записаны в решение 22.

**Что гейт оставил на диске** (вне репозитория, воспроизводимо):

- `D:\m4-gate\customers.csv` — 436 байт, UTF-8 без BOM, CRLF, 12 строк
- `D:\m4-gate\orders.csv` — 635 байт, ASCII, CRLF, 19 строк
- `D:\m4-gate\export1.csv`, `export2.csv` — по 136 байт, идентичны
- `D:\m4-gate\pbi\` — проект `gate.pbip`, 23 файла, 50 КБ

Оба CSV сгенерированы Python (`csv.writer`, `lineterminator="\r\n"`), а
не редиректом `sqlite3.exe` — первая попытка дала `\r\r\n`, см. B6.
Скрипт-генератор одноразовый, в репозиторий не кладётся: те же данные
воспроизводятся запросами из 2.2 через `.once`.

---

## 5. Прогон `tools/check_consistency.py`

### 5.1. Прогон 2026-07-31

Выполнен 2026-07-31 в конце сессии, на состоянии репозитория после
коммитов `18bf501` и `4b4ea6c`. Вывод дословно:

```
[OK]   6.1: распарсено 24 строк, построчная сумма 478–642
[OK]   CLAUDE.md: 478–642 совпадает с частью 6.1
[OK]   6.2 Итого: 478–642 совпадает с частью 6.1
[WARN] M0: сумма 3 шагов 7–9 ч при вилке 6.1 7–9 — совпадение по обеим границам: признак подгонки суммы под вилку, а не подтверждение оценок (решение 24)
[WARN] M1: сумма 3 шагов 8–10 ч при вилке 6.1 8–10 — совпадение по обеим границам: признак подгонки суммы под вилку, а не подтверждение оценок (решение 24)
[WARN] M2: сумма 8 шагов 30–40 ч при вилке 6.1 30–40 — совпадение по обеим границам: признак подгонки суммы под вилку, а не подтверждение оценок (решение 24)
[OK]   M3: сумма 12 шагов 60–77 ч при вилке 6.1 60–80
[OK]   часть 1: заявлено 30 умений, построчный подсчёт по секциям без пометки 'вне топ-15' совпадает
[OK]   часть 1: всего 36 ID умений во всех секциях (A1..K2), включая 'вне топ-15'
[FAIL] умения без шапки 'Умение: <ID>' ни в одном шаге program/**/*.md: B1, B2, B3, B4, B5, C1, C2, C3, C4, E1, E2, F1, F2, F3, G1, G2, G3, H1, H3, J1, J2, J3, J4, K1, K2
[OK]   program/M*/step-NN.md: все 26 шагов объявили вид — 25 с умением, 1 без; инфраструктурные: program/M3/step-09.md
[OK]   design/decisions.md: 24 решений, нумерация сплошная 1..24
[OK]   DEFERRED.md: 15 строк отложенного, все 36 путей существуют
[OK]   все 4 начатых модулей имеют step-00.md
[OK]   program/M*/data/README.md: сверено 11 строк из раздела «Проверка строк» во всех 4 файлах
[OK]   эталоны на состоянии базы своего шага: сверено 8 пар (CSV ↔ запрос) в модулях: M3. НЕ покрыто: эталоны PostgreSQL (step-09..step-12 — нужен поднятый сервер, числа проверены вручную одним прогоном); activity_log (не коммитится, миллионы строк — сверяется через sha256 контрольной точки); модули без SQL-эталонов: M0, M1, M2
[OK]   program/M*/data/: проверено 38 файлов в 4 папках, ни одного не покрытого README/.gitignore не найдено
[OK]   program/**/*.md: запрещённых слов и POSIX-команд в коде не найдено

Есть проблемы — см. [FAIL] выше.
```

Единственный `[FAIL]` — умения ненаписанных модулей (M4 и далее);
состояние существовало до гейта и этим гейтом не вызвано. C1–C4 в этом
списке — ровно те умения Power BI, ради которых гейт и проводился.

Три `[WARN]` по M0–M2 — новая проверка решения 24, к гейту отношения не
имеет.

### 5.2. Прогон 2026-08-14 (конец захода по Части 3)

Выполнен в конце захода 2026-08-13 → 2026-08-14, до коммита. Вывод
дословно:

```
[OK]   6.1: распарсено 24 строк, построчная сумма 478–642
[OK]   CLAUDE.md: 478–642 совпадает с частью 6.1
[OK]   6.2 Итого: 478–642 совпадает с частью 6.1
[WARN] M0: сумма 3 шагов 7–9 ч при вилке 6.1 7–9 — совпадение по обеим границам: признак подгонки суммы под вилку, а не подтверждение оценок (решение 24)
[WARN] M1: сумма 3 шагов 8–10 ч при вилке 6.1 8–10 — совпадение по обеим границам: признак подгонки суммы под вилку, а не подтверждение оценок (решение 24)
[WARN] M2: сумма 8 шагов 30–40 ч при вилке 6.1 30–40 — совпадение по обеим границам: признак подгонки суммы под вилку, а не подтверждение оценок (решение 24)
[OK]   M3: сумма 12 шагов 60–77 ч при вилке 6.1 60–80
[WARN] M4: сумма 4 шагов 20–27 ч при вилке 6.1 40–60 — сумма целиком вне вилки: сначала искать недостающее содержание, потом править вилку в blueprint (решение 24)
[OK]   часть 1: заявлено 30 умений, построчный подсчёт по секциям без пометки 'вне топ-15' совпадает
[OK]   часть 1: всего 36 ID умений во всех секциях (A1..K2), включая 'вне топ-15'
[FAIL] умения без шапки 'Умение: <ID>' ни в одном шаге program/**/*.md: B1, B2, B3, B4, B5, C2, C3, C4, E1, E2, F1, F2, F3, G1, G2, G3, H1, H3, J1, J2, J3, J4, K1, K2
[OK]   program/M*/step-NN.md: все 30 шагов объявили вид — 28 с умением, 2 без; инфраструктурные: program/M3/step-09.md, program/M4/step-01.md
[OK]   design/decisions.md: 26 решений, нумерация сплошная 1..26
[OK]   DEFERRED.md: 15 строк отложенного, все 36 путей существуют
[OK]   все 5 начатых модулей имеют step-00.md
[OK]   program/M*/data/README.md: сверено 14 строк из раздела «Проверка строк» во всех 5 файлах
[OK]   эталоны на состоянии базы своего шага: сверено 8 пар (CSV ↔ запрос) в модулях: M3. НЕ покрыто: эталоны PostgreSQL (step-09..step-12 — нужен поднятый сервер, числа проверены вручную одним прогоном); activity_log (не коммитится, миллионы строк — сверяется через sha256 контрольной точки); модули без SQL-эталонов: M0, M1, M2, M4
[OK]   program/M*/data/: проверено 46 файлов в 5 папках, ни одного не покрытого README/.gitignore не найдено
[OK]   program/**/*.md: запрещённых слов и POSIX-команд в коде не найдено

Есть проблемы — см. [FAIL] выше.
код возврата: 1
```

Отличия от 5.1, все объясняются работой между заходами, а не этим
заходом (в нём правился только `research/tools-gate.md`):

- `[FAIL]` короче на один ID — **C1** ушёл из списка: шаг с этим умением
  написан. Остальные 24 ID на месте;
- добавился `[WARN] M4: сумма 4 шагов 20–27 ч при вилке 40–60` — это
  ровно пункт 6 открытой очереди раздела 4 (часы M4, решение 22 / 2.10),
  и теперь он виден машинно, а не только по записи в гейте;
- 30 шагов вместо 26, 26 решений вместо 24, 5 модулей вместо 4 — M4
  написан до `step-04.md`.

`[FAIL]` — по-прежнему умения ненаписанных модулей; заходом не вызван и
им не чинится.

---

## 6. Решения

**Затронуты: 15, 22, 26. Заведены: нет.**

Ни одно решение этими заходами не правится — гейт только фиксирует.

- **Решение 15** — затронуто Частью 1, поправка **уже внесена** вне этой
  сессии (`design/decisions.md:590`, коммит `18bf501`). Сверено с диском.
  Со стороны гейта закрыто.
- **Решение 22** — затронуто Частью 2, **не правлено**. Подлежит правке
  по шести пунктам открытой очереди раздела 3: признак установленности
  (P4), смягчение по датам и требование явного M-кода (P2, P7, P8),
  PBIR третьей preview-функцией и перенос части слоя 3 в машинную
  проверку (P5), правило «в TMDL проверяется отсутствие строки» (2.9),
  нормализация десятичного разделителя в `compare_csv.py` (P9), часы M4
  в части 6.1 (2.10).
- **Решение 26** — затронуто Частью 3, **не правлено**. Порядок решения
  26 («пройти все четыре шага, прежде чем что-либо править») на заходе
  2026-08-13 → 2026-08-14 соблюдён: заведены пять новых дефектов
  (R13–R17) и уточнён R12, не починен ни один. Всего по Части 3 —
  семнадцать. Пройдены шаги 01 и 02 из четырёх;
  пункты 2–5 решения 26 остаются открытыми (3.6).

Судьба C1–C4 определена и записана в 2.10: **все четыре остаются
машинными**, ни один не уходит в чеклист автора.

Запись в `program/pilot-report.md` не делается: заход не правил ни одного
шага. Она появится вместе с правками, следующими из этого гейта.
